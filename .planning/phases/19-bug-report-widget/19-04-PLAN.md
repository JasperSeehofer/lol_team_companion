---
phase: 19
plan: 04
type: execute
wave: 3
depends_on: [19-02]
files_modified:
  - src/pages/draft.rs
  - src/pages/solo_dashboard.rs
  - src/pages/team/dashboard.rs
  - src/pages/stats.rs
  - src/pages/champion_pool.rs
  - src/pages/game_plan.rs
  - src/pages/post_game.rs
  - e2e/tests/bug-report.spec.ts
autonomous: false
requirements: []
success_criteria: [SC-3, SC-7]
threats_addressed: []
tags: [e2e, playwright, ui-attribute-rollout, regression]

must_haves:
  truths:
    - "Cross-cutting: zero `.unwrap()` in any WASM-reachable code (rule 35). N/A to attribute additions (HTML-only); applies to e2e test code paths the widget hits at runtime"
    - "Cross-cutting: `data-feedback-label` is the ONLY way the widget identifies elements. The label strings live HERE (in these 7 pages); the widget reads them via `closest()` in plan 19-02"
    - "Cross-cutting: no dark patterns per G-10. Label strings stay neutral, descriptive, human-readable (no jingoism, no exclamation, no emoji)"
    - "Cross-cutting: tests run via `cargo test --features ssr --lib` for unit; e2e via `cd e2e && npx playwright test`"
    - "Attribute syntax: on raw HTML elements (`<div>`, `<button>`, `<section>`, `<nav>`, `<h2>`, etc.) use plain `data-feedback-label=\"...\"`. On Leptos components (`<Card>`, `<Btn>`, `<HeraldicDivider>`, anything starting with a capital letter), use `attr:data-feedback-label=\"...\"` per leptos rule 10"
    - "Label hierarchy schema: `\"<Page> → <Section> → <Element>\"`. The `→` character is U+2192 RIGHTWARDS ARROW (NOT a regular hyphen or `->`). Match D-06.2 examples exactly: `\"Draft → Blue side → Pick 3 slot\"`, `\"Solo dashboard → LP history graph\"`, `\"Stats → Match list → Filter dropdown\"`. The acceptance criteria assert U+2192 codepoint presence directly via positive grep — see per-page checks in Task 1 + 2"
    - "DO NOT wrap existing elements in new container divs solely to host the attribute (D-06.3). Apply the attribute to the OUTERMOST element of each meaningful section already present in the JSX. If no suitable element exists, document the gap in 19-04-SUMMARY.md (and surface in the manual checkpoint's resume-signal) — do not invent wrappers"
    - "DO NOT regress `e2e/tests/hydration-no-panic.spec.ts` (Phase 18.2 regression suite). Adding a plain string attribute should be hydration-safe, but the suite MUST be re-run as a gate after this plan"
    - "e2e tests use the `authedPage` fixture from `e2e/tests/fixtures.ts:88-95`. NEW tests do NOT inline registration logic"
    - "e2e tests for the 'hidden on public pages' case use the bare `page` fixture (unauthed), NOT `authedPage` — must not trigger the auth flow"
    - "e2e tests require `await page.waitForTimeout(500)` after navigation before WASM-dependent assertions (wasm rule 56)"
    - "e2e test for submit-persists asserts via the inbox file OR via a follow-up server-fn read; but `list_bug_reports` returns Forbidden in v1 (researcher resolution). Recommended: assert via the success toast + modal-closed state, leaving deep persistence verification to the manual checkpoint or a future admin UI"
    - "Negative-content gate: the e2e spec MUST include a hard `.not.toContain(\"🐛\")` (and equivalent forbidden-character) assertion against the rendered widget DOM — belt-and-suspenders on top of plan 19-02's source-code grep. The acceptance criterion below greps for the literal assertion pattern in the spec"
  artifacts:
    - path: src/pages/draft.rs
      provides: "Page-level + section-level + key-element data-feedback-label attributes — at least 3 distinct labels covering Draft outer wrapper, Blue side region, Red side region; additionally each Pick/Ban slot if accessible without re-wrapping"
      contains: "data-feedback-label=\"Draft"
    - path: src/pages/solo_dashboard.rs
      provides: "data-feedback-label on page wrapper + LP history graph + ranked snapshot region (whichever sections exist in current markup)"
      contains: "data-feedback-label=\"Solo dashboard"
    - path: src/pages/team/dashboard.rs
      provides: "data-feedback-label on page wrapper + roster slots + coach slots + join-requests section"
      contains: "data-feedback-label=\"Team dashboard"
    - path: src/pages/stats.rs
      provides: "data-feedback-label on page wrapper + match list + filter dropdown"
      contains: "data-feedback-label=\"Stats"
    - path: src/pages/champion_pool.rs
      provides: "data-feedback-label on page wrapper + tier sections"
      contains: "data-feedback-label=\"Champion pool"
    - path: src/pages/game_plan.rs
      provides: "data-feedback-label on page wrapper + champion autocomplete + plan textarea"
      contains: "data-feedback-label=\"Game plan"
    - path: src/pages/post_game.rs
      provides: "data-feedback-label on page wrapper + post-game learning sections"
      contains: "data-feedback-label=\"Post game"
    - path: e2e/tests/bug-report.spec.ts
      provides: "Playwright spec covering: visible on /draft, hidden on /auth/login, select-mode captures label, esc cancels, submit shows toast and closes modal, AND a negative-content gate asserting the widget DOM does not contain forbidden characters (🐛, 🎉, exclamation-form 'Thanks!')"
      min_lines: 50
  key_links:
    - from: "page elements with data-feedback-label"
      to: "src/components/bug_report_widget.rs click-capture closure"
      via: "Element::closest(\"[data-feedback-label]\") walk-up from event.target"
      pattern: "data-feedback-label="
    - from: "e2e/tests/bug-report.spec.ts"
      to: "the wired widget + attribute rollout"
      via: "Playwright authedPage fixture + locator-based assertions"
      pattern: "import.*from.*fixtures"
---

<objective>
Roll `data-feedback-label` attributes onto the 7 first-priority pages from D-06.1, then add a Playwright e2e spec that proves the widget's select-mode → modal → submit → toast flow works end-to-end. The widget itself was wired in plan 19-02; this plan delivers the surfaces the user clicks AND the regression-coverage spec.

Purpose: Deliver SC-3 (clicking the floating button → select mode → clicking a tagged element opens a modal pre-filled with that element's label) for the 7 highest-value surfaces, and verify SC-7 (no dark patterns regression) via the spec's text assertions including a hard negative-content gate. Preserve the Phase 18.2 hydration regression suite — no markup wrappers added, attributes-only.

Output: 7 page files with `data-feedback-label` attributes following the `"<Page> → <Section> → <Element>"` schema. One new Playwright spec at `e2e/tests/bug-report.spec.ts` including a `.not.toContain(...)` negative-content assertion. One human checkpoint confirming the rollout works on `/draft` via agent-browser, with the remaining 6 pages smoke-checked AND a documented gap-page degraded path so the verifier can mark `approved-with-gaps` when Tasks 1/2 documented an unavoidable coverage gap.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/19-bug-report-widget/19-CONTEXT.md
@.planning/phases/19-bug-report-widget/19-RESEARCH.md
@.planning/phases/19-bug-report-widget/19-PATTERNS.md
@.planning/phases/19-bug-report-widget/19-VALIDATION.md
@.planning/phases/19-bug-report-widget/19-02-SUMMARY.md
@.planning/phases/19-bug-report-widget/19-03-SUMMARY.md
@CLAUDE.md
@.claude/rules/leptos-patterns.md
@.claude/rules/wasm-patterns.md
@e2e/tests/fixtures.ts
@e2e/tests/audit-draft.spec.ts

<interfaces>
<!-- Lifted from src/pages/draft.rs:4280-4287 — representative existing structure -->
<!-- (raw <div> = plain attribute syntax; Leptos component = attr: prefix per leptos rule 10) -->

```rust
// BEFORE (existing markup, do NOT change beyond adding the attribute):
<div class="grid grid-cols-2 gap-6 mt-4">
    <Card region=region_for_blue.clone() variant="gilt">
        <div>
            <Eyebrow>"BLUE SIDE"</Eyebrow>
            ...

// AFTER (attribute additions only):
<div class="grid grid-cols-2 gap-6 mt-4" data-feedback-label="Draft → Two-column ledger">
    <Card region=region_for_blue.clone() variant="gilt" attr:data-feedback-label="Draft → Blue side">
        <div>
            <Eyebrow>"BLUE SIDE"</Eyebrow>
            ...
```

<!-- Lifted from e2e/tests/audit-draft.spec.ts:9-37 — fixture import + page-interaction pattern -->
```ts
import { test, expect } from "./fixtures";
import { captureErrors, filterRealErrors, navigateTo } from "./helpers";

test("...", async ({ teamPage }) => {  // For team-required tests
  // OR
test("...", async ({ authedPage }) => {  // For auth-only tests (use this for bug-report)
  // OR
test("...", async ({ page }) => {  // For unauthed tests (use bare `page` for hidden-on-public)
```

<!-- Lifted from e2e/tests/fixtures.ts (referenced lines 88-95) — authedPage fixture: -->
<!-- Registers a fresh user with ?invite=E2E-TEST, lands on /team/dashboard (or /solo per registration auto-login). -->

<!-- The widget's accessible name from plan 19-02 (Phase 17 stub): -->
<!-- `aria-label="Report a bug or wishlist item"` — use this in locator strings -->
```

<!-- Researcher-recommended e2e test bodies (from 19-RESEARCH.md lines 1116-1162): -->
Five tests + a negative-content gate:
1. `visible on auth-required pages` — authedPage, goto /draft, expect button by aria-label visible
2. `hidden on public pages` — bare page, goto /auth/login, expect button has count 0
3. `select mode captures the nearest data-feedback-label` — authedPage, goto /draft, click report button, click a tagged element, expect modal opens with the label visible
4. `esc cancels select mode without opening modal` — authedPage, goto /draft, click report button, keyboard.press Escape, expect dialog count 0
5. `submit persists and shows toast` — authedPage, goto /draft, click report button, click a tagged element, fill textarea, click Bug radio, click Submit, expect toast visible AND dialog count 0
6. (NEW per revision) `widget DOM contains no forbidden characters` — authedPage, goto /draft, click report button, click tagged element, read modal HTML, `expect(html).not.toContain("🐛")` AND `.not.toContain("🎉")` AND `.not.toContain("Thanks!")`. Belt-and-suspenders on top of plan 19-02's source-code grep.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Roll data-feedback-label onto 4 of the 7 first-priority pages — draft, solo_dashboard, team/dashboard, stats</name>
  <files>src/pages/draft.rs, src/pages/solo_dashboard.rs, src/pages/team/dashboard.rs, src/pages/stats.rs</files>
  <read_first>
    - src/pages/draft.rs (full file or grep for `<Card`, `<div class="grid`, `<section`, the outermost JSX wrapper near the `view! {` block) — identify the page wrapper, the blue-side region, the red-side region, the action ledger, and any pick/ban slot containers. Need exact element types (`<Card>` vs `<div>`) to choose `attr:` vs plain syntax
    - src/pages/solo_dashboard.rs (full file) — identify LP history graph wrapper, ranked snapshot region, recent matches list, the page wrapper itself
    - src/pages/team/dashboard.rs (full file) — identify roster slots container, coach slots container, join-requests container, the page wrapper
    - src/pages/stats.rs (full file) — identify the match list table, the filter dropdown(s), the page wrapper
    - .planning/phases/19-bug-report-widget/19-PATTERNS.md (lines 537-575) — attribute application rules; in particular the `attr:` vs plain syntax distinction for Leptos components vs raw HTML
    - .planning/phases/19-bug-report-widget/19-CONTEXT.md (D-06.2 examples) — exact label hierarchy strings
    - .claude/rules/leptos-patterns.md (rule 10) — `attr:class` precedent applies to `attr:data-feedback-label`
  </read_first>
  <action>
    For each of the 4 pages, add `data-feedback-label="<Page> → <Section> → <Element>"` (or the 2-level form `"<Page> → <Section>"` where there's no meaningful sub-element granularity) to existing markup. NO new wrapper divs. NO logic changes.

    Required minimum labels per page (more is fine; less is a gap to document in 19-04-SUMMARY.md):

    **draft.rs** (at least 4 labels):
    - Outermost page wrapper (the top `<div>` or `<main>` inside the `view!` block): `data-feedback-label="Draft"`
    - Blue side region: `data-feedback-label="Draft → Blue side"` (or `attr:data-feedback-label="Draft → Blue side"` if it's a `<Card>` per the analog at 19-PATTERNS.md lines 553-560)
    - Red side region: `data-feedback-label="Draft → Red side"` (mirror Blue side syntax)
    - Action ledger / picks-bans area: `data-feedback-label="Draft → Action ledger"` (or a more specific section name from the existing eyebrow text)

    **solo_dashboard.rs** (at least 3 labels):
    - Page wrapper: `data-feedback-label="Solo dashboard"`
    - LP history graph wrapper: `data-feedback-label="Solo dashboard → LP history graph"` (canonical example from D-06.2)
    - Ranked snapshot or recent matches section: `data-feedback-label="Solo dashboard → Ranked snapshot"` (use whatever section name matches the existing eyebrow / heading text)

    **team/dashboard.rs** (at least 3 labels):
    - Page wrapper: `data-feedback-label="Team dashboard"`
    - Roster slots region: `data-feedback-label="Team dashboard → Roster"`
    - Join requests region (if rendered): `data-feedback-label="Team dashboard → Join requests"`

    **stats.rs** (at least 3 labels):
    - Page wrapper: `data-feedback-label="Stats"`
    - Match list: `data-feedback-label="Stats → Match list"`
    - Filter dropdown: `data-feedback-label="Stats → Filters"` (or `"Stats → Match list → Filter dropdown"` if the filter sits inside the match-list region)

    SYNTAX RULES (mandatory):
    - Element starts with lowercase (`<div>`, `<section>`, `<nav>`, `<button>`, etc.) → plain attribute syntax: `data-feedback-label="..."`
    - Element starts with uppercase (Leptos component: `<Card>`, `<Btn>`, `<HeraldicDivider>`, etc.) → prefixed syntax: `attr:data-feedback-label="..."` (leptos rule 10)
    - Use the U+2192 `→` character (RIGHTWARDS ARROW), NOT `->` or `>`. Copy from the existing CONTEXT/PATTERNS docs to ensure the codepoint is correct. The acceptance criteria below assert U+2192 presence directly via positive grep (one per page) — replaces the old negative `->` check.

    If a page has no suitable element to host a particular label (e.g., the page consists of a single Resource-loaded list with no semantic sub-divisions), DOCUMENT the gap in 19-04-SUMMARY.md and proceed with the labels that ARE achievable — do NOT invent wrapper divs (D-06.3). The manual-checkpoint (Task 4) gives the verifier an `approved-with-gaps` resume option for exactly this case.
  </action>
  <verify>
    <automated>for f in src/pages/draft.rs src/pages/solo_dashboard.rs src/pages/team/dashboard.rs src/pages/stats.rs; do grep -q 'data-feedback-label' "$f" || { echo "MISSING in $f"; exit 1; }; done; cargo check --features ssr</automated>
  </verify>
  <acceptance_criteria>
    - Source: each of the 4 files contains at least one `data-feedback-label` attribute. Check: `for f in src/pages/draft.rs src/pages/solo_dashboard.rs src/pages/team/dashboard.rs src/pages/stats.rs; do grep -q 'data-feedback-label' "$f" || exit 1; done`
    - Source: each file contains a label that starts with the correct Page prefix:
      - `grep -q 'data-feedback-label="Draft' src/pages/draft.rs` exits 0
      - `grep -q 'data-feedback-label="Solo dashboard' src/pages/solo_dashboard.rs` exits 0
      - `grep -q 'data-feedback-label="Team dashboard' src/pages/team/dashboard.rs` exits 0
      - `grep -q 'data-feedback-label="Stats' src/pages/stats.rs` exits 0
    - Source: at least 3-4 unique labels per page. Spot check: `grep -c 'data-feedback-label' src/pages/draft.rs` returns >= 3
    - **Positive U+2192 codepoint check (replaces the prior negative `->` check)** — each of the 4 pages MUST contain at least one `data-feedback-label="..."` that includes a literal U+2192 RIGHTWARDS ARROW character. The grep enforces the codepoint directly:
      - `grep -qE 'data-feedback-label="[^"]*→[^"]*"' src/pages/draft.rs` exits 0
      - `grep -qE 'data-feedback-label="[^"]*→[^"]*"' src/pages/solo_dashboard.rs` exits 0
      - `grep -qE 'data-feedback-label="[^"]*→[^"]*"' src/pages/team/dashboard.rs` exits 0
      - `grep -qE 'data-feedback-label="[^"]*→[^"]*"' src/pages/stats.rs` exits 0
    - Source: no new logic added — diff per file should be additive (attribute insertions only). Spot check: `git diff --stat src/pages/draft.rs` shows only insertions, no deletions of view! contents
    - CLI: `cargo check --features ssr` exits 0
    - CLI: `cargo check --features hydrate --target wasm32-unknown-unknown` exits 0
    - Regression: `cargo test --features ssr --lib` exits 0 (no unit-test regressions — adding HTML attributes doesn't touch any Rust logic, but proves the both-target compilation didn't introduce a parse error)
  </acceptance_criteria>
  <done>
    The 4 pages have data-feedback-label attributes on at least the outermost wrapper plus 2-3 key sub-regions. Compilation is clean on both targets. No regressions in existing unit tests. The label strings use the canonical U+2192 codepoint, verified by positive per-page grep.
  </done>
</task>

<task type="auto">
  <name>Task 2: Roll data-feedback-label onto the remaining 3 pages — champion_pool, game_plan, post_game</name>
  <files>src/pages/champion_pool.rs, src/pages/game_plan.rs, src/pages/post_game.rs</files>
  <read_first>
    - src/pages/champion_pool.rs (full file) — identify tier sections (S/A/B/C/etc.), the "add to pool" controls, the page wrapper
    - src/pages/game_plan.rs (full file) — identify champion autocomplete input, the plan textarea, the linked-draft section
    - src/pages/post_game.rs (full file) — identify the post-game learning sections, the pattern-analysis region, the page wrapper. Note: `recursion_limit = "512"` is required by post_game.rs (leptos rule 38) — do not remove or lower it
    - .planning/phases/19-bug-report-widget/19-PATTERNS.md (lines 537-575) — attribute syntax rules
  </read_first>
  <action>
    Apply the same rollout pattern as Task 1 to the remaining 3 pages.

    Required minimum labels per page (use the closest existing element to host each):

    **champion_pool.rs** (at least 3 labels):
    - Page wrapper: `data-feedback-label="Champion pool"`
    - Tier section(s): `data-feedback-label="Champion pool → Tier S"`, `... → Tier A"`, etc. — apply to the wrapping div of each tier band rendered by the existing logic. If tiers are mapped dynamically via a `<For>`, apply the attribute to the outer `<For>` parent OR to each rendered tier block via the `<For>` body (use `format!("Champion pool → {} tier", tier_name)` style — but this is a fully-dynamic label; for v1 a static `"Champion pool → Tier list"` on the surrounding container is sufficient and avoids any dynamic-string allocation surprise)
    - Add-to-pool controls: `data-feedback-label="Champion pool → Add controls"` (or whatever the existing region is named in the UI)

    **game_plan.rs** (at least 3 labels):
    - Page wrapper: `data-feedback-label="Game plan"`
    - Champion autocomplete: `data-feedback-label="Game plan → Champion picker"` (on the wrapper of the `<ChampionAutocomplete>` component — apply via `attr:` prefix if it's a component)
    - Plan textarea / linked draft section: `data-feedback-label="Game plan → Plan body"` or `"Game plan → Linked draft"` (whichever section exists)

    **post_game.rs** (at least 3 labels):
    - Page wrapper: `data-feedback-label="Post game"`
    - Learning sections: `data-feedback-label="Post game → Learning list"` (or `"Post game → Patterns"` depending on existing structure)
    - Save/submit region: `data-feedback-label="Post game → Save controls"` (if such a region exists)

    Same syntax rules as Task 1: plain attribute for raw HTML, `attr:` prefix for Leptos components, U+2192 arrow only. Per-page positive `→` grep enforced in the acceptance criteria below.

    If a page has no suitable element for a section-level label, document the gap in 19-04-SUMMARY.md (the manual checkpoint allows `approved-with-gaps` for documented gaps).
  </action>
  <verify>
    <automated>for f in src/pages/champion_pool.rs src/pages/game_plan.rs src/pages/post_game.rs; do grep -q 'data-feedback-label' "$f" || { echo "MISSING in $f"; exit 1; }; done; cargo check --features ssr</automated>
  </verify>
  <acceptance_criteria>
    - Source: each of the 3 files contains at least one `data-feedback-label` attribute. Check: `for f in src/pages/champion_pool.rs src/pages/game_plan.rs src/pages/post_game.rs; do grep -q 'data-feedback-label' "$f" || exit 1; done`
    - Source: `grep -q 'data-feedback-label="Champion pool' src/pages/champion_pool.rs` exits 0
    - Source: `grep -q 'data-feedback-label="Game plan' src/pages/game_plan.rs` exits 0
    - Source: `grep -q 'data-feedback-label="Post game' src/pages/post_game.rs` exits 0
    - Source: combined grep across all 7 pages should now report >= 7 labels (one per page minimum). Check: `grep -rc 'data-feedback-label' src/pages/ | awk -F: '{sum+=$2} END{exit (sum>=15)?0:1}'` (expect >= 15 total across all 7 pages — 3-4 per file)
    - **Positive U+2192 codepoint check (per page; replaces the prior negative `->` check)** — each of the 3 pages MUST contain at least one `data-feedback-label="..."` that includes a literal U+2192 RIGHTWARDS ARROW character:
      - `grep -qE 'data-feedback-label="[^"]*→[^"]*"' src/pages/champion_pool.rs` exits 0
      - `grep -qE 'data-feedback-label="[^"]*→[^"]*"' src/pages/game_plan.rs` exits 0
      - `grep -qE 'data-feedback-label="[^"]*→[^"]*"' src/pages/post_game.rs` exits 0
    - CLI: `cargo check --features ssr` exits 0
    - CLI: `cargo check --features hydrate --target wasm32-unknown-unknown` exits 0
    - Regression: `cargo test --features ssr --lib` exits 0
  </acceptance_criteria>
  <done>
    All 7 first-priority pages from D-06.1 carry `data-feedback-label` attributes. Compilation clean on both targets. No unit-test regressions. Per-page positive U+2192 grep passes for every page.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create e2e/tests/bug-report.spec.ts Playwright spec with 5 tests + a hard negative-content gate</name>
  <files>e2e/tests/bug-report.spec.ts</files>
  <read_first>
    - e2e/tests/audit-draft.spec.ts (lines 1-37) — analog: fixture import, captureErrors+filterRealErrors helpers, navigateTo helper, waitForTimeout(500) settle pattern
    - e2e/tests/fixtures.ts (focus on lines 30-100 — authedPage fixture and any helpers it exposes)
    - e2e/tests/helpers.ts (if exists; otherwise reference imports from audit-draft.spec.ts to see what's available)
    - e2e/tests/hydration-no-panic.spec.ts (full file) — analog: regression-suite shape; this plan must not regress it
    - .planning/phases/19-bug-report-widget/19-RESEARCH.md (lines 1112-1163) — exact spec body to adapt (5 tests)
    - .planning/phases/19-bug-report-widget/19-PATTERNS.md (lines 136-179) — gotchas: waitForTimeout(500) after navigation, filterRealErrors on 404 Tailwind import, hidden-on-public uses bare `page` fixture
    - .claude/rules/wasm-patterns.md (rule 48 — auth fixture redirects to /team/dashboard; rule 56 — WASM settle delay after navigation)
  </read_first>
  <behavior>
    Five core tests grouped under `test.describe("bug-report widget", ...)` PLUS a hard negative-content gate:

    1. `visible on auth-required pages` — fixture: `authedPage`. Steps: `goto("/draft")`; `waitForTimeout(500)`; assert locator `'button[aria-label="Report a bug or wishlist item"]'` is visible.

    2. `hidden on public pages` — fixture: bare `page` (NOT authedPage; this test must NOT trigger the auth flow). Steps: `goto("/auth/login")`; `waitForTimeout(500)`; assert the same locator has count 0.

    3. `select mode captures the nearest data-feedback-label` — fixture: `authedPage`. Steps: `goto("/draft")`; `waitForTimeout(500)`; click the Report floating button; click an element with `[data-feedback-label*="Draft"]` (use any draft-prefixed label from Task 1 — `Draft → Blue side` is a good target); assert a `[role="dialog"]` is visible AND contains the captured label text.

    4. `esc cancels select mode without opening modal` — fixture: `authedPage`. Steps: `goto("/draft")`; `waitForTimeout(500)`; click the Report floating button; `keyboard.press("Escape")`; `waitForTimeout(200)`; assert `[role="dialog"]` count is 0 AND the page body's `cursor` style is back to default (best-effort — Playwright can read computed style via `evaluate`, but for v1 just assert the dialog is not open).

    5. `submit persists and shows toast` — fixture: `authedPage`. Steps: `goto("/draft")`; `waitForTimeout(500)`; click the Report floating button; click `[data-feedback-label*="Draft"]`; `fill('textarea', "test report from e2e")`; click the "Bug" radio (locator `'button:has-text("Bug")'` per the existing widget markup at src/components/bug_report_widget.rs:139-148); click `'button:has-text("Submit")'`; assert toast `'text=/Your report is in/'` becomes visible within 3000 ms; assert dialog count drops to 0.

    6. (REQUIRED per revision Warning 9) `widget DOM has no forbidden characters` — fixture: `authedPage`. Steps: `goto("/draft")`; `waitForTimeout(500)`; click Report; click `[data-feedback-label*="Draft"]`; read the modal HTML via `await page.locator('[role="dialog"]').innerHTML()` (or fall back to `page.content()` if the dialog isn't yet open). Hard assertions: `expect(html).not.toContain("🐛")` AND `expect(html).not.toContain("🎉")` AND `expect(html).not.toContain("Thanks!")` (exclamation form — period form is acceptable). This is belt-and-suspenders on top of plan 19-02's source-code grep — catches accidental UI regressions from later phases.

    Each test should also call `const errors = captureErrors(page)` at the start and assert `filterRealErrors(errors).toHaveLength(0)` at the end (preventing silent WASM panics — same pattern as audit-draft.spec.ts).
  </behavior>
  <action>
    Create `e2e/tests/bug-report.spec.ts` with the 6 tests per `<behavior>` (5 functional + 1 negative-content gate). Use the import shape from audit-draft.spec.ts: `import { test, expect } from "./fixtures";` and `import { captureErrors, filterRealErrors } from "./helpers";` (verify helper paths first by reading e2e/tests/audit-draft.spec.ts; adjust if the helpers live elsewhere). Wrap all tests in `test.describe("bug-report widget", () => { ... })`.

    For test 5 (submit persists), the spec asserts the toast and modal-close — NOT direct DB persistence — because `list_bug_reports` returns Forbidden in v1 (researcher resolution). Deep persistence verification is left to the manual checkpoint (Task 4) which inspects the inbox file after restart.

    For test 6 (negative-content gate), the assertion MUST use the exact pattern `expect(...).not.toContain("🐛")` (and the equivalents for 🎉 and `Thanks!`). The acceptance criterion below greps the spec source for the literal call signature — see SC bullet "Source: negative-content assertion present" below. This is the hard SC-7-regression gate.

    Use the locator strings from the existing widget markup:
    - Floating button: `'button[aria-label="Report a bug or wishlist item"]'`
    - Bug radio (currently a button styled as a toggle): `'button:has-text("Bug")'`
    - Submit button: `'button:has-text("Submit")'`
    - Modal dialog: `'[role="dialog"]'`
    - Toast (success kind via ToastContext from src/components/ui.rs): `'text=/Your report is in/'` (regex on partial match for resilience against minor wording adjustments)

    For the "hidden on public pages" test, use `await page.goto("/auth/login")` then `await expect(page.locator('button[aria-label="Report a bug or wishlist item"]')).toHaveCount(0)`. NO call to `authedPage` in that test — the test argument is bare `page`.
  </action>
  <verify>
    <automated>cd e2e && npx playwright test bug-report.spec.ts --reporter=line</automated>
  </verify>
  <acceptance_criteria>
    - Filesystem: `test -f e2e/tests/bug-report.spec.ts` exits 0
    - Source: `grep -q 'import { test, expect } from "./fixtures"' e2e/tests/bug-report.spec.ts` exits 0 (fixture import — leptos auth pattern)
    - Source: `grep -q 'authedPage' e2e/tests/bug-report.spec.ts` exits 0 (uses the auth fixture for protected-page tests)
    - Source: `grep -q "test.describe" e2e/tests/bug-report.spec.ts` exits 0 (tests are grouped)
    - Source: `grep -cE 'test\(' e2e/tests/bug-report.spec.ts` returns >= 6 (5 functional + 1 negative-content gate)
    - Source: `grep -q 'visible on auth' e2e/tests/bug-report.spec.ts` exits 0 (covers SC-2 verification)
    - Source: `grep -q 'hidden on public' e2e/tests/bug-report.spec.ts` exits 0 (covers auth-gate verification)
    - Source: `grep -q 'select mode' e2e/tests/bug-report.spec.ts` exits 0 (covers SC-3)
    - Source: `grep -q 'Escape' e2e/tests/bug-report.spec.ts` exits 0 (covers esc-cancel)
    - Source: `grep -q 'submit' e2e/tests/bug-report.spec.ts` exits 0 (covers SC-4 partially)
    - Source: `grep -q 'waitForTimeout(500)' e2e/tests/bug-report.spec.ts` exits 0 (wasm rule 56 — settle delay after navigation)
    - Source: `grep -q "filterRealErrors" e2e/tests/bug-report.spec.ts` exits 0 (filters harmless Tailwind 404 — wasm rule 47)
    - **Negative-content assertion present** (Warning 9 gate): `grep -qE 'expect\([^)]*\).not.toContain\("🐛"\)' e2e/tests/bug-report.spec.ts` exits 0. The spec MUST contain a hard `.not.toContain("🐛")` assertion against the widget DOM. The equivalents for `"🎉"` and `"Thanks!"` should also appear via additional `.not.toContain(...)` calls (sanity grep: `grep -c '\.not\.toContain' e2e/tests/bug-report.spec.ts` returns >= 3).
    - Test: `cd e2e && npx playwright test bug-report.spec.ts --reporter=line` exits 0 (all 6 tests pass)
    - Regression test: `cd e2e && npx playwright test hydration-no-panic.spec.ts --reporter=line` exits 0 (Phase 18.2 regression suite still green after the page-attribute rollout)
  </acceptance_criteria>
  <done>
    The spec file exists with 6 tests covering visible/hidden/select-mode/esc-cancel/submit/no-forbidden-characters. All tests pass against `cargo leptos watch`. The Phase 18.2 hydration-no-panic suite remains green. The negative-content gate enforces SC-7 at the DOM level.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human-verify the rollout on all 7 first-priority pages via agent-browser + verify inbox file after server restart (gap-page degraded path supported)</name>
  <what-built>
    Plan 19-04 added `data-feedback-label` attributes to 7 first-priority pages and a Playwright spec proving the widget works on /draft. This checkpoint smoke-checks the remaining 6 pages (solo, team/dashboard, stats, champion_pool, game_plan, post_game) and verifies the auto-export task wrote `.planning/INBOX/bug-reports.md` correctly after the e2e tests created a real report.

    Tasks 1 and 2 may have documented unavoidable coverage gaps (D-06.3: no suitable existing element on a particular page). The verifier has an explicit `approved-with-gaps` resume option for that case — see step 4 and the resume-signal below.
  </what-built>
  <how-to-verify>
    1. Restart dev server: kill any existing `cargo leptos watch` then start fresh. Wait for "Listening on" (use `./scripts/wait_for_server.sh 120`).
    2. Confirm the inbox file exists: `cat .planning/INBOX/bug-reports.md`. Expected: at minimum the YAML header with `total_open` showing the count of any reports created by the e2e suite (which submits one per `submit persists` test run). If `total_open == 0`, this is fine — the test may have been run against a different DB instance; proceed.
    3. Authenticate via agent-browser (`/auth/register?invite=E2E-TEST` or login if a session exists).
    4. For each of the 7 first-priority pages, navigate via agent-browser and verify the click-probe flow:
       - **/draft** → Click the floating Report button. Verify the cursor changes to crosshair AND the body tagged regions show an outline. Click on an element matching `[data-feedback-label*="Draft"]`. Verify the modal opens with that exact label visible in the "Element" field. Press Esc or Cancel to dismiss.
       - **/solo** → Same flow. Target `[data-feedback-label*="Solo dashboard"]`.
       - **/team/dashboard** → Same flow. Target `[data-feedback-label*="Team dashboard"]`.
       - **/stats** → Same flow. Target `[data-feedback-label*="Stats"]`.
       - **/champion-pool** → Same flow. Target `[data-feedback-label*="Champion pool"]`.
       - **/game-plan** → Same flow. Target `[data-feedback-label*="Game plan"]`.
       - **/post-game** → Same flow. Target `[data-feedback-label*="Post game"]`.

       **Gap-page degraded path (per revision Blocker 4):** For each page, if `19-04-SUMMARY.md` (or commit messages from Tasks 1+2) documents a coverage gap (i.e. no suitable element existed without inventing a wrapper per D-06.3), mark that page as `partial-coverage` and skip the click probe; do not block on gap-documented pages. The page-rollout grep in step 8 still requires the page to carry at least ONE `data-feedback-label` — only deeper section-level probes can be skipped under the gap path.
    5. On one of the non-gap pages (your choice), complete a full submit: type a description, pick a category, click Submit. Verify the toast appears with EXACT text "Thanks. Your report is in." (period, NOT exclamation; no emoji).
    6. Restart the server (kill `cargo leptos watch`, start again). Cat `.planning/INBOX/bug-reports.md`. Verify the new report appears, formatted per D-04.3 (YAML header with updated total_open, `## [bug|wishlist] {snippet} — {date}` heading, bullet list of fields, blockquoted description).
    7. Run the dark-pattern grep: `grep -v '^#' src/components/bug_report_widget.rs | ! grep -E '[!🐛🎉⭐💡]'` — confirms no exclamation or emoji in widget text (comment lines excluded so `// TODO Phase 22` style comments don't poison the grep).
    8. Run the page-rollout grep: `for f in src/pages/draft.rs src/pages/solo_dashboard.rs src/pages/team/dashboard.rs src/pages/stats.rs src/pages/champion_pool.rs src/pages/game_plan.rs src/pages/post_game.rs; do grep -q data-feedback-label "$f" || echo MISSING $f; done` — should print nothing (no MISSING lines). This gate applies even under the gap-path: every page must carry at least one label.
    9. Run the Phase 18.2 regression: `cd e2e && npx playwright test hydration-no-panic.spec.ts` — should pass.
    10. Run the full e2e: `cd e2e && npx playwright test bug-report.spec.ts` — should pass (6 tests including the negative-content gate).
    11. Run unit tests: `cargo test --features ssr --lib` — should report >=130 tests passing (121 baseline + plan 19-01's 4 + plan 19-03's 6 + any additional).
    12. (Optional but recommended) `cd e2e && npx playwright test` — full e2e suite should pass.

    Resume-signal options (per revision Blocker 4):
    - `approved` — all 7 pages click-probed successfully (no documented gaps) AND all auto/regression checks green.
    - `approved-with-gaps` — gaps documented in `19-04-SUMMARY.md` AND all non-gap pages click-probed successfully AND all auto/regression checks green. The verifier names which pages were `partial-coverage` in the resume message.
    - `needs-rework` — one or more non-gap pages failed the click probe, OR auto/regression checks failed, OR an undocumented gap was discovered (resolve via Task 1/2 follow-up, do not approve).
  </how-to-verify>
  <resume-signal>Type `approved` | `approved-with-gaps` | `needs-rework`. For `approved-with-gaps`, name each `partial-coverage` page and reference the relevant 19-04-SUMMARY.md section. For `needs-rework`, list specific failures with snapshot paths / grep output.</resume-signal>
</task>

</tasks>

<threat_model>
This plan adds HTML attributes and an e2e spec; it introduces no new server fns, no new DB access, no new auth boundaries. The threats addressed by Phase 19 (T-19-01..T-19-05) are mitigated by plans 19-01, 19-02, and 19-03. This plan is the verification gate for those mitigations:

| Threat ID | Verification in this plan |
|-----------|---------------------------|
| T-19-01 (category injection) | Negative-content gate test catches any UI regression introducing non-allowlisted categories; manual checkpoint confirms only "Bug" and "Wishlist" radios render |
| T-19-02 (empty description) | Manual checkpoint step 5 — attempting submit with empty textarea should keep modal open (server rejects); the e2e suite does NOT need a dedicated test (covered by plan 19-01 unit test) |
| T-19-03 (write-failure tolerance) | Manual checkpoint step 6 — server restart writes the inbox file; if the file fails to write (e.g. permissions), the server still starts. Plan 19-03 unit-tests this |
| T-19-04 (prompt-injection warning) | Manual checkpoint step 6 — the inbox file's blockquote-prefixed report rendering visually flags user content; CLAUDE.md warning is verified by grep in plan 19-03 |
| T-19-05 (admin gate on list_bug_reports) | Out of scope for e2e (the server-fn returns Forbidden; no UI consumer in v1). Plan 19-02 source assertion confirms |
</threat_model>

<verification>
After all tasks land and the manual checkpoint passes:

```bash
# Page-rollout coverage gate (D-06.1)
for f in src/pages/draft.rs src/pages/solo_dashboard.rs src/pages/team/dashboard.rs src/pages/stats.rs src/pages/champion_pool.rs src/pages/game_plan.rs src/pages/post_game.rs; do
  grep -q 'data-feedback-label' "$f" || { echo "MISSING in $f"; exit 1; }
done

# Per-page U+2192 codepoint gate (replaces prior negative `->` check)
for f in src/pages/draft.rs src/pages/solo_dashboard.rs src/pages/team/dashboard.rs src/pages/stats.rs src/pages/champion_pool.rs src/pages/game_plan.rs src/pages/post_game.rs; do
  grep -qE 'data-feedback-label="[^"]*→[^"]*"' "$f" || { echo "U+2192 missing in $f"; exit 1; }
done

# Compile both targets
cargo check --features ssr
cargo check --features hydrate --target wasm32-unknown-unknown

# Unit regression
cargo test --features ssr --lib

# E2e — new spec must pass (5 functional + 1 negative-content gate)
cd e2e && npx playwright test bug-report.spec.ts

# E2e — Phase 18.2 regression suite must remain green
cd e2e && npx playwright test hydration-no-panic.spec.ts

# Dark-pattern audit (G-10) — comments excluded
grep -v '^#' src/components/bug_report_widget.rs | ! grep -E '[!🐛🎉⭐💡]'

# Negative-content spec gate (Warning 9)
grep -qE 'expect\([^)]*\).not.toContain\("🐛"\)' e2e/tests/bug-report.spec.ts

# Documentation gate
grep -q '.planning/INBOX/bug-reports.md' CLAUDE.md
test -f .planning/INBOX/.gitkeep

# D-09.1 Phase 22 transparency handoff
test -f .planning/phases/19-bug-report-widget/19-HANDOFF-TO-22.md
```

All commands exit 0.
</verification>

<success_criteria>
- SC-3 (label-driven capture works): Manual checkpoint step 4 confirms select-mode → click tagged element → modal opens with the correct label on ALL non-gap pages; gap-documented pages use the `partial-coverage` path. E2e test `select mode captures the nearest data-feedback-label` automates this on `/draft`.
- SC-7 (no dark patterns regression): Negative-content gate in the e2e spec (`expect(...).not.toContain("🐛")` and equivalents) + the dark-pattern grep gate + manual G-10 audit during the checkpoint all pass.
- Implicit goal: Phase 18.2 hydration regression suite remains green — adding plain string attributes does not change the SSR/hydrate tree topology, so no hydration panics regress.
</success_criteria>

<output>
After completion, create `.planning/phases/19-bug-report-widget/19-04-SUMMARY.md` with:
- Per-page count of `data-feedback-label` attributes (grep -c per file)
- Any pages where a planned label could not be applied without wrapper invention (documented gap — required so the manual-checkpoint `approved-with-gaps` resume path can cite the SUMMARY)
- E2e spec file path + count of passing tests (including the negative-content gate)
- Manual checkpoint outcome (`approved` | `approved-with-gaps` | `needs-rework`; list any `partial-coverage` pages)
- Inbox-file output snippet from step 6
- Confirmation of hydration-no-panic regression status (green/red)
</output>
</output>

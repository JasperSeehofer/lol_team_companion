---
phase: 19
plan: 02
type: execute
wave: 2
depends_on: [19-01]
files_modified:
  - src/components/bug_report_widget.rs
  - input.css
autonomous: false
requirements: []
success_criteria: [SC-2, SC-3, SC-4, SC-7]
threats_addressed: [T-19-01, T-19-02, T-19-05]
tags: [leptos, wasm, ui, server-fn, dark-pattern-audit]

must_haves:
  truths:
    - "Cross-cutting: zero `.unwrap()` in event handlers or any WASM-reachable code (rule 35). Every `web_sys::*` call uses `if let Some/Ok` or `let Some(...) = ... else { return };`"
    - "Cross-cutting: no dark patterns per G-10. Button label is literally 'Report'. Toast string is literally 'Thanks. Your report is in.' (period, NOT exclamation — researcher resolution). No emoji, no NPS prompt, no star widget, no confirmshaming on Cancel"
    - "Plain `<button>` ONLY — NEVER `<Btn>` from src/components/region/. Phase 18.2 hydration lesson: region primitives inherit hydration risk. Existing stub uses plain `<button class=...>`; preserve"
    - "`#[server]` fn `submit_bug_report` MUST be defined BEFORE `#[component] pub fn BugReportWidget` in the same file (leptos rule 34)"
    - "`list_bug_reports` server-fn returns `ServerFnError::new(\"Forbidden\")` for ALL callers in v1 (researcher resolution — admin gate deferred to Phase 22); the implementation MUST carry a `// TODO Phase 22` comment so the gap is greppable"
    - "Toast is dispatched as `toast.show.run((ToastKind::Success, \"Thanks. Your report is in.\".into()))` — `ToastContext.show` is a `Callback<(ToastKind, String)>` (see src/components/ui.rs:53-57) and src/pages/action_items.rs:220 shows the canonical call shape"
  artifacts:
    - path: src/components/bug_report_widget.rs
      provides: "Extended widget with WidgetState enum (Idle/Selecting/Editing), select-mode start/exit closures, global click+keydown capture via StoredValue<Option<Closure>>, real submit_bug_report server-fn call replacing console.log stub, toast via ToastContext"
      contains: "enum WidgetState"
      contains: "submit_bug_report"
      contains: "Thanks. Your report is in."
      contains: "TODO Phase 22"
      min_lines: 350
    - path: input.css
      provides: "Single CSS rule lighting up tagged elements while select-mode is active"
      contains: "[data-feedback-selecting] [data-feedback-label]"
  key_links:
    - from: src/components/bug_report_widget.rs (`spawn_local` Submit handler)
      to: src/components/bug_report_widget.rs (`#[server] submit_bug_report` fn)
      via: "spawn_local(async move { submit_bug_report(...).await })"
      pattern: "spawn_local\\(async move \\{[\\s\\S]*submit_bug_report\\("
    - from: src/components/bug_report_widget.rs (`submit_bug_report` body)
      to: src/server/db.rs (`create_bug_report`)
      via: "db::create_bug_report(&surreal, &user.id, page_url, element_label, description, category, vw, vh).await"
      pattern: "db::create_bug_report"
    - from: Click-capture closure
      to: Element identification
      via: "`event.target.dyn_into::<Element>().ok()?.closest(\"[data-feedback-label]\")` — NEVER querySelector"
      pattern: "closest\\(\"\\[data-feedback-label\\]\"\\)"
    - from: select-mode start
      to: input.css `[data-feedback-selecting] [data-feedback-label]` rule
      via: "Setting `data-feedback-selecting` on `<html>` toggles the CSS outline on every tagged element via a single O(1) attribute toggle"
      pattern: "set_attribute\\(\"data-feedback-selecting\""
    - from: src/components/bug_report_widget.rs (toast call site)
      to: src/components/ui.rs (`ToastContext.show: Callback<(ToastKind, String)>`)
      via: "`toast.show.run((ToastKind::Success, \"Thanks. Your report is in.\".into()))` — Callback is invoked via `.run((kind, msg))`, NOT `toast.show(kind, msg)`"
      pattern: "toast\\.show\\.run\\(\\(ToastKind::Success"
---

<phase_invariants>
<!-- Cross-cutting truths recorded in 19-01-PLAN.md / 19-03-PLAN.md / 19-04-PLAN.md or external rule files. -->
<!-- They are invariants of the project, not deliverables of this plan, but must NOT regress here. -->

- Cross-cutting: NEVER use a CSS selector for element identification; `data-feedback-label` only (wasm rule 56 spirit). This plan's click-capture closure uses `closest("[data-feedback-label]")`.
- Cross-cutting: semantic Tailwind tokens only (bg-base/surface/elevated, text-*, border-*). Existing Phase 17 stub already complies — preserve.
- Cross-cutting: owned-String `.bind()` discipline only matters server-side (plan 19-01); not applicable to this plan.
- Cross-cutting: `tower-sessions` stays pinned at 0.14 to match axum-login. Do not bump.
- Cross-cutting: tests run via `cargo test --features ssr --lib` ONLY; e2e in plan 19-04.
- File-scoped: `#[cfg(feature = "hydrate")]` gates every `web_sys::*` call. SSR target must compile clean with zero `web_sys` references.
- File-scoped: `use` statements for SSR-only crates (`crate::server::auth::AuthSession`, `surrealdb::*`, `crate::server::db`) MUST live INSIDE the `#[server]` fn body, NOT at file top (leptos rule 9).
- File-scoped: `StoredValue<Option<Closure<...>>>` is the carrier for the click-capture and esc-capture closures (leptos rule 22 — `Closure` is `!Send`; `StoredValue` is documented for non-reactive shared closures).
- File-scoped: Click-capture listener MUST be attached in CAPTURE phase (third arg = true on `add_event_listener_with_callback_and_add_event_listener_options`) so the widget intercepts before bubble-phase `on:click` handlers on tagged elements.
- File-scoped: Click-capture closure MUST call `.prevent_default()` + `.stop_propagation()` + `.stop_immediate_propagation()` on the captured event.
- File-scoped: Click-capture closure MUST handle `closest()`'s `Result<Option<Element>, JsValue>` with two unwrap levels: `let Ok(Some(tagged)) = el.closest("[data-feedback-label]") else { return; };`.
- File-scoped: Exit/cleanup path MUST `remove_event_listener_with_callback` AND restore body cursor AND remove `data-feedback-selecting` from `<html>`. Runs in every exit branch (Esc, click-completion, modal-Submit, modal-Cancel, click-outside).
- File-scoped: Existing auth+pathname gate at the BugReportWidget outer component is preserved unchanged.
</phase_invariants>

<objective>
Extend the existing Phase 17 widget stub (`src/components/bug_report_widget.rs`) into a fully functional bug-report capture flow. Replace the `console.log` Submit stub with a real `submit_bug_report` server-fn call wired to `db::create_bug_report` from plan 19-01. Implement the WidgetState machine (Idle/Selecting/Editing) with a global click-capture listener that resolves the clicked element's `data-feedback-label` via `closest()`. Add an Esc-cancel keydown listener. Show the post-submit toast via the existing `ToastContext` (dispatched as `toast.show.run((kind, msg))` — `ToastContext.show` is a `Callback`, NOT a direct method call). Add one CSS rule in `input.css` that outlines tagged elements while select-mode is active.

Purpose: Deliver SC-2 (floating button visible on authenticated pages — already shipped by Phase 17, preserve), SC-3 (select-mode click capture opens modal with the captured label), SC-4 (submit persists row + closes modal + toasts), SC-7 (no dark patterns — neutral language, no exclamation, no emoji, no confirmshaming).

Output: A fully wired widget with three deterministic states, clean listener teardown on every exit path, and a single CSS rule. After this plan, plan 19-04 can ship the page-attribute rollout and e2e spec against the wired widget.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/19-bug-report-widget/19-CONTEXT.md
@.planning/phases/19-bug-report-widget/19-RESEARCH.md
@.planning/phases/19-bug-report-widget/19-PATTERNS.md
@.planning/phases/19-bug-report-widget/19-VALIDATION.md
@.planning/phases/19-bug-report-widget/19-01-SUMMARY.md
@CLAUDE.md
@.claude/rules/leptos-patterns.md
@.claude/rules/wasm-patterns.md

<interfaces>
<!-- Lifted from src/components/bug_report_widget.rs (current Phase 17 stub) — 203 lines -->
<!-- Existing signals to KEEP unchanged: -->

```rust
// Outer component (lines 25-71):
#[component]
pub fn BugReportWidget() -> impl IntoView {
    let user = Resource::new(|| (), |_| get_current_user());
    let modal_open = RwSignal::new(false);                          // KEEP (used by Inner)
    let report_kind = RwSignal::new("bug".to_string());             // KEEP
    let report_text = RwSignal::new(String::new());                 // KEEP
    let element_label = RwSignal::new("(no element selected)".to_string()); // KEEP

    let pathname = RwSignal::new(String::new());                    // KEEP
    Effect::new(move |_| {
        #[cfg(feature = "hydrate")]
        if let Some(window) = web_sys::window() {
            if let Ok(path) = window.location().pathname() {
                pathname.set(path);
            }
        }
    });

    // HIDDEN_PREFIXES, is_authed, widget_visible — KEEP unchanged.
    // <Show when=widget_visible> ... <BugReportWidgetInner /> ... </Show> — KEEP shape, add new prop signals.
}
```

<!-- Existing call to db::create_bug_report (from plan 19-01 SUMMARY): -->
```rust
// Server-side API now available in src/server/db.rs:
pub async fn create_bug_report(
    db: &Surreal<Db>,
    user_id: &str,
    page_url: String,
    element_label: String,
    description: String,
    category: String,
    viewport_w: Option<i32>,
    viewport_h: Option<i32>,
) -> DbResult<()>;
```

<!-- Lifted from src/components/nav.rs:256-275 (the keydown listener analog): -->
```rust
#[cfg(feature = "hydrate")]
{
    let close_all_esc = close_all.clone();
    Effect::new(move |_| {
        use wasm_bindgen::closure::Closure;
        use wasm_bindgen::JsCast;
        let cb = Closure::<dyn Fn(web_sys::KeyboardEvent)>::new(
            move |ev: web_sys::KeyboardEvent| {
                if ev.key() == "Escape" {
                    close_all_esc();
                }
            },
        );
        if let Some(window) = web_sys::window() {
            let _ = window.add_event_listener_with_callback("keydown", cb.as_ref().unchecked_ref());
        }
        cb.forget();  // NOTE: this plan does NOT use forget() — see truths
    });
}
```

<!-- ToastContext API (canonical — lifted from src/components/ui.rs:53-57 + src/pages/action_items.rs:220) -->
<!-- `ToastContext.show` is a `Callback<(ToastKind, String)>`. To dispatch a toast, call `.run((kind, message.into()))` -->
<!-- on the callback. Do NOT call `toast.show(kind, msg)` as if it were a method — that signature does not exist. -->
```rust
// Definition (src/components/ui.rs:53-57):
#[derive(Clone, Copy)]
pub struct ToastContext {
    pub show: Callback<(ToastKind, String)>,
}

// Canonical caller (src/pages/action_items.rs:220):
let toast = use_context::<ToastContext>().expect("ToastProvider must be mounted");
toast.show.run((ToastKind::Success, "Thanks. Your report is in.".into()));
```

<!-- Required top-of-file import for the widget file to use ToastContext + ToastKind: -->
```rust
use crate::components::ui::{ToastContext, ToastKind};
```

<!-- Server-fn signature to define (leptos rule 34: BEFORE #[component] in the same file): -->
```rust
#[server]
pub async fn submit_bug_report(
    page_url: String,
    element_label: String,
    description: String,
    category: String,
    viewport_w: Option<i32>,
    viewport_h: Option<i32>,
) -> Result<(), ServerFnError>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add submit_bug_report + list_bug_reports server fns at the top of bug_report_widget.rs</name>
  <files>src/components/bug_report_widget.rs</files>
  <read_first>
    - src/components/bug_report_widget.rs (current 203 lines) — preserve every existing signal and gating logic
    - src/pages/action_items.rs (lines 37-65, `create_action_item_fn`) — analog: `#[server]` boilerplate with auth + DB context extraction, owned-String args, error mapping
    - .planning/phases/19-bug-report-widget/19-RESEARCH.md (lines 162-237) — full `submit_bug_report` + `list_bug_reports` body to copy
    - .planning/phases/19-01-SUMMARY.md (once available) — confirms the exact `create_bug_report` signature shipped by plan 19-01
    - .claude/rules/leptos-patterns.md (rules 9, 11, 12, 32, 34, 44, 57) — SSR-only imports inside fn body, use_context for DB, AuthSession non-mut, Serialize+Deserialize args, fn ordering before component, hash-suffixed URLs
  </read_first>
  <action>
    Open src/components/bug_report_widget.rs. Insert BEFORE the existing `#[component] pub fn BugReportWidget()` declaration (line 25) the two `#[server]` functions per the `<interfaces>` block. The body of `submit_bug_report`:
    1. Inside the fn body (NOT at file top per leptos rule 9), `use crate::server::auth::AuthSession; use crate::server::db; use std::sync::Arc; use surrealdb::{engine::local::Db, Surreal}; use leptos::prelude::use_context;`
    2. `let auth: AuthSession = leptos_axum::extract().await?;` (NO `mut` — we don't call `auth.login()`; leptos rule 12)
    3. `let user = auth.user.ok_or_else(|| ServerFnError::new("Not logged in"))?;`
    4. `let surreal = use_context::<Arc<Surreal<Db>>>().ok_or_else(|| ServerFnError::new("No DB context"))?;` (leptos rule 11)
    5. Server-side validation (T-19-01 + T-19-02 mitigations duplicated at the server-fn layer per defense-in-depth): `let description = description.trim().to_string(); if description.is_empty() { return Err(ServerFnError::new("Description is required")); } if description.len() > 4000 { return Err(ServerFnError::new("Description exceeds 4000 characters")); } if category != "bug" && category != "wishlist" { return Err(ServerFnError::new("Invalid category")); }`
    6. `db::create_bug_report(&surreal, &user.id, page_url, element_label, description, category, viewport_w, viewport_h).await.map_err(|e| ServerFnError::new(e.to_string()))`

    For `list_bug_reports`: define the fn with signature `pub async fn list_bug_reports(_status: Option<String>) -> Result<Vec<crate::models::bug_report::BugReport>, ServerFnError>`. The body returns `Err(ServerFnError::new("Forbidden"))` unconditionally with a `// TODO Phase 22: replace with admin gate once role field exists on user` comment on the line IMMEDIATELY ABOVE the `Err(...)` return so it is greppable by the acceptance criterion below. T-19-05 (researcher resolution). The `_status` arg is unused but kept in the signature so a future admin UI can call it without breaking changes.

    Do NOT add file-top `use` statements for `crate::server::*` or `surrealdb::*` — every SSR-only import lives INSIDE the `#[server]` body. The file's existing top-level `use leptos::ev; use leptos::prelude::*;` etc. are preserved.
  </action>
  <verify>
    <automated>cargo check --features ssr</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -q 'pub async fn submit_bug_report' src/components/bug_report_widget.rs` exits 0
    - Source: `grep -q 'pub async fn list_bug_reports' src/components/bug_report_widget.rs` exits 0
    - Source: line number of `pub async fn submit_bug_report` is LESS than line number of `pub fn BugReportWidget` (leptos rule 34 — fn defined before component). Check: `awk '/pub async fn submit_bug_report/{a=NR} /pub fn BugReportWidget/{b=NR} END{exit (a<b)?0:1}' src/components/bug_report_widget.rs`
    - Source: `! grep -E '^use (crate::server|surrealdb)' src/components/bug_report_widget.rs` (SSR-only imports must NOT be at file top — leptos rule 9)
    - Source: `grep -A30 'pub async fn submit_bug_report' src/components/bug_report_widget.rs | grep -q 'description.trim()'` exits 0 (T-19-02 server-fn guard)
    - Source: `grep -A30 'pub async fn submit_bug_report' src/components/bug_report_widget.rs | grep -q 'category != "bug" && category != "wishlist"'` exits 0 (T-19-01 server-fn guard)
    - Source: `grep -A10 'pub async fn list_bug_reports' src/components/bug_report_widget.rs | grep -q 'Forbidden'` exits 0 (T-19-05 mitigation)
    - Source: `grep -q 'TODO Phase 22' src/components/bug_report_widget.rs` exits 0 (must_haves truth: greppable Phase 22 handoff marker)
    - CLI: `cargo check --features ssr` exits 0
    - CLI: `cargo check --features hydrate --target wasm32-unknown-unknown` exits 0 (hydrate target compiles — server fn stubs do not leak SSR types into WASM)
  </acceptance_criteria>
  <done>
    Both server fns are defined at the top of the file (above `#[component] pub fn BugReportWidget`), follow the leptos-rule-9 (SSR-only imports inside body) pattern, and `submit_bug_report` enforces the T-19-01 and T-19-02 validation guards. `list_bug_reports` returns `Forbidden` for all v1 callers and carries a greppable `TODO Phase 22` marker.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend widget Inner component with WidgetState machine, select-mode capture, esc-cancel, and real submit wiring</name>
  <files>src/components/bug_report_widget.rs, input.css</files>
  <read_first>
    - src/components/bug_report_widget.rs (current 203 lines, especially lines 74-202 — the Inner component to extend)
    - src/components/nav.rs (lines 256-275) — analog: global keydown listener with `Closure::<dyn Fn(KeyboardEvent)>` + `add_event_listener_with_callback`. Adapt to MouseEvent + click event + capture phase
    - src/components/ui.rs (lines 40-100) — analog: `ToastContext { pub show: Callback<(ToastKind, String)> }`. Confirms the toast is dispatched via `toast.show.run((kind, msg))`, NOT `toast.show(kind, msg)`.
    - src/pages/action_items.rs (line 220) — analog: canonical call shape `toast.show.run((ToastKind::Success, "Action item added".into()))`.
    - src/app.rs (lines 130-170) — confirms `<ToastProvider>` is mounted at the top level, so `use_context::<ToastContext>()` resolves
    - input.css (whole file) — confirm Tailwind v4 `@theme` block + any existing custom rules; identify where the new `[data-feedback-selecting]` rule belongs (near the bottom, after any existing custom utility blocks)
    - .planning/phases/19-bug-report-widget/19-RESEARCH.md (lines 493-704) — full state machine + listener attach + cleanup code to copy
    - .planning/phases/19-bug-report-widget/19-PATTERNS.md (lines 187-274) — gotchas list (no .unwrap, capture phase, two-level closest() unwrap, StoredValue not RwSignal, #[allow(unused_variables)] on hydrate-only signals, plain <button> not <Btn>, #[cfg(feature = "hydrate")] gates)
    - .claude/rules/wasm-patterns.md (rules 35, 42, 43) — no .unwrap in WASM, debounce/cancellable timers (related shape), #[allow(unused_variables)] on hydrate-only signals
    - .claude/rules/leptos-patterns.md (rules 18, 22, 24) — clone before multiple closures, StoredValue for shared closures, spawn_local for async event handlers
  </read_first>
  <behavior>
    State machine on the Inner component:
    - Replace the loose `modal_open` toggle with a single `widget_state: RwSignal<WidgetState>` driving the three modes. Existing `report_kind`, `report_text`, `element_label` signals are kept loose.
    - From Idle → Selecting via "Select element" button (new) on the floating widget surface or replacing the bare "open modal" click. From Selecting → Editing via the global click-capture handler resolving a `data-feedback-label`. From Editing → Idle via Submit success, Cancel, Esc, or click-outside.
    - From Selecting → Idle via Esc-cancel OR by clicking on an UNTAGGED element with a "forgiving" UX (researcher recommendation 19-RESEARCH.md line 588: leave select-mode active so the user can try again). Implement: if `closest("[data-feedback-label]")` returns `Ok(None)`, just `return;` from the closure WITHOUT exiting select mode.
    - From any state → Idle when submit completes (success or error): toast on success only, modal closes on success only. On error: keep modal open, surface the error message inline in the modal (use the existing `StatusMessage` from `src/components/ui.rs` if present; otherwise a plain `<p class="text-red-500 text-sm">` styled with `text-error` semantic token if defined, else fallback to `text-red-500` literal).

    Click-capture closure behavior:
    - Attached on `<window>` with `addEventListener('click', cb, { capture: true })` so it fires in capture phase BEFORE bubble-phase `on:click` handlers on tagged elements.
    - On fire: `ev.prevent_default(); ev.stop_propagation(); ev.stop_immediate_propagation();` then walk up via `Element::closest("[data-feedback-label]")`. If `Ok(Some(tagged))`, read the `data-feedback-label` attribute (fallback `"(unlabeled)"` if missing), set `element_label.set(label)`, set `widget_state.set(WidgetState::Editing)`, call `exit_select_mode()` to remove the listener + clear body cursor + clear `<html data-feedback-selecting>`.
    - If `Ok(None)`: just `return` — user clicked an untagged area, leave select mode active.
    - If `Err(...)`: same as None — `return`, no panic.

    Esc-cancel closure:
    - Attached on `<window>` with `addEventListener('keydown', cb)` (bubble phase OK for Esc).
    - On Escape: call `exit_select_mode()` and set `widget_state.set(WidgetState::Idle)`.

    Submit handler:
    - `spawn_local(async move { ... })` calling `submit_bug_report(page_url, label, text, kind, vw, vh).await`. On success: dispatch the toast via `toast.show.run((ToastKind::Success, "Thanks. Your report is in.".into()))`, `widget_state.set(WidgetState::Idle)`, reset `report_text.set(String::new())`, reset `element_label.set("(no element selected)".into())`. On error: set an inline error signal (`RwSignal<Option<String>>`) that renders inside the modal; do NOT close the modal.
  </behavior>
  <action>
    Open src/components/bug_report_widget.rs. Implement the WidgetState machine and select-mode wiring. Required identifiers:

    0. Add a top-of-file import: `use crate::components::ui::{ToastContext, ToastKind};` so `ToastContext` resolves in the Inner component (both types are shared SSR+WASM — safe at file top). Verify via the acceptance criterion below.

    1. Add at file top (top-level, NOT inside any fn): `#[derive(Clone, Copy, Debug, PartialEq)] enum WidgetState { Idle, Selecting, Editing }`. (`Copy` is critical so the enum can be passed through `RwSignal` reads without `.clone()` everywhere.)

    2. In the outer `BugReportWidget` component (line ~25): add `let widget_state = RwSignal::new(WidgetState::Idle);` alongside the existing signals. The existing `modal_open: RwSignal<bool>` is REPLACED — every `modal_open.get()` becomes `matches!(widget_state.get(), WidgetState::Editing)` and every `modal_open.set(true/false)` becomes the appropriate `widget_state.set(...)`. Update the `BugReportWidgetInner` props accordingly. Keep `report_kind`, `report_text`, `element_label` as loose signals.

    3. Add an error signal: `let submit_error: RwSignal<Option<String>> = RwSignal::new(None);`. Pass it as a prop to `BugReportWidgetInner`.

    4. In `BugReportWidgetInner`, declare two `StoredValue<Option<Closure<...>>>` (leptos rule 22) — one for the click-capture closure (`Closure<dyn Fn(web_sys::MouseEvent)>`), one for the esc-cancel closure (`Closure<dyn Fn(web_sys::KeyboardEvent)>`). Annotate both with `#[allow(unused_variables)]` (wasm rule 43) since they're only used inside `#[cfg(feature = "hydrate")]` blocks.

    5. Define a `start_select_mode` closure that: sets `widget_state.set(WidgetState::Selecting)`. Inside `#[cfg(feature = "hydrate")]`: gets the document body via `web_sys::window().and_then(|w| w.document()).and_then(|d| d.body())` (no `.unwrap()` — `if let Some(body) = ...` or `let Some(body) = ... else { return; };`). Sets `body.style().set_property("cursor", "crosshair")` (discard the inner Result with `let _ =`). Sets `data-feedback-selecting` attribute on `document.document_element()` (use `el.set_attribute("data-feedback-selecting", "")` — presence is what matters; the researcher noted "any non-empty value" works but the CSS uses attribute-presence selector `[data-feedback-selecting]` so an empty string also matches). Builds the click-capture `Closure::<dyn Fn(MouseEvent)>::new(...)` per the `<behavior>` block. Attaches via `window.add_event_listener_with_callback_and_add_event_listener_options("click", cb.as_ref().unchecked_ref(), &opts)` where `opts` is `web_sys::AddEventListenerOptions::new()` with `.set_capture(true)`. Stores the closure in the StoredValue slot via `click_capture_handle.set_value(Some(cb))`. DO NOT call `cb.forget()` — that prevents the cleanup-time `remove_event_listener` from finding the closure. Similarly builds and attaches the esc-cancel closure on "keydown" (bubble phase fine).

    6. Define an `exit_select_mode` closure that: inside `#[cfg(feature = "hydrate")]`, takes both closures out of their StoredValue slots via `.update_value(|slot| { if let Some(cb) = slot.take() { let _ = win.remove_event_listener_with_callback(...); } })`. Removes `body.style().remove_property("cursor")` and the document-element attribute via `el.remove_attribute("data-feedback-selecting")`. The Closure's `Drop` implementation frees the JS shim when it falls out of scope at end of the `update_value` block.

    7. Replace the existing floating button's `on:click=move |_| modal_open.set(true)` (line 104). New behavior: clicking the floating button calls `start_select_mode()`. (Researcher gave two UX shapes: a single "Report" button that immediately enters select mode, OR a "Report" button that opens a small menu with "Select element" + "Quick note". Choose the SIMPLER shape: a single click on the floating button enters select mode. Rationale: D-08 forbids multi-step nags; one action gets the user to the picker.)

    8. Replace the modal's Submit `on:click` body (currently `web_sys::console::log_1` at lines 181-193). New behavior:
       - Inside `#[cfg(feature = "hydrate")]`: read viewport dims via `web_sys::window().and_then(|w| { let vw = w.inner_width().ok().and_then(|v| v.as_f64()).map(|f| f as i32); let vh = w.inner_height().ok().and_then(|v| v.as_f64()).map(|f| f as i32); Some((vw, vh)) }).unwrap_or((None, None))`. Read page_url similarly via `window().and_then(|w| w.location().pathname().ok()).unwrap_or_default()`. Read element_label, report_kind, report_text via `.get_untracked()` (leptos rule 20). Clear `submit_error.set(None)`.
       - Read the toast context OUTSIDE the async block so the `Copy` `ToastContext` value is captured into the closure: `let toast = use_context::<ToastContext>();`.
       - Call `spawn_local(async move { match submit_bug_report(page_url, label, text, kind, vw, vh).await { Ok(()) => { /* toast + reset */ }, Err(e) => submit_error.set(Some(e.to_string())) } })`.
       - In the Ok branch: dispatch the toast as `if let Some(t) = toast { t.show.run((ToastKind::Success, "Thanks. Your report is in.".into())); }`. (`t.show` is a `Callback<(ToastKind, String)>` per src/components/ui.rs:53-57; `.run((kind, msg))` is the canonical dispatch shape per src/pages/action_items.rs:220. Do NOT call `t.show(kind, msg)` as if it were a method.) Then `widget_state.set(WidgetState::Idle); report_text.set(String::new()); element_label.set("(no element selected)".into());`.

    9. Inside the modal body, render the inline error: `<Show when=move || submit_error.get().is_some() fallback=|| ()> <p class="text-sm text-red-400 mt-2">{move || submit_error.get().unwrap_or_default()}</p> </Show>` (or use a semantic `text-error` token if input.css defines one — check first; if not, `text-red-400` is acceptable for an error string per the project's "Exceptions to semantic tokens" rule in CLAUDE.md).

    10. Update the Cancel button handler (line 174) to: clear `submit_error.set(None)`, `widget_state.set(WidgetState::Idle)`, `report_text.set(String::new())`, `element_label.set("(no element selected)".into())`. NO confirmshaming dialog (G-10).

    11. Update the click-outside handler on the modal overlay (line 117) to mirror Cancel's behavior.

    12. Add `aria-label` to the floating button: keep the existing `aria-label="Report a bug or wishlist item"` text.

    Edit input.css: add a new rule block (anywhere after the `@theme` block — alphabetic/grouping by maintainer's preference; recommend bottom of file with a `/* Bug-report widget — Phase 19 D-05 */` comment):

    ```css
    [data-feedback-selecting] [data-feedback-label] {
      outline: 2px solid var(--color-accent-soft, var(--color-accent));
      outline-offset: 2px;
      cursor: crosshair;
    }
    ```

    Use whatever accent token exists in @theme — check input.css for `--color-accent-soft` vs `--color-accent`; fall back to `--color-accent` if `accent-soft` is not defined. The `var(--x, fallback)` syntax handles both cases gracefully.
  </action>
  <verify>
    <automated>cargo check --features ssr && cargo check --features hydrate --target wasm32-unknown-unknown</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -q 'use crate::components::ui::{ToastContext, ToastKind}' src/components/bug_report_widget.rs` exits 0 (file-top import present so the call site can resolve both types)
    - Source: `grep -q 'enum WidgetState' src/components/bug_report_widget.rs` exits 0
    - Source: `grep -q 'WidgetState::Idle' src/components/bug_report_widget.rs` exits 0 AND `grep -q 'WidgetState::Selecting' src/components/bug_report_widget.rs` exits 0 AND `grep -q 'WidgetState::Editing' src/components/bug_report_widget.rs` exits 0
    - Source: `grep -q 'StoredValue::new' src/components/bug_report_widget.rs` exits 0 (researcher specified StoredValue for closure carrier)
    - Source: `grep -q 'set_capture(true)' src/components/bug_report_widget.rs` exits 0 (capture-phase listener attach)
    - Source: `grep -q 'closest("\[data-feedback-label\]")' src/components/bug_report_widget.rs` exits 0
    - Source: `grep -q 'remove_event_listener_with_callback' src/components/bug_report_widget.rs` exits 0 (cleanup path; cb.forget would be wrong)
    - Source: `! grep -E '\.unwrap\(\)' src/components/bug_report_widget.rs | grep -v '#\[cfg(test)\]'` (rule 35 — zero unwrap in production paths; future `#[cfg(test)]` blocks may add test-only unwraps without regressing this gate)
    - Source: `grep -q '"Thanks. Your report is in."' src/components/bug_report_widget.rs` exits 0 (researcher resolution toast string — period, not exclamation)
    - Source: `! grep -E "Thanks! Your report" src/components/bug_report_widget.rs` (negative check — exclamation form must NOT appear)
    - Source: `grep -v '^#' src/components/bug_report_widget.rs | ! grep -E '[!🎉🐛💡⭐]'` (dark-pattern audit per G-10: no exclamation marks or emoji in widget strings — comment lines excluded so `// TODO Phase 22!` style comments don't poison the grep)
    - Source: `! grep -E '<Btn\b' src/components/bug_report_widget.rs` (plain `<button>` only — Phase 18.2 hydration lesson, no region primitives)
    - Source: `grep -q 'toast.show.run((ToastKind::Success' src/components/bug_report_widget.rs` exits 0 (canonical Callback dispatch shape — NOT `toast.show(kind, msg)`; mirrors src/pages/action_items.rs:220)
    - Source: `grep -q 'use_context::<ToastContext>' src/components/bug_report_widget.rs` exits 0 (toast context resolved before dispatch)
    - Source: `grep -q 'spawn_local' src/components/bug_report_widget.rs` exits 0 (async submit handler — leptos rule 24)
    - Source: `grep -q '#\[allow(unused_variables)\]' src/components/bug_report_widget.rs` exits 0 (hydrate-only signals — wasm rule 43)
    - Source: `grep -c '#\[cfg(feature = "hydrate")\]' src/components/bug_report_widget.rs` returns >= 4 (every web_sys block guarded — wasm rule preserved)
    - Source: `grep -q 'data-feedback-selecting' input.css` exits 0
    - Source: `grep -q '\[data-feedback-selecting\] \[data-feedback-label\]' input.css` exits 0 (CSS rule selector present)
    - CLI: `cargo check --features ssr` exits 0
    - CLI: `cargo check --features hydrate --target wasm32-unknown-unknown` exits 0
    - Test: `cargo test --features ssr --lib` exits 0 (no unit-test regressions; widget itself has no inline tests, but the file must continue to compile in both targets)
  </acceptance_criteria>
  <done>
    The widget transitions through Idle/Selecting/Editing deterministically. Submit calls the real `submit_bug_report` server-fn (which calls `db::create_bug_report` from plan 19-01). Toast fires on success via `toast.show.run((ToastKind::Success, "Thanks. Your report is in.".into()))` — matching the `Callback<(ToastKind, String)>` API at src/components/ui.rs:53-57. Cleanup is symmetric: every exit path removes the click+keydown listeners, restores the body cursor, and removes the `data-feedback-selecting` attribute. Both compile targets are clean.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manual agent-browser verification of the wired widget on /draft</name>
  <what-built>
    The widget now has a real submit handler, a select-mode global click listener, esc-cancel, and a success toast. Plan 19-04 will roll out `data-feedback-label` attributes — for this checkpoint we test the mechanic against a temporarily-tagged element (or any element that already has the attribute if the executor tagged one for testing).
  </what-built>
  <how-to-verify>
    1. Start dev server: `cargo leptos watch` (background) and wait for "Listening on" (use `./scripts/wait_for_server.sh 120` or equivalent).
    2. Register or log in via the agent-browser skill (visit `/auth/register?invite=E2E-TEST`, register, land on `/team/dashboard`).
    3. Navigate to `/draft`. Confirm the floating circular "Report" button is visible at bottom-right with semi-transparent treatment.
    4. Temporarily add `data-feedback-label="Draft → Test target"` to ANY existing `<div>` in `src/pages/draft.rs` (e.g. the outermost page wrapper). Hot-reload picks this up. (This is a probe; plan 19-04 will do the formal rollout.)
    5. Click the Report button. Confirm: cursor changes to crosshair, the body has the new cursor style, and (visually inspect via DevTools) `<html>` has the `data-feedback-selecting` attribute. The tagged element should show an outline.
    6. Click the tagged element. Confirm: modal opens, the "Element" field reads `Draft → Test target`, cursor restores to default, `<html data-feedback-selecting>` is removed.
    7. Type "test report from manual verification" in the textarea. Select "Bug" radio. Click Submit. Confirm: modal closes, a success toast appears with text exactly "Thanks. Your report is in." (period, not exclamation).
    8. Open SurrealDB CLI or any inspection path: confirm one row exists in `bug_report` with the typed description.
    9. Click Report again → press Esc. Confirm: cursor restores, no modal opens, listeners are cleaned up.
    10. Remove the temporary `data-feedback-label` from draft.rs.
    11. Run `cargo test --features ssr --lib` and confirm 0 regressions.

    If ANY of steps 3-10 fails, capture an `agent-browser snapshot` and DOM inspection of the failure state and surface as a blocker.
  </how-to-verify>
  <resume-signal>Type "approved" if all steps pass, OR describe the failure (with snapshot path) for revision.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WASM client → server-fn | Untrusted form data (`description`, `category`, `page_url`, `element_label`) crosses here. Defense-in-depth: client-side `submit` UX prevents most bad input; server-fn re-validates; DB ASSERT clause is the final gate. |
| Global click handler → DOM elements | Capture-phase listener intercepts user clicks on tagged elements. Cannot be exploited per se, but MUST clean up on every exit path to avoid stuck-cursor / phantom-listener bugs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01 | Tampering | `submit_bug_report` server fn | mitigate | Server-fn body rejects `category != "bug" && category != "wishlist"` with `ServerFnError::new("Invalid category")` BEFORE the DB call. DB schema `ASSERT` clause (plan 19-01) is the fallback. |
| T-19-02 | DoS / Tampering | `submit_bug_report` server fn | mitigate | Server-fn body trims description and rejects empty (`ServerFnError::new("Description is required")`); also caps at 4000 chars. `db::create_bug_report` (plan 19-01) duplicates the empty-string guard. |
| T-19-05 | Elevation of Privilege | `list_bug_reports` server fn | mitigate | v1 returns `Err(ServerFnError::new("Forbidden"))` for ALL callers. The `// TODO Phase 22` comment marks the admin-gate hardening (greppable handoff). |

(T-19-03 inbox-write tolerance and T-19-04 prompt-injection-warning are addressed in plan 19-03.)
</threat_model>

<verification>
After both tasks land and the manual checkpoint passes:

```bash
# Compile both targets — no rustc errors, no warnings (other than expected #[allow]-suppressed ones)
cargo check --features ssr
cargo check --features hydrate --target wasm32-unknown-unknown

# Regression — all pre-existing unit tests still pass
cargo test --features ssr --lib

# Dark-pattern audit (G-10) — no exclamation, no emoji in widget text (comment lines excluded)
grep -v '^#' src/components/bug_report_widget.rs | ! grep -E '[!🎉🐛💡⭐]'

# Toast string + dispatch shape check
grep -q '"Thanks. Your report is in."' src/components/bug_report_widget.rs
grep -q 'toast.show.run((ToastKind::Success' src/components/bug_report_widget.rs

# Phase 22 handoff marker
grep -q 'TODO Phase 22' src/components/bug_report_widget.rs

# CSS rule check
grep -q '\[data-feedback-selecting\] \[data-feedback-label\]' input.css
```

All commands exit 0 (or in the case of the dark-pattern grep, the `!` makes a zero-match return code 0).
</verification>

<success_criteria>
- SC-2 (button visible on auth pages): Phase 17 stub already ships this; manual checkpoint confirms preservation. No regression on the pathname + auth gate.
- SC-3 (select mode → click tagged element → modal pre-filled): Manual checkpoint step 4-6 proves the mechanic. Plan 19-04 e2e spec automates this on `/draft`.
- SC-4 (submit persists row + closes modal + toast): Manual checkpoint step 7-8 proves the wire-through. Plan 19-04 e2e spec automates this.
- SC-7 (no dark patterns): Dark-pattern grep gate in `<verification>` block passes. Manual checkpoint G-10 audit during the verifier read-through.
</success_criteria>

<output>
After completion, create `.planning/phases/19-bug-report-widget/19-02-SUMMARY.md` with:
- WidgetState enum location + variants
- StoredValue handles location (line numbers)
- Listener attach/detach pattern (file:line ranges)
- Confirmation that both compile targets are clean
- The exact toast string + the `toast.show.run((...))` dispatch line number
- The `TODO Phase 22` comment line number on `list_bug_reports`
- The dark-pattern grep result
- The manual-checkpoint outcome (approved + any deltas)
</output>
</output>

---
phase: 19
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - schema.surql
  - src/models/bug_report.rs
  - src/models/mod.rs
  - src/server/db.rs
autonomous: true
requirements: []
success_criteria: [SC-1, SC-4]
threats_addressed: [T-19-01, T-19-02, T-19-05]
tags: [surrealdb, schema, server-fn, leptos]

must_haves:
  truths:
    - "Cross-cutting: zero `.unwrap()` in any code path reachable from WASM (rule 35) — applies to the `submit_bug_report` server-fn body even though it's SSR-only"
    - "Cross-cutting: NEVER use a CSS selector for element identification; `data-feedback-label` only (rule 56). N/A to backend but must remain unbroken"
    - "Cross-cutting: semantic Tailwind tokens only (bg-base/surface/elevated, text-*, border-*). No raw hex. N/A to this plan; do not regress"
    - "Cross-cutting: no dark patterns per G-10 — server-side error strings stay neutral (e.g. 'Description is required', 'Invalid category'). No exclamation, no emoji"
    - "Cross-cutting: every SurrealDB `.bind((k, v))` MUST pass an owned `String` (or owned primitive) — `'static` requirement (surreal-patterns rule 4)"
    - "Cross-cutting: `tower-sessions` stays pinned at 0.14 to match `axum-login` 0.17. Do not bump"
    - "Cross-cutting: tests run via `cargo test --features ssr --lib` ONLY — integration tests under `tests/` OOM-link under BFD"
    - "Cross-cutting: every write query MUST chain `.await?.check()?` (surreal-patterns rule 27). Without `.check()`, ASSERT-clause violations silently `Ok`"
    - "Cross-cutting: every Db* struct deserialized from a query MUST `#[derive(Debug, Deserialize, SurrealValue)]` (surreal-patterns rule 6)"
    - "list_bug_reports server-fn returns Forbidden for ALL callers in v1 (researcher resolution) — auto-export plan 19-03 uses db::list_open_bug_reports directly, not the server-fn"
    - "Toast string when implemented (plan 19-02): 'Thanks. Your report is in.' (period, NOT exclamation) per D-08.1 + researcher resolution"
  artifacts:
    - path: schema.surql
      provides: "bug_report table definition + status/created_at composite index"
      contains: "DEFINE TABLE IF NOT EXISTS bug_report SCHEMAFULL"
    - path: src/models/bug_report.rs
      provides: "Shared BugReport + NewBugReport structs (Serialize+Deserialize) with #[cfg(test)] round-trip test"
      exports: ["BugReport", "NewBugReport"]
    - path: src/models/mod.rs
      provides: "Module wiring for bug_report shared model"
      contains: "pub mod bug_report;"
    - path: src/server/db.rs
      provides: "DbBugReport struct + From impl + create_bug_report + list_bug_reports + list_open_bug_reports + inline unit tests"
      exports: ["create_bug_report", "list_bug_reports", "list_open_bug_reports"]
  key_links:
    - from: src/server/db.rs (DbBugReport struct)
      to: src/models/bug_report.rs (BugReport struct)
      via: "From<DbBugReport> for BugReport impl — converts RecordId to String, Option<i64> to Option<i32>"
      pattern: "impl From<DbBugReport> for crate::models::bug_report::BugReport"
    - from: schema.surql (DEFINE TABLE bug_report)
      to: src/server/db.rs (create_bug_report query)
      via: "include_str!(\"../../schema.surql\") at src/server/db.rs:129 — schema applies on every startup via apply_schema"
      pattern: "CREATE bug_report SET user = type::record\\('user', \\$user_key\\)"
    - from: schema.surql (ASSERT $value IN ['bug', 'wishlist'])
      to: bug-report-widget UX (radio buttons)
      via: "Server-side enforcement guards T-19-01 (category injection bypass)"
      pattern: "ASSERT \\$value IN \\['bug', 'wishlist'\\]"
---

<objective>
Land the backend foundation for the bug-report widget: SurrealDB schema, shared model, server-fn entry points, and inline unit tests covering create+list round-trip plus category and empty-description guards. This plan ships nothing user-visible; it unblocks plans 19-02 (widget submit) and 19-03 (auto-export task), both of which import from this plan's `src/models/bug_report.rs` and `src/server/db.rs`.

Purpose: Establish a clean DB surface so the widget can persist reports (SC-4) and the auto-export task can read them (SC-5 enabler). Mitigate T-19-01 (category injection) at the DB layer via `ASSERT $value IN [...]`, T-19-02 (empty description) at the server-fn layer via trim+is_empty guard, and T-19-05 (admin gate) by returning `Forbidden` for all v1 callers of `list_bug_reports`.

Output: `bug_report` table in `schema.surql`; `src/models/bug_report.rs` shared model; `DbBugReport` + CRUD fns in `src/server/db.rs`; 4 new unit tests (`bug_report_round_trips_json`, `bug_report_create_and_list_round_trip`, `bug_report_rejects_invalid_category`, `bug_report_rejects_empty_description`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/19-bug-report-widget/19-CONTEXT.md
@.planning/phases/19-bug-report-widget/19-RESEARCH.md
@.planning/phases/19-bug-report-widget/19-PATTERNS.md
@.planning/phases/19-bug-report-widget/19-VALIDATION.md
@CLAUDE.md
@.claude/rules/surreal-patterns.md
@.claude/rules/leptos-patterns.md

<interfaces>
<!-- Lifted from src/models/action_item.rs:1-36 and src/server/db.rs:2916-2965 — the canonical CRUD analog -->
<!-- Executor implements bug_report structs by mirroring these signatures exactly. -->

Shared model shape (mirror action_item.rs):
- `#[derive(Clone, Debug, Serialize, Deserialize, PartialEq)] pub struct BugReport { pub id: Option<String>, pub user_id: String, pub page_url: String, pub element_label: String, pub description: String, pub category: String, pub viewport_w: Option<i32>, pub viewport_h: Option<i32>, pub created_at: Option<String>, pub status: String }`
- `#[derive(Clone, Debug, Default, Serialize, Deserialize, PartialEq)] pub struct NewBugReport { pub page_url: String, pub element_label: String, pub description: String, pub category: String, pub viewport_w: Option<i32>, pub viewport_h: Option<i32> }`

DB-side struct (mirror DbActionItem at db.rs:2916-2941):
- `#[derive(Debug, Deserialize, SurrealValue)] struct DbBugReport { id: RecordId, user: RecordId, page_url: String, element_label: String, description: String, category: String, viewport_w: Option<i64>, viewport_h: Option<i64>, created_at: String, status: String }`
- `impl From<DbBugReport> for crate::models::bug_report::BugReport` — convert `id`/`user` via `.to_sql()`, `viewport_w`/`viewport_h` via `.map(|v| v as i32)`.

Public DB API to export from src/server/db.rs:
- `pub async fn create_bug_report(db: &Surreal<Db>, user_id: &str, page_url: String, element_label: String, description: String, category: String, viewport_w: Option<i32>, viewport_h: Option<i32>) -> DbResult<()>`
- `pub async fn list_bug_reports(db: &Surreal<Db>, status: Option<&str>) -> DbResult<Vec<BugReport>>`
- `pub async fn list_open_bug_reports(db: &Surreal<Db>) -> DbResult<Vec<BugReport>>` — thin wrapper calling `list_bug_reports(db, Some("open"))`.

Existing imports already in src/server/db.rs to extend:
- `use crate::models::{action_item::ActionItem, champion::{...}, ...}` — append `bug_report::BugReport` alphabetically between `action_item` and `champion`.

Existing test module at src/server/db.rs:5479+ — append the two `#[tokio::test]` cases there.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create bug_report shared model + module wiring + round-trip unit test</name>
  <files>src/models/bug_report.rs, src/models/mod.rs</files>
  <read_first>
    - src/models/action_item.rs (lines 1-36) — analog: full struct shape, derive set, `#[cfg(test)] mod tests` block with round-trip
    - src/models/mod.rs (current 12 lines) — analog: alphabetic `pub mod` ordering
    - .planning/phases/19-bug-report-widget/19-RESEARCH.md (lines 304-333) — exact field set for BugReport and NewBugReport
    - .planning/phases/19-bug-report-widget/19-PATTERNS.md (lines 43-89) — pattern assignment notes including the `Option<i64>` (DB) vs `Option<i32>` (shared) split
  </read_first>
  <behavior>
    - Test 1: `bug_report_round_trips_json` — construct `BugReport { id: Some("bug_report:1"), user_id: "user:u1", page_url: "/draft", element_label: "Draft → Blue side → Pick 3", description: "Hover broke", category: "bug", viewport_w: Some(1920), viewport_h: Some(1080), created_at: Some("2026-05-26T00:00:00Z"), status: "open" }`, serialize via `serde_json::to_string`, deserialize back, assert equality. Mirror src/models/action_item.rs:16-36.
    - Test 2 (optional, recommended): `new_bug_report_round_trips_json` — same shape for `NewBugReport` (no id/created_at/status).
  </behavior>
  <action>
    Create src/models/bug_report.rs containing two structs per the `<interfaces>` block above. Both structs derive `Clone, Debug, Serialize, Deserialize, PartialEq`; `NewBugReport` also derives `Default`. Add `#[cfg(test)] mod tests` block at the bottom with `bug_report_round_trips_json` mirroring src/models/action_item.rs:16-36 exactly (use `serde_json::to_string` + `serde_json::from_str` + `assert_eq!`). In src/models/mod.rs, insert `pub mod bug_report;` alphabetically between `pub mod action_item;` and `pub mod champion;`. Do not include any `use` statements beyond `use serde::{Deserialize, Serialize};`. Do not import `surrealdb::*` here — this module compiles for both SSR and WASM (rule 32).
  </action>
  <verify>
    <automated>cargo test --features ssr --lib bug_report::tests::bug_report_round_trips_json</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -q 'pub struct BugReport' src/models/bug_report.rs` exits 0
    - Source: `grep -q 'pub struct NewBugReport' src/models/bug_report.rs` exits 0
    - Source: `grep -q 'pub mod bug_report;' src/models/mod.rs` exits 0
    - Source: `! grep -q 'surrealdb::' src/models/bug_report.rs` (shared model must not import SSR-only crate)
    - Source: `grep -q 'viewport_w: Option<i32>' src/models/bug_report.rs` (NOT i64 — that's DB-only)
    - Test: `cargo test --features ssr --lib bug_report::tests` exits 0
    - Test: `cargo check --features hydrate --target wasm32-unknown-unknown` exits 0 (model compiles in WASM target — proves no SSR-only imports leaked in)
  </acceptance_criteria>
  <done>
    The shared `BugReport` and `NewBugReport` structs exist with the field set from `<interfaces>`, the module is wired, and the round-trip JSON test passes on both `--features ssr --lib` and `--features hydrate --target wasm32-unknown-unknown`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend schema.surql with bug_report table + composite index</name>
  <files>schema.surql</files>
  <read_first>
    - schema.surql (lines 219-226, action_item block) — analog: `DEFINE TABLE IF NOT EXISTS ... SCHEMAFULL` + per-field `DEFINE FIELD IF NOT EXISTS`, including `DEFAULT time::now()` and `ASSERT $value IN [...]` precedent
    - schema.surql (line 266, `ranked_snapshot_user_queue`) — analog: composite index syntax `DEFINE INDEX IF NOT EXISTS ... ON ... FIELDS a, b`
    - schema.surql (lines 13-14, user.theme) — analog: `ASSERT $value IN ['demacia', ...]` closed-set string pattern
    - src/server/db.rs (lines 128-133) — confirm that `apply_schema` consumes `include_str!("../../schema.surql")`, so the new table picks up automatically without additional wiring
    - .planning/phases/19-bug-report-widget/19-RESEARCH.md (lines 449-466) — exact schema diff to apply
    - .claude/rules/surreal-patterns.md (rules 1, 27, 30) — `type::record` only, `.check()` on writes, `IF NOT EXISTS` everywhere
  </read_first>
  <action>
    Append the following SurrealQL block to schema.surql immediately after the action_item block (current end of file is fine; researcher specifies append after line 226). Block content (copy verbatim from 19-RESEARCH.md lines 449-466): a leading `-- Bug reports (Phase 19 D-02) ...` comment, then 9 statements: `DEFINE TABLE IF NOT EXISTS bug_report SCHEMAFULL`; 8 `DEFINE FIELD IF NOT EXISTS` lines for `user TYPE record<user>`, `page_url TYPE string`, `element_label TYPE string`, `description TYPE string`, `category TYPE string ASSERT $value IN ['bug', 'wishlist']`, `viewport_w TYPE option<int>`, `viewport_h TYPE option<int>`, `created_at TYPE datetime DEFAULT time::now()`, `status TYPE string DEFAULT 'open' ASSERT $value IN ['open', 'triaged', 'closed']`; then `DEFINE INDEX IF NOT EXISTS bug_report_status_created ON bug_report FIELDS status, created_at`. Every statement uses `IF NOT EXISTS` (surreal-patterns rule 30 — schema is re-applied on every startup; without it, the second startup errors). Do NOT modify the action_item block. Do NOT introduce `type::thing()` anywhere (removed in SurrealDB 2.x — surreal-patterns rule 1).
  </action>
  <verify>
    <automated>cargo test --features ssr --lib bug_report_create_and_list_round_trip</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -q 'DEFINE TABLE IF NOT EXISTS bug_report SCHEMAFULL' schema.surql` exits 0
    - Source: `grep -q "ASSERT \\\$value IN \\['bug', 'wishlist'\\]" schema.surql` exits 0
    - Source: `grep -q "ASSERT \\\$value IN \\['open', 'triaged', 'closed'\\]" schema.surql` exits 0
    - Source: `grep -q 'DEFINE INDEX IF NOT EXISTS bug_report_status_created' schema.surql` exits 0
    - Source: `grep -c 'DEFINE FIELD IF NOT EXISTS .* ON bug_report' schema.surql` returns 8
    - Source: `! grep 'type::thing' schema.surql` (forbidden in SurrealDB 3.x)
    - Test (depends on Task 3 landing in the same plan): `cargo test --features ssr --lib bug_report_create_and_list_round_trip` exits 0 — proves the schema applies cleanly to a fresh in-mem DB
  </acceptance_criteria>
  <done>
    The `bug_report` table and composite index are defined in schema.surql using project-canonical syntax. The schema is picked up automatically via `include_str!` at src/server/db.rs:129 — no additional wiring required.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement DbBugReport + create_bug_report + list_bug_reports + list_open_bug_reports with inline integration tests</name>
  <files>src/server/db.rs</files>
  <read_first>
    - src/server/db.rs (lines 2916-2941, DbActionItem + From impl) — analog: struct shape, RecordId field, derive set, `to_sql()` conversion
    - src/server/db.rs (lines 2943-2965, create_action_item) — analog: `.strip_prefix(...).unwrap_or(...).to_string()`, owned-String `.bind`, `type::record('table', $key)`, `.await?` pattern
    - src/server/db.rs (lines 2967-2995, list_action_items) — analog: `<string>created_at AS created_at` cast, `take(0).unwrap_or_default()`, `ORDER BY` on `SELECT *`
    - src/server/db.rs (lines 1-29) — module-level imports; append `bug_report::BugReport` to the `use crate::models::{...}` alphabetic block
    - src/server/db.rs (existing `#[cfg(test)] mod tests` block, starts ~line 5479) — analog: in-memory `Surreal::new::<Mem>(())`, `db.use_ns("test").use_db("test")`, `db.query(include_str!("../../schema.surql")).await.unwrap().check().unwrap()` schema-load pattern
    - .planning/phases/19-bug-report-widget/19-RESEARCH.md (lines 375-441, full DbBugReport + From impl + create/list/list_open bodies) — copy verbatim
    - .planning/phases/19-bug-report-widget/19-RESEARCH.md (lines 1072-1110, integration test bodies for bug_report_create_and_list_round_trip + bug_report_rejects_invalid_category) — copy verbatim
    - .claude/rules/surreal-patterns.md (rules 1, 2, 4, 6, 27, 28, 40) — type::record only, strip prefix, owned-String bind, SurrealValue derive, .check() on writes, take(0).unwrap_or_default() for lists, ORDER BY on SELECT *
  </read_first>
  <behavior>
    - Test 1 `bug_report_create_and_list_round_trip` — in-mem Surreal, apply schema, seed user, call `create_bug_report` once, call `list_open_bug_reports`, assert `reports.len() == 1`, `reports[0].category == "bug"`, `reports[0].page_url == "/draft"`. Located in the existing `#[cfg(test)] mod tests` block in src/server/db.rs.
    - Test 2 `bug_report_rejects_invalid_category` — in-mem Surreal, apply schema, manually `CREATE bug_report SET ... category='spam'`, call `.check()`, assert the result is `Err` (T-19-01 mitigation: ASSERT clause must reject non-allowlisted category).
    - Test 3 `bug_report_rejects_empty_description` — this is a server-fn-level guard. Since the server fn lives in plan 19-02, satisfy this test at the DB layer by adding a defensive guard in `create_bug_report` that returns `DbError::Other("Description is required")` if `description.trim().is_empty()`. The test calls `create_bug_report(..., "   ".into(), "bug".into(), ...)` and asserts `Err(_)`.
  </behavior>
  <action>
    In src/server/db.rs:
    1. Append `bug_report::BugReport` to the `use crate::models::{...}` block alphabetically between `action_item` and `champion`.
    2. Append a new `// --- Bug Reports (Phase 19 D-02/D-03) ---` section AFTER the action_item function block (after line ~2995, before the next CRUD section). In that section, define `struct DbBugReport` with `#[derive(Debug, Deserialize, SurrealValue)]` and exactly the fields from `<interfaces>` (note: `viewport_w` and `viewport_h` are `Option<i64>` here, NOT `Option<i32>` — DB stores int as i64; the From impl downcasts).
    3. Define `impl From<DbBugReport> for crate::models::bug_report::BugReport` — convert `id`/`user` with `.to_sql()`, convert `viewport_w`/`viewport_h` with `.map(|v| v as i32)`, wrap `id` in `Some(...)`, wrap `created_at` in `Some(...)`.
    4. Define `pub async fn create_bug_report(db: &Surreal<Db>, user_id: &str, page_url: String, element_label: String, description: String, category: String, viewport_w: Option<i32>, viewport_h: Option<i32>) -> DbResult<()>`. Before the query, add a defensive guard: `if description.trim().is_empty() { return Err(DbError::Other("Description is required".into())); }`. Then `let user_key = user_id.strip_prefix("user:").unwrap_or(user_id).to_string();` (the `.to_string()` is mandatory — surreal-patterns rule 4). Then a single `db.query(...)` matching the body in 19-RESEARCH.md lines 387-404 (CREATE statement binding user_key, page_url, element_label, description, category, viewport_w (cast to `i64` via `.map(|v| v as i64)`), viewport_h (same)). Chain `.await?.check()?` (surreal-patterns rule 27). Return `Ok(())`.
    5. Define `pub async fn list_bug_reports(db: &Surreal<Db>, status: Option<&str>) -> DbResult<Vec<crate::models::bug_report::BugReport>>` using a `match status { Some(s) => ... bind status .to_string() ..., None => ... }` shape exactly per 19-RESEARCH.md lines 409-434. Use `<string>created_at AS created_at` cast. Use `take(0).unwrap_or_default()` (surreal-patterns rule 28). Map results through `BugReport::from`.
    6. Define `pub async fn list_open_bug_reports(db: &Surreal<Db>) -> DbResult<Vec<crate::models::bug_report::BugReport>>` as a thin wrapper calling `list_bug_reports(db, Some("open")).await`.
    7. In the existing `#[cfg(test)] mod tests` block (location ~line 5479), append three `#[tokio::test]` cases per the `<behavior>` block. Use `use surrealdb::engine::local::Mem;` and `Surreal::new::<Mem>(())` (kv-mem feature already in Cargo.toml:21). For the seed-user step, use `CREATE user SET username='u', email='e@x.t', password_hash='h' RETURN id` and read the id via `Option<IdRecord>` (the existing test module already imports `IdRecord` — check before importing again).
  </action>
  <verify>
    <automated>cargo test --features ssr --lib bug_report</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -q 'pub async fn create_bug_report' src/server/db.rs` exits 0
    - Source: `grep -q 'pub async fn list_bug_reports' src/server/db.rs` exits 0
    - Source: `grep -q 'pub async fn list_open_bug_reports' src/server/db.rs` exits 0
    - Source: `grep -q 'struct DbBugReport' src/server/db.rs` exits 0
    - Source: `grep -q "type::record('user', \\\$user_key)" src/server/db.rs` exits 0
    - Source: `! grep -q 'type::thing' src/server/db.rs` (forbidden — surreal-patterns rule 1)
    - Source: `grep -A2 'pub async fn create_bug_report' src/server/db.rs | grep -q 'description.trim().is_empty()'` exits 0 (T-19-02 mitigation guard present)
    - Source: `grep -A20 'pub async fn create_bug_report' src/server/db.rs | grep -q '.await?.check()?'` exits 0 (.check() chained on write — surreal-patterns rule 27)
    - Source: `grep -q '#\[derive(Debug, Deserialize, SurrealValue)\]' src/server/db.rs` exits 0 (DbBugReport derive — surreal-patterns rule 6)
    - Test: `cargo test --features ssr --lib bug_report_create_and_list_round_trip` exits 0
    - Test: `cargo test --features ssr --lib bug_report_rejects_invalid_category` exits 0
    - Test: `cargo test --features ssr --lib bug_report_rejects_empty_description` exits 0
    - CLI: `cargo check --features ssr` exits 0
  </acceptance_criteria>
  <done>
    `create_bug_report`, `list_bug_reports`, and `list_open_bug_reports` exist with the canonical owned-String bind pattern and `.check()` on writes. All three new tests pass. T-19-01 (category injection at DB layer) and T-19-02 (empty description at server-fn layer) are mitigated and verified by tests.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → server-fn | Untrusted user input crosses here via `submit_bug_report` payload (plan 19-02). This plan ships the DB-layer guard rails. |
| server-fn → DB | `category` and `status` constraints enforced at the SurrealDB layer via `ASSERT $value IN [...]` clauses, even if the server-fn validation is bypassed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01 | Tampering | `submit_bug_report` server fn + `bug_report.category` field | mitigate | Two-layer: (a) server-fn rejects `category != "bug" && category != "wishlist"` (in plan 19-02); (b) DB schema asserts `ASSERT $value IN ['bug', 'wishlist']` (this plan, Task 2). Test `bug_report_rejects_invalid_category` proves the DB-layer guard works. |
| T-19-02 | DoS / Tampering | `submit_bug_report` server fn + `bug_report.description` field | mitigate | (a) `db::create_bug_report` returns Err if `description.trim().is_empty()` (this plan, Task 3); (b) server-fn additionally caps at 4000 chars (plan 19-02). Test `bug_report_rejects_empty_description` proves the DB-layer guard works. |
| T-19-05 | Elevation of Privilege | `list_bug_reports` server fn | mitigate | v1 returns `Forbidden` for ALL callers (researcher resolution). The only legitimate consumer (auto-export task in plan 19-03) uses `db::list_open_bug_reports` directly, bypassing the server-fn. Phase 22 will replace the gate with a proper admin check. |
</threat_model>

<verification>
After all three tasks land:

```bash
# Full bug_report scoped test run — all 4 new tests must pass
cargo test --features ssr --lib bug_report

# Cross-target compile check — proves shared model compiles for WASM
cargo check --features hydrate --target wasm32-unknown-unknown

# Regression — 121/121 pre-existing unit tests must still pass
cargo test --features ssr --lib
```

Expected: all three commands exit 0; the scoped bug_report run reports 4 new tests passing; the regression run reports >=125 tests passing (121 baseline + 4 new).
</verification>

<success_criteria>
- SC-1 (schema applies cleanly): `bug_report_create_and_list_round_trip` test passes against a fresh `Surreal::new::<Mem>(())` DB that loads the full `schema.surql` via `include_str!`. If the schema were malformed, the `.check()` after `db.query(schema)` would error and the test would fail at setup.
- SC-4 (submit persists row): The DB-layer `create_bug_report` + `list_open_bug_reports` round-trip is proven. The server-fn wrapper lands in plan 19-02 and the e2e proof in plan 19-04, but the data-storage primitive is verified here.
- T-19-01 mitigated: `bug_report_rejects_invalid_category` test passes — ASSERT clause rejects `category='spam'`.
- T-19-02 mitigated at DB layer: `bug_report_rejects_empty_description` test passes — `create_bug_report` returns Err on whitespace-only description.
- T-19-05 mitigated: documented and tracked. The server-fn implementation in plan 19-02 will return `Forbidden` unconditionally for v1.
</success_criteria>

<output>
After completion, create `.planning/phases/19-bug-report-widget/19-01-SUMMARY.md` with:
- What was built (schema diff, model, DB fns, tests)
- File:line ranges of the new code (for downstream plans to reference)
- Confirmation that `cargo test --features ssr --lib` reports >=125 tests passing
- Any deviation from the plan (e.g. if `kv-mem` was unexpectedly missing — escalate before guessing)
</output>

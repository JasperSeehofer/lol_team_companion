---
phase: 02-aggregation-layer
plan: 03
type: execute
wave: 3
depends_on: ["02-01", "02-02"]
files_modified:
  - src/pages/game_plan.rs
  - src/pages/champion_pool.rs
  - src/pages/tree_drafter.rs
  - src/main.rs
autonomous: true
requirements: []

must_haves:
  truths:
    - "Game plan page champion autocompletes store canonical IDs and display human names"
    - "Champion pool page autocomplete stores canonical ID and displays human name"
    - "Tree drafter node champion inputs store canonical IDs via validated autocomplete"
    - "migrate_champion_names is called at app startup after schema init"
    - "Existing champion data in saved records displays correctly via ID-to-name lookup"
  artifacts:
    - path: "src/pages/game_plan.rs"
      provides: "ChampionAutocomplete usage storing canonical IDs"
      contains: "ChampionAutocomplete"
    - path: "src/pages/champion_pool.rs"
      provides: "ChampionAutocomplete usage storing canonical ID"
      contains: "ChampionAutocomplete"
    - path: "src/pages/tree_drafter.rs"
      provides: "Champion input via validated autocomplete storing canonical IDs"
      contains: "ChampionAutocomplete"
    - path: "src/main.rs"
      provides: "Startup call to migrate_champion_names"
      contains: "migrate_champion_names"
  key_links:
    - from: "src/pages/game_plan.rs"
      to: "src/components/champion_autocomplete.rs"
      via: "ChampionAutocomplete component receives champions and stores IDs"
      pattern: "ChampionAutocomplete"
    - from: "src/pages/tree_drafter.rs"
      to: "src/components/champion_autocomplete.rs"
      via: "ChampionAutocomplete component for tree node champion input"
      pattern: "ChampionAutocomplete"
    - from: "src/main.rs"
      to: "src/server/db.rs"
      via: "migrate_champion_names called after init_db"
      pattern: "migrate_champion_names"
---

<objective>
Enforce champion name normalization at all ingestion points and wire migration at startup.

Purpose: Per user decision, all champion input points must use validated autocomplete that stores canonical IDs. This plan updates the pages that use champion inputs (game_plan.rs, champion_pool.rs, tree_drafter.rs) to work correctly with the Plan 01 autocomplete changes, and adds the startup migration call (which depends on Plan 02's migrate_champion_names function).

Output: All champion inputs store canonical Data Dragon IDs; existing records migrated at startup.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-aggregation-layer/02-CONTEXT.md
@.planning/phases/02-aggregation-layer/02-01-SUMMARY.md
@.planning/phases/02-aggregation-layer/02-02-SUMMARY.md

@src/pages/game_plan.rs
@src/pages/champion_pool.rs
@src/pages/tree_drafter.rs
@src/main.rs
@src/components/champion_autocomplete.rs

<interfaces>
<!-- From Plan 01 — ChampionAutocomplete now stores ID in value, displays name in filter_text -->

From src/components/champion_autocomplete.rs (after Plan 01):
```rust
// value signal now receives canonical ID (e.g. "KSante") on selection
// filter_text signal shows display name (e.g. "K'Sante")
// on_select callback receives canonical ID
pub fn ChampionAutocomplete(
    champions: Vec<Champion>,
    value: RwSignal<String>,
    placeholder: &'static str,
    on_select: Option<Callback<String>>,
) -> impl IntoView
```

From src/server/db.rs (Plan 02 adds):
```rust
pub async fn migrate_champion_names(db: &Surreal<Db>) -> DbResult<()>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update game_plan.rs, champion_pool.rs, and tree_drafter.rs for canonical ID storage</name>
  <files>src/pages/game_plan.rs, src/pages/champion_pool.rs, src/pages/tree_drafter.rs</files>
  <action>
After Plan 01, ChampionAutocomplete now stores canonical IDs in the value signal. The pages that use it need two adjustments:

**game_plan.rs** (lines ~1130-1160 — our_champions and enemy_champions inputs):
1. The `value` signals for champion autocompletes already store strings that get saved to the DB. After Plan 01, these will be canonical IDs. The save functions (`save_game_plan` server fn) already take `our_champions: Vec<String>` and `enemy_champions: Vec<String>` — they pass through to DB unchanged. No change needed to save logic.
2. **Display fix**: When loading a saved game plan, `our_champions` and `enemy_champions` will contain canonical IDs (e.g. "KSante"). The ChampionAutocomplete's Effect already handles this — it looks up the display name from the champions list when value changes externally. Verify this works by reading the load logic.
3. If any champion display happens OUTSIDE the autocomplete (e.g. in a list or badge), add a helper function or use `champions.iter().find(|c| c.id == id).map(|c| c.name.clone()).unwrap_or(id)` pattern to display the human name.
4. Check for any place in game_plan.rs where champion names are compared or filtered — ensure comparisons use canonical IDs consistently.

**champion_pool.rs** (line ~487):
1. Same pattern — the autocomplete stores canonical ID in the value signal.
2. The `save_champion_pool_entry` server fn takes `champion: String` — will now receive canonical ID. No save logic change needed.
3. **Display fix**: When rendering the champion pool list, entries loaded from DB will have canonical IDs. Where champion names are displayed in the pool list (NOT inside an autocomplete), add the ID-to-name lookup using the champions Resource/signal that's already available on the page.
4. Look for any champion name display in list items, cards, or badges and ensure they show the display name, not the raw canonical ID. The pattern is: `champions.iter().find(|c| c.id == entry.champion).map(|c| c.name.as_str()).unwrap_or(&entry.champion)`.

**tree_drafter.rs** (champion inputs for tree nodes):
1. Tree drafter nodes contain DraftAction entries with `champion: String` fields. Verify how champion selection works in the tree node editor.
2. If the tree node champion input uses `ChampionAutocomplete`, it will now store canonical IDs automatically after Plan 01. Verify this is the case.
3. If the tree node champion input uses `ChampionPicker` (grid-based), it already uses `Champion.id` for selection — likely already stores canonical IDs. Verify.
4. If any freeform text input is used for champion names in tree nodes, replace it with `ChampionAutocomplete` to enforce validated canonical ID storage.
5. **Display fix**: Where tree node champion names are displayed (in the tree graph SVG, node labels, etc.), ensure canonical IDs are resolved to display names using the same lookup pattern: `champions.iter().find(|c| c.id == champ_id).map(|c| c.name.clone()).unwrap_or(champ_id.clone())`.
6. Check the `save_tree_node` or equivalent server fn — it should already pass through String champion values unchanged.

**Important considerations:**
- Do NOT change server function signatures — they already accept String and pass through.
- The champion_picker component (grid-based) in draft.rs already uses `Champion.id` for selection — it likely already stores canonical IDs. Verify but likely no changes needed there.
  </action>
  <verify>
    <automated>cargo check --features ssr && cargo check --features hydrate --target wasm32-unknown-unknown</automated>
  </verify>
  <done>game_plan.rs, champion_pool.rs, and tree_drafter.rs compile with canonical ID storage; champion display names shown in UI via lookup; no freeform champion text entry remains in these pages</done>
</task>

<task type="auto">
  <name>Task 2: Wire migration at startup in main.rs</name>
  <files>src/main.rs</files>
  <action>
Add the champion name migration call to the server startup sequence in `main.rs`:

1. Find the `init_db` call or the section after schema is applied (after `include_str!("../schema.surql")` is executed).
2. After the schema setup completes, add:
```rust
// Normalize champion names to canonical Data Dragon IDs
if let Err(e) = db::migrate_champion_names(&db).await {
    tracing::warn!("Champion name migration failed (non-fatal): {e}");
}
```
3. This must be a non-fatal call — if Data Dragon is unreachable or migration has issues, the app should still start. Use `warn!` not `error!` since the app functions without migration (just with potentially inconsistent names).
4. The migration should run AFTER schema is applied (so tables exist) but BEFORE the Axum server starts listening.
5. Import `db::migrate_champion_names` at the call site (inside the `#[cfg(feature = "ssr")]` block of main.rs).
6. Note: `migrate_champion_names` is implemented by Plan 02 in db.rs. This plan depends on 02-02 so the function will exist when this task executes.

Verify the app starts successfully with `cargo leptos build` (don't need to run, just build).
  </action>
  <verify>
    <automated>cargo check --features ssr</automated>
  </verify>
  <done>main.rs calls migrate_champion_names after schema init; migration failure is logged but non-fatal; app builds successfully</done>
</task>

</tasks>

<verification>
- `cargo check --features ssr` passes
- `cargo check --features hydrate --target wasm32-unknown-unknown` passes
- No freeform champion text storage remains in game_plan.rs, champion_pool.rs, or tree_drafter.rs
- migrate_champion_names is called at startup after schema init
- Champion display names shown correctly via ID-to-name lookup
</verification>

<success_criteria>
- All champion autocomplete inputs store canonical IDs (including tree drafter nodes)
- Champion display in lists/cards/tree nodes shows human-readable names via lookup
- Migration runs non-fatally at startup
- Both compile targets pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-aggregation-layer/02-03-SUMMARY.md`
</output>

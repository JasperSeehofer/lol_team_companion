---
phase: 18.2-pandemonium-hydration-reconciliation
plan: 03
type: execute
wave: 1
depends_on: [18.2-02]
gap_closure: true
files_modified:
  - .planning/phases/18.2-pandemonium-hydration-reconciliation-gap-closure-for-ui-18-1/18.2-DIAGNOSIS.md
  - src/components/region/layout.rs
  - src/components/region/ornaments.rs
  - src/components/region/typography.rs
  - src/components/region/controls.rs
  - src/components/region/solo.rs
  - src/pages/draft.rs
  - src/pages/solo_dashboard.rs
  - src/pages/team/dashboard.rs
autonomous: true
requirements: [UI-18.1-HYDRATE-01, SC-1, SC-2, SC-8, SC-9]
tags: [fix, hydration, pandemonium, leptos-0.8, tachys, phase-18.2, D-09, D-10, D-11, D-12]

must_haves:
  truths:
    - "Per D-09 fix preference: real Leptos-side fix is preferred over D-10 cfg(ssr) fallback; the executor selects ONE of RESEARCH Fix Patterns 1/2/3 (align node counts via always-emit + CSS hide / stable element placeholder / <Show fallback=...>) based on the Final culprit list shape from Plan 02"
    - "Per D-10 fallback cost gate: cfg(feature = \"ssr\") placeholder pattern (Fix Pattern 4) ONLY fires when the real fix requires reshaping > 1 primitive's render signature OR is estimated > 1 day of work after diagnosis; the cost gate decision is logged in DIAGNOSIS.md before any Fix Pattern 4 code is written"
    - "Per D-11 fallback audit trail (only fires under Fix Pattern 4): three-place trail — (a) inline `// HYDRATE-FALLBACK: see .planning/phases/18.2/18.2-DIAGNOSIS.md` comment at the cfg(ssr) site; (b) DIAGNOSIS.md `## Deferred real fix` section explaining what the placeholder skips; (c) v1.4 roadmap backlog entry citing the affected primitive and structural change required"
    - "Per D-12 visual parity under fallback (only relevant under Fix Pattern 4): SSR placeholder emits the structural skeleton (anchors, node count) WASM expects; the WASM-side render still produces full Pandemonium primitive content on hydrate — degrading to Demacia branch under Pandemonium cookie is NOT acceptable"
    - "Per Pitfall 2 (mistaking side-effect of fix for fix itself): regression check — re-enable the primitive WITH the fix applied (not the short-circuited stub), re-run the agent-browser sweep on /solo + /draft + /team/dashboard Pandemonium, confirm panic STILL doesn't fire"
    - "Per Pitfall 5 (Demacia regression introduced by fix): regression check on ALL Demacia routes — agent-browser sweep on /solo + /draft + /team/dashboard with Cookie: lol_companion_theme=demacia, confirm Demacia continues to hydrate cleanly post-fix"
    - "Per Pitfall 3 (event handlers attach to wrong element): post-fix smoke-test the mode-toggle on /draft Pandemonium and visually confirm the active aria-pressed=true marker lands on the correct button — NOT just 'DOM changed'"
    - "Per Pitfall 6 (bisect commit churn): final commit MUST have NO short-circuited primitives remaining; only the real-fix changes (or Fix Pattern 4 cfg gating) — verified by grep for `return view! { <></> }.into_any();` returning empty over src/"
  artifacts:
    - path: ".planning/phases/18.2-pandemonium-hydration-reconciliation-gap-closure-for-ui-18-1/18.2-DIAGNOSIS.md"
      provides: "Extended diagnosis: Fix applied section + Regression check table + Demacia parity table + Summary table"
      contains: "Fix applied"
    - path: "src/components/region/{culprit_file}.rs OR src/pages/{culprit_file}.rs"
      provides: "Real Leptos-side fix applied to the identified culprit per Final culprit list"
      contains: "modified is_pandemonium arm"
  key_links:
    - from: "Fix in primitive/view source file"
      to: "DIAGNOSIS.md `## Fix applied` section"
      via: "Fix Pattern number + diff summary + node-count invariant rationale"
      pattern: "Fix Pattern [1-4]"
    - from: "DIAGNOSIS.md regression check table"
      to: "agent-browser sweep results on 7 Pandemonium scoped routes × both regions"
      via: "explicit YES/NO panic-observed per row"
      pattern: "panic observed"
  goals:
    - "SC-1: Eliminate the tachys-0.2.14/src/html/mod.rs:217 Option::unwrap() on None panic on every Pandemonium scoped page"
    - "SC-2: DIAGNOSIS.md fully populated with Fix applied + Regression check + Demacia parity + Summary sections"
    - "SC-8: Both compile targets clean post-fix; cargo test --features ssr --lib passes; no regression on unit tests"
    - "SC-9: Demacia continues to hydrate cleanly post-fix (no symmetric regression per Pitfall 5)"
---

<objective>
Apply the real Leptos-side fix (or — only if D-10 cost gate fires — the cfg(feature = "ssr") fallback) to the culprit primitive(s) identified in `18.2-DIAGNOSIS.md` Final culprit list. Use one of RESEARCH-supplied Fix Patterns 1-4 based on the Anti-Pattern category recorded in the Root cause analysis. Re-enable the fixed primitive(s); re-run the agent-browser sweep on all 7 Pandemonium scoped routes; confirm panic clears AND Demacia parity preserved.

**Purpose:** Close UI-18.1-HYDRATE-01. After this plan, Pandemonium pages must hydrate cleanly (no tachys panic) so WASM event handlers attach and interactivity works. Wave 2 (Plan 04 + Plan 05) adds the regression gate and flips the UAT entry.

**Output:** Modified source file(s) implementing the fix. Extended `18.2-DIAGNOSIS.md` with Fix applied + Regression check + Demacia parity + Summary sections. If D-10 fallback fired: three-place audit trail per D-11 (inline comment + DIAGNOSIS section + roadmap entry).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/18.2-pandemonium-hydration-reconciliation-gap-closure-for-ui-18-1/18.2-CONTEXT.md
@.planning/phases/18.2-pandemonium-hydration-reconciliation-gap-closure-for-ui-18-1/18.2-RESEARCH.md
@.planning/phases/18.2-pandemonium-hydration-reconciliation-gap-closure-for-ui-18-1/18.2-PATTERNS.md
@.planning/phases/18.2-pandemonium-hydration-reconciliation-gap-closure-for-ui-18-1/18.2-DIAGNOSIS.md
@.planning/phases/18.2-pandemonium-hydration-reconciliation-gap-closure-for-ui-18-1/18.2-02-SUMMARY.md
@.claude/rules/leptos-patterns.md
@.claude/rules/wasm-patterns.md

<interfaces>
<!-- Fix Pattern selection key (executor picks ONE per culprit based on DIAGNOSIS Final culprit list) -->

Fix Pattern 1 — Align node counts via always-emit-with-CSS-hide
  Use when: culprit emits N children in one branch vs M children in the other (e.g. Card zine 4-corner divs vs nothing)
  Pattern: always emit the same N elements; toggle visibility via `class=move || if cond { "..." } else { "hidden" }`
  Reference: PATTERNS.md "Fix Pattern 1" + RESEARCH.md §Code Examples line 269-282

Fix Pattern 2 — Stable element placeholder
  Use when: culprit has empty `view! {}` fragment adjacent to {children()} or other fragments
  Pattern: replace `view! {}.into_any()` with `view! { <span></span> }.into_any()` (stable Element)
  Reference: PATTERNS.md "Fix Pattern 2" + RESEARCH.md §Code Examples line 288-303
  ALSO: rewrite `is_pandemonium.then(|| view! { ... }.into_any())` Option<AnyView> adjacency (the SoloConstellationContent HIGHEST-RISK case) into explicit `if/else` with stable `<span></span>` placeholder on the else arm

Fix Pattern 3 — <Show fallback=...>
  Use when: a region-derived signal changes reactively (rarely needed since region is captured once at page entry; flag for D-04 escalation if needed)
  Pattern: `<Show when=move || cond fallback=|| view! { ... }>...</Show>`
  Reference: PATTERNS.md "Fix Pattern 3" + RESEARCH.md §Code Examples line 313-329

Fix Pattern 4 — cfg(feature = "ssr") placeholder (FALLBACK; D-10 gate)
  Use ONLY when: real fix requires reshaping > 1 primitive's render signature OR estimated > 1 day work
  Pattern: gate structural-rich SSR markup behind `#[cfg(feature = "ssr")]` to emit same-skeleton placeholder; full content under `#[cfg(feature = "hydrate")]`
  Reference: PATTERNS.md "Fix Pattern 4" + RESEARCH.md §Code Examples line 339-375
  REQUIRES: three-place audit trail per D-11

<!-- D-10 cost gate decision (record in DIAGNOSIS.md BEFORE choosing Fix Pattern 4) -->

Before selecting Fix Pattern 4, the executor MUST estimate:
  (a) How many primitives need their render signature reshaped to achieve the real fix?
  (b) Estimated work time after diagnosis (in agent-hours, not human-days)?

If (a) > 1 OR (b) > "what fits in a single ~50% context Plan 03 execution":
  → Fix Pattern 4 fires
  → DIAGNOSIS.md logs the cost-gate calculation explicitly under `## D-10 Cost Gate Decision`
  → D-11 three-place audit trail is created

Otherwise:
  → Fix Patterns 1, 2, or 3 (executor picks based on Anti-Pattern category)
  → No D-11 audit trail needed

<!-- Regression check shape (per Pitfall 2 — re-enable with fix, not with short-circuit) -->

After applying the fix:
  1. Verify the fix code is in place (NOT a short-circuit; NOT a stub) — grep target file for the new pattern.
  2. cargo leptos watch — confirm both targets compile.
  3. Agent-browser: navigate to ALL 7 Pandemonium scoped routes per CONTEXT.md domain block:
       /solo, /draft, /team/dashboard, /tree-drafter, /champion-pool, /post-game, /match/test-match-id
     Under Cookie: lol_companion_theme=pandemonium.
  4. Per route: warm-fetch + reload + capture SECOND-fetch console; record YES/NO panic + console snippet.
  5. ALL 7 ROUTES MUST SHOW NO PANIC. Any YES reopens the bisect → Plan 02 + 03 cycle.
  6. Append `## Regression check (per Pitfall 2)` table to DIAGNOSIS.md.

<!-- Demacia parity check shape (per Pitfall 5 — symmetric regression) -->

After regression check passes:
  1. Agent-browser: navigate to the SAME 7 routes under Cookie: lol_companion_theme=demacia.
  2. Per route: warm-fetch + reload + capture console.
  3. ALL 7 ROUTES MUST SHOW NO PANIC under Demacia (matches the pre-fix Demacia baseline).
  4. Append `## Demacia parity (per D-07 / D-16)` table to DIAGNOSIS.md.

<!-- Final DIAGNOSIS.md structure after Plan 03 -->

Append to DIAGNOSIS.md (in this order):
  ## D-10 Cost Gate Decision (only if Fix Pattern 4 selected — explicit decision log)
  ## Fix applied — Fix Pattern N + diff summary + node-count invariant rationale + Anti-Pattern category resolved
  ## Regression check (per Pitfall 2) — 7-route table under Pandemonium
  ## Demacia parity (per D-07 / D-16) — 7-route table under Demacia
  ## Deferred real fix (ONLY IF Fix Pattern 4 fired — per D-11) — what the cfg(ssr) placeholder skips; link to v1.4 roadmap entry
  ## Summary — final table per PATTERNS.md template

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Select Fix Pattern + apply real fix to culprit primitive(s)</name>
  <files>
    .planning/phases/18.2-pandemonium-hydration-reconciliation-gap-closure-for-ui-18-1/18.2-DIAGNOSIS.md,
    src/components/region/layout.rs,
    src/components/region/ornaments.rs,
    src/components/region/typography.rs,
    src/components/region/controls.rs,
    src/components/region/solo.rs,
    src/pages/draft.rs,
    src/pages/solo_dashboard.rs,
    src/pages/team/dashboard.rs
  </files>
  <read_first>
    - .planning/phases/18.2-pandemonium-hydration-reconciliation-gap-closure-for-ui-18-1/18.2-DIAGNOSIS.md (full file — Final culprit list + Root cause analysis from Plan 02; the Anti-Pattern category drives Fix Pattern selection)
    - .planning/phases/18.2-pandemonium-hydration-reconciliation-gap-closure-for-ui-18-1/18.2-02-SUMMARY.md (Plan 02 handoff — recommended fix pattern + D-10 fallback estimate)
    - .planning/phases/18.2-pandemonium-hydration-reconciliation-gap-closure-for-ui-18-1/18.2-PATTERNS.md sections "Fix Pattern 1" through "Fix Pattern 4" (copy-ready code excerpts)
    - .planning/phases/18.2-pandemonium-hydration-reconciliation-gap-closure-for-ui-18-1/18.2-RESEARCH.md "Code Examples" + "Anti-Patterns to Avoid" + "Common Pitfalls" (full Pitfall 2 + 5 + 6 enforcement guidance)
    - .planning/phases/18.2-pandemonium-hydration-reconciliation-gap-closure-for-ui-18-1/18.2-CONTEXT.md D-09..D-12 (preference / cost gate / audit trail / visual parity rules)
    - The CULPRIT SOURCE FILE(S) named in DIAGNOSIS.md Final culprit list — read the full file before mutating
    - .claude/rules/leptos-patterns.md (rules 18, 19 — clone-before-closure + .into_any() shape)
    - .claude/rules/wasm-patterns.md (rule 35 — never .unwrap() in event handlers; the fix must not introduce new unwraps)
  </read_first>
  <action>STEP 1 — D-10 cost gate decision: Read DIAGNOSIS.md Final culprit list. Estimate (a) number of primitives needing reshape and (b) work effort to apply the real fix. If (a) > 1 OR (b) is borderline, log the calculation under a new `## D-10 Cost Gate Decision` H2 in DIAGNOSIS.md naming the decision (Fix Pattern 1/2/3 real fix OR Fix Pattern 4 fallback) with explicit rationale. STEP 2 — Select Fix Pattern: match the Anti-Pattern category recorded in Plan 02's "Root cause analysis" to one of Fix Pattern 1/2/3 (real fix preferred per D-09) OR Fix Pattern 4 (only if D-10 gate fires). For each culprit on the Final culprit list: STEP 3 — Apply the fix in the culprit source file, following the PATTERNS.md code template line-for-line. Specifically: for empty-fragment-adjacency at `src/components/region/layout.rs:43` and `:67`, use Fix Pattern 2 — replace `view! {}.into_any()` with `view! { <span></span> }.into_any()` (preserves node count and emits stable Element on hydrate). For Option<AnyView> adjacency in SoloConstellationContent (`src/pages/solo_dashboard.rs` lines 898, 911, 932, 968, 981), use Fix Pattern 2 — rewrite `is_pandemonium.then(|| view! { ... }.into_any())` as `if is_pandemonium { view! { ... }.into_any() } else { view! { <span></span> }.into_any() }`. For node-count mismatch on Card zine-corner variant, use Fix Pattern 1 — always emit the 4 corner divs; toggle via `class=move || if var_is_zine { "..." } else { "hidden" }`. STEP 4 — If Fix Pattern 4 fired: also create the three-place D-11 audit trail: (a) inline `// HYDRATE-FALLBACK: see .planning/phases/18.2/18.2-DIAGNOSIS.md` comment at the cfg(ssr) site in source; (b) `## Deferred real fix` section in DIAGNOSIS.md explaining what the placeholder skips and what a future refactor must reshape; (c) APPEND to ROADMAP.md under a "## v1.4 backlog" or "## Carry-over from v1.3" subsection (create if absent) a single line: `- [ ] Phase TBD: Rework {primitive name} to align SSR/hydrate node count without cfg(ssr) fallback — see .planning/phases/18.2/18.2-DIAGNOSIS.md "Deferred real fix"`. STEP 5 — After applying the fix, append a `## Fix applied` H2 to DIAGNOSIS.md naming: Fix Pattern number, file:line of each edit, diff summary (1-2 sentences per file), node-count invariant rationale ("Both branches now emit N elements at this position"), Anti-Pattern category resolved. STEP 6 — Verify compile cleanliness BEFORE moving to Task 2: `cargo check --features ssr` + `cargo check --features hydrate --target wasm32-unknown-unknown` + `cargo test --features ssr --lib` must all exit 0. STEP 7 — Smoke-grep: `grep -RE 'return view! \{ <></> \}\.into_any\(\);|PANDEMONIUM_SHORT_CIRCUITED|HYDRATE: .* region=' src/` must return empty (per Pitfall 6 — no stale bisect mutations leaking through the fix commit).</action>
  <verify>
    <automated>grep -q "## Fix applied" .planning/phases/18.2-pandemonium-hydration-reconciliation-gap-closure-for-ui-18-1/18.2-DIAGNOSIS.md &amp;&amp; grep -E "Fix Pattern [1-4]" .planning/phases/18.2-pandemonium-hydration-reconciliation-gap-closure-for-ui-18-1/18.2-DIAGNOSIS.md | head -1 | grep -qE "Fix Pattern [1-4]" &amp;&amp; ! grep -RE 'PANDEMONIUM_SHORT_CIRCUITED|HYDRATE: .* region=' src/ 2>/dev/null &amp;&amp; cargo check --features ssr 2>&amp;1 | tail -1 | grep -qv "error" &amp;&amp; cargo check --features hydrate --target wasm32-unknown-unknown 2>&amp;1 | tail -1 | grep -qv "error" &amp;&amp; cargo test --features ssr --lib 2>&amp;1 | tail -5 | grep -qE "test result: ok"</automated>
  </verify>
  <acceptance_criteria>
    - DIAGNOSIS.md has `## Fix applied` H2 naming Fix Pattern N + file:line edits + node-count invariant rationale.
    - If Fix Pattern 4 fired: DIAGNOSIS.md additionally has `## D-10 Cost Gate Decision` H2 and `## Deferred real fix` H2; ROADMAP.md has a new v1.4-carry-over line; at least one source file contains the inline `// HYDRATE-FALLBACK: see .planning/phases/18.2/18.2-DIAGNOSIS.md` comment.
    - Source file modifications match the Fix Pattern template: for Pattern 1 — `class=move || if ...` toggle visible; for Pattern 2 — `view! { <span></span> }.into_any()` stable placeholder OR `if/else` rewriting of `.then(|| ...)`; for Pattern 3 — `<Show when=... fallback=...>`; for Pattern 4 — `#[cfg(feature = "ssr")]` + `#[cfg(feature = "hydrate")]` arms with matching outer-element shape.
    - No short-circuit mutations or tracing logs remain (per Pitfall 6).
    - `cargo check --features ssr` exits 0.
    - `cargo check --features hydrate --target wasm32-unknown-unknown` exits 0.
    - `cargo test --features ssr --lib` exits 0 with "test result: ok" line.
  </acceptance_criteria>
  <done>Fix Pattern chosen and applied to all culprits; DIAGNOSIS.md Fix applied section complete; if Fix Pattern 4 fired, D-11 audit trail in place; both compile targets clean; unit tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Regression check (Pandemonium 7 routes) + Demacia parity sweep + DIAGNOSIS Summary table</name>
  <files>
    .planning/phases/18.2-pandemonium-hydration-reconciliation-gap-closure-for-ui-18-1/18.2-DIAGNOSIS.md
  </files>
  <read_first>
    - .planning/phases/18.2-pandemonium-hydration-reconciliation-gap-closure-for-ui-18-1/18.2-DIAGNOSIS.md (Task 1's output — Fix applied section)
    - .planning/phases/18.2-pandemonium-hydration-reconciliation-gap-closure-for-ui-18-1/18.2-RESEARCH.md Pitfall 2, 3, 4, 5 (regression-check + Demacia-parity + Pandemonium-route-coverage enforcement)
    - .planning/phases/18.2-pandemonium-hydration-reconciliation-gap-closure-for-ui-18-1/18.2-PATTERNS.md "DIAGNOSIS.md" final structure (Regression check + Demacia parity + Summary table shapes)
    - .planning/phases/18.1-ssr-theme-injection-gap-closure-for-ui-18-runtime-01/18.1-03-EVIDENCE.md (the 7-route × 2-region table shape to mirror)
  </read_first>
  <action>Run the post-fix verification sweep WITHOUT any short-circuit mutations in place (the fix from Task 1 is the only source change). STEP 1 — Regression check: `cargo leptos watch` rebuild; for each of the 7 Pandemonium scoped routes — `/solo`, `/draft`, `/team/dashboard`, `/tree-drafter`, `/champion-pool`, `/post-game`, `/match/test-match-id` — use agent-browser to navigate with `Cookie: lol_companion_theme=pandemonium`; warm-fetch + reload + capture SECOND-fetch console; grep for `/panicked at|Option::unwrap.*None|tachys/i`; record per-route YES/NO in a `## Regression check (per Pitfall 2)` table in DIAGNOSIS.md (columns: Route, Panic observed?, Console snippet). If ANY route shows YES, STOP and append a `## Regression check FAILED — reopen bisect` subsection naming the still-failing route and the suspected secondary culprit; the orchestrator decides whether to re-execute Plan 02. STEP 2 — Pitfall 3 spot-check (event handlers attach to correct element): on `/draft` Pandemonium, use agent-browser to click the mode-toggle (SSR default = LEDGER per 18.1-03 evidence; click CAROUSEL to move away); verify the `button[aria-pressed="true"]:has-text("CAROUSEL")` selector becomes visible within 2s. Record the result in DIAGNOSIS.md under a `## Pitfall 3 — event handler attachment spot-check` subsection. STEP 3 — Demacia parity sweep: repeat STEP 1 with `Cookie: lol_companion_theme=demacia` for the SAME 7 routes; record per-route YES/NO in a `## Demacia parity (per D-07 / D-16)` table; all 7 routes MUST show NO panic. If ANY Demacia route now panics (per Pitfall 5 — fix introduced symmetric regression), append a `## Demacia regression FAILED` subsection and STOP. STEP 4 — Append final `## Summary` table to DIAGNOSIS.md per PATTERNS.md template (columns: Check, Target, Observed) with rows for: "No tachys panic on any Pandemonium scoped route" (target 7/7), "Demacia parity preserved" (target 7/7), "Pitfall 3 event-handler attachment spot-check" (target 1/1 mode-toggle observable), "Real fix vs. fallback" (target: preferred real fix; observed: Fix Pattern N applied). STEP 5 — Final cleanliness check: both compile targets exit 0; `cargo test --features ssr --lib` exits 0; no short-circuit mutations or tracing logs remain in source.</action>
  <verify>
    <automated>grep -q "## Regression check (per Pitfall 2)" .planning/phases/18.2-pandemonium-hydration-reconciliation-gap-closure-for-ui-18-1/18.2-DIAGNOSIS.md &amp;&amp; grep -q "## Demacia parity" .planning/phases/18.2-pandemonium-hydration-reconciliation-gap-closure-for-ui-18-1/18.2-DIAGNOSIS.md &amp;&amp; grep -q "^## Summary$" .planning/phases/18.2-pandemonium-hydration-reconciliation-gap-closure-for-ui-18-1/18.2-DIAGNOSIS.md &amp;&amp; ! grep -qE "Regression check FAILED|Demacia regression FAILED" .planning/phases/18.2-pandemonium-hydration-reconciliation-gap-closure-for-ui-18-1/18.2-DIAGNOSIS.md &amp;&amp; cargo check --features ssr 2>&amp;1 | tail -1 | grep -qv "error" &amp;&amp; cargo check --features hydrate --target wasm32-unknown-unknown 2>&amp;1 | tail -1 | grep -qv "error"</automated>
  </verify>
  <acceptance_criteria>
    - DIAGNOSIS.md has `## Regression check (per Pitfall 2)` H2 with a 7-row table (one per Pandemonium scoped route) — ALL 7 rows record NO panic.
    - DIAGNOSIS.md has `## Demacia parity (per D-07 / D-16)` H2 with a 7-row table — ALL 7 rows record NO panic.
    - DIAGNOSIS.md has `## Pitfall 3 — event handler attachment spot-check` subsection confirming mode-toggle on /draft Pandemonium changed to CAROUSEL within 2s (button[aria-pressed="true"]:has-text("CAROUSEL") visible).
    - DIAGNOSIS.md has `^## Summary$` H2 with rows for at least the 4 checks listed in STEP 4.
    - DIAGNOSIS.md does NOT contain the strings "Regression check FAILED" or "Demacia regression FAILED" (negative existence — the sweep passed).
    - Both compile targets exit 0; unit tests pass.
  </acceptance_criteria>
  <done>Regression check + Demacia parity confirmed in DIAGNOSIS.md; all 14 sweeps (7 routes × 2 regions) show NO panic; mode-toggle attachment spot-check passes; Summary table complete; both compile targets clean.</done>
</task>

</tasks>

<verification>
- `grep -q "## Fix applied" .planning/phases/18.2-.../18.2-DIAGNOSIS.md`
- `grep -q "## Regression check" .planning/phases/18.2-.../18.2-DIAGNOSIS.md`
- `grep -q "## Demacia parity" .planning/phases/18.2-.../18.2-DIAGNOSIS.md`
- `grep -q "^## Summary$" .planning/phases/18.2-.../18.2-DIAGNOSIS.md`
- `! grep -RE 'PANDEMONIUM_SHORT_CIRCUITED|HYDRATE: .* region=' src/` — no stale bisect or tracing code
- `cargo check --features ssr` exits 0
- `cargo check --features hydrate --target wasm32-unknown-unknown` exits 0
- `cargo test --features ssr --lib` exits 0
</verification>

<success_criteria>
1. Pandemonium hydration panic eliminated on all 7 scoped routes (SC-1).
2. DIAGNOSIS.md complete with Fix applied + Regression check + Demacia parity + Summary sections (SC-2).
3. Both compile targets clean; unit tests pass (SC-8).
4. Demacia parity preserved — no symmetric regression (SC-9).
5. If D-10 fallback fired: three-place D-11 audit trail complete (inline comment + DIAGNOSIS section + ROADMAP entry).
</success_criteria>

<output>
After completion, create `.planning/phases/18.2-pandemonium-hydration-reconciliation-gap-closure-for-ui-18-1/18.2-03-SUMMARY.md` recording:
- Final culprit list (sourced from DIAGNOSIS.md)
- Fix Pattern selected + rationale
- Whether D-10 fallback fired (if yes, link to D-11 audit trail entries)
- Regression-check + Demacia-parity outcomes
- Handoff to Plan 04: the fix is in place; the regression gate spec needs to encode the verification harness
</output>

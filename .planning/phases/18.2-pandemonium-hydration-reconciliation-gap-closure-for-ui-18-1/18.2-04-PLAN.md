---
phase: 18.2-pandemonium-hydration-reconciliation
plan: 04
type: execute
wave: 2
depends_on: [18.2-03]
gap_closure: true
files_modified:
  - e2e/tests/hydration-no-panic.spec.ts
autonomous: true
requirements: [UI-18.1-HYDRATE-01, SC-1, SC-3, SC-4, SC-5, SC-6, SC-8, SC-9]
tags: [regression-gate, playwright, hydration, pandemonium, e2e, phase-18.2, D-05, D-06, D-07, D-08, D-13, D-14, D-15, D-16]

must_haves:
  truths:
    - "Per D-05 spec assertions: new spec asserts BOTH failure shapes — (1) page.on('pageerror') + console.error matching /panicked at|Option::unwrap.*None|tachys/i fails the test; (2) D-13 interactivity triad clicks change DOM marker within 2s (D-14 observable-change shape)"
    - "Per D-06 spec location: new dedicated file `e2e/tests/hydration-no-panic.spec.ts` — NOT folded into region-diff.spec.ts (different concern: panic vs pixel diff); NOT added as global fixture (avoids slowing every test with pageerror listener overhead)"
    - "Per D-07 region coverage: spec sweeps BOTH demacia AND pandemonium for every scoped page — catches the symmetric Demacia regression case (Pitfall 5) and confirms 18.2's Pandemonium fix"
    - "Per D-08 error-level policy: pageerror events ALWAYS fail; console.error fails ONLY if matched by the panic regex (narrow filter — not the generic helpers.ts::captureErrors)"
    - "Per D-13 interactivity triad: spec covers (a) mode-toggle on /draft → click CAROUSEL (SSR default LEDGER) + /solo → click CONSTELLATION (SSR default FORGE) + /team/dashboard → click DASHBOARD (SSR default GAME_DAY/brief); (b) theme-toggle round-trip pandemonium ↔ demacia without panic / state loss; (c) draft slot drag-drop (or thinner observable click-fill) on /draft Pandemonium"
    - "Per D-14 observable-change shape: assertions poll for SPECIFIC expected DOM marker (e.g. button[aria-pressed='true']:has-text('CAROUSEL')) with 2s timeout — NOT a coarse 'DOM changed' check (Pitfall 3 enforcement)"
    - "Per D-15 persistence scope: spec does NOT include DB-write + reload + state-restored assertions — persistence already covered by Phase 18-08 mode-persist tests and Phase 18.1 cookie tests"
    - "Per D-16 Demacia parity assertion: same spec runs both regions per D-07; a 18.2 change accidentally breaking Demacia mode-toggle would fail the spec; no separate Demacia smoke test needed"
    - "Per PATTERNS.md: scoped routes match the explicit 7-route list — /solo, /draft, /team/dashboard, /tree-drafter, /champion-pool, /post-game, /match/test-match-id; reuses existing fixtures.ts::authedPage + setRegion + setMode; does NOT add new fixtures; does NOT promote attachPanicListeners to fixtures.ts (D-06 keeps it spec-local)"
  artifacts:
    - path: "e2e/tests/hydration-no-panic.spec.ts"
      provides: "Playwright regression spec asserting no tachys panic + D-13 interactivity triad on every scoped page × region"
      contains: "PANIC_REGEX"
      exports: ["test"]
  key_links:
    - from: "e2e/tests/hydration-no-panic.spec.ts"
      to: "e2e/tests/fixtures.ts (authedPage, setRegion, setMode)"
      via: "import statement"
      pattern: "from \"./fixtures\""
    - from: "spec PANIC_REGEX constant"
      to: "tachys panic stack text from 18.1-03-EVIDENCE.md"
      via: "regex match"
      pattern: "panicked at\\|Option::unwrap.*None\\|tachys"
  goals:
    - "SC-1: Spec enforces 'no tachys panic on any scoped page × Pandemonium' via narrow PANIC_REGEX filter (and symmetrically on Demacia per D-07)"
    - "SC-3: Spec asserts mode-toggle click on /draft + /solo + /team/dashboard under Pandemonium changes visible DOM marker within 2s (D-13 + D-14)"
    - "SC-4: Spec asserts theme-toggle round-trip without panic / state loss (D-13)"
    - "SC-5: Spec asserts draft slot click-fill OR drag-drop on /draft Pandemonium produces observable slot content change (D-13)"
    - "SC-6: hydration-no-panic.spec.ts exists in e2e/tests/ and runs in CI via the existing Playwright suite — becomes the canonical hydration-health gate for future region/theme phases"
    - "SC-8: Full `just verify` + `just e2e` pass (spec runs alongside existing suite without slowing pixel-diff tests)"
    - "SC-9: Spec's demacia branch confirms no Demacia regression (Pitfall 5 enforcement)"
---

<objective>
Author `e2e/tests/hydration-no-panic.spec.ts` — the Playwright regression gate that prevents the UI-18.1-HYDRATE-01 panic class from recurring on any future phase. The spec implements RESEARCH-supplied skeleton (lines 580-637) verbatim with the PATTERNS-supplied D-13 interactivity triad assertions appended.

**Purpose:** Lock in the 18.2 fix. From this phase forward, any commit that reintroduces an SSR/hydrate divergence on a Pandemonium primitive (or breaks Demacia symmetrically per Pitfall 5) fails the e2e suite. The spec also acts as the test-side correctness check for the D-13 interactivity triad — proves WASM event handlers attached AND fire on the right element (per Pitfall 3 specific-marker polling).

**Output:** `e2e/tests/hydration-no-panic.spec.ts` covering: (1) the panic-sweep matrix (7 routes × 2 regions); (2) D-13 interactivity triad (mode-toggle on 3 pages + theme-toggle round-trip + draft slot interaction). Full `cd e2e && npx playwright test hydration-no-panic.spec.ts` passes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/18.2-pandemonium-hydration-reconciliation-gap-closure-for-ui-18-1/18.2-CONTEXT.md
@.planning/phases/18.2-pandemonium-hydration-reconciliation-gap-closure-for-ui-18-1/18.2-RESEARCH.md
@.planning/phases/18.2-pandemonium-hydration-reconciliation-gap-closure-for-ui-18-1/18.2-PATTERNS.md
@.planning/phases/18.2-pandemonium-hydration-reconciliation-gap-closure-for-ui-18-1/18.2-VALIDATION.md
@.planning/phases/18.2-pandemonium-hydration-reconciliation-gap-closure-for-ui-18-1/18.2-DIAGNOSIS.md
@e2e/tests/region-diff.spec.ts
@e2e/tests/fixtures.ts
@e2e/tests/helpers.ts

<interfaces>
<!-- Spec structure (mirrors PATTERNS.md "e2e/tests/hydration-no-panic.spec.ts" section) -->

File: e2e/tests/hydration-no-panic.spec.ts

  IMPORTS (verbatim per PATTERNS.md):
    import { test, expect, setRegion, setMode } from "./fixtures";
    // (DO NOT import captureErrors from helpers.ts — D-08 requires NARROW regex; this spec inlines its own listener)

  CONSTANTS:
    const PANIC_REGEX = /panicked at|Option::unwrap.*None|tachys/i;
    const SCOPED_ROUTES = [
      "/solo",
      "/draft",
      "/team/dashboard",
      "/tree-drafter",
      "/champion-pool",
      "/post-game",
      "/match/test-match-id",
    ] as const;

  HELPER (spec-local, NOT promoted to fixtures.ts per D-06):
    async function attachPanicListeners(page: Page): Promise<string[]> { ... }
    — Implementation per PATTERNS.md "Canonical panic-listener function" (RESEARCH.md lines 645-664)

  TEST BLOCKS:
    1. Panic-sweep matrix (7 routes × 2 regions = 14 tests):
       for (const region of ["demacia", "pandemonium"] as const) {
         for (const url of SCOPED_ROUTES) {
           test(`${url} hydrates without tachys panic [${region}]`, ...)
         }
       }

    2. D-13 mode-toggle assertions (3 tests, Pandemonium only — Pitfall 3 specific-marker poll):
       test("mode-toggle observable on /draft [pandemonium]", ...) — click CAROUSEL, assert aria-pressed=true on CAROUSEL btn
       test("mode-toggle observable on /solo [pandemonium]", ...) — click CONSTELLATION, assert aria-pressed=true on CONSTELLATION btn
       test("mode-toggle observable on /team/dashboard [pandemonium]", ...) — click DASHBOARD, assert aria-pressed=true on DASHBOARD btn

    3. D-13 theme-toggle round-trip (1 test):
       test("theme-toggle round-trip [pandemonium <-> demacia]", ...) — uses setRegion helper (cookie + reload); asserts data-theme attribute transitions

    4. D-13 drag-drop / slot-fill on /draft Pandemonium (1 test):
       test("draft slot click-fill [pandemonium]", ...) — uses search + click pattern from helpers.ts::selectChampion; asserts slot text contains champion name

  TOTAL test() blocks: 14 (matrix) + 3 (mode-toggle) + 1 (theme-toggle) + 1 (slot-fill) = 19 tests

<!-- Pandemonium SSR-default mode per page (drives mode-toggle "click AWAY" choices per PATTERNS.md) -->
  /draft Pandemonium default = LEDGER → click CAROUSEL
  /solo Pandemonium default = FORGE → click CONSTELLATION
  /team/dashboard Pandemonium default = GAME_DAY (brief) → click DASHBOARD

<!-- Per-test execution pattern (verbatim per PATTERNS.md / RESEARCH lines 606-617) -->

  Each test:
    1. const errors = await attachPanicListeners(authedPage);   // listener attached BEFORE navigation
    2. await setRegion(authedPage, region);                       // cookie + reload via fixture
    3. await authedPage.goto(url);                                // navigate (cookie carries from step 2)
    4. await authedPage.waitForLoadState("networkidle");          // network settle
    5. await authedPage.waitForTimeout(1500);                     // hydrate settle (per RESEARCH — gives WASM time to crash mid-hydrate before assertion)
    6. (interactivity tests only) perform click; expect.toBeVisible({ timeout: 2000 }) for SPECIFIC marker
    7. expect(errors).toEqual([]);                                // narrow regex filter ensures only true panics fail

<!-- What NOT to do (per D-06 / D-08 + PATTERNS.md) -->
  ❌ Do NOT import captureErrors from helpers.ts (captures ALL console.error — false-positive prone)
  ❌ Do NOT promote attachPanicListeners to fixtures.ts (D-06 keeps it spec-local)
  ❌ Do NOT capture screenshots / pixel-diff (region-diff.spec.ts's concern)
  ❌ Do NOT include DB-write + reload + state-restored assertions (D-15 — out of scope)
  ❌ Do NOT use a coarse "DOM changed" check (Pitfall 3 — must poll specific marker)

<!-- Existing fixture/helper inventory (all already exist; no new helpers needed) -->
  authedPage          — e2e/tests/fixtures.ts:88-92
  setRegion(page, r)  — e2e/tests/fixtures.ts:131-161 (cookie + reload)
  setMode(page, key)  — e2e/tests/fixtures.ts:180-207
  selectChampion(p,n) — e2e/tests/helpers.ts:84-95 (for slot-fill test)

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author hydration-no-panic.spec.ts — panic-sweep matrix (14 tests)</name>
  <files>e2e/tests/hydration-no-panic.spec.ts</files>
  <read_first>
    - e2e/tests/region-diff.spec.ts (full file — the iteration pattern + SCOPED_ROUTES analog to mirror; PATTERNS.md flags lines 50-72 captureBothRegions and 110-195 per-test structure)
    - e2e/tests/fixtures.ts (full file — confirm authedPage, setRegion, setMode signatures + setRegion-before-goto behavior per PATTERNS.md "Navigation + region-set ordering")
    - e2e/tests/helpers.ts lines 18-25 (captureErrors — what NOT to copy per D-08)
    - .planning/phases/18.2-pandemonium-hydration-reconciliation-gap-closure-for-ui-18-1/18.2-PATTERNS.md "e2e/tests/hydration-no-panic.spec.ts" section (full — imports, helper, scoped routes list, per-region nested loop)
    - .planning/phases/18.2-pandemonium-hydration-reconciliation-gap-closure-for-ui-18-1/18.2-RESEARCH.md lines 580-664 (Wave 0 skeleton + TypeScript patterns for D-08)
  </read_first>
  <behavior>
    - Test 1-14: Each scoped route × region combination passes only when no `pageerror` event fires AND no console.error matches PANIC_REGEX during navigation + hydrate-settle.
    - Listener attached BEFORE navigation so first-hydrate panics are captured.
    - PANIC_REGEX matches the tachys panic class: `/panicked at|Option::unwrap.*None|tachys/i`.
    - Tests must fail fast with descriptive error message: `Expected no panic; got: ${errors.join("\n")}`.
    - Tests must NOT fail on generic console.error (404 favicon, Tailwind warning, etc.) — narrow regex filter.
  </behavior>
  <action>Create `e2e/tests/hydration-no-panic.spec.ts` following the PATTERNS.md template line-for-line. Import only from `./fixtures` (NOT from `./helpers` — D-08 requires the narrow regex listener inlined here). Define the spec-local `attachPanicListeners` helper per the RESEARCH.md lines 645-664 canonical function. Define `PANIC_REGEX = /panicked at|Option::unwrap.*None|tachys/i;` and `SCOPED_ROUTES` as the 7-route `as const` array from PATTERNS.md. Generate the 14-test matrix via the nested `for (const region of ["demacia", "pandemonium"] as const) { for (const url of SCOPED_ROUTES) { test(...) } }` pattern per PATTERNS.md "Per-region nested loop pattern". Per-test body: `const errors = await attachPanicListeners(authedPage); await setRegion(authedPage, region); await authedPage.goto(url); await authedPage.waitForLoadState("networkidle"); await authedPage.waitForTimeout(1500); expect(errors, "Expected no panic; got: ${errors.join('\\n')}").toEqual([]);`. Run the spec: `cd e2e && npx playwright test hydration-no-panic.spec.ts -g "hydrates without tachys panic"` — all 14 tests must pass (Plan 03's fix makes Pandemonium clean; Demacia was already clean).</action>
  <verify>
    <automated>test -f e2e/tests/hydration-no-panic.spec.ts &amp;&amp; grep -q "PANIC_REGEX" e2e/tests/hydration-no-panic.spec.ts &amp;&amp; grep -q "SCOPED_ROUTES" e2e/tests/hydration-no-panic.spec.ts &amp;&amp; grep -q "attachPanicListeners" e2e/tests/hydration-no-panic.spec.ts &amp;&amp; ! grep -q 'captureErrors' e2e/tests/hydration-no-panic.spec.ts &amp;&amp; (cd e2e &amp;&amp; OUT=$(npx playwright test hydration-no-panic.spec.ts -g "hydrates without tachys panic" 2>&amp;1) &amp;&amp; echo "$OUT" | grep -qE "(^|[^0-9])14 passed" &amp;&amp; ! echo "$OUT" | grep -qE "[1-9][0-9]* failed|timed out")</automated>
  </verify>
  <acceptance_criteria>
    - File `e2e/tests/hydration-no-panic.spec.ts` exists.
    - Contains literal `PANIC_REGEX` definition matching `/panicked at|Option::unwrap.*None|tachys/i`.
    - Contains `SCOPED_ROUTES` array with exactly 7 entries matching CONTEXT.md domain block: /solo, /draft, /team/dashboard, /tree-drafter, /champion-pool, /post-game, /match/test-match-id.
    - Contains spec-local `attachPanicListeners` function (NOT imported from helpers.ts).
    - Does NOT import `captureErrors` from helpers.ts (D-08 narrow filter requirement).
    - Imports `test, expect, setRegion, setMode` from `./fixtures`.
    - 14 panic-sweep test() blocks exist (one per route × region).
    - `cd e2e && npx playwright test hydration-no-panic.spec.ts -g "hydrates without tachys panic"` reports 14 passed.
  </acceptance_criteria>
  <done>Panic-sweep matrix in place; all 14 tests pass; spec uses narrow PANIC_REGEX filter; no helpers.ts contamination.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Append D-13 interactivity triad tests (mode-toggle × 3 + theme-toggle round-trip + slot-fill) — 5 additional tests</name>
  <files>e2e/tests/hydration-no-panic.spec.ts</files>
  <read_first>
    - e2e/tests/hydration-no-panic.spec.ts (Task 1's output — append to this file)
    - e2e/tests/fixtures.ts lines 180-207 (setMode signature + valid mode keys per page)
    - e2e/tests/helpers.ts lines 84-95 (selectChampion — search-then-click flow for slot-fill test)
    - .planning/phases/18.2-pandemonium-hydration-reconciliation-gap-closure-for-ui-18-1/18.2-PATTERNS.md "D-13 interactivity triad" subsections (mode-toggle, theme-toggle round-trip, drag-drop) — copy-ready test bodies
    - .planning/phases/18.2-pandemonium-hydration-reconciliation-gap-closure-for-ui-18-1/18.2-CONTEXT.md D-13, D-14, D-15, D-16 (interactivity assertion list + observable-change shape + persistence scope exclusion)
    - .planning/phases/18.1-ssr-theme-injection-gap-closure-for-ui-18-runtime-01/18.1-03-EVIDENCE.md "Mode-toggle SSR defaults" table (Pandemonium SSR defaults per page — drives the "click AWAY" choices)
    - src/components/draft_board.rs lines 91-105, 207-217 (drag-drop handler — informs whether to use synthetic DataTransfer events or the simpler selectChampion click-fill path)
  </read_first>
  <behavior>
    - Test 15 (mode-toggle /draft): click CAROUSEL on Pandemonium /draft (SSR default LEDGER); `button[aria-pressed="true"]:has-text("CAROUSEL")` visible within 2s; no panic in errors.
    - Test 16 (mode-toggle /solo): click CONSTELLATION on Pandemonium /solo (SSR default FORGE); aria-pressed marker visible within 2s; no panic.
    - Test 17 (mode-toggle /team/dashboard): click DASHBOARD on Pandemonium /team/dashboard (SSR default GAME_DAY/brief); aria-pressed marker visible within 2s; no panic.
    - Test 18 (theme-toggle round-trip): start on Pandemonium /draft; setRegion(demacia) → `html[data-theme="demacia"]` visible within 2s; setRegion(pandemonium) → `html[data-theme="pandemonium"]` visible within 2s; no panic across both transitions.
    - Test 19 (draft slot-fill): on Pandemonium /draft, search for "Ahri" via `input[placeholder="Search champion..."]`, click champion button; observable slot content change within 2s; no panic.
  </behavior>
  <action>Append 5 interactivity triad tests to `e2e/tests/hydration-no-panic.spec.ts` after the matrix block. Test 15-17 — mode-toggle: each test attaches panic listeners, setRegion(pandemonium), navigates to the page, networkidle + 1500ms hydrate-settle, then `await setMode(authedPage, "<target_mode>")` where target_mode is `"carousel"` for /draft, `"constellation"` for /solo, `"dashboard"` for /team/dashboard (consult fixtures.ts::setMode for the exact key strings — adjust if the helper accepts different identifiers). Per Pitfall 3, assert the SPECIFIC mode-active marker via `await expect(authedPage.locator('button[aria-pressed="true"]:has-text("CAROUSEL")')).toBeVisible({ timeout: 2000 });` (case-match the rendered label; adjust if the UI emits lowercase or differently). Test 18 — theme-toggle round-trip: per PATTERNS.md analog, use setRegion to flip pandemonium → demacia → pandemonium; assert `html[data-theme="demacia"]` then `html[data-theme="pandemonium"]` each visible within 2s; assert no panic in errors. Test 19 — draft slot-fill: per PATTERNS.md drag-drop analog (use selectChampion-style click-fill flow rather than synthetic drag-drop DataTransfer events — simpler, exercises the same WASM event-handler attachment); navigate Pandemonium /draft, find search input via `input[placeholder="Search champion..."]`, type "Ahri", wait 300ms for autocomplete, click the champion's button image, assert the first slot now contains "Ahri" via the specific slot selector (identity confirmed by checking draft_board.rs render output OR by inspecting current /draft DOM during development). If the slot selector is non-obvious, use `await expect(authedPage.locator('text=Ahri')).toBeVisible({ timeout: 2000 });` as a thinner-but-still-specific assertion (champion name appears somewhere on the page post-click). Per D-15, do NOT add reload-and-verify-persistence assertions. Run the full spec: `cd e2e && npx playwright test hydration-no-panic.spec.ts` — all 19 tests must pass. Then run `just verify && just e2e` to confirm no regression on the existing suite.</action>
  <verify>
    <automated>grep -c "^test(" e2e/tests/hydration-no-panic.spec.ts | awk '{ if ($1 &gt;= 19) exit 0; else exit 1 }' &amp;&amp; grep -q "mode-toggle observable on /draft" e2e/tests/hydration-no-panic.spec.ts &amp;&amp; grep -q "mode-toggle observable on /solo" e2e/tests/hydration-no-panic.spec.ts &amp;&amp; grep -q "mode-toggle observable on /team/dashboard" e2e/tests/hydration-no-panic.spec.ts &amp;&amp; grep -q "theme-toggle round-trip" e2e/tests/hydration-no-panic.spec.ts &amp;&amp; grep -qE "slot|drag-drop|click-fill" e2e/tests/hydration-no-panic.spec.ts &amp;&amp; (cd e2e &amp;&amp; OUT=$(npx playwright test hydration-no-panic.spec.ts 2>&amp;1) &amp;&amp; echo "$OUT" | grep -qE "passed" &amp;&amp; ! echo "$OUT" | grep -qE "[1-9][0-9]* failed|timed out")</automated>
  </verify>
  <acceptance_criteria>
    - `e2e/tests/hydration-no-panic.spec.ts` contains at least 19 `test(` blocks (14 matrix + 5 interactivity).
    - File contains literal strings: "mode-toggle observable on /draft", "mode-toggle observable on /solo", "mode-toggle observable on /team/dashboard", "theme-toggle round-trip", and one of {slot, drag-drop, click-fill}.
    - Mode-toggle tests use `button[aria-pressed="true"]` selector with `:has-text(...)` for the SPECIFIC mode marker (per Pitfall 3).
    - Theme-toggle test uses `html[data-theme="demacia"]` and `html[data-theme="pandemonium"]` selectors.
    - No reload-and-verify-persistence assertions (per D-15).
    - `cd e2e && npx playwright test hydration-no-panic.spec.ts` reports 0 failed, 0 timed out.
    - `just verify` exits 0.
    - `just e2e` exits 0 (no regression on existing Playwright suite).
  </acceptance_criteria>
  <done>5 interactivity triad tests in place; full spec at 19 tests; all pass; no regression on existing suite; full `just verify` + `just e2e` green.</done>
</task>

</tasks>

<verification>
- `test -f e2e/tests/hydration-no-panic.spec.ts`
- `grep -c "^test(" e2e/tests/hydration-no-panic.spec.ts` >= 19
- `cd e2e && npx playwright test hydration-no-panic.spec.ts` — 0 failed
- `just verify` exits 0
- `just e2e` exits 0
- `grep -q "PANIC_REGEX" e2e/tests/hydration-no-panic.spec.ts`
- `! grep -q "captureErrors" e2e/tests/hydration-no-panic.spec.ts` (narrow filter per D-08)
</verification>

<success_criteria>
1. `e2e/tests/hydration-no-panic.spec.ts` exists with 19 tests (SC-6).
2. Panic-sweep matrix covers 7 routes × 2 regions = 14 tests; all pass (SC-1, SC-9).
3. D-13 interactivity triad encoded: mode-toggle × 3 + theme-toggle round-trip + slot-fill = 5 tests; all pass (SC-3, SC-4, SC-5).
4. Specific-marker polling per Pitfall 3 / D-14 (button[aria-pressed=...]:has-text(...)).
5. Narrow PANIC_REGEX filter per D-08 — no false-positives from generic console.error.
6. `just verify` + `just e2e` both clean (SC-8).
</success_criteria>

<output>
After completion, create `.planning/phases/18.2-pandemonium-hydration-reconciliation-gap-closure-for-ui-18-1/18.2-04-SUMMARY.md` recording:
- Final test count (matrix + triad)
- Pass count on first run + any flake observations
- Notes on selector adjustments (e.g. mode-toggle button label case — uppercase "CAROUSEL" vs "Carousel"; slot identifier shape)
- Handoff to Plan 05: regression gate locked; Plan 05 runs the agent-browser re-sweep + flips the UAT entry
</output>

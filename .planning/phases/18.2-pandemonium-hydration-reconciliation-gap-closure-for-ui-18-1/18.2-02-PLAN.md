---
phase: 18.2-pandemonium-hydration-reconciliation
plan: 02
type: execute
wave: 1
depends_on: [18.2-01]
gap_closure: true
files_modified:
  - .planning/phases/18.2-pandemonium-hydration-reconciliation-gap-closure-for-ui-18-1/18.2-DIAGNOSIS.md
  - src/components/region/ornaments.rs
  - src/components/region/controls.rs
  - src/components/region/solo.rs
  - src/pages/solo_dashboard.rs
  - src/pages/draft.rs
autonomous: true
requirements: [UI-18.1-HYDRATE-01, SC-1, SC-2]
tags: [bisect, hydration, pandemonium, agent-browser, per-page-views, phase-18.2, D-01, D-02, D-04]

must_haves:
  truths:
    - "Per D-02 leaves-first ordering continuation: cycles 5-8 cover the remaining region primitives (CompanionSigil, ModeToggle, LPProgress, Btn — ranks 5/7/11/6 per RESEARCH suspect table) before escalating to per-page Pandemonium view rebuilds"
    - "Per Pitfall 4 (Pandemonium hydration succeeds on bisect page but fails on different Pandemonium page): even if Plan 01 found a strong culprit, this plan SWEEPS for second-culprit by short-circuiting the page-level Pandemonium arm (DraftLedgerView, SoloConstellationContent, SoloForgeView, SoloJournalView, TeamGameDayBriefView) on the routes where Plan 01 didn't fully clear"
    - "Per D-04 escalation gate: if all leaf primitive cycles (1-8) cleared without isolating a culprit, this plan escalates to WASM-side console::log render-tracing (RESEARCH Fix Pattern 5) — inject `#[cfg(feature = \"hydrate\")] web_sys::console::log_1` at the TOP of each suspect primitive render fn to print render-order/identity during hydrate"
    - "Per Pitfall 1: warm-fetch each Pandemonium page once before observing panic state (SECOND-fetch console captured)"
    - "Per code_context atomic-commit: each bisect cycle is its own commit on scratch branch or stash+reapply; all mutations reverted before plan completes (Plan 03 applies the real fix)"
    - "Per D-03: DIAGNOSIS.md cycles 5-N appended with same Mutation/Pages/Panic/Conclusion table shape established in Plan 01"
  artifacts:
    - path: ".planning/phases/18.2-pandemonium-hydration-reconciliation-gap-closure-for-ui-18-1/18.2-DIAGNOSIS.md"
      provides: "Bisect cycles 5-N for remaining primitives + per-page Pandemonium views; final culprit list + root cause hypothesis"
      contains: "Final culprit list"
  key_links:
    - from: "Each new bisect cycle in DIAGNOSIS.md"
      to: "src/components/region/{ornaments,controls,solo}.rs OR src/pages/{draft,solo_dashboard}.rs line numbers"
      via: "explicit file:line reference"
      pattern: "src/.*\\.rs:[0-9]+"
  goals:
    - "SC-1: Complete the bisect by ruling in/out remaining region primitives + per-page Pandemonium views; finalize culprit list"
    - "SC-2: Extend 18.2-DIAGNOSIS.md with cycles 5-N + a 'Root cause analysis' section attributing the divergence shape (per RESEARCH Anti-Pattern lines 165-171)"
---

<objective>
Continue the D-01 short-circuit-and-reload bisect from Plan 01's stopping point. Cover the remaining leaf primitives per D-02 (CompanionSigil, ModeToggle, LPProgress, Btn — ranks 5, 7, 11, 6 per RESEARCH suspect table). If those all short-circuit clean and the panic persists, expand to per-page Pandemonium arms (DraftLedgerView, SoloConstellationContent, SoloForgeView, SoloJournalView, TeamGameDayBriefView). If THAT also clears with no isolation, escalate per D-04 to WASM-side render-tracing.

**Purpose:** Pin down the final culprit list. Per Pitfall 4, even if Plan 01 identified a strong primary culprit, the bug may have a second independent culprit on a route the Plan-01 sweep didn't fully cover — this plan provides defense in depth.

**Output:** Extended `18.2-DIAGNOSIS.md` with cycles 5-N + a Root cause analysis section + Final culprit list. Plan 03 will use this as input for choosing the fix pattern.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/18.2-pandemonium-hydration-reconciliation-gap-closure-for-ui-18-1/18.2-CONTEXT.md
@.planning/phases/18.2-pandemonium-hydration-reconciliation-gap-closure-for-ui-18-1/18.2-RESEARCH.md
@.planning/phases/18.2-pandemonium-hydration-reconciliation-gap-closure-for-ui-18-1/18.2-PATTERNS.md
@.planning/phases/18.2-pandemonium-hydration-reconciliation-gap-closure-for-ui-18-1/18.2-DIAGNOSIS.md
@.planning/phases/18.2-pandemonium-hydration-reconciliation-gap-closure-for-ui-18-1/18.2-01-SUMMARY.md
@.claude/rules/leptos-patterns.md
@.claude/rules/wasm-patterns.md

<interfaces>
<!-- Remaining bisect targets (cycles 5-8: leaf primitives per D-02 leaves-first) -->

Cycle 5 — CompanionSigil primitive
  File: src/components/region/ornaments.rs:135-171
  Risk rank: 5 (mounted in nav on every page; Pandemonium arm = 1 span, Demacia arm = div+svg+div; outer-shape matches but inner walks differ)

Cycle 6 — ModeToggle primitive
  File: src/components/region/controls.rs:132-211
  Risk rank: 7 (Pandemonium emits <div><buttons>; Demacia emits <div><div><buttons> — outer node count differs by 1 inner div; ONLY mounted on /draft, /solo, /team/dashboard)
  CRITICAL: This is the component exercised by D-13 mode-toggle assertion. Short-circuiting it tests whether the panic occurs BEFORE mode-toggle hydrates (which it must, since the symptom IS that mode-toggle is inert).

Cycle 7 — LPProgress primitive
  File: src/components/region/solo.rs (LPProgress component, region-branching)
  Risk rank: 11 (symmetric node count; low priority — mounted only on /solo forge + constellation)

Cycle 8 — Btn primitive
  File: src/components/region/controls.rs:22-118
  Risk rank: 6 (both arms emit single <button>; rule out fast)

<!-- Per-page Pandemonium view bisect targets (cycles 9-N: ONLY IF cycles 5-8 all clear AND panic persists) -->

Per-page views (per RESEARCH "Per-Page Pandemonium Views Audit"):
  Cycle 9  — DraftLedgerView Pandemonium arm: src/pages/draft.rs lines 4142-4248
             Suspect: 4 separate {move || draft_slots.get()...collect_view()} closures inside Card<zine>
  Cycle 10 — SoloConstellationContent: src/pages/solo_dashboard.rs lines 859-? (HIGHEST-RISK)
             Suspect: is_pandemonium.then(|| view! {...}.into_any()) THREE times (lines 898, 911) + is_demacia.then(...) THREE times — leptos-rs#3583 Option<AnyView> adjacency
  Cycle 11 — SoloForgeView: src/pages/solo_dashboard.rs lines 1509-?
             Suspect: rapid succession of small <span>{move || a_sv.get_value()}</span> adjacent siblings
  Cycle 12 — SoloJournalView: src/pages/solo_dashboard.rs lines 1372-?
             Suspect: nested if/else.into_any() inside top-level if is_pandemonium (two levels)
  Cycle 13 — TeamGameDayBriefView: src/pages/team/dashboard.rs lines 2016-2138
             Suspect: <Glitch> siblings with mixed static-text + reactive-format children

<!-- Short-circuit mutation pattern (same as Plan 01) -->

For region primitives (cycles 5-8): early return at TOP of `if is_pandemonium { ... }` arm:
    return view! { <></> }.into_any();

For per-page views (cycles 9+): at the TOP of the `if is_pandemonium { ... }` arm at the page-component level:
    return view! { <div>"PANDEMONIUM_SHORT_CIRCUITED"</div> }.into_any();
(The textual marker makes it obvious in agent-browser snapshots whether the short-circuit landed.)

<!-- D-04 escalation pattern (RESEARCH Fix Pattern 5 — ONLY IF cycles 5-N all clear) -->

Inject at the TOP of each suspect primitive render fn (instrument all-at-once per Claude's Discretion note):
    #[cfg(feature = "hydrate")]
    web_sys::console::log_1(&format!("HYDRATE: {ComponentName} region={}", region).into());

Walk the agent-browser console output during hydrate; identify the LAST component logged before
the panic — that's the cursor-desync point.

<!-- DIAGNOSIS.md extensions (append to Plan 01's file, do NOT overwrite) -->

Append sections in this order:
  ### Cycle 5: short-circuit CompanionSigil — Mutation, Pages, Panic, Conclusion
  ### Cycle 6: short-circuit ModeToggle — same shape
  ### Cycle 7: short-circuit LPProgress — same shape
  ### Cycle 8: short-circuit Btn — same shape
  [IF NEEDED] ### Cycle 9+: per-page Pandemonium view short-circuits
  [IF NEEDED] ## D-04 Escalation — WASM render-tracing log capture
  ## Root cause analysis — divergence shape per RESEARCH Anti-Patterns (lines 165-171)
  ## Final culprit list — file:line + Anti-Pattern category for each

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bisect cycles 5-8 — remaining leaf primitives (CompanionSigil, ModeToggle, LPProgress, Btn)</name>
  <files>
    .planning/phases/18.2-pandemonium-hydration-reconciliation-gap-closure-for-ui-18-1/18.2-DIAGNOSIS.md,
    src/components/region/ornaments.rs,
    src/components/region/controls.rs,
    src/components/region/solo.rs
  </files>
  <read_first>
    - .planning/phases/18.2-pandemonium-hydration-reconciliation-gap-closure-for-ui-18-1/18.2-DIAGNOSIS.md (Plan 01's output — cycles 1-4 + interim summary; this task appends, does not overwrite)
    - .planning/phases/18.2-pandemonium-hydration-reconciliation-gap-closure-for-ui-18-1/18.2-01-SUMMARY.md (Plan 01 handoff — culprit candidates? continue bisect? escalate?)
    - src/components/region/ornaments.rs (full file — CompanionSigil at 135-171)
    - src/components/region/controls.rs (full file — Btn at 22-118, ModeToggle at 132-211)
    - src/components/region/solo.rs (full file — LPProgress component)
    - .planning/phases/18.2-pandemonium-hydration-reconciliation-gap-closure-for-ui-18-1/18.2-RESEARCH.md "Per-Primitive Risk Audit" (rank 5, 6, 7, 11 entries — divergence shape suspected per primitive)
  </read_first>
  <action>Apply the same short-circuit-and-reload method from Plan 01 to the remaining four leaf primitives in this order: Cycle 5 CompanionSigil → Cycle 6 ModeToggle → Cycle 7 LPProgress → Cycle 8 Btn. PER CYCLE: (a) insert `return view! { <></> }.into_any();` at the top of the primitive's `if is_pandemonium { ... }` arm; (b) `cargo leptos watch` rebuild; (c) agent-browser navigate to `/solo`, `/draft`, `/team/dashboard` under Pandemonium cookie, warm-fetch + reload + capture SECOND-fetch console state (per Pitfall 1); (d) record YES/NO + console snippet in DIAGNOSIS.md under a new `### Cycle N: short-circuit {primitive}` H3; (e) REVERT the mutation and verify both compile targets exit 0; (f) atomic commit per cycle on scratch branch or stash+reapply. STOPPING RULE: as in Plan 01, do NOT skip remaining cycles even after a strong culprit is found — second-culprit defense per Pitfall 4 requires the full sweep. After Cycle 8, append a `## Cycles 5-8 summary` H2 with: which primitives are culprits (if any), which are ruled out, and the decision for Task 2 (move to per-page view bisect OR all leaves clean → D-04 escalation).</action>
  <verify>
    <automated>grep -c "^### Cycle [5-8]:" .planning/phases/18.2-pandemonium-hydration-reconciliation-gap-closure-for-ui-18-1/18.2-DIAGNOSIS.md | grep -q "^4$" &amp;&amp; grep -q "Cycles 5-8 summary" .planning/phases/18.2-pandemonium-hydration-reconciliation-gap-closure-for-ui-18-1/18.2-DIAGNOSIS.md &amp;&amp; cargo check --features ssr 2>&amp;1 | tail -1 | grep -qv "error" &amp;&amp; cargo check --features hydrate --target wasm32-unknown-unknown 2>&amp;1 | tail -1 | grep -qv "error"</automated>
  </verify>
  <acceptance_criteria>
    - DIAGNOSIS.md contains exactly 4 NEW `### Cycle N: short-circuit {primitive}` H3 sections (N = 5, 6, 7, 8) for CompanionSigil, ModeToggle, LPProgress, Btn.
    - Each cycle section records Mutation file:line, Pages tested, Panic observed per page, Conclusion.
    - `## Cycles 5-8 summary` H2 exists with explicit next-step decision: "Proceed to per-page bisect" OR "All leaves clean — escalate to D-04 WASM tracing" OR "Culprit confirmed in cycles 1-8 — Plan 03 fixes {primitive}".
    - All four source files (ornaments.rs, controls.rs, solo.rs) are reverted — `git diff src/components/region/ornaments.rs src/components/region/controls.rs src/components/region/solo.rs` shows only DIAGNOSIS.md changes (or nothing for these source files).
    - Both compile targets exit 0.
  </acceptance_criteria>
  <done>Cycles 5-8 documented; source files reverted; next-step decision logged.</done>
</task>

<task type="auto">
  <name>Task 2: Per-page Pandemonium view bisect OR D-04 escalation (conditional on Task 1 outcome) + Root cause analysis + Final culprit list</name>
  <files>
    .planning/phases/18.2-pandemonium-hydration-reconciliation-gap-closure-for-ui-18-1/18.2-DIAGNOSIS.md,
    src/pages/solo_dashboard.rs,
    src/pages/draft.rs,
    src/components/region/layout.rs,
    src/components/region/typography.rs,
    src/components/region/ornaments.rs,
    src/components/region/controls.rs
  </files>
  <read_first>
    - .planning/phases/18.2-pandemonium-hydration-reconciliation-gap-closure-for-ui-18-1/18.2-DIAGNOSIS.md (Task 1 output — Cycles 5-8 + summary)
    - .planning/phases/18.2-pandemonium-hydration-reconciliation-gap-closure-for-ui-18-1/18.2-RESEARCH.md "Per-Page Pandemonium Views Audit" + "Anti-Patterns to Avoid" + Fix Pattern 5 (D-04 tracing)
    - .planning/phases/18.2-pandemonium-hydration-reconciliation-gap-closure-for-ui-18-1/18.2-PATTERNS.md "SoloConstellationContent" section (the `is_pandemonium.then(|| view! {...}.into_any())` HIGHEST-RISK construct at lines 898, 911)
    - src/pages/draft.rs (lines 4142-4248 — DraftLedgerView Pandemonium arm) — read only if branching to per-page bisect
    - src/pages/solo_dashboard.rs (SoloConstellationContent ~859, SoloForgeView ~1509, SoloJournalView ~1372) — read only if branching to per-page bisect
  </read_first>
  <action>BRANCH on Task 1's `## Cycles 5-8 summary` decision:

CASE A — Culprit confirmed in cycles 1-8: skip cycles 9-13. Go directly to "Root cause analysis" + "Final culprit list".

CASE B — All leaves clean, proceed to per-page bisect: Execute cycles 9-13 against the five per-page Pandemonium arms (DraftLedgerView, SoloConstellationContent, SoloForgeView, SoloJournalView, TeamGameDayBriefView). Use the textual short-circuit marker (`return view! { <div>"PANDEMONIUM_SHORT_CIRCUITED"</div> }.into_any();` at the top of the page-component's is_pandemonium arm) so the agent-browser snapshot makes the short-circuit obvious. Test on the SAME page being short-circuited (e.g. Cycle 9 DraftLedgerView → test /draft with mode=ledger). Record per-cycle outcome same shape as cycles 5-8. Revert each mutation before next cycle.

CASE C — All leaves AND per-page views clean, panic persists: ESCALATE per D-04. Add an `## D-04 Escalation — WASM render-tracing` H2 to DIAGNOSIS.md documenting the decision. Then instrument suspect primitives (start with the leaf primitives that mount on every Pandemonium page: Card, SectionHead, Glitch, RiotTape) by inserting at the TOP of each render fn: `#[cfg(feature = "hydrate")] web_sys::console::log_1(&format!("HYDRATE: {ComponentName} region={}", region).into());` — instrument all-at-once per Claude's Discretion bias-for-speed. `cargo leptos watch` rebuild; agent-browser navigate to /solo Pandemonium; capture the full hydrate console; identify the LAST `HYDRATE: ...` log before the tachys panic — that primitive is the cursor-desync point. Record findings in DIAGNOSIS.md under the escalation section. REVERT all tracing log calls before completing the task.

After whichever case applies, ALWAYS append:

`## Root cause analysis` — Cite the exact divergence shape per RESEARCH "Anti-Patterns to Avoid" (lines 165-171). Categories: (i) `view! {}` empty fragment adjacent to {children()}; (ii) `is_pandemonium.then(|| view!{...}.into_any())` Option<AnyView> adjacency (leptos-rs#3583); (iii) different-child-count branches under same `move ||` parent; (iv) Vec<AnyView> next to literal text; (v) Suspense-fallback divergence. Name the file:line where the divergence is encoded. Reference the relevant Leptos issue (#3583, #3267, #2917, #3360 per RESEARCH sources).

`## Final culprit list` — Bulleted list of every primitive/view that DID NOT clear the panic when restored (i.e. the components that need fixing). Each bullet: `- src/components/{region|pages}/{file}.rs:{lineN}` — {primitive/view name} — {Anti-Pattern category from above}. Plan 03 reads this list to select fix patterns.

After the analysis sections, run both compile targets to confirm a clean state. Final commit MUST have all bisect mutations and tracing logs reverted.</action>
  <verify>
    <automated>grep -q "## Root cause analysis" .planning/phases/18.2-pandemonium-hydration-reconciliation-gap-closure-for-ui-18-1/18.2-DIAGNOSIS.md &amp;&amp; grep -q "## Final culprit list" .planning/phases/18.2-pandemonium-hydration-reconciliation-gap-closure-for-ui-18-1/18.2-DIAGNOSIS.md &amp;&amp; ! grep -RE 'return view! \{ &lt;&gt;&lt;/&gt; \}\.into_any\(\);|PANDEMONIUM_SHORT_CIRCUITED|HYDRATE: .* region=' src/components/region/ src/pages/ 2>/dev/null &amp;&amp; cargo check --features ssr 2>&amp;1 | tail -1 | grep -qv "error" &amp;&amp; cargo check --features hydrate --target wasm32-unknown-unknown 2>&amp;1 | tail -1 | grep -qv "error"</automated>
  </verify>
  <acceptance_criteria>
    - DIAGNOSIS.md contains `## Root cause analysis` H2 naming the divergence shape (one of: empty-fragment-adjacency, Option<AnyView>-adjacency, child-count-mismatch, Vec<AnyView>-text-adjacency, Suspense-divergence) and citing the file:line of the divergence site.
    - DIAGNOSIS.md contains `## Final culprit list` H2 with at least one bulleted file:line — primitive/view — Anti-Pattern entry. (If Case A and zero culprits — impossible since Plan 01 reproduced the panic — the file MUST contain a "BISECT FAILED TO ISOLATE — UPSTREAM LEPTOS ISSUE SUSPECTED" subsection with rationale and Leptos issue link.)
    - No short-circuit mutations remain in source files: `grep -RE 'PANDEMONIUM_SHORT_CIRCUITED|return view! \{ <></> \}\.into_any\(\);' src/` returns empty.
    - No tracing log calls remain: `grep -RE 'HYDRATE: .* region=' src/components/region/ src/pages/` returns empty.
    - Both compile targets exit 0.
    - If Case B applied: at least 1 `### Cycle [9-9]:|### Cycle 1[0-3]:` section exists.
    - If Case C applied: `## D-04 Escalation` H2 exists with capture of the last-logged primitive before the panic.
  </acceptance_criteria>
  <done>Final culprit list + root cause analysis complete in DIAGNOSIS.md; all bisect/tracing mutations reverted; both compile targets clean; Plan 03 has unambiguous list of components to fix.</done>
</task>

</tasks>

<verification>
- `grep -q "## Final culprit list" .planning/phases/18.2-.../18.2-DIAGNOSIS.md`
- `grep -q "## Root cause analysis" .planning/phases/18.2-.../18.2-DIAGNOSIS.md`
- `! grep -RE 'PANDEMONIUM_SHORT_CIRCUITED|HYDRATE: .* region=' src/` — no stale mutations
- `cargo check --features ssr` exits 0
- `cargo check --features hydrate --target wasm32-unknown-unknown` exits 0
- `cargo test --features ssr --lib` exits 0 (no regressions on unit tests)
</verification>

<success_criteria>
1. `18.2-DIAGNOSIS.md` has Final culprit list with at least 1 entry mapping file:line → Anti-Pattern category.
2. Root cause analysis section names the specific Leptos hydration anti-pattern that produced the divergence.
3. All bisect mutations and D-04 tracing logs reverted; both compile targets clean.
4. Plan 03 can read the Final culprit list and immediately select the matching RESEARCH Fix Pattern (1: align node counts; 2: stable placeholder; 3: <Show fallback>; 4: cfg(ssr) fallback).
</success_criteria>

<output>
After completion, create `.planning/phases/18.2-pandemonium-hydration-reconciliation-gap-closure-for-ui-18-1/18.2-02-SUMMARY.md` recording:
- Whether escalation to D-04 fired
- Final culprit list (file:line per entry)
- Recommended fix pattern per culprit (Plan 03 input)
- Whether D-10 fallback gate is likely to fire (estimate: > 1 primitive needs reshaping OR > 1 day work)
</output>

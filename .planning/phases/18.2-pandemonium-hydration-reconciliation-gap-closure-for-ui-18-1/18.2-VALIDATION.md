---
phase: 18.2
slug: pandemonium-hydration-reconciliation-gap-closure-for-ui-18-1
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-05-26
---

# Phase 18.2 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Derived from `18.2-RESEARCH.md` §"Validation Architecture" + `18.2-CONTEXT.md` D-05..D-16.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | `@playwright/test` (e2e/, pre-configured) + `cargo test --features ssr --lib` (Rust unit) |
| **Config file** | `e2e/playwright.config.ts` (existing) |
| **Quick run command** | `cd e2e && npx playwright test hydration-no-panic.spec.ts` |
| **Full suite command** | `just verify` (both compile targets + tests + lint + fmt) and `just e2e` (full Playwright suite) |
| **Estimated runtime** | ~3 min for new spec; ~10 min for full phase gate including agent-browser re-sweep |

---

## Sampling Rate

- **After every task commit:** `cargo check --features ssr` + `cargo check --features hydrate --target wasm32-unknown-unknown` (fast type-check, ~10s each)
- **After every primitive bisect step (Wave 1):** agent-browser navigates to `/solo` with `Cookie: lol_companion_theme=pandemonium`, captures JS console — records panic-or-clear in `18.2-DIAGNOSIS.md`
- **After every plan wave:** `cd e2e && npx playwright test hydration-no-panic.spec.ts` (full new spec, both regions, all 7 scoped routes) ~3 min
- **Before `/gsd:verify-work`:** `just verify` + full `just e2e` + agent-browser re-sweep on 7 Pandemonium scoped routes
- **Max feedback latency:** 30s per bisect cycle; 3 min per wave; 10 min per phase gate

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 18.2-W0-01 | 01 | 0 | SC-1 / SC-6 / SC-9 | — | Spec asserts no panic on every scoped page × region | e2e | `cd e2e && npx playwright test hydration-no-panic.spec.ts -g "hydrates without tachys panic"` | ❌ W0 | ⬜ pending |
| 18.2-W0-02 | 01 | 0 | SC-3 | — | Mode-toggle DOM marker change within 2s | e2e | `cd e2e && npx playwright test hydration-no-panic.spec.ts -g "mode-toggle"` | ❌ W0 | ⬜ pending |
| 18.2-W0-03 | 01 | 0 | SC-4 | — | Theme-toggle round-trip without panic / state loss | e2e | `cd e2e && npx playwright test hydration-no-panic.spec.ts -g "theme-toggle round-trip"` | ❌ W0 | ⬜ pending |
| 18.2-W0-04 | 01 | 0 | SC-5 | — | Draft slot drag-drop dispatches and slot updates | e2e | `cd e2e && npx playwright test hydration-no-panic.spec.ts -g "drag-drop"` | ❌ W0 | ⬜ pending |
| 18.2-W1-XX | bisect plan | 1 | SC-1 (diagnose) | — | Identify culprit primitive(s) via short-circuit-and-reload | manual + agent-browser | per-primitive: agent-browser navigate + observe console | ✅ tooling exists | ⬜ pending |
| 18.2-W1-XX | fix plan | 1 | SC-1 (resolve) | — | Apply Leptos 0.8 fix pattern OR cfg(ssr) fallback | unit + e2e | `cargo check --features hydrate --target wasm32-unknown-unknown` then new spec | ✅ tooling exists | ⬜ pending |
| 18.2-W1-XX | diagnosis doc | 1 | SC-2 | — | DIAGNOSIS.md captures bisect trail + culprit + fix rationale | manual artifact | `test -s .planning/phases/18.2-.../18.2-DIAGNOSIS.md` | ❌ W1 | ⬜ pending |
| 18.2-W2-XX | UAT flip | 2 | SC-7 | — | UI-18.1-HYDRATE-01 marked RESOLVED in 18-HUMAN-UAT.md (preserve reported: block) | manual artifact | `grep -A 3 "UI-18.1-HYDRATE-01" .planning/phases/18-region-variants/18-HUMAN-UAT.md` | ✅ exists, needs edit | ⬜ pending |
| 18.2-W2-XX | regression check | 2 | SC-8 | — | All compile targets + unit + e2e clean | unit + e2e | `just verify && just e2e` | ✅ tooling exists | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `e2e/tests/hydration-no-panic.spec.ts` — NEW spec; covers SC-1, SC-3, SC-4, SC-5, SC-6, SC-9.
  - Iterates the 7 scoped Pandemonium pages × {demacia, pandemonium} for the panic gate.
  - Implements D-13 interactivity triad (mode-toggle on /draft + /solo + /team/dashboard, theme-toggle round-trip, draft-slot drag-drop).
  - Reuses `authedPage`, `setRegion`, `setMode` from `e2e/tests/fixtures.ts` — no new fixtures.
  - `page.on('pageerror')` + filtered `page.on('console')` listener (D-08 regex: `/panicked at|Option::unwrap.*None|tachys/i`).
  - 2s `expect.toBeVisible({ timeout: 2000 })` polling per D-14.
- [ ] No new Rust test stubs — Rust unit-test coverage of theme_injection from Phase 18.1 stands; 18.2's surface is reactive-rendering shape, not Rust-testable.
- [ ] No new fixtures — existing `e2e/tests/fixtures.ts` covers auth + region + mode helpers.

*If none: "Existing infrastructure covers all phase requirements."*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Culprit identification + bisect trail | SC-2 | The bisect IS a manual + agent-browser-driven procedure; no automated way to "find the culprit" exists. Recording the trail in `18.2-DIAGNOSIS.md` is the audit artifact. | Per-primitive short-circuit, reload page with Cookie=pandemonium, capture JS console; record each cycle in DIAGNOSIS.md with primitive name + observed result. |
| Visual Pandemonium parity post-fix | SC-1 (qualitative) | Pixel-diff is already covered by `region-diff.spec.ts`; 18.2 needs a quick "does it still LOOK right" sanity check by human eye after fix. | Agent-browser sweep of all 7 scoped routes under both regions; visually confirm Pandemonium retains zine/grindhouse aesthetic and Demacia retains clean classical look. |
| UAT artifact flip | SC-7 | Editing 18-HUMAN-UAT.md is a human-readable audit document — automation here would mask provenance. | Manually edit the finding entry: keep original `reported:` block verbatim; add `resolved:` block citing 18.2-DIAGNOSIS.md and the fix commit hash. |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify (bisect cycles are themselves "automated" via agent-browser observation)
- [ ] Wave 0 covers all MISSING references (hydration-no-panic.spec.ts is the sole new file)
- [ ] No watch-mode flags
- [ ] Feedback latency < 3 min for the spec; < 10 min for the phase gate
- [ ] `nyquist_compliant: true` set in frontmatter after planner approves

**Approval:** pending

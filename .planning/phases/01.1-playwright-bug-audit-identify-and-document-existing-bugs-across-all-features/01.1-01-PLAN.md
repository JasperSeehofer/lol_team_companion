---
phase: 01.1-playwright-bug-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - e2e/tests/fixtures.ts
  - e2e/tests/audit-draft.spec.ts
  - e2e/tests/audit-tree-drafter.spec.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "teamPage fixture registers a user, creates a team, and lands on /team/dashboard"
    - "Draft audit tests exercise save, list, load, update, and delete flows"
    - "Tree-drafter audit tests exercise tree creation, node editing, and tree switching"
    - "Known bug 'saved draft not in list' is reproduced and annotated with test.fail()"
    - "Known bug 'tree drafter unresponsive on switch' is reproduced and annotated with test.fail()"
  artifacts:
    - path: "e2e/tests/fixtures.ts"
      provides: "teamPage fixture and createTeam helper"
      contains: "teamPage"
    - path: "e2e/tests/audit-draft.spec.ts"
      provides: "Draft CRUD audit tests including known bug"
      min_lines: 50
    - path: "e2e/tests/audit-tree-drafter.spec.ts"
      provides: "Tree drafter audit tests including known bug"
      min_lines: 50
  key_links:
    - from: "e2e/tests/audit-draft.spec.ts"
      to: "e2e/tests/fixtures.ts"
      via: "import { test, expect } from './fixtures'"
      pattern: "import.*fixtures"
    - from: "e2e/tests/audit-tree-drafter.spec.ts"
      to: "e2e/tests/fixtures.ts"
      via: "import { test, expect } from './fixtures'"
      pattern: "import.*fixtures"
---

<objective>
Extend e2e fixtures with team-aware auth, then write audit tests for the two features with known bugs: draft planner and tree drafter.

Purpose: Establish the teamPage fixture other audit tests depend on, and immediately test the two confirmed bugs so they are documented with test.fail() annotations.
Output: Updated fixtures.ts, audit-draft.spec.ts, audit-tree-drafter.spec.ts
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01.1-playwright-bug-audit-identify-and-document-existing-bugs-across-all-features/01.1-RESEARCH.md
@e2e/tests/fixtures.ts
@e2e/tests/pipeline.spec.ts
@e2e/tests/pages.spec.ts
@e2e/playwright.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend fixtures with teamPage and createTeam helper</name>
  <files>e2e/tests/fixtures.ts</files>
  <action>
Add a `createTeam(page, name)` async helper function and a `teamPage` fixture to `e2e/tests/fixtures.ts`.

The `createTeam` helper should:
1. Navigate to `/team/roster`
2. Wait for networkidle + 500ms WASM settle
3. Check if `input[name="name"]` is visible (user may already have a team)
4. If visible: fill team name, select "NA" region, click "Create Team", wait for redirect to `/team/dashboard`
5. Wait for networkidle + 1000ms settle after team creation

Pattern: Follow the team creation code already in `pipeline.spec.ts` lines 25-40.

The `teamPage` fixture should:
1. Call `authenticatePage(page)` (existing function)
2. Call `createTeam(page, \`AuditTeam_\${Date.now()}\`)`
3. Pass `page` to `use()`

Update the `Fixtures` type to include `teamPage: Page`.

Export `createTeam` so other test files can use it if needed.

Do NOT modify the existing `authedPage` fixture or `authenticatePage` function.
  </action>
  <verify>
    <automated>cd /home/jasper/Repositories/lol_team_companion/e2e && npx playwright test tests/pages.spec.ts --project=chromium 2>&1 | tail -5</automated>
  </verify>
  <done>fixtures.ts exports teamPage fixture and createTeam helper. Existing tests still pass.</done>
</task>

<task type="auto">
  <name>Task 2: Write draft planner audit tests</name>
  <files>e2e/tests/audit-draft.spec.ts</files>
  <action>
Create `e2e/tests/audit-draft.spec.ts` with the following tests using the `teamPage` fixture (import from `./fixtures`).

Set up console error capture at test level (pageerror + console.error), filter out favicon/404 errors.

**Test 1: "draft: save new draft and verify Update Draft button appears"**
- Navigate to `/draft`, wait for networkidle + 1000ms
- Fill the first textbox with `AuditDraft_${Date.now()}`
- Click `button:has-text("Save Draft")`
- Wait 2000ms for server round-trip
- Assert `button:has-text("Update Draft")` is visible (timeout 5000ms)
- Assert no real console errors

**Test 2: "draft: saved draft appears in saved drafts list" (test.fail — KNOWN BUG)**
- Use `test.fail()` annotation — this documents the known bug
- Comment: `// KNOWN BUG: After saving a draft, it does not appear in the saved drafts list.`
- Comment: `// Suspected cause: Suspense/Resource refetch subscription issue (see RESEARCH.md Pitfall 4)`
- Navigate to `/draft`, wait for networkidle + 1000ms
- Create a uniquely named draft `AuditListBug_${Date.now()}`
- Click "Save Draft", wait 2000ms
- Assert "Update Draft" button visible (save succeeded)
- Look for the saved drafts section on the page (locate text "Saved Drafts" or similar heading, then search within its parent/sibling container)
- Assert the draft name appears in that section (timeout 5000ms)

**Test 3: "draft: load draft from saved list"**
- This test may depend on drafts being visible in the list. If the list bug prevents loading, annotate with test.fail() and a comment explaining dependency on Bug from Test 2.
- Navigate to `/draft`, wait for networkidle + 1500ms
- Save a draft with a unique name
- Wait 2000ms
- Try to find and click the draft name in the saved drafts list
- If found: verify the textbox contains the draft name, verify "Update Draft" button is visible
- If not found (list bug): the test should fail naturally

**Test 4: "draft: delete a draft"**
- Navigate to `/draft`, wait for networkidle + 1000ms
- Save a draft, wait for "Update Draft" to appear
- Look for a delete button (could be a trash icon, "Delete" text, or similar)
- If delete exists: click it, wait 1000ms, verify the draft is no longer loaded (button reverts to "Save Draft" or page resets)
- If no delete button is found: skip with `test.skip("No delete button found on draft page")`

**Test 5: "draft: no console errors during interactions"**
- Navigate to `/draft`, wait for hydration
- Interact: fill name, pick 3 champions from the grid, toggle blue/red side if toggle exists
- Assert no real console errors after all interactions

Use generous timeouts (waitForTimeout) after saves — the server round-trip + WASM refetch cycle is slow.
  </action>
  <verify>
    <automated>cd /home/jasper/Repositories/lol_team_companion/e2e && npx playwright test tests/audit-draft.spec.ts --project=chromium 2>&1 | tail -15</automated>
  </verify>
  <done>audit-draft.spec.ts exists with 5 tests. Tests that pass confirm working features. Tests that fail are annotated with test.fail() if they reproduce known bugs, or left as natural failures for the bug report.</done>
</task>

<task type="auto">
  <name>Task 3: Write tree drafter audit tests</name>
  <files>e2e/tests/audit-tree-drafter.spec.ts</files>
  <action>
Create `e2e/tests/audit-tree-drafter.spec.ts` with the following tests using the `teamPage` fixture.

Set up console error + pageerror capture per test.

**Test 1: "tree-drafter: create a new tree"**
- Navigate to `/tree-drafter`, wait for networkidle + 1000ms
- Look for a "New Tree" or "Create Tree" button/input
- If found: fill tree name `AuditTree_${Date.now()}`, submit
- Wait 2000ms for save
- Verify the tree name appears somewhere on the page (sidebar list or heading)
- Assert no console errors

**Test 2: "tree-drafter: add and edit a node"**
- Navigate to `/tree-drafter`, wait + create a tree (or use existing)
- Look for "Add Node" or similar button
- Click it, wait 500ms
- Look for a node label input, fill with `TestNode_${Date.now()}`
- Wait 2000ms for auto-save
- Verify the node label appears in the tree visualization area (SVG or DOM)
- Assert no console errors

**Test 3: "tree-drafter: switch between trees remains interactive" (test.fail — KNOWN BUG)**
- Use `test.fail()` annotation
- Comment: `// KNOWN BUG: Switching between trees makes the app unresponsive.`
- Comment: `// Suspected cause: suppress_autosave timing issue during batch signal updates (CLAUDE.md rules 54-55)`
- Create two trees: `TreeA_${Date.now()}` and `TreeB_${Date.now()}`
- Wait for each to save
- Click TreeA in the sidebar
- Wait 1000ms
- Click TreeB in the sidebar
- Wait 1000ms
- Click TreeA again
- Wait 500ms
- Verify the page is still interactive: check that "Add Node" (or similar) button is enabled and clickable
- Try clicking it and verify the click was processed (new node input appears, or node count changes)
- Assert no WASM panics via pageerror capture

**Test 4: "tree-drafter: delete a tree"**
- Create a tree, verify it appears
- Look for a delete button/icon near the tree name in sidebar
- If found: click it, handle any confirmation dialog, wait 1000ms, verify tree name is gone from sidebar
- If no delete found: skip with `test.skip("No delete mechanism found")`

Be generous with timeouts. The tree-drafter has debounced auto-save (2000ms timer per CLAUDE.md rule 42), so wait at least 3000ms after edits before asserting state.
  </action>
  <verify>
    <automated>cd /home/jasper/Repositories/lol_team_companion/e2e && npx playwright test tests/audit-tree-drafter.spec.ts --project=chromium 2>&1 | tail -15</automated>
  </verify>
  <done>audit-tree-drafter.spec.ts exists with 4 tests. Known unresponsiveness bug is documented with test.fail(). Other tests verify basic CRUD operations.</done>
</task>

</tasks>

<verification>
Run all existing tests plus new audit tests to ensure no regressions:
```bash
cd e2e && npx playwright test --project=chromium 2>&1 | tail -20
```
All existing tests should pass. New audit tests either pass (feature works) or are annotated with test.fail() (known bug documented).
</verification>

<success_criteria>
- fixtures.ts has teamPage fixture and createTeam helper exported
- audit-draft.spec.ts has 5 tests covering save, list (known bug), load, delete, error capture
- audit-tree-drafter.spec.ts has 4 tests covering create, edit, switch (known bug), delete
- Known bugs are annotated with test.fail() so the suite stays runnable
- Existing e2e tests are unaffected
</success_criteria>

<output>
After completion, create `.planning/phases/01.1-playwright-bug-audit-identify-and-document-existing-bugs-across-all-features/01.1-01-SUMMARY.md`
</output>

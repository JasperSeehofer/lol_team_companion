---
phase: 03-smart-dashboard
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - e2e/tests/audit-team.spec.ts
autonomous: false
requirements:
  - INTL-01

must_haves:
  truths:
    - "E2e test verifies dashboard renders action items panel heading and empty state CTA"
    - "E2e test verifies dashboard renders post-game panel heading and empty state CTA"
    - "E2e test verifies dashboard renders pool gap panel heading and empty state CTA"
  artifacts:
    - path: "e2e/tests/audit-team.spec.ts"
      provides: "Dashboard smart panel e2e tests"
      contains: "Recent Reviews"
  key_links:
    - from: "e2e/tests/audit-team.spec.ts"
      to: "/team/dashboard"
      via: "teamPage fixture navigates to dashboard"
      pattern: "teamPage"
---

<objective>
Add Playwright e2e tests verifying the three new dashboard panels render with correct empty states and CTA links for a new team.

Purpose: The `teamPage` fixture creates a fresh team with no data, making it a natural test for the empty-state CTAs required by INTL-01 SC-5. These tests also confirm the panels render at all (SC-1, SC-2, SC-3).
Output: Updated `e2e/tests/audit-team.spec.ts` with 3 new test cases.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-smart-dashboard/03-RESEARCH.md
@e2e/tests/audit-team.spec.ts
@e2e/tests/fixtures.ts

<interfaces>
<!-- Panel headings from Plan 01 Task 2 -->
- Action Items panel heading: "Open Action Items"
- Post-game panel heading: "Recent Reviews"
- Pool gap panel heading: "Pool Gap Warnings"

<!-- Empty state CTA text from Plan 01 Task 2 -->
- Action Items empty: "No open action items." (already exists)
- Post-game empty: "No post-game reviews yet." with link "Start your first review"
- Pool gaps empty: "No pool gaps detected." with link "Manage champion pools"

<!-- teamPage fixture: registers user, creates team, lands on /team/dashboard -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add dashboard smart panel e2e tests</name>
  <files>e2e/tests/audit-team.spec.ts</files>
  <action>
Add three new tests to `audit-team.spec.ts`, using the existing `teamPage` fixture (which creates a fresh team with no data and lands on `/team/dashboard`). Follow the existing test patterns in the file (captureErrors, filterRealErrors, waitForLoadState, waitForTimeout for WASM settle).

Each test should:
1. Use `teamPage` fixture
2. Ensure page is on `/team/dashboard` (same guard pattern as "dashboard shows roster slots" test)
3. Wait for network idle + WASM settle (500ms)
4. Look for the panel heading
5. Look for the empty state text or CTA link
6. Assert no real console errors

**Test 1: "team: dashboard shows action items panel"**
- Look for heading text "Open Action Items" via `page.locator('h3').filter({ hasText: /Open Action Items/i })`
- Verify visible within 5000ms timeout
- Look for empty state text containing "No open action items" OR for "View all" link to `/action-items`
- Assert filterRealErrors is empty

**Test 2: "team: dashboard shows post-game reviews panel"**
- Look for heading text "Recent Reviews" via `page.locator('h3').filter({ hasText: /Recent Reviews/i })`
- Verify visible within 5000ms timeout
- Look for empty state CTA text "Start your first review" or "No post-game reviews yet"
- Assert filterRealErrors is empty

**Test 3: "team: dashboard shows pool gap warnings panel"**
- Look for heading text "Pool Gap Warnings" via `page.locator('h3').filter({ hasText: /Pool Gap Warnings/i })`
- Verify visible within 5000ms timeout
- Look for empty state CTA text "Manage champion pools" or "No pool gaps detected"
- Assert filterRealErrors is empty

Use `isVisible({ timeout: 5000 }).catch(() => false)` pattern with `console.log("AUDIT-FINDING: ...")` for soft failures (matching the existing test style in this file), but also add hard `expect` assertions for the panel headings being visible — these are new panels that MUST render.
  </action>
  <verify>
    <automated>cd /home/jasper/Repositories/lol_team_companion && npx playwright test audit-team.spec.ts --timeout 60000 2>&1 | tail -20</automated>
  </verify>
  <done>Three new e2e tests pass, confirming dashboard panels render with empty state CTAs for new teams.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify smart dashboard panels visually</name>
  <files>src/pages/team/dashboard.rs</files>
  <action>
Human verifies the smart dashboard panels render correctly in the browser. No code changes needed — this is visual verification of Plan 01 + Task 1 output.

Steps:
1. Start the dev server if not running: `cargo leptos watch`
2. Register a new user and create a new team (or use an existing account)
3. Navigate to `/team/dashboard`
4. Verify three panel sections are visible:
   - "Open Action Items" with "No open action items" or item list
   - "Recent Reviews" with "No post-game reviews yet" or review cards
   - "Pool Gap Warnings" with "No pool gaps detected" or warning cards
5. Each panel has a link to the relevant feature page
6. Panels appear independently (not all at once after a long wait)
7. No error banners or blank sections visible
  </action>
  <verify>Human confirms panels render correctly</verify>
  <done>All three dashboard panels render with correct empty states and CTA links. No visual issues.</done>
</task>

</tasks>

<verification>
1. `npx playwright test audit-team.spec.ts` — all tests pass
2. Visual confirmation that panels render correctly
</verification>

<success_criteria>
- E2e tests for all three panels pass
- Human verifies panels render correctly with empty states and CTAs
- No regressions in existing dashboard functionality
</success_criteria>

<output>
After completion, create `.planning/phases/03-smart-dashboard/03-02-SUMMARY.md`
</output>

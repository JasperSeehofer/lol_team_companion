---
phase: 06-bug-fixes
plan: 03
type: execute
wave: 3
depends_on:
  - "06-01"
  - "06-02"
files_modified:
  - e2e/tests/audit-tree-drafter.spec.ts
  - e2e/tests/audit-draft.spec.ts
  - e2e/tests/audit-game-plan.spec.ts
  - e2e/tests/audit-champion-pool.spec.ts
  - e2e/tests/audit-team.spec.ts
  - e2e/tests/audit-misc-pages.spec.ts
  - e2e/tests/audit-post-game.spec.ts
  - e2e/tests/pipeline.spec.ts
autonomous: true
requirements:
  - TEST-02
must_haves:
  truths:
    - "All existing audit specs import captureErrors and filterRealErrors from helpers.ts instead of defining them locally"
    - "All existing audit specs use navigateTo from helpers.ts for page navigation instead of raw page.goto + manual waits"
    - "Full e2e suite passes green with no test failures"
  artifacts:
    - path: "e2e/tests/audit-tree-drafter.spec.ts"
      provides: "Refactored to use shared helpers"
    - path: "e2e/tests/audit-draft.spec.ts"
      provides: "Refactored to use shared helpers"
    - path: "e2e/tests/audit-game-plan.spec.ts"
      provides: "Refactored to use shared helpers"
    - path: "e2e/tests/audit-champion-pool.spec.ts"
      provides: "Refactored to use shared helpers"
    - path: "e2e/tests/audit-team.spec.ts"
      provides: "Refactored to use shared helpers"
    - path: "e2e/tests/audit-misc-pages.spec.ts"
      provides: "Refactored to use shared helpers"
    - path: "e2e/tests/audit-post-game.spec.ts"
      provides: "Refactored to use shared helpers"
    - path: "e2e/tests/pipeline.spec.ts"
      provides: "Refactored to use shared helpers"
  key_links:
    - from: "e2e/tests/audit-*.spec.ts"
      to: "e2e/tests/helpers.ts"
      via: "import { captureErrors, filterRealErrors, navigateTo }"
      pattern: "from.*./helpers"
---

<objective>
Refactor all existing audit specs to use shared helpers from `helpers.ts` (centralized error capture, navigation with hydration settle) and verify the full e2e suite passes green.

Purpose: Eliminates copy-pasted utility code across audit specs and ensures all tests use consistent patterns (hydration settle, error filtering). Completes TEST-02 by making the Playwright CLI workflow fully operational.
Output: 8 refactored spec files all importing from helpers.ts, full suite green
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06-bug-fixes/06-CONTEXT.md
@.planning/phases/06-bug-fixes/06-01-SUMMARY.md
@.planning/phases/06-bug-fixes/06-02-SUMMARY.md

<interfaces>
From e2e/tests/helpers.ts (created in Plan 01):
```typescript
export function captureErrors(page: Page): string[];
export function filterRealErrors(errors: string[]): string[];
export async function navigateTo(page: Page, route: string): Promise<void>;
export async function createDraft(page: Page, name: string): Promise<void>;
export async function saveGamePlan(page: Page, name: string): Promise<void>;
export function captureHydrationWarnings(page: Page): string[];
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor audit specs to use shared helpers</name>
  <files>e2e/tests/audit-tree-drafter.spec.ts, e2e/tests/audit-draft.spec.ts, e2e/tests/audit-game-plan.spec.ts, e2e/tests/audit-champion-pool.spec.ts, e2e/tests/audit-team.spec.ts, e2e/tests/audit-misc-pages.spec.ts, e2e/tests/audit-post-game.spec.ts, e2e/tests/pipeline.spec.ts</files>
  <read_first>
    - e2e/tests/helpers.ts (available exports — the source of truth for shared utilities)
    - e2e/tests/audit-tree-drafter.spec.ts (has local captureErrors/filterRealErrors — lines 37-50 — to be replaced with import)
    - e2e/tests/audit-draft.spec.ts (check for local captureErrors/filterRealErrors and raw page.goto patterns)
    - e2e/tests/audit-game-plan.spec.ts (check for local captureErrors/filterRealErrors and raw page.goto patterns)
    - e2e/tests/audit-champion-pool.spec.ts (check for local captureErrors/filterRealErrors and raw page.goto patterns)
    - e2e/tests/audit-team.spec.ts (check for local captureErrors/filterRealErrors and raw page.goto patterns)
    - e2e/tests/audit-misc-pages.spec.ts (check for local captureErrors/filterRealErrors and raw page.goto patterns)
    - e2e/tests/audit-post-game.spec.ts (check for local captureErrors/filterRealErrors and raw page.goto patterns)
    - e2e/tests/pipeline.spec.ts (check for raw page.goto + manual waitForLoadState + waitForTimeout patterns)
  </read_first>
  <action>
For each of the 8 spec files, apply these mechanical refactoring steps:

**Step A — Replace local captureErrors/filterRealErrors with import:**
1. If the file defines `captureErrors` and/or `filterRealErrors` locally, delete those function definitions
2. Add `import { captureErrors, filterRealErrors } from "./helpers";` (merge with existing imports if any)
3. All call sites remain unchanged — same function signature

**Step B — Replace raw navigation with navigateTo:**
1. Find patterns like: `await page.goto("/some-route"); await page.waitForLoadState("networkidle"); await page.waitForTimeout(500);`
2. Replace with: `await navigateTo(page, "/some-route");`
3. Add `navigateTo` to the helpers import
4. Note: If a spec has a goto followed by a DIFFERENT wait pattern (e.g., `waitForTimeout(1000)` or `waitForTimeout(2000)`), keep the custom wait — only replace the standard 500ms hydration settle pattern. If the spec uses `waitForTimeout(1000)` after goto, replace with `await navigateTo(page, route); await page.waitForTimeout(500);` (navigateTo does 500ms, extra 500ms for spec-specific needs).

**Step C — Replace raw draft creation with createDraft (where applicable):**
1. If a spec manually creates a draft (fills name input, clicks save, waits), check if `createDraft` from helpers matches the flow
2. Only replace if the flow matches — don't force it for specs with custom draft setup logic

**Spec-specific notes:**
- `audit-tree-drafter.spec.ts`: Has its own `createTree` helper function — keep it (tree creation is spec-specific, not in shared helpers). Only replace `captureErrors`/`filterRealErrors` and raw navigation.
- `pipeline.spec.ts`: Has the most complex flow — replace navigation patterns where straightforward, but preserve the pipeline-specific sequencing.
- Files that don't define captureErrors locally: only apply Step B (navigation refactoring).

**Do NOT change test logic, assertions, or test structure** — only replace utility function implementations with shared imports.
  </action>
  <verify>
    <automated>cd /home/jasper/Repositories/lol_team_companion/e2e && npx playwright test --list 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - e2e/tests/audit-tree-drafter.spec.ts contains `from "./helpers"` import
    - e2e/tests/audit-tree-drafter.spec.ts does NOT contain `function captureErrors` (local definition removed)
    - e2e/tests/audit-tree-drafter.spec.ts does NOT contain `function filterRealErrors` (local definition removed)
    - Every audit-*.spec.ts file that previously had local captureErrors now imports from "./helpers"
    - At least 4 spec files contain `navigateTo` usage (replacing raw goto+wait patterns)
    - No spec file has BOTH a local `captureErrors` function AND an import from "./helpers" (no duplication)
    - `cd e2e && npx playwright test --list` exits 0 (all tests discoverable, no import errors)
  </acceptance_criteria>
  <done>All audit specs import shared utilities from helpers.ts. No local captureErrors/filterRealErrors definitions remain. Navigation uses navigateTo where the standard pattern was used.</done>
</task>

<task type="auto">
  <name>Task 2: Run full e2e suite and fix any failures</name>
  <files>e2e/tests/regression.spec.ts</files>
  <read_first>
    - e2e/tests/regression.spec.ts (all 5 regression tests — verify none are skipped)
    - e2e/tests/helpers.ts (verify exports match what all specs import)
  </read_first>
  <action>
Run the full e2e test suite with `just e2e` (or `cd e2e && npx playwright test` if just is unavailable). This requires the dev server to be running (`cargo leptos watch`).

1. If all tests pass: done.
2. If any tests fail:
   - Read the failure output carefully
   - For refactoring-caused failures (import errors, missing function, wrong signature): fix the import or helpers.ts
   - For regression test failures (BUG-01 through BUG-05): the underlying bug fix from Plan 02 may need adjustment — fix the source Rust code
   - For pre-existing audit test failures unrelated to Phase 6 changes: document as known flaky/failing but do not block Phase 6 completion
3. Re-run until green or until only pre-existing failures remain

After suite passes, verify the regression spec specifically:
`cd e2e && npx playwright test regression.spec.ts`

All 5 regression tests must pass (not skipped, not failing).
  </action>
  <verify>
    <automated>cd /home/jasper/Repositories/lol_team_companion/e2e && npx playwright test 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `cd e2e && npx playwright test regression.spec.ts` exits 0 with 5 passed tests
    - `cd e2e && npx playwright test` exits 0 (full suite green) OR only pre-existing failures documented
    - No test file has TypeScript compilation errors
    - e2e/tests/regression.spec.ts contains zero `test.skip` calls
  </acceptance_criteria>
  <done>Full e2e suite passes green. All 5 regression tests pass. Playwright CLI workflow is fully operational for subsequent phases.</done>
</task>

</tasks>

<verification>
- `cd e2e && npx playwright test` — full suite green (requires running dev server)
- `cd e2e && npx playwright test regression.spec.ts` — 5/5 regression tests pass
- `grep -r "function captureErrors" e2e/tests/audit-*.spec.ts` returns 0 matches (all centralized)
- `grep -c "from.*./helpers" e2e/tests/audit-*.spec.ts e2e/tests/pipeline.spec.ts` shows imports in each file
</verification>

<success_criteria>
- All audit specs use shared helpers (no local captureErrors/filterRealErrors)
- Navigation patterns use navigateTo where applicable
- Full e2e suite passes green
- All 5 regression tests pass (not skipped)
- TEST-02 requirement fully met: Playwright CLI workflow operational with auth fixture, helpers, regression tests
</success_criteria>

<output>
After completion, create `.planning/phases/06-bug-fixes/06-03-SUMMARY.md`
</output>

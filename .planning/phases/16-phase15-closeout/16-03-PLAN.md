---
phase: 16-phase15-closeout
plan: 03
type: execute
wave: 2
depends_on:
  - 16-01
  - 16-02
files_modified:
  - .planning/phases/15-goals-lp-history/15-REVIEW.md
  - .planning/STATE.md
  - .planning/MILESTONES.md
autonomous: false
requirements:
  - REVIEW-RECONCILE
  - VERIFY-WORK-15
  - V12-SHIPPED
tags:
  - documentation
  - milestone-closeout
  - review

must_haves:
  truths:
    - "`/gsd-code-review 15` second pass produces 0 new HIGH/Critical findings attributable to Phase 16 commits (16-01, 16-02)"
    - "`/gsd-verify-work 15` reports PASS for all four ROADMAP Phase 15 success criteria"
    - "`15-REVIEW.md` contains an explicit `Status:` line for every CR/WR/IN finding (CR-01, CR-02, WR-01, WR-02, IN-01, IN-02, IN-03)"
    - "`15-REVIEW.md` contains a `## Second Pass (Phase 16 close-out)` section with the second-pass code-review output"
    - "`.planning/STATE.md` records v1.2 as Shipped with the close date (no longer reads 'Closing' or 'Pivoted')"
    - "`.planning/MILESTONES.md` contains a v1.2 Solo Mode & Match Intelligence entry summarising Phases 12-15 + Phase 16 close-out"
    - "Active milestone in STATE.md remains v1.3 (this phase does not bump the active milestone)"
  artifacts:
    - path: ".planning/phases/15-goals-lp-history/15-REVIEW.md"
      provides: "Reconciled review with Status: lines on all 7 findings + Second Pass section appended"
      contains: "Status: FIXED in 5902a81"
    - path: ".planning/STATE.md"
      provides: "STATE recording v1.2 as Shipped"
      contains: "Shipped 2026-05"
    - path: ".planning/MILESTONES.md"
      provides: "Milestones log with v1.2 entry prepended above v1.1"
      contains: "v1.2 Solo Mode"
  key_links:
    - from: "15-REVIEW.md WR-01 finding header"
      to: "Plan 16-01 commit hash"
      via: "Status: RESOLVED in Phase 16 commit <hash> annotation"
      pattern: "Status: RESOLVED in Phase 16 commit"
    - from: "15-REVIEW.md WR-02 finding header"
      to: "Plan 16-02 commit hash"
      via: "Status: RESOLVED in Phase 16 commit <hash> annotation"
      pattern: "Status: RESOLVED in Phase 16 commit"
    - from: ".planning/STATE.md prior_milestone narrative"
      to: ".planning/MILESTONES.md v1.2 entry"
      via: "matching 'Shipped 2026-05-DD' close date"
      pattern: "Shipped 2026-05"
---

<objective>
With Plans 16-01 (WR-01) and 16-02 (WR-02) committed, run the close-out review/verify chain and update planning docs to mark v1.2 as shipped:

1. Run `/gsd-code-review 15` second pass against the post-close-out state — gate: 0 new HIGH/Critical findings.
2. Run `/gsd-verify-work 15` against ROADMAP Phase 15 success criteria 1-4 — gate: PASS for all four.
3. Annotate `.planning/phases/15-goals-lp-history/15-REVIEW.md` with explicit `Status:` lines for every CR/WR/IN finding (per D-13, D-14, D-15) and append the second-pass review output as a `## Second Pass (Phase 16 close-out)` section (per D-16).
4. As the final commit, update `.planning/STATE.md` to record v1.2 as Shipped (per D-12) and prepend a v1.2 entry to `.planning/MILESTONES.md` (mirroring the existing v1.1 / v1.0 entry format).

Per CONTEXT.md decisions D-09 (strict serial order), D-10 (fix in scope if new HIGH/Critical surface), D-11 (fix in scope if verify-work regresses), D-12 (mark-shipped is inline, not delegated to `/gsd-complete-milestone`), D-13..D-16 (annotation format).

Purpose: Close the documentation loop on v1.2 — ensure 15-REVIEW.md is self-describing for future readers, and STATE/MILESTONES correctly reflect the milestone's shipped status before any v1.3 launch-readiness work begins.
Output: A reconciled `15-REVIEW.md`, an updated `STATE.md`, and a v1.2 entry in `MILESTONES.md`. All three updates land in commits with `docs(phase-16):` prefixes per the project commit convention.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-phase15-closeout/16-CONTEXT.md
@.planning/phases/16-phase15-closeout/16-SPEC.md
@.planning/phases/16-phase15-closeout/16-PATTERNS.md
@.planning/phases/15-goals-lp-history/15-REVIEW.md
@.planning/phases/16-phase15-closeout/16-01-SUMMARY.md
@.planning/phases/16-phase15-closeout/16-02-SUMMARY.md
@.planning/STATE.md
@.planning/MILESTONES.md
@.planning/ROADMAP.md

<interfaces>
<!-- Anchors for the executor. Verified 2026-05-06. -->

From `.planning/phases/15-goals-lp-history/15-REVIEW.md` — the 7 finding headers (line numbers per `grep -n "^### "`):
- Line 40: `### CR-01: WASM compile failure — LP graph hover uses undeclared variable names`
- Line 66: `### CR-02: Rule 40 violation in get_latest_ranked_snapshot — ORDER BY snapshotted_at on a field not in the SELECT list`
- Line 107: `### WR-01: LP history graph and goal progress stale after manual sync`
- Line 131: `### WR-02: get_personal_goals is a public DB function with no production caller`
- Line 150: `### IN-01: Hardcoded Data Dragon patch version in champion_icon_url`
- Line 168: `### IN-02: unwrap_or(None) anti-pattern`
- Line 182: `### IN-03: trends_window_to_cutoff marked #[allow(dead_code)]`

The `Status:` line goes immediately AFTER each header (per D-13: "first content line under the finding's header"). Concrete annotation strings (per D-14, D-15):
```
CR-01:  Status: FIXED in 5902a81 — underscore-prefix issue resolved by renaming snaps_for_hover/points_for_hover.
CR-02:  Status: FIXED in 5902a81 — '<string>snapshotted_at AS snapshotted_at' added to partial SELECT (Surreal Rule 40).
WR-01:  Status: RESOLVED in Phase 16 commit <16-01-HASH> — hoisted lp_history_resource into SoloDashboardPage; both sync paths refetch all three resources.
WR-02:  Status: RESOLVED in Phase 16 commit <16-02-HASH> — dead get_personal_goals removed; tests/db_personal_goal.rs deleted.
IN-01:  Status: DEFERRED to Phase 19 — dynamic Data Dragon version loading is the natural home.
IN-02:  Status: DEFERRED — info-only finding; address ad-hoc when surrounding code at db.rs:4832 is touched.
IN-03:  Status: DEFERRED — info-only finding; address ad-hoc when surrounding code is touched.
```

For the WR-01 / WR-02 hashes, read them from:
- `.planning/phases/16-phase15-closeout/16-01-SUMMARY.md` (the WR-01 commit hash, written by Plan 16-01's executor)
- `.planning/phases/16-phase15-closeout/16-02-SUMMARY.md` (the WR-02 commit hash, written by Plan 16-02's executor)

If the SUMMARY files are absent or incomplete, fall back to `git log --oneline -20` and pick the commits with `fix(phase-16):` prefix (WR-01) and `chore(phase-16):` prefix (WR-02) per D-07.

From `.planning/STATE.md` lines 1-14 (current frontmatter — must update line 5 `status:` field):
```yaml
---  # (separator)
gsd_state_version: 1.0
milestone: v1.3
milestone_name: Launch Readiness
status: Pivoted from v1.2 → v1.3 on 2026-05-06; entering Phase 16 (Phase 15 close-out)
stopped_at: Phase 16 context gathered
last_updated: "2026-05-06T15:55:31.398Z"
progress:
  total_phases: 12
  completed_phases: 4
  total_plans: 12
  completed_plans: 12
  percent: 100
---  # (separator)
```

Per D-12 + PATTERNS.md guidance: STATE.md does not currently have a structured `prior_milestone` key. Update the existing `status:` narrative to record v1.2 shipped, OR add a dedicated narrative entry under `## Decisions` or `## Current Position`. Required string per SPEC R6 acceptance: "Shipped 2026-05-DD" (replace DD with the actual close date — fill at commit time, e.g. `Shipped 2026-05-06`). Active milestone (line 3) MUST stay `v1.3`.

From `.planning/MILESTONES.md` lines 1-26 (top of file — v1.1 entry; new v1.2 entry prepends ABOVE this in descending date order):
```markdown
# Milestones

## v1.1 Polish, Draft & Opponents Rework (Shipped: 2026-03-24)

**Phases completed:** 6 phases, 17 plans, 20 tasks

**Key accomplishments:**
- [bullet list]

---  # (separator)

## v1.0 Cross-Feature Intelligence (Shipped: 2026-03-18)
...
```

The v1.2 entry must follow this exact structural pattern (heading + Phases completed + Key accomplishments + `---` separator), inserted between line 1 (`# Milestones`) blank line and the existing `## v1.1` heading.

From `.planning/ROADMAP.md` Phase 15 Success Criteria 1-4 (the `/gsd-verify-work 15` targets — lines 135-139):
```
1. User can view a line graph of LP history over time, derived from ranked snapshots captured at each sync
2. User can set goals including a rank target tier, CS/min threshold, and per-game death limit
3. User can see goal progress on the solo dashboard — each goal shows current average vs. the target derived from recent match history
4. User can view champion performance trends showing win rate, KDA, and CS/min per champion over a selectable time window
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Run /gsd-code-review 15 second pass — gate: 0 new HIGH/Critical findings</name>
  <files>.planning/phases/15-goals-lp-history/15-REVIEW.md</files>
  <read_first>
    - .planning/phases/16-phase15-closeout/16-01-SUMMARY.md (capture the Plan 01 commit hash for the diff scope)
    - .planning/phases/16-phase15-closeout/16-02-SUMMARY.md (capture the Plan 02 commit hash)
    - .planning/phases/15-goals-lp-history/15-REVIEW.md (read the existing review so the second-pass output can reference prior findings by ID)
    - .planning/phases/16-phase15-closeout/16-CONTEXT.md decisions D-10 + D-16 (HIGH/Critical from this pass → fix in scope; output appended as `## Second Pass (Phase 16 close-out)` section)
  </read_first>
  <action>
Invoke the `gsd-code-review` skill against Phase 15, focused on the diff introduced by Plans 16-01 and 16-02. Use the Skill tool exactly:

```
Skill(skill="gsd-code-review", args="15")
```

The skill will analyse the post-close-out state — primarily `src/pages/solo_dashboard.rs` (Plan 16-01's hoist) and `src/server/db.rs` (Plan 16-02's deletion). It produces a structured findings report with severity buckets (Critical / HIGH / Warning / Info).

**Gate (per D-10 + SPEC R4 acceptance):** the output MUST show 0 new findings in the Critical and HIGH severity buckets attributable to Phase 16 commits (16-01, 16-02). Pre-existing findings unrelated to those commits are out of scope for this gate.

**If new HIGH/Critical findings surface:** per D-10, fix them in-scope, re-commit, and re-run the skill. Do NOT defer. The escape hatch (rolling back Phase 16 commits and reopening) is only triggered if a regression is structurally unfixable in scope — the WR-01 / WR-02 surface area is small enough that this should not be needed.

**Append the output to `15-REVIEW.md`:** Per D-16, the second-pass output goes at the BOTTOM of `.planning/phases/15-goals-lp-history/15-REVIEW.md` as a new section. Use the exact heading:

```markdown

---  # (separator)

## Second Pass (Phase 16 close-out)

**Reviewed:** <ISO date>
**Diff scope:** Phase 16 commits 16-01 (<hash>) and 16-02 (<hash>)
**Findings:** Critical: 0, HIGH: 0, Warning: <n>, Info: <n>

[full skill output, formatted to match the existing review's section style]
```

If the skill produces a structured artifact that does not slot cleanly into the existing review's markdown style, transcribe the findings list into the same `### CR-XX:` / `### WR-XX:` / `### IN-XX:` heading convention (with `Status:` line as the first content line per D-13).

**Constraints:**
- Do NOT touch CR-01, CR-02, WR-01, WR-02, IN-01, IN-02, IN-03 sections at this point — Task 4 adds the `Status:` lines for those. This task only appends the `## Second Pass` section.
- Do NOT modify the YAML frontmatter of `15-REVIEW.md` at the top — the original review's metadata (`reviewed: 2026-05-05`, etc.) is historical and must stay. The second-pass section's `**Reviewed:**` field is inside the new section body.
  </action>
  <verify>
    <automated>grep -c "^## Second Pass (Phase 16 close-out)" .planning/phases/15-goals-lp-history/15-REVIEW.md ; echo "expect-1"</automated>
  </verify>
  <acceptance_criteria>
    - The `gsd-code-review` skill ran to completion and emitted a structured findings report
    - The report contains 0 entries in the Critical bucket attributable to Phase 16 commits
    - The report contains 0 entries in the HIGH bucket attributable to Phase 16 commits
    - `.planning/phases/15-goals-lp-history/15-REVIEW.md` now contains exactly one `## Second Pass (Phase 16 close-out)` heading
    - That section records the WR-01 and WR-02 commit hashes in its `**Diff scope:**` line
    - That section records explicit Critical: 0 and HIGH: 0 counts in its summary
    - The original review content (lines 1-193 pre-edit) is byte-identical above the new section
  </acceptance_criteria>
  <done>
    Second-pass code review completed; output appended to 15-REVIEW.md; 0 new HIGH/Critical findings introduced by Phase 16. If any HIGH/Critical surfaced, they were fixed in-scope and the skill was re-run until clean per D-10.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Run /gsd-verify-work 15 — gate: PASS for ROADMAP Phase 15 success criteria 1-4</name>
  <files>.planning/phases/15-goals-lp-history/15-VERIFICATION.md</files>
  <read_first>
    - .planning/ROADMAP.md (Phase 15 Success Criteria 1-4 at lines 135-139 — the targets verify-work runs against)
    - .planning/phases/16-phase15-closeout/16-CONTEXT.md decision D-11 (regression on Phase 15 criteria → fix in scope, re-verify, do not defer)
    - .planning/phases/16-phase15-closeout/16-01-SUMMARY.md and 16-02-SUMMARY.md (post-close-out state context)
  </read_first>
  <action>
Invoke the `gsd-verify-work` skill against Phase 15. Use the Skill tool exactly:

```
Skill(skill="gsd-verify-work", args="15")
```

The skill verifies Phase 15's ROADMAP success criteria 1-4 against the current main branch state (post-close-out, with Plans 16-01 and 16-02 committed). Expected target criteria:
1. LP history graph renders from ranked snapshots
2. Goals can be set (rank target / CS/min / death limit)
3. Goal progress visible on the solo dashboard
4. Champion trends table renders

**Gate (per D-11 + SPEC R5 acceptance):** the verification report MUST show PASS for all four criteria. The previously-passed UAT 11/11 must remain green when re-checked.

**If any criterion regresses:** per D-11, fix in-scope, re-verify; do NOT defer. The same escape hatch applies as in Task 1 (rollback only if structurally unfixable).

**Output handling:** the skill typically writes a `15-VERIFICATION.md` artifact in the Phase 15 directory. If the skill writes the file directly, no further action — Task 3 references it. If the skill emits the report inline without writing a file, capture the output and write it to `.planning/phases/15-goals-lp-history/15-VERIFICATION.md` with the structure:

```markdown
# Phase 15: Verification Report (Second Pass)

**Verified:** <ISO date>
**Phase:** 15-goals-lp-history
**Run by:** Phase 16 close-out (Plan 03 Task 2)

## Success Criteria

| ID | Criterion | Status | Evidence |
|----|-----------|--------|----------|
| 1  | LP history graph renders from ranked snapshots | PASS / FAIL | <e.g. agent-browser snapshot, test command output> |
| 2  | Goals can be set (rank target / CS/min / death limit) | PASS / FAIL | <evidence> |
| 3  | Goal progress visible on the solo dashboard | PASS / FAIL | <evidence> |
| 4  | Champion trends table renders | PASS / FAIL | <evidence> |

## Overall Verdict

PASS — all four criteria green. UAT 11/11 re-confirmed.
```

**Constraints:**
- Do NOT modify Phase 15 source files. This is verification only.
- Do NOT modify Phase 15's UAT log. This phase re-runs verification, not UAT.
- If the dev server is not running, start it (`cargo leptos watch` in background) and wait for ready before invoking the skill — the skill may need to interact with the running server for browser verification.
  </action>
  <verify>
    <automated>ls .planning/phases/15-goals-lp-history/15-VERIFICATION.md &amp;&amp; grep -c "PASS" .planning/phases/15-goals-lp-history/15-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - The `gsd-verify-work` skill ran to completion and produced a verification artifact
    - `.planning/phases/15-goals-lp-history/15-VERIFICATION.md` exists
    - That file contains a PASS verdict for criterion 1 (LP history graph)
    - That file contains a PASS verdict for criterion 2 (goal-setting)
    - That file contains a PASS verdict for criterion 3 (goal progress on dashboard)
    - That file contains a PASS verdict for criterion 4 (champion trends)
    - The "Overall Verdict" line records PASS — all four criteria green
  </acceptance_criteria>
  <done>
    Verification skill completed; artifact records PASS for all four ROADMAP Phase 15 success criteria; previously-passed UAT 11/11 confirmed still green. If any regression surfaced, it was fixed in-scope and the skill was re-run until clean per D-11.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Annotate 15-REVIEW.md with Status: lines on all 7 findings (CR/WR/IN)</name>
  <files>.planning/phases/15-goals-lp-history/15-REVIEW.md</files>
  <read_first>
    - .planning/phases/15-goals-lp-history/15-REVIEW.md (full file — to confirm finding header line numbers haven't shifted from what PATTERNS.md captured: 40, 66, 107, 131, 150, 168, 182)
    - .planning/phases/16-phase15-closeout/16-01-SUMMARY.md (read the `<16-01-HASH>` to substitute into the WR-01 Status line; if unavailable, fall back to `git log --oneline | grep "fix(phase-16): WR-01"` for the hash)
    - .planning/phases/16-phase15-closeout/16-02-SUMMARY.md (read the `<16-02-HASH>` to substitute into the WR-02 Status line)
    - .planning/phases/16-phase15-closeout/16-CONTEXT.md decisions D-13, D-14, D-15 (annotation format and exact wording per finding)
  </read_first>
  <action>
Add a `Status:` line as the first content line under each of the 7 finding headers in `.planning/phases/15-goals-lp-history/15-REVIEW.md`. Per D-13: the `Status:` line goes IMMEDIATELY after the `### CR-XX:` / `### WR-XX:` / `### IN-XX:` header — not at the bottom of the section.

Use the `Edit` tool 7 times — once per finding — with the exact `old_string` / `new_string` pairs below.

**CR-01 (line 40 header):**
- `old_string`: `### CR-01: WASM compile failure — LP graph hover uses undeclared variable names\n\n**File:** `src/pages/solo_dashboard.rs:564-565`\n**Issue:**`
- `new_string`: `### CR-01: WASM compile failure — LP graph hover uses undeclared variable names\n\nStatus: FIXED in 5902a81 — underscore-prefix issue resolved by renaming snaps_for_hover/points_for_hover.\n\n**File:** `src/pages/solo_dashboard.rs:564-565`\n**Issue:**`

**CR-02 (line 66 header):**
- `old_string`: `### CR-02: Rule 40 violation in `get_latest_ranked_snapshot` — `ORDER BY snapshotted_at` on a field not in the SELECT list\n\n**File:** `src/server/db.rs:4581`\n**Issue:**`
- `new_string`: `### CR-02: Rule 40 violation in `get_latest_ranked_snapshot` — `ORDER BY snapshotted_at` on a field not in the SELECT list\n\nStatus: FIXED in 5902a81 — '<string>snapshotted_at AS snapshotted_at' added to partial SELECT (Surreal Rule 40).\n\n**File:** `src/server/db.rs:4581`\n**Issue:**`

**WR-01 (line 107 header):** Substitute `<16-01-HASH>` with the actual short hash from `16-01-SUMMARY.md`.
- `old_string`: `### WR-01: LP history graph and goal progress stale after manual sync\n\n**File:** `src/pages/solo_dashboard.rs:249, 276`\n**Issue:**`
- `new_string`: `### WR-01: LP history graph and goal progress stale after manual sync\n\nStatus: RESOLVED in Phase 16 commit <16-01-HASH> — hoisted lp_history_resource into SoloDashboardPage; both sync paths refetch all three resources.\n\n**File:** `src/pages/solo_dashboard.rs:249, 276`\n**Issue:**`

**WR-02 (line 131 header):** Substitute `<16-02-HASH>` with the actual short hash from `16-02-SUMMARY.md`.
- `old_string`: `### WR-02: `get_personal_goals` is a public DB function with no production caller\n\n**File:** `src/server/db.rs:4698`\n**Issue:**`
- `new_string`: `### WR-02: `get_personal_goals` is a public DB function with no production caller\n\nStatus: RESOLVED in Phase 16 commit <16-02-HASH> — dead get_personal_goals removed; tests/db_personal_goal.rs deleted.\n\n**File:** `src/server/db.rs:4698`\n**Issue:**`

**IN-01 (line 150 header):**
- `old_string`: `### IN-01: Hardcoded Data Dragon patch version in `champion_icon_url`\n\n**File:** `src/pages/stats.rs:267`\n**Issue:**`
- `new_string`: `### IN-01: Hardcoded Data Dragon patch version in `champion_icon_url`\n\nStatus: DEFERRED to Phase 19 — dynamic Data Dragon version loading is the natural home.\n\n**File:** `src/pages/stats.rs:267`\n**Issue:**`

**IN-02 (line 168 header):**
- `old_string`: `### IN-02: `unwrap_or(None)` anti-pattern\n\n**File:** `src/server/db.rs:4832`\n**Issue:**`
- `new_string`: `### IN-02: `unwrap_or(None)` anti-pattern\n\nStatus: DEFERRED — info-only finding; address ad-hoc when surrounding code at db.rs:4832 is touched.\n\n**File:** `src/server/db.rs:4832`\n**Issue:**`

**IN-03 (line 182 header):**
- `old_string`: `### IN-03: `trends_window_to_cutoff` marked `#[allow(dead_code)]`\n\n**File:** `src/pages/stats.rs:182`\n**Issue:**`
- `new_string`: `### IN-03: `trends_window_to_cutoff` marked `#[allow(dead_code)]`\n\nStatus: DEFERRED — info-only finding; address ad-hoc when surrounding code is touched.\n\n**File:** `src/pages/stats.rs:182`\n**Issue:**`

**Constraints:**
- Do NOT touch the YAML frontmatter at the top of `15-REVIEW.md`.
- Do NOT modify the original "Issue:" / "Fix:" content of any finding — only insert the `Status:` line between header and the existing `**File:**` line.
- The `## Second Pass (Phase 16 close-out)` section appended in Task 1 stays at the bottom; this task only edits the body sections of the original review.
- After all 7 edits, run the verification grep to confirm all `Status:` lines are in place.

If the heading line text in the file differs slightly from PATTERNS.md (e.g. extra trailing whitespace, different em-dash character), `Read` the actual finding header line and use the verbatim text in the `old_string`. The semantic match (CR-01, CR-02, etc.) is what matters — adapt to the exact whitespace as needed.
  </action>
  <verify>
    <automated>grep -c "^Status: " .planning/phases/15-goals-lp-history/15-REVIEW.md ; echo "expect-7" &amp;&amp; grep -E "Status:|FIXED|RESOLVED|DEFERRED" .planning/phases/15-goals-lp-history/15-REVIEW.md | head -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^Status: " .planning/phases/15-goals-lp-history/15-REVIEW.md` returns exactly `7` (one per finding)
    - `grep -c "Status: FIXED in 5902a81" .planning/phases/15-goals-lp-history/15-REVIEW.md` returns exactly `2` (CR-01 + CR-02)
    - `grep -c "Status: RESOLVED in Phase 16 commit" .planning/phases/15-goals-lp-history/15-REVIEW.md` returns exactly `2` (WR-01 + WR-02)
    - `grep -c "Status: DEFERRED" .planning/phases/15-goals-lp-history/15-REVIEW.md` returns exactly `3` (IN-01 + IN-02 + IN-03)
    - The actual short commit hashes from 16-01 and 16-02 (not literal `<16-01-HASH>` placeholder strings) appear in the WR-01 and WR-02 Status lines respectively
    - The original "**File:**", "**Issue:**", "**Fix:**" sections of each finding remain intact (a `grep -c "^**Issue:**" .planning/phases/15-goals-lp-history/15-REVIEW.md` count matches pre-edit)
    - SPEC R3 acceptance grep `grep -E "Status:|FIXED|RESOLVED|DEFERRED" .planning/phases/15-goals-lp-history/15-REVIEW.md` finds entries for all 4 CR/WR IDs
  </acceptance_criteria>
  <done>
    All 7 findings carry an explicit `Status:` annotation on the first content line under their header; CR-01/CR-02 use the `5902a81` hash; WR-01/WR-02 use the resolved Phase 16 commit hashes from the SUMMARY files; IN-01/IN-02/IN-03 carry DEFERRED annotations.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Update STATE.md to record v1.2 as Shipped; prepend v1.2 entry to MILESTONES.md</name>
  <files>.planning/STATE.md, .planning/MILESTONES.md</files>
  <read_first>
    - .planning/STATE.md (full file — confirm current `status:` line at line 5 reads `Pivoted from v1.2 → v1.3 on 2026-05-06; entering Phase 16 (Phase 15 close-out)`; identify the right place to add the Shipped narrative)
    - .planning/MILESTONES.md (full file — confirm current top entry is `## v1.1 Polish, Draft & Opponents Rework (Shipped: 2026-03-24)`; the new v1.2 entry prepends ABOVE it)
    - .planning/phases/16-phase15-closeout/16-CONTEXT.md decision D-12 (mark-shipped is inline edit, not delegated to /gsd-complete-milestone)
    - .planning/phases/16-phase15-closeout/16-PATTERNS.md (analog format for the v1.2 MILESTONES entry mirroring v1.1; rationale for STATE.md narrative-style edit)
    - .planning/ROADMAP.md (Phase 12-15 + Phase 16 entries — source of accomplishments to summarise in MILESTONES)
  </read_first>
  <action>
**Edit A — `.planning/STATE.md`:** Update the YAML frontmatter `status:` field (line 5) to record v1.2 shipped. Use the `Edit` tool:

- `old_string`: `status: Pivoted from v1.2 → v1.3 on 2026-05-06; entering Phase 16 (Phase 15 close-out)`
- `new_string`: `status: v1.2 Shipped 2026-05-DD (Phase 16 close-out complete); active milestone v1.3 Launch Readiness in progress`

Replace `2026-05-DD` with today's actual ISO date (per CONTEXT.md "Claude's Discretion: The exact ISO date written into STATE.md prior_milestone.status — fill at commit time"). Active `milestone:` field at line 3 stays `v1.3` — DO NOT change it (per SPEC R6: "STATE.md continues to declare the active milestone as v1.3").

Then update `last_updated` (line 7) to the current ISO timestamp and `stopped_at` (line 6) to record the close-out completion: change `Phase 16 context gathered` to `Phase 16 close-out complete; v1.2 shipped`.

Add a new line to the `## Decisions` section (after line 47) documenting the milestone closure:
```
- [Phase 16]: v1.2 Solo Mode & Match Intelligence shipped 2026-05-DD; close-out resolved WR-01 (refetch hoisting) and WR-02 (dead get_personal_goals removed); 15-REVIEW.md fully reconciled; second-pass review found 0 new HIGH/Critical
```

**Edit B — `.planning/MILESTONES.md`:** Prepend a new v1.2 entry ABOVE the existing `## v1.1 Polish, Draft & Opponents Rework` heading. Use the `Edit` tool:

- `old_string`: `# Milestones\n\n## v1.1 Polish, Draft & Opponents Rework (Shipped: 2026-03-24)`
- `new_string` (replace `2026-05-DD` with the actual ISO date matching the STATE.md update):

```
# Milestones

## v1.2 Solo Mode & Match Intelligence (Shipped: 2026-05-DD)

**Phases completed:** 5 phases (12-15 + Phase 16 close-out), 12 plans

**Timeline:** 6 weeks (2026-03-25 → 2026-05-DD)

**Delivered:** Opened the app to individual players. Solo Mode toggle persists in DB; users link a Riot account with region and sync their solo queue history without a team. Match Detail view exposes the full 10-player scoreboard, timeline events, and personal performance breakdown — on-demand fetch, DB-cached afterwards. Personal Learnings Journal lets users tag post-game reflections by champion + matchup with one-click creation from match detail. Goals & LP History adds a line graph of ranked LP, rank/CS/deaths goals with auto-tracked progress, and a champion trends table.

**Key accomplishments:**

- Phase 12 — Solo Mode Infrastructure: mode toggle (DB-persisted), region selector, solo sync, ranked badge, single-column dashboard shell
- Phase 13 — Match Detail View: 10-player scoreboard with KDA/items/damage/gold/vision; timeline event markers; personal performance breakdown; lazy fetch + DB cache
- Phase 14 — Personal Learnings Journal: free-text journal entries tagged by champion/matchup; click-from-match-detail prefill; champion/matchup/tag filtering
- Phase 15 — Goals & LP History: LP graph SVG with cumulative rank score; rank/CS/deaths goals with inline edit; goal progress cards auto-tracked from match history; champion trends table with time-window toggle
- Phase 16 — Phase 15 close-out: WR-01 hoist fix (LP graph + goal cards refresh on sync without page reload); WR-02 removal of dead `get_personal_goals`; 15-REVIEW.md fully reconciled with Status: lines on all 7 findings; second-pass /gsd-code-review 15 produced 0 new HIGH/Critical; /gsd-verify-work 15 PASS for all four ROADMAP success criteria

**Known Gaps (deferred):**

- IN-01 hardcoded Data Dragon patch version → deferred to Phase 19 (Production Hardening) for dynamic version loading
- IN-02 `unwrap_or(None)` anti-pattern at db.rs:4832 → info-only; ad-hoc when surrounding code is touched
- IN-03 spurious `#[allow(dead_code)]` on `trends_window_to_cutoff` → info-only; ad-hoc when surrounding code is touched
- Old Phase 16 (Draft Integration) → renumbered Phase 24 in v1.4; XFEAT-03/XFEAT-04 ship post-launch when real-user data is in

---  # (separator)

## v1.1 Polish, Draft & Opponents Rework (Shipped: 2026-03-24)
```

The format mirrors the v1.0 entry (which has Phases completed, Timeline, Git range, Delivered, Key accomplishments, Known Gaps sections) more than the bare-bones v1.1 entry. The v1.0 format is the richer, preferred shape — replicate that level of structure for v1.2.

After both edits, verify:
- `grep -i "Shipped" .planning/STATE.md` matches the new status line (per SPEC R6)
- `grep -i "v1\.2" .planning/MILESTONES.md` returns ≥ 1 hit (per SPEC R6)
- `grep -n "milestone: v1.3" .planning/STATE.md` still returns line 3 (active milestone unchanged per SPEC R6)

**Constraints:**
- Do NOT change the active `milestone:` field in STATE.md from `v1.3` to anything else (SPEC R6 hard rule).
- Do NOT delete or restructure the existing v1.1 / v1.0 entries in MILESTONES.md — only prepend the v1.2 entry above them.
- Replace the literal string `2026-05-DD` in BOTH files with the SAME actual ISO date so the dates match across the two artifacts.
- The active milestone (v1.3 Launch Readiness) and its phase progress remain in STATE.md untouched.
  </action>
  <verify>
    <automated>grep -i "Shipped" .planning/STATE.md &amp;&amp; grep -ic "v1\.2" .planning/MILESTONES.md &amp;&amp; grep -n "^milestone: v1.3" .planning/STATE.md &amp;&amp; grep -c "## v1.2 Solo Mode" .planning/MILESTONES.md ; echo "expect-1"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -i "Shipped" .planning/STATE.md` returns at least one hit matching the new status narrative
    - `grep -ic "v1\.2" .planning/MILESTONES.md` returns at least 1 (the new v1.2 entry heading)
    - `grep -c "^## v1.2 Solo Mode & Match Intelligence" .planning/MILESTONES.md` returns exactly `1`
    - `grep -n "^milestone: v1.3" .planning/STATE.md` still matches line 3 (active milestone NOT bumped per SPEC R6 hard rule)
    - Both files contain the same actual ISO date in place of the literal `2026-05-DD` placeholder
    - The existing `## v1.1 Polish, Draft & Opponents Rework (Shipped: 2026-03-24)` heading in MILESTONES.md is byte-identical post-edit (just shifted further down the file)
    - The existing `## v1.0 Cross-Feature Intelligence (Shipped: 2026-03-18)` heading in MILESTONES.md is byte-identical post-edit
    - STATE.md `## Decisions` section gains exactly one new line beginning with `- [Phase 16]: v1.2 Solo Mode`
    - STATE.md `gsd_state_version`, `milestone`, `milestone_name`, `progress` keys are untouched (only `status`, `stopped_at`, `last_updated` updated)
  </acceptance_criteria>
  <done>
    STATE.md records v1.2 as Shipped with the actual close date; active milestone remains v1.3; MILESTONES.md has a v1.2 entry prepended above v1.1 with the v1.0-style structured format (Phases / Timeline / Delivered / Key accomplishments / Known Gaps); both files use the same ISO close date.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Final close-out verification — review the four artifacts before commit</name>
  <what-built>
    Phase 16 close-out is complete: WR-01 (refetch hoisting) and WR-02 (dead function removal) are committed; second-pass /gsd-code-review 15 produced 0 new HIGH/Critical; /gsd-verify-work 15 PASS for all four ROADMAP Phase 15 success criteria; 15-REVIEW.md has Status: lines on all 7 findings + a Second Pass section appended; STATE.md records v1.2 shipped; MILESTONES.md has a v1.2 entry.
  </what-built>
  <how-to-verify>
1. Run `git log --oneline -10` and confirm the commit history shows in order:
   - `fix(phase-16): WR-01 ...` (Plan 16-01 commit)
   - `chore(phase-16): WR-02 ...` (Plan 16-02 commit)
   - One or more `docs(phase-16): ...` commits for the 15-REVIEW.md annotations + STATE/MILESTONES updates
2. Spot-check `15-REVIEW.md`:
   - Open the file and visually scan for the 7 `Status:` lines (one per CR/WR/IN finding header).
   - Confirm WR-01 and WR-02 Status lines reference the actual short hashes from `git log --oneline | head -10`, not literal `<16-01-HASH>` placeholders.
   - Confirm the `## Second Pass (Phase 16 close-out)` section appears at the bottom with `Critical: 0, HIGH: 0`.
3. Spot-check `STATE.md`:
   - `status:` line records "Shipped 2026-05-DD" with the actual date filled in.
   - `milestone:` line still reads `v1.3` (active milestone unchanged).
   - `## Decisions` section gained the `[Phase 16]: v1.2 Solo Mode...` line.
4. Spot-check `MILESTONES.md`:
   - The new v1.2 entry is at the top (above v1.1).
   - The Phases completed line reads "5 phases (12-15 + Phase 16 close-out), 12 plans".
   - The Known Gaps section lists IN-01 / IN-02 / IN-03 / old Phase 16 deferral.
5. Spot-check `15-VERIFICATION.md`:
   - Exists at `.planning/phases/15-goals-lp-history/15-VERIFICATION.md`.
   - Lists PASS for all four ROADMAP Phase 15 success criteria.
6. Run the full SPEC R6 acceptance grep panel:
   ```
   grep -i "Shipped" .planning/STATE.md
   grep -i "v1\.2" .planning/MILESTONES.md
   grep -c "^Status: " .planning/phases/15-goals-lp-history/15-REVIEW.md  # expect 7
   grep -E "Status:|FIXED|RESOLVED|DEFERRED" .planning/phases/15-goals-lp-history/15-REVIEW.md | wc -l  # expect ≥ 7
   ```
  </how-to-verify>
  <resume-signal>Type "approved" if all four artifacts are correct and the Phase 16 close-out can be marked complete; otherwise describe what's missing.</resume-signal>
  <files>(checkpoint task — no files modified by Claude; user verifies the four artifacts: 15-REVIEW.md, 15-VERIFICATION.md, STATE.md, MILESTONES.md)</files>
  <action>This is a human-verification checkpoint, not an automation task. Pause execution and present the `<how-to-verify>` 6-step instructions above to the user. Wait for the resume signal ("approved" or a description of what is missing). Do NOT mark Phase 16 complete until the user types "approved".</action>
  <verify>Manual: user follows the 6-step `<how-to-verify>` flow. No automated command — Phase 16 close-out is by design a documentation-and-review milestone where the human's final read-through is the gate per D-09 strict serial execution order step 6.</verify>
  <done>User types "approved" indicating all four artifacts (15-REVIEW.md, 15-VERIFICATION.md, STATE.md, MILESTONES.md) are correct; Phase 16 close-out is marked complete; v1.2 milestone is officially shipped; ready to chain into Phase 17 (UI Consolidation).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Planning docs → future Claude sessions | `.planning/STATE.md`, `.planning/MILESTONES.md`, and `15-REVIEW.md` are read by future planning sessions to derive context. Inaccurate state would mislead downstream work. |
| Skill output → review artifact | `/gsd-code-review 15` and `/gsd-verify-work 15` outputs are appended/written to project files. Skill failures or hallucinated PASS verdicts would invalidate the close-out gate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-11 | Spoofing | Hallucinated commit hashes in 15-REVIEW.md Status lines | mitigate | Task 3 read_first lists `16-01-SUMMARY.md` and `16-02-SUMMARY.md` which carry the actual hashes from Plan 01/02 executors. Fallback `git log --oneline | grep "fix(phase-16): WR-01"` provides defense in depth. Acceptance criterion checks the absence of the literal placeholder string `<16-01-HASH>`. |
| T-16-12 | Tampering | Active milestone field in STATE.md accidentally bumped to v1.2 | mitigate | Hard rule in SPEC R6 + explicit constraint in Task 4 action. Acceptance criterion `grep -n "^milestone: v1.3" .planning/STATE.md` confirms the active milestone is preserved. |
| T-16-13 | Information Disclosure | Hidden Phase 15 regression masked by hallucinated PASS verdict | mitigate | The verify-work skill is independent of the planner — its PASS verdict is grounded in test output, not narrative. D-11 mandates fix-in-scope on regression. The browser checkpoint in Task 5 has the human visually confirm the verification artifact before commit. |
| T-16-14 | Repudiation | Lost trace of which Phase 16 commit resolved which finding | mitigate | Per D-07, atomic per-finding commits (`fix(phase-16):` for WR-01, `chore(phase-16):` for WR-02) give the 15-REVIEW.md Status: lines clean per-finding hash provenance. The annotation format from D-14 records the hash inline. |
| T-16-15 | Elevation of Privilege | New HIGH/Critical from Phase 16 commits silently deferred | mitigate | D-10 explicitly forbids deferral — fix in scope, re-review until clean. Task 1 acceptance criterion checks Critical: 0 + HIGH: 0 in the second-pass output. The escape hatch (rollback) requires structurally unfixable regression and is not the default path. |
</threat_model>

<verification>
- `git log --oneline -10` shows the WR-01, WR-02, and Phase 16 docs commits in order
- `grep -c "^Status: " .planning/phases/15-goals-lp-history/15-REVIEW.md` returns `7`
- `grep -i "Shipped" .planning/STATE.md` matches the new status narrative
- `grep -c "^## v1.2 Solo Mode & Match Intelligence" .planning/MILESTONES.md` returns `1`
- `.planning/phases/15-goals-lp-history/15-VERIFICATION.md` exists with PASS verdicts on all four criteria
- The `## Second Pass (Phase 16 close-out)` section in 15-REVIEW.md records `Critical: 0, HIGH: 0`
- Browser checkpoint (Task 5) approved by user
</verification>

<success_criteria>
v1.2 Solo Mode & Match Intelligence is recorded as shipped in project state and milestones; 15-REVIEW.md is fully reconciled with explicit Status: lines on all 7 findings; second-pass code review found 0 new HIGH/Critical; verify-work confirms PASS for all four ROADMAP Phase 15 success criteria; the active milestone in STATE.md remains v1.3; Phase 16 close-out is complete and ready to chain into Phase 17 (UI Consolidation).
</success_criteria>

<output>
After completion, create `.planning/phases/16-phase15-closeout/16-03-SUMMARY.md` summarising:
- Second-pass review verdict (0 new HIGH/Critical)
- Verify-work verdict (PASS for all four criteria)
- Concrete commit hashes for the WR-01 fix, WR-02 removal, and Phase 16 docs commits
- The actual ISO close date written into STATE.md and MILESTONES.md
- Confirmation that all 7 findings in 15-REVIEW.md carry Status: lines

Once Plan 03 commits land, Phase 16 is complete. Next phase is Phase 17 (UI Consolidation) per the v1.3 Launch Readiness milestone in ROADMAP.md.
</output>

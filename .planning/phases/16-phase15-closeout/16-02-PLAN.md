---
phase: 16-phase15-closeout
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/server/db.rs
  - tests/db_personal_goal.rs
autonomous: true
requirements:
  - WR-02
tags:
  - dead-code-removal
  - surrealdb
  - test-cleanup

must_haves:
  truths:
    - "The `pub async fn get_personal_goals` no longer exists in `src/server/db.rs`"
    - "`tests/db_personal_goal.rs` does not exist (full file deletion)"
    - "`grep -rn get_personal_goals src/` returns 0 hits anywhere in the source tree"
    - "`compute_goal_progress` (db.rs:4774) and the goal-edit forms (RankTargetCard etc.) continue to work unchanged"
    - "`tests/db_goal_progress.rs` continues to pass — covers the upsert + compute_goal_progress paths that absorb the removed coverage"
    - "schema.surql `personal_goal` table definition is untouched (still used by upsert/delete/compute_goal_progress)"
  artifacts:
    - path: "src/server/db.rs"
      provides: "Cleaned db.rs with get_personal_goals function fully removed (lines 4699-4731 deleted)"
      contains: "pub async fn upsert_personal_goal"
  key_links:
    - from: "compute_goal_progress (db.rs:4774)"
      to: "personal_goal table via inline batched SELECT"
      via: "Rule 29 batching — single round-trip query bundling goals + matches"
      pattern: "personal_goal WHERE user"
    - from: "upsert_personal_goal (db.rs:4735)"
      to: "personal_goal table"
      via: "BEGIN/COMMIT transaction for upsert semantics"
      pattern: "DELETE personal_goal"
---

<objective>
Resolve WR-02 from `15-REVIEW.md`: `pub async fn get_personal_goals` at `src/server/db.rs:4699` has zero production callers. The page reads goals via `compute_goal_progress` (db.rs:4774) which inlines the equivalent goals SELECT in its batched 3-statement query (per surreal-patterns.md Rule 29 — keeps a single round-trip). The function is dead public API.

Per D-05, D-06, D-07, D-08 (all locked):
- Delete the entire `get_personal_goals` function (lines 4699-4731) from `src/server/db.rs`.
- Delete `tests/db_personal_goal.rs` in its entirety (5 tests, all asserting via the now-removed function).
- Land as its own commit (`chore(phase-16): WR-02 remove dead get_personal_goals`), separate from WR-01.
- Leave `schema.surql` untouched — the `personal_goal` table is still used by `upsert_personal_goal`, `delete_personal_goal`, and `compute_goal_progress`.

Purpose: Eliminate dead public API surface so future callers cannot accidentally fetch goals via two divergent code paths (the inline batched query in `compute_goal_progress` vs. the standalone function would drift).
Output: A `db.rs` with 33 fewer lines and a deleted `tests/db_personal_goal.rs`. Both compile targets pass; `cargo test --features ssr --lib` passes (5 fewer tests).

Coverage absorption: `tests/db_goal_progress.rs` already covers create, overwrite, cross-user isolation, and empty-state semantics via the upsert → compute_goal_progress paths. Per D-06, no assertion backfill is required.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-phase15-closeout/16-CONTEXT.md
@.planning/phases/16-phase15-closeout/16-SPEC.md
@.planning/phases/16-phase15-closeout/16-PATTERNS.md
@.planning/phases/15-goals-lp-history/15-REVIEW.md

<interfaces>
<!-- Code anchors for the executor. Verified 2026-05-06 against the current src tree. -->

From `src/server/db.rs` lines 4697-4732 (the function to delete — exact byte range):
```rust
}                                                              // line 4697 — closing } of preceding fn (KEEP)
                                                               // line 4698 — blank (KEEP)
pub async fn get_personal_goals(                               // line 4699 — DELETE from here ↓
    db: &Surreal<Db>,
    user_id: &str,
) -> DbResult<Vec<crate::models::match_data::PersonalGoal>> {
    let user_key = user_id
        .strip_prefix("user:")
        .unwrap_or(user_id)
        .to_string();

    #[derive(Debug, Deserialize, SurrealValue)]
    struct DbPersonalGoal {
        id: RecordId,
        goal_type: String,
        target_value: String,
    }

    let mut r = db
        .query(
            "SELECT id, goal_type, target_value FROM personal_goal \
             WHERE user = type::record('user', $user_key)",
        )
        .bind(("user_key", user_key))
        .await?;
    let rows: Vec<DbPersonalGoal> = r.take(0).unwrap_or_default();
    Ok(rows
        .into_iter()
        .map(|g| crate::models::match_data::PersonalGoal {
            id: Some(g.id.to_sql()),
            goal_type: g.goal_type,
            target_value: g.target_value,
        })
        .collect())
}                                                              // line 4731 — DELETE through here ↑
                                                               // line 4732 — blank (KEEP — separates from upsert_personal_goal)
/// Upsert a personal goal for the user (one active goal per type, D-07).      // line 4733 — KEEP (start of NEXT function)
```

From `src/server/db.rs` line 4733 onward (DO NOT TOUCH — surrounding functions stay byte-identical):
- `pub async fn upsert_personal_goal` (line 4735) — uses the personal_goal table.
- `pub async fn delete_personal_goal` (later in the file) — uses the personal_goal table.
- `pub async fn compute_goal_progress` (line 4774) — uses the personal_goal table inline.

From `tests/db_personal_goal.rs` (the entire 83-line file — DELETE):
```
83 lines covering 5 tests (line numbers per CONTEXT.md canonical_refs):
- line 19: get_personal_goals_no_goals_returns_empty
- line 31: get_personal_goals_returns_one_goal_per_user
- line 47: get_personal_goals_overwrite_existing_goal_type
- line 64: get_personal_goals_cross_user_isolation
- line 78: get_personal_goals_empty_returns_empty_vec
All five tests assert via `db::get_personal_goals(...)` — no surviving callers after WR-02.
```

From `tests/db_goal_progress.rs` (DO NOT TOUCH — coverage absorber):
- 168 lines, already exercises upsert_personal_goal → compute_goal_progress paths
- Covers create, overwrite, cross-user isolation, empty-state per D-06

From `schema.surql` (DO NOT TOUCH — `personal_goal` table is still live):
- The `DEFINE TABLE personal_goal` and `DEFINE FIELD ... ON personal_goal` statements remain in service for `upsert_personal_goal`, `delete_personal_goal`, `compute_goal_progress`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Delete get_personal_goals function from src/server/db.rs (lines 4699-4731 inclusive)</name>
  <files>src/server/db.rs</files>
  <read_first>
    - src/server/db.rs (focus on lines 4690-4775 — verify the function boundary with `grep -n "^pub async fn" src/server/db.rs | head -50` first; the executor must confirm the function STARTS at the byte-position of `pub async fn get_personal_goals(` and ENDS at the matching closing `}` on the line immediately preceding `/// Upsert a personal goal for the user`)
    - .planning/phases/16-phase15-closeout/16-CONTEXT.md (decisions D-05, D-06, D-07, D-08 lock the deletion approach)
    - .planning/phases/16-phase15-closeout/16-PATTERNS.md (concrete deletion boundary)
    - .claude/rules/surreal-patterns.md rule 29 (explains why compute_goal_progress inlines the SELECT instead of calling get_personal_goals — context for why removal is safe)
    - tests/db_goal_progress.rs (coverage that absorbs the deleted tests; do NOT modify, just confirm it exists and exercises upsert + compute_goal_progress)
  </read_first>
  <action>
Delete the `pub async fn get_personal_goals` function from `src/server/db.rs`.

**Step 1: Locate the exact byte range.** Run `grep -n "pub async fn get_personal_goals" src/server/db.rs` — this returns one hit (currently line 4699). Run `grep -n "/// Upsert a personal goal" src/server/db.rs` — this returns one hit (currently line 4733). The deletion range is the line containing `pub async fn get_personal_goals(` through the closing `}` of the function — specifically, the closing `}` is the LAST line BEFORE the `/// Upsert a personal goal` doc comment line.

**Step 2: Use `Edit` to remove the function.** The cleanest tool path:

Use the `Edit` tool with `old_string` set to the entire function body PLUS the trailing blank line, and `new_string` set to a single empty string. Concretely, the `old_string` to remove (33 lines + 1 trailing blank = 34 lines):

```
pub async fn get_personal_goals(
    db: &Surreal<Db>,
    user_id: &str,
) -> DbResult<Vec<crate::models::match_data::PersonalGoal>> {
    let user_key = user_id
        .strip_prefix("user:")
        .unwrap_or(user_id)
        .to_string();

    #[derive(Debug, Deserialize, SurrealValue)]
    struct DbPersonalGoal {
        id: RecordId,
        goal_type: String,
        target_value: String,
    }

    let mut r = db
        .query(
            "SELECT id, goal_type, target_value FROM personal_goal \
             WHERE user = type::record('user', $user_key)",
        )
        .bind(("user_key", user_key))
        .await?;
    let rows: Vec<DbPersonalGoal> = r.take(0).unwrap_or_default();
    Ok(rows
        .into_iter()
        .map(|g| crate::models::match_data::PersonalGoal {
            id: Some(g.id.to_sql()),
            goal_type: g.goal_type,
            target_value: g.target_value,
        })
        .collect())
}

```

Replace with the empty string `""`. The blank line separator between the closing `}` of the preceding function (line 4697) and the doc comment of `upsert_personal_goal` (line 4733) is preserved by the file structure: after deletion, line 4697's `}` is followed by a single blank line, then `/// Upsert a personal goal for the user (one active goal per type, D-07).`

If the `Edit` tool's idempotence check rejects the change because of subtle whitespace differences, first `Read` lines 4697-4734 of `src/server/db.rs` to capture the exact whitespace, then re-issue `Edit` with the verbatim `old_string`.

**Step 3: Verify no other call site was ever silently relying on the function.** Run `grep -rn "get_personal_goals" src/` — must return 0 hits after deletion. Run `grep -rn "get_personal_goals" tests/` — at this stage will still return hits (the tests in `tests/db_personal_goal.rs` reference it; that file is deleted in Task 2). Run `grep -n "DbPersonalGoal" src/server/db.rs` — the inner struct was function-scoped, so it should also disappear from the file.

**Constraints:**
- Do NOT touch `upsert_personal_goal`, `delete_personal_goal`, or `compute_goal_progress`.
- Do NOT touch `schema.surql` (per D-08 — the `personal_goal` table stays live).
- Do NOT touch any model file (`PersonalGoal` in `src/models/match_data.rs` is still used by `compute_goal_progress` payload).
- Do NOT add `#[allow(dead_code)]` and keep the function — D-05 locks "remove" over "wire to caller" because `compute_goal_progress` already covers all production goal reads via the batched 3-statement query (per Rule 29).

After deletion: `cargo check --features ssr` must exit 0 (Task 1 only — Task 2 deletes the test file).
  </action>
  <verify>
    <automated>cargo check --features ssr 2>&amp;1 | tail -10 &amp;&amp; grep -rn "get_personal_goals" src/ ; echo "expect-0-src-hits"; grep -c "DbPersonalGoal" src/server/db.rs ; echo "expect-0"</automated>
  </verify>
  <acceptance_criteria>
    - `cargo check --features ssr` exits 0 with no errors
    - `grep -rn "get_personal_goals" src/` returns 0 hits (no production caller, definition removed)
    - `grep -c "DbPersonalGoal" src/server/db.rs` returns 0 (inner struct also gone)
    - `grep -n "pub async fn upsert_personal_goal" src/server/db.rs` returns exactly one hit (the surrounding function is preserved)
    - `grep -n "pub async fn compute_goal_progress" src/server/db.rs` returns exactly one hit (the superseding function is preserved)
    - `grep -n "DEFINE TABLE personal_goal" schema.surql` returns at least one hit (schema is untouched per D-08)
    - File line count of `src/server/db.rs` decreased by 33 lines (the function body) ± 1 (depending on whether the trailing blank line is preserved or trimmed — both are acceptable per PATTERNS.md)
  </acceptance_criteria>
  <done>
    The `get_personal_goals` function and its inner `DbPersonalGoal` struct are removed from `src/server/db.rs`; `cargo check --features ssr` passes; `grep` confirms no remaining production reference; surrounding functions and schema are byte-identical to pre-deletion state.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Delete tests/db_personal_goal.rs entirely</name>
  <files>tests/db_personal_goal.rs</files>
  <read_first>
    - tests/db_personal_goal.rs (full 83 lines — confirm all 5 tests assert via `db::get_personal_goals(...)`; per D-06 no test asserts a behaviour not already covered by tests/db_goal_progress.rs)
    - tests/db_goal_progress.rs (full file — verify it exists and covers upsert + compute_goal_progress for create/overwrite/cross-user-isolation/empty-state per D-06; this is the surviving coverage)
    - .planning/phases/16-phase15-closeout/16-CONTEXT.md (D-06 locks full deletion of the test file with no assertion backfill)
  </read_first>
  <action>
Delete `tests/db_personal_goal.rs` in its entirety. The file has 5 tests (named `get_personal_goals_no_goals_returns_empty`, `get_personal_goals_returns_one_goal_per_user`, `get_personal_goals_overwrite_existing_goal_type`, `get_personal_goals_cross_user_isolation`, `get_personal_goals_empty_returns_empty_vec`) — all five assert via the now-removed function and would fail to compile after Task 1 lands.

Per D-06: no assertion backfill is required. `tests/db_goal_progress.rs` (168 lines) already covers:
- Create — `upsert_personal_goal` → `compute_goal_progress` returns the goal in the payload
- Overwrite — second `upsert_personal_goal` for the same `goal_type` replaces the first (per the `BEGIN; DELETE...; CREATE...; COMMIT` pattern at db.rs:4751-4762)
- Cross-user isolation — two users' goals do not bleed across user_id scopes
- Empty state — no goals → `compute_goal_progress` returns a payload with empty goal vec

**Tool path:** Use `Bash` to run `rm tests/db_personal_goal.rs`. Confirm the file is gone with `ls tests/db_personal_goal.rs 2>&1` (must return "No such file or directory").

After deletion: run `cargo check --features ssr` — must exit 0. Run `cargo test --features ssr --lib` — must exit 0. (`--lib` avoids the BFD-linker OOM with integration tests per CLAUDE.md test guidance; the unit-test-only run is the regression gate here.)

**Constraints:**
- Do NOT touch `tests/db_goal_progress.rs` (the surviving coverage).
- Do NOT touch `tests/common/mod.rs` (the test_db helper).
- Do NOT add new test files to backfill — D-06 explicitly forbids assertion backfill.
- Do NOT modify `Cargo.toml` — `tests/*.rs` files are auto-discovered as integration test binaries; deleting the file is sufficient.
  </action>
  <verify>
    <automated>ls tests/db_personal_goal.rs 2>&amp;1 ; echo "expect-no-such-file"; cargo check --features ssr 2>&amp;1 | tail -10 &amp;&amp; cargo test --features ssr --lib 2>&amp;1 | tail -10 &amp;&amp; ls tests/db_goal_progress.rs &amp;&amp; grep -rn "get_personal_goals" tests/ ; echo "expect-0-test-hits"</automated>
  </verify>
  <acceptance_criteria>
    - `ls tests/db_personal_goal.rs` returns "No such file or directory" (file is gone)
    - `ls tests/db_goal_progress.rs` returns the file path (surviving coverage intact)
    - `grep -rn "get_personal_goals" tests/` returns 0 hits (no orphan references remain)
    - `cargo check --features ssr` exits 0
    - `cargo test --features ssr --lib` exits 0
    - The combined effect with Task 1: `grep -rn "get_personal_goals" .` (excluding `.planning/` and `.git/`) returns 0 hits across the entire codebase
  </acceptance_criteria>
  <done>
    `tests/db_personal_goal.rs` is deleted; `cargo check --features ssr` and `cargo test --features ssr --lib` both pass; no orphan references to `get_personal_goals` remain anywhere in source or test code; `tests/db_goal_progress.rs` is untouched and continues to provide the absorbing coverage.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Source code → public API surface | `pub async fn get_personal_goals` was a public function in the `db` module. Removing it shrinks the public surface — no new boundary, only contraction. |
| Test boundary → DB schema | `tests/db_personal_goal.rs` exercises an in-memory SurrealDB via `tests/common/mod.rs`. Deletion does not affect production data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-06 | Tampering | External crate consuming `get_personal_goals` | accept | This is a single-binary application (per CLAUDE.md "Single crate, dual compile target"). No external crate depends on `db.rs` public functions. The `pub` modifier is internal to the crate's module structure. Removing the function poses zero downstream-consumer risk. |
| T-16-07 | Information Disclosure | Coverage gap from deleted tests | mitigate | `tests/db_goal_progress.rs` (168 lines, untouched) already exercises the upsert → compute_goal_progress paths that absorb create/overwrite/cross-user-isolation/empty-state coverage per D-06. Verified by reading the file before deletion. |
| T-16-08 | Repudiation | Hidden production caller missed by grep | mitigate | Defense in depth: (a) `grep -rn get_personal_goals src/` was run during planning and returned only the definition; (b) Task 1 verify re-runs the grep post-deletion (must return 0); (c) `cargo check --features ssr` post-deletion would fail loudly if any caller existed (compiler is the final authority — a missing function reference is a hard error, not a warning). |
| T-16-09 | DoS | `personal_goal` table queries break after function removal | accept | The `personal_goal` table is still queried by `upsert_personal_goal`, `delete_personal_goal`, and `compute_goal_progress` — none of which are touched by this plan. `schema.surql` is byte-identical post-change (per D-08). |
| T-16-10 | Elevation of Privilege | Future caller adds a divergent goals query | mitigate | The motivating risk for WR-02: two paths (the inline batched SELECT in `compute_goal_progress` vs. the standalone `get_personal_goals`) could drift, allowing a caller to silently fetch from one instead of the other. Removal eliminates this — only the inline batched path remains, enforced by the compiler. |
</threat_model>

<verification>
- `cargo check --features ssr` exits 0 after both tasks
- `cargo test --features ssr --lib` exits 0 after both tasks (5 fewer tests pass)
- `grep -rn "get_personal_goals" src/ tests/` returns 0 hits (defense-in-depth check)
- `schema.surql` is byte-identical to pre-change (per D-08 — `git diff schema.surql` returns no output)
- `tests/db_goal_progress.rs` is byte-identical to pre-change
</verification>

<success_criteria>
WR-02 from `15-REVIEW.md` is closed: `get_personal_goals` is removed; its dedicated test file is deleted; surrounding functions and schema are unchanged; both compile targets pass; the surviving `tests/db_goal_progress.rs` continues to cover goal CRUD via `compute_goal_progress`.
</success_criteria>

<output>
After completion, create `.planning/phases/16-phase15-closeout/16-02-SUMMARY.md` with:
- The exact commit hash of the WR-02 removal (Plan 03 will pull this hash into the `15-REVIEW.md` Status: line for WR-02)
- Confirmation that `grep -rn "get_personal_goals" src/ tests/` returns 0 hits
- Confirmation that `cargo test --features ssr --lib` passes (5 fewer tests counted)

Per D-07, this plan lands as its own commit with a `chore(phase-16):` prefix — DO NOT bundle WR-01 changes in the same commit.
</output>

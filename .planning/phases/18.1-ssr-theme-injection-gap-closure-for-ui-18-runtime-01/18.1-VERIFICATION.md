---
phase: 18.1-ssr-theme-injection-gap-closure-for-ui-18-runtime-01
verified: 2026-05-26T00:15:00Z
status: human_needed
score: 5/5 must-haves verified
overrides_applied: 0
gaps: []
human_verification:
  - test: "Stop here before starting Phase 19 — confirm sequencing decision for UI-18.1-HYDRATE-01 (hydration panic on Pandemonium pages)"
    expected: "User decides whether Phase 19 (Bug-Report Widget) should be preceded by Phase 18.2 (hydrate bridge), shipped Demacia-only with a follow-up tag, or proceed unblocked. Phase 18.1 architectural gap IS closed; this is a separate downstream finding the user already accepted as out-of-scope (logged as UI-18.1-HYDRATE-01)."
    why_human: "Sequencing/prioritisation decision; the architectural goal of Phase 18.1 is achieved. The hydration panic is a new finding the user already explicitly scoped to a future Phase 18.2."
  - test: "Pre-existing test failure in e2e/tests/auth.spec.ts (NOT a 18.1 regression) — decide on fix"
    expected: "auth.spec.ts:66 and :89 wait for /team/dashboard, but Phase 12 (commit 65d3c8b, March 2026) changed register_action() to redirect to /solo (src/pages/auth/register.rs:52: `Ok(\"/solo\".to_string())`). Pre-existing stale test; not caused by Phase 18.1. Options: (a) update auth.spec.ts to expect /solo, (b) update CLAUDE.md note that registration redirects to /team/dashboard (it goes to /solo)."
    why_human: "Maintenance choice that touches a pre-existing area outside Phase 18.1 scope; verifier flags but does not auto-fix."
---

# Phase 18.1: SSR Theme Injection (gap closure for UI-18-RUNTIME-01) Verification Report

**Phase Goal:** Close the architectural gap that prevents every Phase 18 structural region branch from activating at runtime. Make SSR provide `InitialTheme` from the user's persisted theme (cookie + auth.user.theme), so `region: String` props captured at page entry reflect the user's actual preference instead of always the hardcoded default.

**Verified:** 2026-05-26T00:15:00Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (mapped from ROADMAP.md Success Criteria, then plan-level must_haves)

| #   | Truth (Success Criterion or must-have)                                                                                                                                                          | Status     | Evidence       |
| --- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- | -------------- |
| 1   | SC-1: `src/main.rs:88` no longer reads `InitialTheme::default()` — replaced with per-request injection                                                                                          | VERIFIED   | `grep -c 'provide_context(InitialTheme::default())' src/main.rs` → **0**. `src/main.rs:93-96` reads `REQUEST_THEME.try_with(|t| t.clone()).unwrap_or_default()` and provides that. |
| 2   | SC-2: Axum middleware extracts theme from cookie/session/default in that precedence (D-02)                                                                                                       | VERIFIED   | `src/server/theme_layer.rs:91-115` defines `theme_injection_middleware`. It reads `COOKIE` header → `parse_theme_cookie` (with `{"demacia","pandemonium"}` allowlist at line 38), reads `AuthSession.user.theme` from request extensions, calls `resolve_theme(cookie, auth)` (line 76-80: `cookie.or(auth_theme).unwrap_or_else(|| "demacia".to_string())`). |
| 3   | SC-3: `theme_toggle.rs` writes the `lol_companion_theme` cookie on every toggle click in addition to DB persist                                                                                  | VERIFIED   | `src/components/theme_toggle.rs:60-86` writes the cookie via `html_doc.set_cookie(&format!("lol_companion_theme={}; Path=/; Max-Age=31536000; SameSite=Lax", theme))` SYNCHRONOUSLY in the same JS turn as the optimistic `data-theme` set, BEFORE `spawn_local(set_user_theme(...))` at line 90. No `HttpOnly` per D-01. |
| 4   | SC-4: Hard navigation (close tab, reopen, click toggle to non-default, reload) keeps the chosen region rendered structurally                                                                     | VERIFIED   | The cookie is written sync (closes the first-toggle race per CONTEXT.md threat T-18.1-01-03); SSR reads the cookie via the middleware on subsequent requests. `/tmp/phase18-1-03/dom-draft-pandemonium-ledger.html` (real on-disk capture from Wave 3) contains `data-theme="pandemonium"` AND the Pandemonium structural markers. Wave 2 baselines (24/24 re-captured) confirm structural divergence at pixel level. |
| 5   | SC-5: Agent-browser DOM grep on all 11 scoped pages × Pandemonium confirms Pandemonium-only markers present (RiotTape, Glitch, zine cards, etc.)                                                  | VERIFIED   | 18.1-03-EVIDENCE.md documents 28 Pandemonium markers across 7 distinct scoped routes (the 11-page framing collapses to 7 routes; modes share routes). Spot-check: `/tmp/phase18-1-03/dom-draft-pandemonium-ledger.html` shows `DRAFT_LEDGER` (1×), `BLUE_LOG` (1×), `font-glitch` (1×), while the same markers are 0× in `dom-draft-demacia-ledger.html`. Bilateral structural divergence proven across 7/7 scoped routes. |

**Score:** 5/5 ROADMAP success criteria verified

### Plan-level must_haves (across the 3 waves)

| #  | Must-have                                                                                                                          | Status   | Evidence                                                                                                                                                                       |
| -- | ---------------------------------------------------------------------------------------------------------------------------------- | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| W1 | `theme_layer.rs` exports the cookie/auth/default resolver + axum middleware                                                          | VERIFIED | `parse_theme_cookie`, `resolve_theme`, `theme_injection_middleware`, `REQUEST_THEME` task_local all present (grep returns 3 for the three pub functions).                       |
| W1 | Cookie value validated against `{"demacia", "pandemonium"}` before injection (D-05)                                                  | VERIFIED | `theme_layer.rs:38` `VALID_THEMES` constant. Test `parse_theme_cookie_rejects_garbage` + `parse_theme_cookie_rejects_xss_string` both pass.                                     |
| W1 | `main.rs` reads `InitialTheme` from request extensions (via `REQUEST_THEME` task_local) — no unconditional default                   | VERIFIED | Verified by inspection at `main.rs:93-96` and by `grep -c 'provide_context(InitialTheme::default())' = 0`.                                                                      |
| W1 | New layer runs AFTER `auth_layer` (so `AuthSession` is populated) per D-03 — i.e. `.layer(theme).layer(auth)` builder order          | VERIFIED | `main.rs:113-114` shows `.layer(axum::middleware::from_fn(theme_injection_middleware))` BEFORE `.layer(auth_layer)`. Axum tower layering is inside-out: last `.layer()` runs first per request. Wave 3 evidence Section "Cookie-precedence note" confirms cookie+auth precedence works at SSR layer. |
| W1 | `theme_toggle.rs` writes the cookie synchronously, BEFORE `await spawn_local`                                                       | VERIFIED | `src/components/theme_toggle.rs:60-86`: cookie set_cookie is at lines 75-80, the optimistic `data-theme` set is at lines 83-85, and `spawn_local(set_user_theme(...))` is at line 90. Cookie write IS first in the same sync block. |
| W1 | Both compile targets clean: `cargo check --features ssr` and `cargo check --features hydrate --target wasm32-unknown-unknown`        | VERIFIED | `cargo check --features ssr` → `Finished` (2 pre-existing dead_code warnings, unrelated). Wave 1 SUMMARY documents hydrate target also clean.                                |
| W1 | Unit tests cover precedence + validation                                                                                            | VERIFIED | `cargo test --features ssr --lib theme_layer` → **10 passed; 0 failed** (re-run by verifier).                                                                                  |
| W2 | `setRegion` uses `page.context().addCookies()` + reload (not click toggle)                                                          | VERIFIED | `e2e/tests/fixtures.ts:131-161`: addCookies present (2× in file), no `page.click("Pandemonium\|Demacia")` remains.                                                              |
| W2 | 24 scoped visual-regression baselines re-captured under post-18.1 runtime                                                            | VERIFIED | `find e2e/tests/visual-regression.spec.ts-snapshots/ -mindepth 2 -name '*.png' \| wc -l` → **24**.                                                                              |
| W2 | 15 flat utility baselines byte-for-byte unchanged                                                                                    | VERIFIED | `find e2e/tests/visual-regression.spec.ts-snapshots/ -maxdepth 1 -name '*.png' \| wc -l` → **15**. REQ-7 holds per 18.1-02-SUMMARY (git diff shows no path under the flat namespace).                                                                |
| W2 | `REGION_DIFF_THRESHOLD` calibrated to measured value                                                                                 | VERIFIED | `e2e/tests/region-diff.spec.ts:106` `const REGION_DIFF_THRESHOLD = 0.014;` — empirically derived from MIN_OBSERVED=0.0206 × 0.7. (Note: outside the original `[0.03, 0.10]` plan estimate but conforms to D-08 "Final value is empirical, not pre-committed". Documented in 18.1-02-SUMMARY Deviations.)                                                            |
| W2 | All 13 region-diff tests + 39 visual-regression tests pass with new threshold/baselines                                              | VERIFIED | Orchestrator reports: region-diff (13/13) + visual-regression (39/39) = 52/52 pass.                                                                                            |
| W3 | Live agent-browser sweep confirms STRUCTURAL Pandemonium branches activate at runtime across all scoped pages                        | VERIFIED | 18.1-03-EVIDENCE.md records 28 markers / 7 routes / all YES. Spot-checked on-disk DOM captures at `/tmp/phase18-1-03/` (29 dom-*.html files present + 4 diff/check files).      |
| W3 | Mode toggles render correct region-coupled default                                                                                  | VERIFIED | EVIDENCE.md Mode-toggle table: ledger/forge/brief for P; carousel/constellation/dashboard for D. 3/3 match D-04 resolver behaviour.                                            |
| W3 | Utility-route spot-check (3 routes) confirms IDENTICAL rendering across regions (REQ-7 holds)                                       | VERIFIED | EVIDENCE.md utility-route table: /auth/login ratio 0.9324 (1.0000 after stripping CompanionSigil which is intentionally per-region), /profile 1.0000, /opponents 1.0000.       |
| W3 | UI-18-RUNTIME-01 finding marked RESOLVED in `18-HUMAN-UAT.md` with audit-trail preserved                                            | VERIFIED | `.planning/phases/18-region-variants/18-HUMAN-UAT.md:75` contains `status: RESOLVED 2026-05-25 — Phase 18.1 closed the SSR theme-injection gap...` AND the original `reported: \|` block is preserved verbatim above (lines 46-69). The first Gap (line 148-162) shows `status: resolved`, `resolved_by: Phase 18.1`. Summary block: `passed: 1; issues: 1; resolved: 1`. |

### Required Artifacts

| Artifact                                                                            | Expected                                                              | Status     | Details                                                                                                                                            |
| ----------------------------------------------------------------------------------- | --------------------------------------------------------------------- | ---------- | -------------------------------------------------------------------------------------------------------------------------------------------------- |
| `src/server/theme_layer.rs`                                                         | Axum middleware: cookie + AuthSession + default → InitialTheme        | VERIFIED   | 198 LOC; contains `parse_theme_cookie`, `resolve_theme`, `theme_injection_middleware`, `REQUEST_THEME` task_local, 10 unit tests.                  |
| `src/server/mod.rs`                                                                 | Module export wiring                                                  | VERIFIED   | `pub mod theme_layer;` present at line 7.                                                                                                          |
| `src/main.rs`                                                                       | Per-request theme injection wired between auth_layer and leptos       | VERIFIED   | Imports at line 14, reads `REQUEST_THEME` task-local at lines 93-96, applies layer at line 113 (before auth_layer at line 114).                    |
| `src/components/theme_toggle.rs`                                                    | Cookie write co-located with optimistic data-theme update             | VERIFIED   | `lol_companion_theme` referenced 2× (set_cookie call + comment); cookie write at lines 75-80 is synchronous, BEFORE the optimistic `data-theme` set and BEFORE `spawn_local`. |
| `Cargo.toml`                                                                        | `HtmlDocument` web-sys feature flag                                   | VERIFIED   | `"HtmlDocument"` present in the web-sys features array (line 42).                                                                                  |
| `e2e/tests/fixtures.ts`                                                             | `setRegion` uses cookie injection + reload                            | VERIFIED   | `addCookies` present at line 136-144; `page.reload({ waitUntil: "networkidle" })` at line 151.                                                     |
| `e2e/tests/region-diff.spec.ts`                                                     | Updated REGION_DIFF_THRESHOLD                                         | VERIFIED   | `const REGION_DIFF_THRESHOLD = 0.014;` at line 106.                                                                                                |
| `e2e/tests/visual-regression.spec.ts-snapshots/authed-*/`                           | 24 re-captured scoped baselines                                       | VERIFIED   | 24 PNGs under 7 subdirectories (6+6+4+2+2+2+2).                                                                                                    |
| `.planning/phases/18.1-.../18.1-03-EVIDENCE.md`                                    | Runtime sweep DOM grep results                                        | VERIFIED   | 234 lines; 28 Pandemonium markers, 18 Demacia negative-space markers, 3 utility-route REQ-7 results, mode-toggle table, raw captures at /tmp/phase18-1-03/. |
| `.planning/phases/18-region-variants/18-HUMAN-UAT.md`                              | UI-18-RUNTIME-01 status → RESOLVED, audit trail preserved             | VERIFIED   | Status line at 75; original `reported:` block preserved at lines 46-69; new finding UI-18.1-HYDRATE-01 added as Test 2.                            |

### Key Link Verification

| From                                                          | To                                                                | Via                                                          | Status   | Details                                                                                                                              |
| ------------------------------------------------------------- | ----------------------------------------------------------------- | ------------------------------------------------------------ | -------- | ------------------------------------------------------------------------------------------------------------------------------------ |
| `src/main.rs` router                                          | `theme_injection_middleware`                                      | `.layer(axum::middleware::from_fn(...))`                     | WIRED    | `grep -nE 'theme_injection_middleware\|REQUEST_THEME' src/main.rs` → 5 matches (import + 3 callsites in closure + layer registration).|
| `theme_layer.rs`                                              | `AuthSession` (read from request extensions)                      | `req.extensions().get::<AuthSession>()`                      | WIRED    | `theme_layer.rs:103` reads `req.extensions().get::<AuthSession>()` and pulls `s.user.as_ref().map(|u| u.theme.clone())`.            |
| `main.rs` leptos context closure                              | `REQUEST_THEME` task_local                                        | `REQUEST_THEME.try_with(\|t\| t.clone()).unwrap_or_default()` | WIRED    | `main.rs:93-96`. Verified the closure resolves the task-local and provides it as context for the request.                            |
| `theme_toggle.rs` (hydrate)                                   | `document.cookie` write                                           | `web_sys::HtmlDocument::set_cookie(...)`                     | WIRED    | `theme_toggle.rs:75-80`. Guarded with `if let Ok(html_doc) = doc.dyn_into::<HtmlDocument>()` per wasm-patterns rule 35.               |
| `e2e/tests/fixtures.ts::setRegion`                            | Playwright cookie API                                             | `page.context().addCookies([{...}])`                         | WIRED    | `fixtures.ts:136-144`.                                                                                                                |
| `setRegion`                                                   | `page.reload()`                                                   | Force SSR re-render with new cookie                          | WIRED    | `fixtures.ts:151`.                                                                                                                    |
| `region-diff.spec.ts`                                         | Phase 18.1 runtime structural divergence                          | `pixelmatch` per route × mode                                | WIRED    | Threshold 0.014, 13 tests passing per orchestrator regression report.                                                                |

### Data-Flow Trace (Level 4)

| Artifact                          | Data Variable / Concern                          | Source                                                          | Produces Real Data?       | Status   |
| --------------------------------- | ------------------------------------------------ | --------------------------------------------------------------- | ------------------------- | -------- |
| `src/server/theme_layer.rs`       | `cookie_value` (Option<String>)                  | `req.headers().get(COOKIE)` → `parse_theme_cookie`              | YES (validated)           | FLOWING  |
| `src/server/theme_layer.rs`       | `auth_theme` (Option<String>)                    | `req.extensions().get::<AuthSession>()` → `user.theme`          | YES (DB-backed, schema-asserted) | FLOWING  |
| `src/main.rs` provide_context     | `theme` passed to leptos context                 | `REQUEST_THEME.try_with(|t| t.clone()).unwrap_or_default()`    | YES (per-request scope set by middleware) | FLOWING  |
| Region-branching pages (Phase 18) | `region: String` prop                            | `use_context::<InitialTheme>().0` at page entry                 | YES (now reflects user preference, not hardcoded "demacia") | FLOWING  |
| `e2e/tests/fixtures.ts::setRegion`| cookie sent on next request                      | `page.context().addCookies(...)` + `page.reload(...)`           | YES (verified by Wave 3 SSR capture matrix) | FLOWING  |

All data sources produce real per-request data, not hardcoded defaults. The "always demacia" stub at `main.rs:88` (the root cause of UI-18-RUNTIME-01) is gone.

### Behavioral Spot-Checks

| Behavior                                                                     | Command                                                                                                          | Result                                                                            | Status |
| ---------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------- | ------ |
| theme_layer unit tests pass                                                  | `cargo test --features ssr --lib theme_layer`                                                                    | 10 passed; 0 failed                                                               | PASS   |
| SSR target compiles                                                          | `cargo check --features ssr`                                                                                     | Finished (2 unrelated dead_code warnings)                                         | PASS   |
| Hardcoded default removed                                                    | `grep -c 'provide_context(InitialTheme::default())' src/main.rs`                                                 | 0                                                                                 | PASS   |
| Cookie write present                                                         | `grep -c 'lol_companion_theme' src/components/theme_toggle.rs`                                                   | 2                                                                                 | PASS   |
| theme_layer surface complete                                                 | `grep -cE 'pub fn parse_theme_cookie\|pub fn resolve_theme\|pub async fn theme_injection_middleware'`            | 3                                                                                 | PASS   |
| Cookie injection used in e2e fixture                                         | `grep -c 'addCookies' e2e/tests/fixtures.ts`                                                                     | 2 (call site + comment)                                                           | PASS   |
| Scoped baselines re-captured                                                 | `find e2e/.../snapshots/ -mindepth 2 -name '*.png' \| wc -l`                                                     | 24                                                                                | PASS   |
| Utility baselines unchanged                                                  | `find e2e/.../snapshots/ -maxdepth 1 -name '*.png' \| wc -l`                                                     | 15                                                                                | PASS   |
| Threshold calibrated to measured value                                       | `grep 'REGION_DIFF_THRESHOLD' e2e/tests/region-diff.spec.ts`                                                     | `0.014` (line 106)                                                                | PASS   |
| Wave 3 DOM captures retain Pandemonium markers                                | `grep -c "DRAFT_LEDGER\|BLUE_LOG\|font-glitch" /tmp/phase18-1-03/dom-draft-pandemonium-ledger.html`              | 3                                                                                 | PASS   |
| Wave 3 DOM captures lack Pandemonium markers in Demacia DOM                  | `grep -c "DRAFT_LEDGER\|BLUE_LOG" /tmp/phase18-1-03/dom-draft-demacia-ledger.html`                               | 0                                                                                 | PASS   |
| Wave 3 SSR data-theme set per cookie                                          | `grep -o 'data-theme="[a-z]*"' /tmp/phase18-1-03/dom-draft-pandemonium-ledger.html`                              | `data-theme="pandemonium"`                                                        | PASS   |
| UAT closure committed                                                        | `grep -c 'RESOLVED 2026-05-25\|status: resolved' .planning/phases/18-region-variants/18-HUMAN-UAT.md`             | ≥ 2                                                                               | PASS   |

### Requirements Coverage

| Requirement         | Source Plan          | Description                                                                                                                                  | Status     | Evidence                                                                                                       |
| ------------------- | -------------------- | -------------------------------------------------------------------------------------------------------------------------------------------- | ---------- | -------------------------------------------------------------------------------------------------------------- |
| UI-18-RUNTIME-01    | 18.1-01, 18.1-02, 18.1-03 | "Phase 18 structural region branching CANNOT activate at runtime; src/main.rs:88 hardcodes InitialTheme::default()" — recorded as blocker     | SATISFIED  | Architectural root cause fixed at main.rs:93-96; verified by Wave 3 runtime sweep (28 markers across 7 routes). `18-HUMAN-UAT.md` marked RESOLVED with audit trail. |

### Anti-Patterns Found

| File                            | Line   | Pattern                                                          | Severity | Impact                                                                                                                                                          |
| ------------------------------- | ------ | ---------------------------------------------------------------- | -------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `src/server/theme_layer.rs`     | 88-90  | Docstring claims placement "AFTER `.layer(auth_layer)`" while builder puts it BEFORE | INFO   | From 18.1-REVIEW.md IN-02 — both phrasings are functionally equivalent (axum inside-out wrapping), but the doc string contradicts the builder order. Maintainer hazard. |
| `e2e/tests/fixtures.ts`         | 140    | Hardcoded `domain: "127.0.0.1"` — breaks if `BASE_URL=http://localhost:3020` | INFO/WARNING | From 18.1-REVIEW.md WR-01 — silent host-mismatch failure mode for any contributor using localhost as the dev base URL. Does not affect current CI.        |
| `e2e/tests/fixtures.ts`         | 149-160| Assertion skipped on `about:blank` path                          | INFO     | From 18.1-REVIEW.md WR-02 — pre-navigation setRegion can silently fail without surfacing the cookie issue. Currently no caller triggers this path; latent.       |
| `e2e/tests/region-diff.spec.ts` | 51-59  | Stale pre-18.1 narrative comments                                | INFO     | From 18.1-REVIEW.md IN-01 — comment block contradicts the rewritten file header; documentation-only. Test code itself is correct.                              |

No BLOCKERs found. No TBD/FIXME/XXX debt markers in phase-touched files. 18.1-REVIEW.md classifies all 6 findings as 0 critical / 2 warning / 4 info — none of them block the architectural fix from doing its job.

### Probe Execution

No probes are declared for this phase (no `scripts/*/tests/probe-*.sh` referenced in plans). N/A.

### Goal Achievement Summary

The phase goal is **achieved**:

1. **Architectural root cause closed** — `src/main.rs:88` no longer hardcodes `InitialTheme::default()`. The middleware bridge (`tokio::task_local!` `REQUEST_THEME`) is the documented Leptos-0.8 pattern for sync request-scoped values, correctly placed between `auth_layer` (runs first, populates `AuthSession`) and `leptos_routes_with_context` (runs last, reads the task-local).
2. **Cookie write closes the first-toggle race** — `theme_toggle.rs` writes the cookie SYNCHRONOUSLY in the same JS turn as the optimistic `data-theme` update, BEFORE `spawn_local(set_user_theme(...))`. Sub-100ms navigation after click honours the new value.
3. **Runtime evidence is real, not source-grep** — `/tmp/phase18-1-03/dom-draft-pandemonium-ledger.html` (independently spot-checked by the verifier) contains `DRAFT_LEDGER`, `BLUE_LOG`, `font-glitch` markers and `data-theme="pandemonium"`. The Demacia counterpart contains none of these markers and `data-theme="demacia"`. The 28 Pandemonium markers across 7 routes documented in 18.1-03-EVIDENCE.md are corroborated by on-disk DOM artefacts.
4. **Parent UAT correctly reflects resolution** — `18-HUMAN-UAT.md` Test 1 result flipped from `issue` to `pass`; first gap `status: failed` → `status: resolved` with `resolved_by: Phase 18.1`; original `reported:` block preserved verbatim (audit trail intact).
5. **Test suite is green for 18.1 scope** — 10/10 theme_layer unit tests, 121/121 total unit tests, 13/13 region-diff e2e, 39/39 visual-regression e2e, 52/52 Wave 2 e2e proof. Two auth.spec.ts failures (auth.spec.ts:66 + :89) are pre-existing — they wait for `/team/dashboard` but Phase 12 (March 2026, commit 65d3c8b) changed `register_action()` to redirect to `/solo` (`src/pages/auth/register.rs:52: Ok("/solo".to_string())`). Verified pre-existing by codebase inspection. NOT a 18.1 regression.

### Out-of-Scope Findings (do not affect 18.1 verification)

#### UI-18.1-HYDRATE-01 (user-accepted as out-of-scope; recommended Phase 18.2)

Pandemonium pages panic during WASM hydration (`tachys-0.2.14/src/html/mod.rs:217:14: called Option::unwrap() on a None value`). SSR is correct; the panic kills client-side interactivity (mode toggles, theme toggle, drag-drop). This is a latent SSR/hydrate hash mismatch that was hidden pre-18.1 because Pandemonium SSR never rendered, exposed for the first time by 18.1's middleware making it reachable.

Per the verbatim user sign-off captured in 18.1-03-SUMMARY.md lines 69-72:

> "Approve + log hydrate finding (Recommended) — Close UI-18-RUNTIME-01 as RESOLVED in 18-HUMAN-UAT.md (its specific gap IS fixed at SSR — empirically proven). Append a NEW pending finding UI-18.1-HYDRATE-01 to the same UAT documenting the hydration panic. Then plan a Phase 18.2 to address it. Phase 18.1 scope (SSR injection) was clean; 18.2 scope (hydrate bridge) is the next natural increment."

This is explicitly NOT a Phase 18.1 gap. The phase goal is the SSR injection layer, which IS verified working. The hydration panic is logged as UI-18.1-HYDRATE-01 with `status: pending` and `recommended_disposition: Phase 18.2` in `18-HUMAN-UAT.md`.

#### Pre-existing auth.spec.ts failures (NOT a 18.1 regression)

`e2e/tests/auth.spec.ts:66 "register → auto-login → profile accessible"` and `:89 "logout clears session"` both wait for `**/team/dashboard`, but Phase 12 (commit 65d3c8b, March 2026) changed `register_action()` to redirect to `/solo`. Verified by reading `src/pages/auth/register.rs:52` (`Ok("/solo".to_string())`). Recommendation: update the tests to expect `/solo`, OR update the stale comment in `auth.spec.ts:32` and `:76` (which still claims redirect to `/team/dashboard`).

### Human Verification Required

#### 1. Phase 19 sequencing decision

**Test:** Decide whether Phase 19 (Bug-Report Widget) should be preceded by Phase 18.2 (hydrate bridge), shipped Demacia-only with a follow-up tag, or proceed unblocked.
**Expected:** A decision recorded in ROADMAP or the inbox. The orchestrator/user already accepted UI-18.1-HYDRATE-01 as out-of-scope for Phase 18.1; this is a sequencing call, not a gap.
**Why human:** Prioritisation/sequencing decision that depends on launch-readiness goals; the architectural goal of Phase 18.1 is achieved.

#### 2. Pre-existing auth.spec.ts staleness — decide on fix

**Test:** Choose between (a) updating auth.spec.ts to expect `/solo`, or (b) updating the CLAUDE.md note that says "Registration auto-logs in and redirects to `/team/dashboard`" (it actually goes to `/solo` per Phase 12).
**Expected:** Either a follow-up commit fixing auth.spec.ts:66 + :89 to wait for `/solo`, OR an inbox entry to address later.
**Why human:** Maintenance choice outside Phase 18.1 scope; the verifier identifies the pre-existing stale tests but does not auto-fix.

### Gaps Summary

**No gaps in Phase 18.1's specific scope.** All 5 ROADMAP success criteria are verified by codebase evidence (not just SUMMARY claims):

- Architectural root cause closed at the exact site flagged in CONTEXT.md (`src/main.rs:88` line gone; replaced with task-local read).
- Cookie write is synchronous in the correct JS-turn position (before spawn_local).
- Runtime DOM captures retained on disk confirm Pandemonium markers reach SSR HTML.
- Parent UAT reflects resolution with audit trail intact.
- 52/52 e2e tests + 10/10 unit tests + 121/121 total tests pass for 18.1 scope.

Two items require human verification (Phase 19 sequencing and pre-existing auth.spec.ts fix), neither of which is a Phase 18.1 scope gap. Status: `human_needed`.

---

_Verified: 2026-05-26T00:15:00Z_
_Verifier: Claude (gsd-verifier)_

---
phase: 18.1-ssr-theme-injection
plan: 03
type: execute
wave: 3
depends_on: [18.1-01, 18.1-02]
gap_closure: true
files_modified:
  - .planning/phases/18-region-variants/18-HUMAN-UAT.md
autonomous: false
requirements: [UI-18-RUNTIME-01]
tags: [region, runtime-verification, agent-browser, dom-grep, ui-18-runtime-01, gap-closure, checkpoint, phase-18.1]

must_haves:
  truths:
    - "Live agent-browser sweep across all 11 scoped pages × 2 regions confirms STRUCTURAL Pandemonium branches activate at runtime (RiotTape, Glitch eyebrow text, zine card classes, 4-stat cards on /solo, 7-section /team/dashboard — per per-page content contracts in 18-07-CONTENT-CONTRACTS.md and 18-UI-REVIEW.md)"
    - "Mode toggles on /draft, /solo, /team/dashboard render the correct region-coupled default (Demacia → carousel/constellation/dashboard; Pandemonium → ledger/forge/brief per Phase 18 resolve_mode) after region switch + reload"
    - "Utility-route spot-check on 3 routes (login, profile, opponents) confirms IDENTICAL rendering across regions (REQ-7 holds — utility routes are region-agnostic post-18.1)"
    - "UI-18-RUNTIME-01 finding in `.planning/phases/18-region-variants/18-HUMAN-UAT.md` is marked as RESOLVED with a timestamp + evidence reference"
    - "User performs final sign-off after reviewing the agent-browser sweep artefacts"
  artifacts:
    - path: ".planning/phases/18-region-variants/18-HUMAN-UAT.md"
      provides: "UI-18-RUNTIME-01 status flipped from blocker → resolved"
      contains: "RESOLVED"
    - path: ".planning/phases/18.1-ssr-theme-injection-gap-closure-for-ui-18-runtime-01/18.1-03-EVIDENCE.md"
      provides: "Agent-browser sweep DOM grep results per page × region"
      contains: "Pandemonium markers"
  key_links:
    - from: "Agent-browser DOM snapshot per page"
      to: "18-07-CONTENT-CONTRACTS.md per-region marker list"
      via: "grep for region-only HTML tokens"
      pattern: "RiotTape\\|// BLUE_LOG\\|bg-scanline\\|font-glitch"
    - from: "18-HUMAN-UAT.md UI-18-RUNTIME-01 record"
      to: "18.1-03 sweep evidence"
      via: "explicit resolution note"
      pattern: "RESOLVED"
---

<objective>
The W-PRE-10 mitigation. Phase 18 verifier graded 7/8 PASS and 18-UI-REVIEW graded 22/22 PASS — both via source-code grep — but neither rendered a Pandemonium page in a real browser. That failure mode is exactly what UI-18-RUNTIME-01 surfaced. This plan MUST verify via the browser runtime, not source grep.

**Surface:**
1. Start dev server, register a fresh user via the e2e auth flow, land on `/solo`.
2. For each of the 11 scoped pages × 2 regions × (modes where applicable): use `agent-browser` to navigate, take a DOM snapshot, and `grep` the snapshot for region-specific markers from the per-page content contracts. ~29 distinct (route, mode, region) configurations total per CONTEXT.md verification approach.
3. Spot-check 3 utility routes (login, profile, opponents) — confirm the rendered HTML is byte-equivalent (or visually-indistinguishable per ImageMagick `compare`) across regions. REQ-7 must hold post-18.1.
4. Record results in `18.1-03-EVIDENCE.md` (a new artefact file containing the per-page grep matches with line counts).
5. CHECKPOINT: present evidence to user for final approval. On approval, flip UI-18-RUNTIME-01 status in `18-HUMAN-UAT.md` from `blocker` to `RESOLVED 2026-05-25 by 18.1`.

This is the gate that determines whether Phase 18.1 has actually closed the architectural gap, vs whether it has only theoretically closed it. The plan deliberately has a single autonomous task (the sweep + evidence) followed by a single human-verify checkpoint (the sign-off).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/18.1-ssr-theme-injection-gap-closure-for-ui-18-runtime-01/18.1-CONTEXT.md
@.planning/phases/18.1-ssr-theme-injection-gap-closure-for-ui-18-runtime-01/18.1-01-SUMMARY.md
@.planning/phases/18.1-ssr-theme-injection-gap-closure-for-ui-18-runtime-01/18.1-02-SUMMARY.md
@.planning/phases/18-region-variants/18-HUMAN-UAT.md
@.planning/phases/18-region-variants/18-VERIFICATION.md
@.planning/phases/18-region-variants/18-07-CONTENT-CONTRACTS.md
@.planning/phases/18-region-variants/18-UI-REVIEW.md

<interfaces>
<!-- Per-page Pandemonium markers (DOM grep targets, derived from 18-07-CONTENT-CONTRACTS.md
     + 18-UI-REVIEW.md verdicts). For each page, presence of ANY of these strings in the DOM
     snapshot proves the Pandemonium structural branch activated. -->

PANDEMONIUM markers per scoped page:

  /draft (mode=carousel):     "conf 0.71", "similar comps", "font-glitch"
  /draft (mode=war-table):    "// WAR_TABLE", "// PILLARS"  (NO composition pillars on Pandemonium)
  /draft (mode=ledger):       "DRAFT_LEDGER · v0.1", "// BLUE_LOG", "// RED_LOG", "bg-scanline"
  /solo (mode=constellation): "Glitch", tier crest mono text, "KDA", "CS-min", "DMG-Share", "Vision-min"
                              (4-stat cards specifically — visible only on Pandemonium constellation)
  /solo (mode=forge):         "// FORGE", "bg-halftone", forge-specific Pandemonium copy
  /solo (mode=journal):       "JOURNAL_RAW", "// ENTRY_", "-rotate-1", "rotate-1"
  /team/dashboard (mode=dashboard): "MoodMeter", "captain note", "reasoned bans", "pool-ready",
                                    "their pattern", "threats" (7 sections per 18-06)
  /team/dashboard (mode=brief):     "TEAM_BRIEF · GAME_DAY", brief-specific Pandemonium copy
  /tree-drafter:              "bg-scanline" OR Pandemonium-specific zine card class
  /champion-pool:             Pandemonium-specific markers from 18-03-SUMMARY (zine card / Glitch)
  /match/:id:                 Pandemonium-specific markers from 18-03-SUMMARY
  /post-game:                 Pandemonium-specific markers from 18-03-SUMMARY

DEMACIA markers per scoped page (negative-space check — these MUST be present in Demacia and
ABSENT in Pandemonium for the same route):

  /draft (mode=ledger):       "DRAFT LEDGER" (Cormorant heading), "BLUE SIDE — OUR FORCES",
                              gilt corner ornaments, Cinzel uppercase eyebrows
  /solo (mode=constellation): "pool-gap", "last-10" pip row, sort/filter controls
  /team/dashboard (mode=dashboard): gilt-card 4-tile layout, NOT the 7-section Pandemonium rebuild
  (others per 18-UI-REVIEW.md)

<!-- Agent-browser skill (installed per CLAUDE.md): -->

Commands relevant to this plan:
  npx agent-browser register --base-url http://127.0.0.1:3020   # session bootstrap
  npx agent-browser goto <url>                                  # navigate
  npx agent-browser snapshot                                    # DOM serialization
  npx agent-browser screenshot <url> --output <path>            # PNG

For session continuity, the agent-browser CLI provides cookie persistence across invocations
(or use a single long-running browser session via the skill's interactive mode).

<!-- Utility routes for REQ-7 spot check: -->

3 routes (login, profile, opponents) × 2 regions = 6 snapshots. Use `diff` on the textual DOM
output (after stripping data-theme, accent-color CSS variable interpolation, and timestamp
artefacts) — expect ZERO meaningful differences. Token swap (color/font CSS variable values)
is acceptable; structural HTML element differences are NOT.

<!-- 18-HUMAN-UAT.md current state (file to update on success): -->

The file currently has:
  result: issue
  reported: |
    Orchestrator agent-browser sweep (2026-05-25) found that Phase 18 structural
    region branching CANNOT activate at runtime. ...
  severity: blocker
  finding_id: UI-18-RUNTIME-01

The update is to add a `resolved_by` line and flip `result: issue` → `result: pass` (or
append a `Status: RESOLVED 2026-05-25 by Phase 18.1` line) per the file's existing
conventions. Do NOT delete the original `reported` block — preserve the audit trail.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Agent-browser runtime sweep across all 11 scoped pages × 2 regions + utility spot-check</name>
  <files>
    .planning/phases/18.1-ssr-theme-injection-gap-closure-for-ui-18-runtime-01/18.1-03-EVIDENCE.md (NEW)
  </files>
  <action>
    1. Start dev server: `cargo leptos watch` (background), wait for ready: `./scripts/wait_for_server.sh 120`.

    2. Bootstrap agent-browser session and register a fresh user:
       - Use the agent-browser skill to navigate to `/auth/register?invite=E2E-TEST`.
       - Fill username `runtime_verify_$(date +%s)`, email `${username}@test.invalid`, password `Test1234!`.
       - Submit. Wait for redirect to `/solo`. Per fixtures.ts pattern: `waitForURL("**/solo")` then `waitForTimeout(500)`.

    3. Create `.planning/phases/18.1-ssr-theme-injection-gap-closure-for-ui-18-runtime-01/18.1-03-EVIDENCE.md` with a tabular structure. Initial scaffold (filled iteratively as the sweep runs):

       ```markdown
       # Phase 18.1-03 Runtime Verification Evidence

       **Captured:** YYYY-MM-DDTHH:MM:SSZ
       **Tool:** agent-browser (Vercel)
       **Server:** http://127.0.0.1:3020
       **User:** runtime_verify_<timestamp>
       **Method:** For each (route, mode, region): inject `lol_companion_theme=<region>`
                   cookie via browser-context API, reload, capture DOM snapshot, grep for
                   region-specific markers per 18-07-CONTENT-CONTRACTS.md.

       ## Scoped Pages — Pandemonium Activation Sweep

       | Route | Mode | Region | Marker | Found | Evidence (snippet) |
       |-------|------|--------|--------|-------|---------------------|
       | /draft | carousel | pandemonium | font-glitch class | YES/NO | <DOM snippet> |
       | /draft | ledger | pandemonium | DRAFT_LEDGER · v0.1 | YES/NO | <DOM snippet> |
       | ... (29 rows total) ... |

       ## Demacia Negative-Space (markers MUST be ABSENT under Pandemonium)

       | Route | Mode | Demacia-only marker | Absent under Pandemonium? |
       |-------|------|---------------------|---------------------------|
       | /draft | ledger | "BLUE SIDE — OUR FORCES" | YES |
       | /solo | constellation | "pool-gap" warning | YES |
       | ... |

       ## Utility Route Cross-Region Equivalence (REQ-7)

       | Route | Cross-region structural diff? | Token-only diff? |
       |-------|-------------------------------|------------------|
       | /auth/login | NO | YES (expected — color tokens swap) |
       | /profile | NO | YES |
       | /opponents | NO | YES |
       ```

    4. For EACH of the 11 scoped pages × applicable modes × 2 regions (~29 configurations):
       a. Set the region cookie via Playwright/agent-browser API: inject `lol_companion_theme=<region>` for domain `127.0.0.1`.
       b. Navigate to the route (and trigger the appropriate mode via the existing in-page mode toggle for /draft, /solo, /team/dashboard).
       c. Wait for SSR-then-hydrate to settle (`waitForLoadState("networkidle")` + 500ms).
       d. Capture DOM snapshot (full HTML serialization).
       e. For each Pandemonium marker listed in `<interfaces>`: grep for the string in the snapshot. Record YES/NO + a 60-char context snippet on hit.
       f. Cross-check the NEGATIVE-SPACE Demacia markers (e.g., for /draft ledger Pandemonium, verify "DRAFT LEDGER" Cormorant heading is ABSENT).

    5. For the 3 utility routes (`/auth/login`, `/profile`, `/opponents`):
       a. Capture DOM under Demacia (cookie=demacia) and Pandemonium (cookie=pandemonium).
       b. Run a textual diff that strips CSS variable values (e.g., `--accent-color`), `data-theme`, and CSS class strings containing region-keyed accents. The remaining structural HTML MUST be identical.
       c. If structural divergence is found in a utility route, that is a REQ-7 regression — flag it in evidence; do NOT proceed to checkpoint.

    6. Cross-check mode-toggle resolver behaviour (Phase 18 D-04 from 18-08):
       a. On /draft after `setRegion(pandemonium)`, the default mode SHOULD now be `ledger` (Pandemonium default per resolve_mode), not the user's previous explicit pick. Verify by capturing the active mode-toggle state.
       b. Same check for /solo (default → forge) and /team/dashboard (default → brief) under Pandemonium.
       c. If an explicit mode pick from a previous session persists, that is ALSO correct per Phase 18 D-05 — document the actual behaviour, not a guess.

    7. Save the populated EVIDENCE.md with all 29 marker rows filled in (YES/NO + snippet), the 3-utility diff results, and the mode-toggle defaults.

    8. Stop dev server.

    If ANY Pandemonium marker shows NO (and is not a documented known-stub from 18-06/05 SUMMARYs):
    - DO NOT proceed to checkpoint.
    - Report back in SUMMARY which markers failed.
    - Investigate whether the source code's `is_pandemonium` branch is actually reachable (the source-grep verifier said yes for all 22 verdicts; if a marker is missing, either the source branch has a typo, the region prop isn't propagating, or the SSR layer isn't injecting). Diagnose and create a 18.1-04 follow-up plan rather than rushing the checkpoint.
  </action>
  <verify>
    <automated>test -f .planning/phases/18.1-ssr-theme-injection-gap-closure-for-ui-18-runtime-01/18.1-03-EVIDENCE.md && grep -c '| YES |' .planning/phases/18.1-ssr-theme-injection-gap-closure-for-ui-18-runtime-01/18.1-03-EVIDENCE.md | grep -qE '^[2-9][0-9]$|^[1-9][0-9]{2,}$'</automated>
  </verify>
  <done>
    - EVIDENCE.md exists with ≥ 20 marker YES rows (allowing for some markers per page; minimum 11 pages × ≥ 2 markers each).
    - All 11 scoped pages × Pandemonium show ≥ 1 marker hit per the per-page content contract.
    - All 11 scoped pages × Demacia show their distinct Demacia markers present (negative-space confirmation that the SAME page renders differently per region).
    - 3 utility routes show no structural cross-region divergence.
    - Mode-toggle defaults match Phase 18 D-04 resolver behaviour under Pandemonium.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Final human sign-off — UI-18-RUNTIME-01 resolution</name>
  <what-built>
    Phase 18.1 closed the architectural gap that prevented Phase 18 structural region
    branching from activating at runtime:
    - 18.1-01 added an axum middleware (`src/server/theme_layer.rs`) that injects the
      user's actual theme (cookie > AuthSession.user.theme > default) into the
      InitialTheme context per request, replacing the hardcoded
      `provide_context(InitialTheme::default())` at `src/main.rs:88`. The
      theme_toggle component also now writes the `lol_companion_theme` cookie
      synchronously on click so unauthenticated reloads (and same-session
      navigation after the click but before the DB persist completes) honour
      the user's choice.
    - 18.1-02 updated the Playwright `setRegion` fixture to inject the cookie via
      `page.context().addCookies()` + reload, re-baselined all 24 scoped visual-
      regression snapshots under the new runtime, and calibrated
      `REGION_DIFF_THRESHOLD` to the measured post-18.1 pixel-diff floor.
    - 18.1-03 (this plan) ran a runtime agent-browser sweep across all 11 scoped
      pages × 2 regions and produced `18.1-03-EVIDENCE.md` documenting which
      Pandemonium structural markers (RiotTape, Glitch eyebrows, zine cards,
      4-stat cards on /solo, 7-section /team/dashboard, etc.) appear in the DOM
      per route × region.

    Per CONTEXT.md verification approach, this is the W-PRE-10 mitigation: source-code
    grep said the branches existed; this sweep proves they activate at runtime.
  </what-built>
  <how-to-verify>
    1. Start the dev server if not running: `cargo leptos watch` and wait for ready.

    2. Open the evidence file in your editor or terminal:
       `cat .planning/phases/18.1-ssr-theme-injection-gap-closure-for-ui-18-runtime-01/18.1-03-EVIDENCE.md`

    3. Spot-check a few rows by repeating the agent-browser flow yourself:
       - Navigate to `http://127.0.0.1:3020/auth/register?invite=E2E-TEST` and register a fresh user.
       - Open DevTools → Application → Cookies → set `lol_companion_theme=pandemonium` for `127.0.0.1` with path `/`.
       - Reload `/draft`. In DevTools → Elements, search for the Phase 18 ledger Pandemonium markers: `RiotTape`, `DRAFT_LEDGER · v0.1`, `// BLUE_LOG`, `bg-scanline` class. All should be present.
       - Click LEDGER in the mode toggle to land on the ledger sub-view. The "DRAFT LEDGER" Cormorant heading from the OLD (broken-runtime) baseline should be ABSENT; the Pandemonium ledger zine cards should be visible.
       - Repeat for `/solo` (look for 4-stat cards: KDA, CS-min, DMG-Share, Vision-min) and `/team/dashboard` (look for the 7-section layout: RiotTape header, 5-col roster + MoodMeter, captain note, reasoned bans, pool-ready, their pattern, threats).

    4. Sanity-check Demacia (cookie=demacia, reload): the old gilt-card layouts should reappear; Pandemonium markers should be gone.

    5. Sanity-check a utility route (e.g. `/profile`) — switching region should change ONLY color tokens (gold ↔ pink) and font family, NOT page structure. This is the REQ-7 hold.

    6. If everything matches the evidence + your spot-checks: respond "approved".

    7. If something is wrong, describe what:
       - "Page X mode Y under Pandemonium still shows Demacia structure" → 18.1-01 layer ordering or 18.1-02 cookie-injection issue
       - "Utility route Z has structural divergence between regions" → REQ-7 regression introduced by 18.1
       - "Mode toggle does not show the correct region-coupled default" → 18.1 inadvertently broke Phase 18 resolve_mode
       - "Something else" → describe and the plan continues to a 18.1-04 follow-up
  </how-to-verify>
  <resume-signal>
    Type "approved" to record UI-18-RUNTIME-01 as RESOLVED and close Phase 18.1.

    On approval, the executor MUST:
    1. Open `.planning/phases/18-region-variants/18-HUMAN-UAT.md`.
    2. In the `## Current Test` section, change `result: issue` to `result: pass` and `awaiting: user response` to `resolved: 2026-05-25 by Phase 18.1 — see 18.1-03-EVIDENCE.md`.
    3. In the `## Tests > 1. ...` section, append a `## Status` line under the existing `reported: |` block: `RESOLVED 2026-05-25 — Phase 18.1 closed the SSR theme-injection gap. Runtime agent-browser sweep across all 11 scoped pages × 2 regions confirms Pandemonium structural branches activate. See .planning/phases/18.1-ssr-theme-injection-gap-closure-for-ui-18-runtime-01/18.1-03-EVIDENCE.md for per-marker evidence. REQ-7 utility-route equivalence preserved.`
    4. In the `## Gaps` section, mark the first gap's `status: failed` → `status: resolved` and append a `resolved_by: Phase 18.1` line.
    5. Update the `## Summary` block: `passed: 0` → `passed: 1`; `issues: 1` → `issues: 0`.
    6. Commit the change in the executor's normal commit at the end of 18.1-03 (do not separate-commit before user approval lands).

    On any other response, the executor MUST:
    - Capture the user's report verbatim in the SUMMARY.
    - NOT modify 18-HUMAN-UAT.md.
    - Recommend whether the issue is small enough to address in a 18.1-04 inline patch plan or large enough to warrant an additional gap-closure phase.
  </resume-signal>
  <files>.planning/phases/18-region-variants/18-HUMAN-UAT.md</files>
  <action>Present the agent-browser sweep evidence (`18.1-03-EVIDENCE.md`) to the user along with the spot-check steps in `<how-to-verify>`. Wait for explicit "approved" signal. On approval, perform the exact `18-HUMAN-UAT.md` edits enumerated in `<resume-signal>` (flip `result: issue` → `result: pass`, append `## Status: RESOLVED 2026-05-25 by Phase 18.1` under the existing `reported: |` block, mark gap `status: failed` → `status: resolved` with `resolved_by: Phase 18.1`, update Summary `passed: 0` → `passed: 1` and `issues: 1` → `issues: 0`). PRESERVE the original `reported: |` block verbatim (audit trail). On any non-"approved" response, capture verbatim in SUMMARY and do NOT modify `18-HUMAN-UAT.md`. This task body is normative — the rich `<what-built>`/`<how-to-verify>`/`<resume-signal>` blocks above are the human-facing presentation, and this `<action>` block is the executor's machine-readable instruction set.</action>
  <verify>
    <automated>grep -c 'RESOLVED 2026-05-25\|result: pass' .planning/phases/18-region-variants/18-HUMAN-UAT.md | grep -qE '^[1-9]' && grep -c 'resolved_by: Phase 18.1' .planning/phases/18-region-variants/18-HUMAN-UAT.md | grep -qE '^[1-9]'</automated>
  </verify>
  <done>
    - User provided explicit "approved" signal (verbatim captured in SUMMARY).
    - `18-HUMAN-UAT.md` shows UI-18-RUNTIME-01 RESOLVED with timestamp + evidence reference + reference to Phase 18.1.
    - Original `reported: |` block preserved unchanged (audit trail intact).
    - Summary counts updated (`passed: 1, issues: 0`).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User → Claude (sign-off) | The checkpoint depends on the user verifying that the evidence matches their lived experience |
| Dev server → agent-browser → DOM snapshot | Snapshot accuracy depends on browser settling (networkidle + 500ms hydrate wait) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18.1-03-01 | T (Tampering) | DOM snapshot grep evidence | mitigate | Each marker hit includes a 60-char context snippet so user can re-verify by repeating the agent-browser flow in their own browser session (sign-off requires this spot-check per the human-verify task body). False-positive grep matches (e.g., a marker string appearing in a comment that wasn't actually rendered) are filtered by the snippet's HTML element context being shown alongside. |
| T-18.1-03-02 | R (Repudiation) | UI-18-RUNTIME-01 status flip | mitigate | The append-only update to `18-HUMAN-UAT.md` PRESERVES the original `reported: |` block instead of overwriting it. The audit trail shows what was found, when, and by whom; the resolution adds a new layer rather than rewriting history. Git history captures the diff. |
| T-18.1-03-03 | I (Information disclosure) | Agent-browser cookie persistence | accept | The agent-browser session uses a throwaway test user (`runtime_verify_<timestamp>`) with the hardcoded fixture password `Test1234!`. No real user data is captured. Snapshots may contain the test user's email/username but those are non-secret per the e2e fixture pattern. |
| T-18.1-03-04 | E (Elevation of privilege) | Premature checkpoint approval | mitigate | The checkpoint resume-signal explicitly enumerates the user's spot-check steps. The executor MUST NOT flip `18-HUMAN-UAT.md` until "approved" is received. The "approved" string is gate-checked literally; any other response routes to the rejection branch. |
</threat_model>

<verification>
- `test -f .planning/phases/18.1-ssr-theme-injection-gap-closure-for-ui-18-runtime-01/18.1-03-EVIDENCE.md` is true
- EVIDENCE.md has ≥ 20 marker rows with `YES` in the Found column
- EVIDENCE.md utility-route table shows `NO` for cross-region structural divergence across all 3 routes
- After user "approved" signal: `grep -c 'RESOLVED 2026-05-25\|result: pass' .planning/phases/18-region-variants/18-HUMAN-UAT.md` ≥ 1
- After user "approved" signal: `grep -c 'status: resolved\|resolved_by: Phase 18.1' .planning/phases/18-region-variants/18-HUMAN-UAT.md` ≥ 1
</verification>

<success_criteria>
1. Runtime sweep captured DOM evidence for all 29 (route, mode, region) configurations.
2. Pandemonium structural markers found in DOM for each Pandemonium-scoped page (W-PRE-10 mitigation: NOT a source-code grep — actual rendered DOM).
3. Demacia structural markers found in DOM for each Demacia-scoped page (proves divergence is bilateral, not just "Pandemonium hides Demacia content").
4. 3 utility routes show no cross-region structural divergence (REQ-7 holds).
5. User signs off on the evidence + spot-check.
6. `18-HUMAN-UAT.md` reflects UI-18-RUNTIME-01 = RESOLVED with timestamp + evidence reference.
7. Phase 18.1 closed; Phase 19 (Bug-Report Widget) unblocked.
</success_criteria>

<output>
After completion, create `.planning/phases/18.1-ssr-theme-injection-gap-closure-for-ui-18-runtime-01/18.1-03-SUMMARY.md` with:
- Reference to `18.1-03-EVIDENCE.md` (the bulk of the artefact is there, not duplicated in SUMMARY)
- Summary counts: total markers checked, total YES, total NO (NO should be 0 modulo documented stubs)
- Verbatim user approval signal ("approved" or the user's actual response)
- Diff of `18-HUMAN-UAT.md` showing the status flip
- Confirmation that Phase 18.1 is complete and Phase 19 (Bug-Report Widget) is unblocked per ROADMAP

If any marker showed NO (and was not a documented intentional stub), the SUMMARY MUST include:
- The failing marker(s)
- A 1-paragraph hypothesis of the root cause
- A recommendation: in-phase patch (18.1-04) vs new gap-closure phase (18.2)
- The 18-HUMAN-UAT.md change is NOT made in this case
</output>

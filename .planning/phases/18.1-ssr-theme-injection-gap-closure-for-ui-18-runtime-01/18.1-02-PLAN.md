---
phase: 18.1-ssr-theme-injection
plan: 02
type: execute
wave: 2
depends_on: [18.1-01]
gap_closure: true
files_modified:
  - e2e/tests/fixtures.ts
  - e2e/tests/region-diff.spec.ts
  - e2e/tests/visual-regression.spec.ts-snapshots/authed-draft/
  - e2e/tests/visual-regression.spec.ts-snapshots/authed-solo/
  - e2e/tests/visual-regression.spec.ts-snapshots/authed-team-dashboard/
  - e2e/tests/visual-regression.spec.ts-snapshots/authed-tree-drafter/
  - e2e/tests/visual-regression.spec.ts-snapshots/authed-champion-pool/
  - e2e/tests/visual-regression.spec.ts-snapshots/authed-match-detail/
  - e2e/tests/visual-regression.spec.ts-snapshots/authed-post-game/
autonomous: true
requirements: [UI-18-RUNTIME-01]
tags: [region, e2e, playwright, visual-regression, baselines, gap-closure, phase-18.1]

must_haves:
  truths:
    - "e2e/tests/fixtures.ts `setRegion` helper sets the `lol_companion_theme` cookie via `page.context().addCookies()` AND triggers a reload so the next SSR request honours the new region (D-Discretion 'a' in CONTEXT.md spec_lock)"
    - "All 24 scoped visual-regression baselines under authed-{draft,solo,team-dashboard,tree-drafter,champion-pool,match-detail,post-game}/ are re-captured under the post-18.1-01 runtime — they now show STRUCTURAL Pandemonium branches (RiotTape, Glitch, zine cards, 4-stat cards on /solo, 7-section /team/dashboard, etc.), not just Pandemonium tokens over Demacia layout"
    - "15 flat utility baselines are byte-for-byte unchanged (REQ-7 holds — utility routes look identical pre/post 18.1)"
    - "`REGION_DIFF_THRESHOLD` in `region-diff.spec.ts` is bumped from 0.005 to a measured value in the range [0.03, 0.10] based on the actual diffs observed after re-baselining"
    - "All 13 region-diff.spec.ts tests pass with the new threshold AND the new baselines"
    - "visual-regression.spec.ts full suite passes against the new baselines"
  artifacts:
    - path: "e2e/tests/fixtures.ts"
      provides: "setRegion helper that uses cookie injection + reload (not just optimistic click)"
      contains: "addCookies"
    - path: "e2e/tests/region-diff.spec.ts"
      provides: "Updated REGION_DIFF_THRESHOLD"
      contains: "REGION_DIFF_THRESHOLD"
    - path: "e2e/tests/visual-regression.spec.ts-snapshots/authed-draft/"
      provides: "6 re-captured baselines (3 modes × 2 regions)"
      min_lines: 0
    - path: "e2e/tests/visual-regression.spec.ts-snapshots/authed-solo/"
      provides: "6 re-captured baselines"
      min_lines: 0
    - path: "e2e/tests/visual-regression.spec.ts-snapshots/authed-team-dashboard/"
      provides: "4 re-captured baselines (2 modes × 2 regions)"
      min_lines: 0
    - path: "e2e/tests/visual-regression.spec.ts-snapshots/authed-tree-drafter/"
      provides: "2 re-captured baselines"
      min_lines: 0
    - path: "e2e/tests/visual-regression.spec.ts-snapshots/authed-champion-pool/"
      provides: "2 re-captured baselines"
      min_lines: 0
    - path: "e2e/tests/visual-regression.spec.ts-snapshots/authed-match-detail/"
      provides: "2 re-captured baselines"
      min_lines: 0
    - path: "e2e/tests/visual-regression.spec.ts-snapshots/authed-post-game/"
      provides: "2 re-captured baselines"
      min_lines: 0
  key_links:
    - from: "e2e/tests/fixtures.ts::setRegion"
      to: "page.context().addCookies(...)"
      via: "Playwright cookie injection API"
      pattern: "addCookies"
    - from: "e2e/tests/fixtures.ts::setRegion"
      to: "page.reload()"
      via: "Force SSR re-render with the new cookie"
      pattern: "reload"
    - from: "region-diff.spec.ts"
      to: "Phase 18.1 runtime structural divergence"
      via: "pixelmatch ratio per route × mode"
      pattern: "REGION_DIFF_THRESHOLD"
---

<objective>
With 18.1-01 landed, structural region branching now activates at runtime — but the e2e test infrastructure was built against the broken-runtime baselines (24 scoped snapshots that showed Demacia structure with Pandemonium tokens overlaid) and a `setRegion` helper that clicks the toggle without reloading. Both must be updated:

1. **`setRegion` helper:** Switch from "click toggle, wait for `data-theme` to flip" to "inject the `lol_companion_theme` cookie via `page.context().addCookies()` AND reload" so the NEXT SSR-rendered page honours the new region. Per CONTEXT.md Claude's Discretion item (a), update the helper in place (backwards-compatible for every caller). Tests that already call `setRegion(page, "pandemonium")` followed by `page.goto(...)` will then naturally pick up the new region; tests that call `setRegion` and then inspect the CURRENT page must reload (which the new helper does automatically).

2. **24 scoped baselines re-capture:** Delete the 24 PNG files under `authed-{draft,solo,team-dashboard,tree-drafter,champion-pool,match-detail,post-game}/*-chromium-linux.png`, run `npx playwright test visual-regression.spec.ts --update-snapshots`, and commit the new baselines. These captures are the FIRST time these pages render their actual Pandemonium structural branches in a baseline.

3. **`REGION_DIFF_THRESHOLD` calibration:** With structural divergence now active, the per-route pixel diff will jump from the prior 1.5–2.5% range to roughly 5–15% (educated estimate per CONTEXT.md D-08). Empirically measure by running `region-diff.spec.ts`, reading the actual ratios from test output, and setting the threshold to ~80% of the LOWEST observed ratio (leaving headroom for token noise without dropping below "structural divergence detected" floor). Expected resting value: between 0.03 and 0.10.

4. **15 utility baselines:** MUST NOT change. Verify by re-running `visual-regression.spec.ts` for the public-* and authed-* flat (non-subdirectory) baselines — they should match byte-for-byte. If any utility baseline diverges, that is a REQ-7 regression and the plan FAILS verification.

Output: e2e suite green against post-18.1 runtime; 24 scoped baselines reflect actual structural Pandemonium; threshold calibrated to measured reality; no regression on utility routes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.claude/rules/wasm-patterns.md
@.planning/phases/18.1-ssr-theme-injection-gap-closure-for-ui-18-runtime-01/18.1-CONTEXT.md
@.planning/phases/18.1-ssr-theme-injection-gap-closure-for-ui-18-runtime-01/18.1-01-SUMMARY.md
@e2e/tests/fixtures.ts
@e2e/tests/region-diff.spec.ts
@e2e/tests/visual-regression.spec.ts

<interfaces>
<!-- Existing setRegion in e2e/tests/fixtures.ts:110-127 (replace body): -->
```typescript
export async function setRegion(
  page: Page,
  region: "demacia" | "pandemonium"
): Promise<void> {
  // OLD body — DELETE:
  //   click toggle button, wait 700ms for WASM Effect to flip data-theme

  // NEW body:
  // 1. Inject the cookie at the browser-context level so it's sent on the next request:
  //    await page.context().addCookies([{
  //      name: "lol_companion_theme",
  //      value: region,
  //      url: page.url() (or "http://127.0.0.1:3020" if page hasn't navigated yet)
  //      path: "/",
  //      sameSite: "Lax",
  //    }]);
  // 2. Reload the page so SSR re-renders with the cookie:
  //    await page.reload({ waitUntil: "networkidle" });
  //    await page.waitForTimeout(500); // WASM hydrate
  // 3. Assert: const themeAttr = await page.getAttribute("html", "data-theme");
  //    if (themeAttr !== region) throw new Error(`setRegion failed: expected ${region}, got ${themeAttr}`);
}
```

<!-- Existing visual-regression.spec.ts snapshot directories (24 scoped PNGs): -->
authed-draft/
  demacia-carousel-chromium-linux.png
  demacia-war-table-chromium-linux.png
  demacia-ledger-chromium-linux.png
  pandemonium-carousel-chromium-linux.png
  pandemonium-war-table-chromium-linux.png
  pandemonium-ledger-chromium-linux.png
authed-solo/
  demacia-constellation-chromium-linux.png
  demacia-forge-chromium-linux.png
  demacia-journal-chromium-linux.png
  pandemonium-constellation-chromium-linux.png
  pandemonium-forge-chromium-linux.png
  pandemonium-journal-chromium-linux.png
authed-team-dashboard/
  demacia-dashboard-chromium-linux.png
  demacia-brief-chromium-linux.png
  pandemonium-dashboard-chromium-linux.png
  pandemonium-brief-chromium-linux.png
authed-tree-drafter/
  demacia-chromium-linux.png
  pandemonium-chromium-linux.png
authed-champion-pool/
  demacia-chromium-linux.png
  pandemonium-chromium-linux.png
authed-match-detail/
  demacia-chromium-linux.png
  pandemonium-chromium-linux.png
authed-post-game/
  demacia-chromium-linux.png
  pandemonium-chromium-linux.png

<!-- 15 flat utility baselines that MUST NOT change: -->
public-auth-login-chromium-linux.png
public-auth-register-invited-chromium-linux.png
public-closed-beta-chromium-linux.png
public-legal-datenschutz-chromium-linux.png
public-legal-impressum-chromium-linux.png
authed-action-items-chromium-linux.png
authed-admin-invites-chromium-linux.png
authed-analytics-chromium-linux.png
authed-game-plan-chromium-linux.png
authed-opponents-chromium-linux.png
authed-personal-learnings-chromium-linux.png
authed-profile-chromium-linux.png
authed-stats-chromium-linux.png
authed-team-builder-chromium-linux.png
authed-team-roster-chromium-linux.png

<!-- Playwright addCookies signature: -->
page.context().addCookies([{
  name: string,
  value: string,
  url?: string,        // OR domain + path
  domain?: string,
  path?: string,
  expires?: number,
  httpOnly?: boolean,
  secure?: boolean,
  sameSite?: "Strict" | "Lax" | "None"
}])
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update setRegion fixture to cookie-injection + reload model</name>
  <files>e2e/tests/fixtures.ts</files>
  <action>
    Replace the body of `export async function setRegion(page, region)` (currently lines 110–127) with the cookie-injection model:

    1. Use `page.context().addCookies([...])` with:
       - `name: "lol_companion_theme"`
       - `value: region` (already typed `"demacia" | "pandemonium"` — D-05 allowlist trivially satisfied)
       - `url: "http://127.0.0.1:3020"` (matches the dev server addr from Cargo.toml `site-addr`)
       - `path: "/"`
       - `sameSite: "Lax"`
    2. Call `await page.reload({ waitUntil: "networkidle" })` so the next SSR response carries `data-theme` matching the new cookie.
    3. `await page.waitForTimeout(500)` — same WASM hydrate settle pause used elsewhere in the fixture (wasm-patterns rule 56).
    4. Read `data-theme` from `<html>` and throw if it does not match `region`. Keep the existing assertion shape.
    5. Update the JSDoc comment to reflect the new behaviour: "Sets the `lol_companion_theme` cookie at the browser-context level and reloads so SSR honours the new region. Previously clicked the toggle and waited for WASM Effect to flip `data-theme` post-hydration; that approach only flipped tokens, not structural region branches. Post-18.1, structural branches activate when SSR sees the cookie. This helper is backwards-compatible — every existing caller continues to work because the reload happens transparently."

    Edge case: when `setRegion` is called BEFORE any `page.goto()` (e.g., as the very first action), `page.url()` returns `"about:blank"`. The `url` field in `addCookies` requires a real origin to derive the domain. Workaround: pass `url: "http://127.0.0.1:3020"` unconditionally (which is the dev server's base URL); Playwright accepts this and the cookie is scoped to that origin. After `addCookies`, callers will typically `goto()` shortly after; if they don't, `page.reload()` against `about:blank` is a no-op and the next real navigation picks up the cookie. The new helper still calls `page.reload()` unconditionally because most callers DO have a current page; for the rare pre-goto case, the assertion at step 4 reads `data-theme` from `<html>` which is empty on `about:blank` and the helper throws — which is correct behaviour (caller misuse).

    Do NOT change the function signature; do NOT introduce a `setRegionAndReload` alternative (per CONTEXT.md spec_lock Discretion item: option (a) is chosen).
  </action>
  <verify>
    <automated>grep -c 'addCookies' e2e/tests/fixtures.ts | grep -q '^[1-9]'</automated>
  </verify>
  <done>
    - `setRegion` uses `page.context().addCookies(...)` + `page.reload(...)` not click-toggle.
    - JSDoc explains the rationale and references Phase 18.1.
    - Cookie attributes (`name`, `path`, `sameSite`) match D-01 exactly.
    - The function signature is unchanged so existing callers in `region-diff.spec.ts` and `visual-regression.spec.ts` need no edits.
  </done>
</task>

<task type="auto">
  <name>Task 2: Re-capture all 24 scoped visual-regression baselines</name>
  <files>
    e2e/tests/visual-regression.spec.ts-snapshots/authed-draft/,
    e2e/tests/visual-regression.spec.ts-snapshots/authed-solo/,
    e2e/tests/visual-regression.spec.ts-snapshots/authed-team-dashboard/,
    e2e/tests/visual-regression.spec.ts-snapshots/authed-tree-drafter/,
    e2e/tests/visual-regression.spec.ts-snapshots/authed-champion-pool/,
    e2e/tests/visual-regression.spec.ts-snapshots/authed-match-detail/,
    e2e/tests/visual-regression.spec.ts-snapshots/authed-post-game/
  </files>
  <action>
    1. Start dev server: `cargo leptos watch` (background), wait for ready: `./scripts/wait_for_server.sh 120`.

    2. Delete ONLY the 24 scoped baselines (NOT the 15 flat utility baselines):
       ```
       rm -f e2e/tests/visual-regression.spec.ts-snapshots/authed-draft/*.png
       rm -f e2e/tests/visual-regression.spec.ts-snapshots/authed-solo/*.png
       rm -f e2e/tests/visual-regression.spec.ts-snapshots/authed-team-dashboard/*.png
       rm -f e2e/tests/visual-regression.spec.ts-snapshots/authed-tree-drafter/*.png
       rm -f e2e/tests/visual-regression.spec.ts-snapshots/authed-champion-pool/*.png
       rm -f e2e/tests/visual-regression.spec.ts-snapshots/authed-match-detail/*.png
       rm -f e2e/tests/visual-regression.spec.ts-snapshots/authed-post-game/*.png
       ```

    3. Re-capture by running visual-regression in update mode for ONLY the scoped tests:
       ```
       cd e2e && npx playwright test visual-regression.spec.ts --update-snapshots
       ```
       Playwright regenerates missing baselines on the first run. The new baselines reflect post-18.1 SSR behaviour: `setRegion(page, "pandemonium")` injects the cookie, reloads, and the page renders its Pandemonium structural branches.

    4. Verify NEW baselines exist and look structurally distinct:
       ```
       ls e2e/tests/visual-regression.spec.ts-snapshots/authed-draft/ | wc -l    # expect 6
       ls e2e/tests/visual-regression.spec.ts-snapshots/authed-solo/ | wc -l     # expect 6
       ls e2e/tests/visual-regression.spec.ts-snapshots/authed-team-dashboard/ | wc -l  # expect 4
       ls e2e/tests/visual-regression.spec.ts-snapshots/authed-tree-drafter/ | wc -l   # expect 2
       ls e2e/tests/visual-regression.spec.ts-snapshots/authed-champion-pool/ | wc -l  # expect 2
       ls e2e/tests/visual-regression.spec.ts-snapshots/authed-match-detail/ | wc -l   # expect 2
       ls e2e/tests/visual-regression.spec.ts-snapshots/authed-post-game/ | wc -l      # expect 2
       ```

    5. Spot-check one baseline pair (e.g., `authed-draft/demacia-ledger` vs `authed-draft/pandemonium-ledger`) by opening both PNGs in image viewer or by using ImageMagick `compare`:
       ```
       compare -metric AE \
         e2e/tests/visual-regression.spec.ts-snapshots/authed-draft/demacia-ledger-chromium-linux.png \
         e2e/tests/visual-regression.spec.ts-snapshots/authed-draft/pandemonium-ledger-chromium-linux.png \
         /tmp/diff.png ; echo
       ```
       Expect the differing-pixel count to indicate a substantial visual delta (>> 1% of total pixels). If close to zero, the cookie injection isn't reaching the SSR layer — STOP and debug 18.1-01.

    6. Re-run the FULL visual-regression suite WITHOUT `--update-snapshots` to confirm all 39 baselines match (24 newly captured + 15 unchanged utility):
       ```
       cd e2e && npx playwright test visual-regression.spec.ts
       ```
       Expect: all tests pass. If any of the 15 utility baselines FAIL, that is a REQ-7 regression (utility routes should look identical pre/post 18.1) — STOP and investigate before proceeding to Task 3.

    7. Stop dev server.

    Edge case: if `visual-regression.spec.ts` references the old utility-route snapshot names that include a `data-theme="demacia"` artifact in the rendered HTML (e.g., from a hardcoded query param), the utility baselines might pixel-shift by 1px due to the new SSR theme injection. Per D-06, an unauthenticated visit with no cookie still SSR-renders "demacia", so the utility baselines (which use the default fixture state — fresh user, no cookie, OR public routes) should be unchanged. If they DO shift, document the pixel-shift cause in SUMMARY before flagging a regression.
  </action>
  <verify>
    <automated>cd e2e && npx playwright test visual-regression.spec.ts --reporter=line 2>&1 | tail -5 | grep -q 'passed\|0 failed'</automated>
  </verify>
  <done>
    - 24 scoped baselines re-captured at exactly the expected counts (6 + 6 + 4 + 2 + 2 + 2 + 2 = 24).
    - 15 utility baselines are byte-for-byte unchanged (re-run of visual-regression.spec.ts passes them without `--update-snapshots`).
    - Spot-check of one ledger pair shows substantial pixel divergence (proves structural branching activated).
    - Full visual-regression.spec.ts run is green.
  </done>
</task>

<task type="auto">
  <name>Task 3: Calibrate REGION_DIFF_THRESHOLD against measured post-18.1 ratios</name>
  <files>e2e/tests/region-diff.spec.ts</files>
  <action>
    1. Start dev server and run `region-diff.spec.ts` with the CURRENT threshold (0.005) plus verbose output:
       ```
       cd e2e && npx playwright test region-diff.spec.ts --reporter=list 2>&1 | tee /tmp/region-diff-output.txt
       ```
       This run is expected to PASS (the actual diffs are now well above 0.005), but the verbose output shows the actual `ratio` for each of the 13 tests.

    2. Capture the per-test ratios by adding a temporary `console.log(ratio)` immediately before each `expect(ratio).toBeGreaterThan(REGION_DIFF_THRESHOLD)` in `region-diff.spec.ts`, OR by changing the assertion to `expect.soft(ratio).toBeGreaterThan(REGION_DIFF_THRESHOLD)` and capturing the value in the test name with `test.info().annotations.push(...)`. The simplest approach: temporarily change each `expect(...).toBeGreaterThan(REGION_DIFF_THRESHOLD)` to a `console.log(\`route=X mode=Y ratio=\${ratio}\`)` followed by the original assertion. Run, scrape the 13 ratios from stdout, then revert the console.logs.

    3. Identify the MINIMUM observed ratio across all 13 tests (likely from a small-mismatch route like `/champion-pool` or `/tree-drafter` where the region delta is smaller). Call this `MIN_OBSERVED`.

    4. Set `REGION_DIFF_THRESHOLD` to `MIN_OBSERVED * 0.7` (30% headroom below the minimum measured value), rounded to 2 significant figures. Expected resting value in the range [0.03, 0.10] per CONTEXT.md D-08.

    5. Update the comment block above `REGION_DIFF_THRESHOLD` (currently lines 75–78) to:
       ```
       // Phase 18.1 calibration: with SSR theme injection landed, structural region
       // branches now activate at runtime. Measured per-route diffs are X% to Y%
       // (lowest: /<lowest-route>/<mode> at <MIN_OBSERVED>%). Threshold set at 70% of
       // the minimum to leave headroom for token-noise drift while still catching
       // "structural divergence detected" vs "token-only diff (pre-18.1)".
       // Pre-18.1 the diffs were 1.5–2.5% (token swap only); 0.005 caught that.
       // The original SPEC value of 0.40 (40%) assumed wholesale layout swap and was
       // never achievable; 0.40 is documented as a deviation in 18-09-SUMMARY.
       ```

    6. Update the comment block at lines 19–22 (the "NOTE on navigation order" block) to reflect that post-18.1 the helper handles cookie injection itself, so the "must set region AFTER goto" caveat is now ALSO solved by the helper doing its own reload internally. Tests that call `setRegion(page, "pandemonium")` before `goto(...)` work because the cookie is set in the browser context BEFORE the goto; tests that call `setRegion` after `goto` work because the helper reloads. Both patterns are valid post-18.1.

    7. Re-run the full `region-diff.spec.ts` suite with the new threshold:
       ```
       cd e2e && npx playwright test region-diff.spec.ts --reporter=line
       ```
       Expect: all 13 tests pass with the new threshold AND the new baselines from Task 2.

    8. Stop dev server.

    Sanity check: if `MIN_OBSERVED` is below 0.01, that suggests the cookie injection did NOT reach the SSR layer for that route — investigate by running `setRegion(page, "pandemonium")` then `await page.evaluate(() => document.cookie)` inside that specific test to confirm the cookie is set; if not, the `addCookies` URL parameter may not match the test's actual navigation host. Fix by widening `addCookies` to also include `domain: "127.0.0.1"` if needed.
  </action>
  <verify>
    <automated>cd e2e && npx playwright test region-diff.spec.ts --reporter=line 2>&1 | tail -3 | grep -q 'passed'</automated>
  </verify>
  <done>
    - `REGION_DIFF_THRESHOLD` is updated to a measured value in [0.03, 0.10] (calibration documented in the comment block).
    - All 13 region-diff tests pass against the new threshold + new baselines.
    - The MIN_OBSERVED ratio is documented in the comment so future maintainers can re-derive the calibration if needed.
    - No temporary `console.log` debug statements remain in the file.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Playwright test runner → Playwright `BrowserContext` | Test code sets cookies via `addCookies` API; cookies cross into the simulated browser at the context layer |
| Playwright `BrowserContext` → dev server (127.0.0.1:3020) | Cookie is sent as a `Cookie` header on every request to the server origin |
| Dev server theme_layer → SSR HTML | Cookie value (now validated by 18.1-01's `parse_theme_cookie`) flows into rendered `data-theme` attribute and `InitialTheme` context |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18.1-02-01 | T (Tampering) | Test cookie value | accept | The `region` parameter is statically typed `"demacia" | "pandemonium"` in TypeScript — the compiler rejects any other literal value. Even if a test author bypasses the type via `as any`, the dev-server's 18.1-01 `parse_theme_cookie` validation catches invalid values and falls through to default. No path from test cookie to DB write; no impact on other users. |
| T-18.1-02-02 | I (Information disclosure) | Baseline PNGs in git | accept | Baselines capture authenticated UI; they include the test fixture's username (`pagesuser_<timestamp>_<id>`) and the timestamped test email. These are non-secret test-fixture credentials documented in the e2e auth flow; the password is `Test1234!` (hardcoded in fixtures.ts). No real PII reaches the baselines. |
| T-18.1-02-03 | T (Tampering) | REGION_DIFF_THRESHOLD calibration | mitigate | Threshold is empirically derived from MIN_OBSERVED × 0.7 (30% headroom). If a future change reduces region divergence below the threshold (regression), the test fails fast. Threshold is NOT pinned to an arbitrary value; calibration rationale is documented in the comment so future maintainers can re-measure. |
| T-18.1-02-04 | D (Denial of service) | Playwright test runtime | accept | Re-baselining 24 snapshots takes ~3–5 minutes on the dev VPS-class hardware. This is a one-shot operation during 18.1; no recurring cost. |
</threat_model>

<verification>
- `grep -c 'addCookies' e2e/tests/fixtures.ts` ≥ 1 (cookie injection present)
- `grep -c 'await page.click.*Pandemonium\|await page.click.*Demacia' e2e/tests/fixtures.ts` == 0 (old click-based body fully replaced)
- `find e2e/tests/visual-regression.spec.ts-snapshots/ -mindepth 2 -name '*.png' | wc -l` == 24 (24 scoped baselines present)
- `find e2e/tests/visual-regression.spec.ts-snapshots/ -maxdepth 1 -name '*.png' | wc -l` == 15 (15 utility baselines unchanged)
- `cd e2e && npx playwright test visual-regression.spec.ts --reporter=line` exits 0
- `cd e2e && npx playwright test region-diff.spec.ts --reporter=line` exits 0
- `grep 'REGION_DIFF_THRESHOLD' e2e/tests/region-diff.spec.ts | grep -oP '0\.\d+'` returns a value ≥ 0.03 and ≤ 0.10 (calibration in expected band)
</verification>

<success_criteria>
1. e2e fixture `setRegion` uses cookie injection + reload (D-Discretion item a from CONTEXT.md).
2. 24 scoped baselines re-captured under post-18.1 runtime; structural Pandemonium branches visible in each Pandemonium baseline.
3. 15 utility baselines byte-for-byte unchanged (REQ-7 holds).
4. `REGION_DIFF_THRESHOLD` calibrated to measured reality (0.03–0.10 range).
5. Both e2e specs (`visual-regression.spec.ts` and `region-diff.spec.ts`) pass green.
</success_criteria>

<output>
After completion, create `.planning/phases/18.1-ssr-theme-injection-gap-closure-for-ui-18-runtime-01/18.1-02-SUMMARY.md` with:
- Old vs new `setRegion` implementation diff (3–5 lines each)
- The 13 measured `region-diff` ratios (per route × mode) with the MIN_OBSERVED highlighted
- The chosen `REGION_DIFF_THRESHOLD` value and the formula used (`MIN_OBSERVED × 0.7`)
- File counts: 24 scoped re-captured, 15 utility unchanged
- Any baselines that surprised on re-capture (e.g., a route that did NOT diverge as expected — that is a Phase 18 source-code bug surfaced by the post-18.1 runtime and should be flagged but NOT fixed in this phase)
</output>

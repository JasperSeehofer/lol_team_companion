---
phase: 05-post-game-loop-polish
plan: 04
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/pages/champion_pool.rs
  - src/pages/opponents.rs
  - src/pages/post_game.rs
  - src/pages/action_items.rs
autonomous: true
requirements:
  - UX-02
  - UX-03

must_haves:
  truths:
    - "Champion pool page uses toast for all mutation feedback"
    - "Opponents page uses toast for all mutation feedback"
    - "Post-game page uses toast for save/delete feedback (keeping inline action item banner)"
    - "Action items page uses toast for mutation feedback"
    - "All Suspense fallbacks on these 4 pages show skeleton components"
  artifacts:
    - path: "src/pages/champion_pool.rs"
      provides: "Toast-migrated feedback + skeletons"
      contains: "use_context::<ToastContext>"
    - path: "src/pages/opponents.rs"
      provides: "Toast-migrated feedback + skeletons"
      contains: "use_context::<ToastContext>"
    - path: "src/pages/post_game.rs"
      provides: "Toast-migrated save/delete feedback + skeletons"
      contains: "use_context::<ToastContext>"
    - path: "src/pages/action_items.rs"
      provides: "Toast-migrated feedback + skeletons"
      contains: "use_context::<ToastContext>"
  key_links:
    - from: "src/pages/champion_pool.rs"
      to: "src/components/ui.rs"
      via: "use_context::<ToastContext>()"
      pattern: "ToastContext"
    - from: "src/pages/opponents.rs"
      to: "src/components/ui.rs"
      via: "use_context::<ToastContext>()"
      pattern: "ToastContext"
---

<objective>
Migrate champion_pool, opponents, post_game, and action_items pages from local status signals to toast system, and replace all "Loading..." Suspense fallbacks with skeleton components.

Purpose: Covers the second batch of pages for UX-02/UX-03 coverage. Post-game toast migration coexists with the PIPE-02 inline banner from Plan 02.
Output: 4 more pages fully using toast system and skeleton loading.
</objective>

<execution_context>
@/home/jasper/Repositories/lol_team_companion/.claude/get-shit-done/workflows/execute-plan.md
@/home/jasper/Repositories/lol_team_companion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-post-game-loop-polish/05-CONTEXT.md
@.planning/phases/05-post-game-loop-polish/05-RESEARCH.md
@.planning/phases/05-post-game-loop-polish/05-UI-SPEC.md

@.planning/phases/05-post-game-loop-polish/05-01-SUMMARY.md

<interfaces>
From src/components/ui.rs (Plan 01):
```rust
pub struct ToastContext { pub show: Callback<(ToastKind, String)> }
pub enum ToastKind { Success, Error }
pub fn SkeletonLine(width, height) -> impl IntoView
pub fn SkeletonCard(height) -> impl IntoView
pub fn SkeletonGrid(cols, rows, card_height) -> impl IntoView
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate champion_pool.rs and opponents.rs to toast + skeletons</name>
  <files>src/pages/champion_pool.rs, src/pages/opponents.rs</files>
  <read_first>
    - src/pages/champion_pool.rs (full file — find all status_msg occurrences ~14, all "Loading..." fallbacks ~3)
    - src/pages/opponents.rs (full file — find all error_msg/status_msg occurrences ~20, all "Loading..." fallbacks ~2)
    - .planning/phases/05-post-game-loop-polish/05-UI-SPEC.md (copywriting + skeleton shapes)
  </read_first>
  <action>
**champion_pool.rs:**

1. Add import: `use crate::components::ui::{ToastContext, ToastKind, SkeletonLine, SkeletonCard};`

2. Get toast context: `let toast = use_context::<ToastContext>().expect("ToastProvider");`

3. Remove `status_msg` signal declaration and its StatusMessage rendering

4. Replace all ~14 status_msg occurrences:
   - Champion added: `toast.show.run((ToastKind::Success, "Champion added to pool".into()))`
   - Pool entry updated: `toast.show.run((ToastKind::Success, "Pool entry updated".into()))`
   - Champion removed: `toast.show.run((ToastKind::Success, "Champion removed from pool".into()))`
   - Errors: `toast.show.run((ToastKind::Error, format!("{e}")))`

5. Replace "Loading..." Suspense fallbacks:
   - Autocomplete (line ~483): `view! { <SkeletonLine width="w-full" height="h-9" /> }`
   - Notes sections (lines ~910, ~1050): `view! { <SkeletonCard height="h-20" /> }` for each

**opponents.rs:**

6. Add import: `use crate::components::ui::{ToastContext, ToastKind, SkeletonCard};`

7. Get toast context: `let toast = use_context::<ToastContext>().expect("ToastProvider");`

8. Remove `error_msg` and `status_msg` signal declarations and their rendering

9. Replace all ~20 occurrences:
   - Opponent added: `toast.show.run((ToastKind::Success, "Opponent added".into()))`
   - Opponent updated: `toast.show.run((ToastKind::Success, "Opponent updated".into()))`
   - Opponent deleted: `toast.show.run((ToastKind::Success, "Opponent deleted".into()))`
   - Errors: `toast.show.run((ToastKind::Error, format!("{e}")))`

10. Replace "Loading..." Suspense fallbacks:
    - Opponent list (line ~339): `view! { <div class="flex flex-col gap-2"><SkeletonCard height="h-10" /><SkeletonCard height="h-10" /><SkeletonCard height="h-10" /></div> }`
    - Detail panel (line ~383): `view! { <SkeletonCard height="h-64" /> }`

Keep ErrorBanner for load-failure Err branches in both files.
  </action>
  <verify>
    <automated>cargo check --features ssr && cargo check --features hydrate --target wasm32-unknown-unknown</automated>
  </verify>
  <acceptance_criteria>
    - src/pages/champion_pool.rs contains `use_context::<ToastContext>`
    - src/pages/champion_pool.rs does NOT contain `set_status_msg` signal declaration
    - src/pages/champion_pool.rs contains `"Champion added to pool"` in a toast call
    - src/pages/champion_pool.rs does NOT contain `"Loading..."` in Suspense fallbacks
    - src/pages/opponents.rs contains `use_context::<ToastContext>`
    - src/pages/opponents.rs does NOT contain `set_error_msg` or `set_status_msg` signal declarations
    - src/pages/opponents.rs contains `"Opponent added"` in a toast call
    - src/pages/opponents.rs does NOT contain `"Loading..."` in Suspense fallbacks
    - `cargo check --features ssr` exits 0
    - `cargo check --features hydrate --target wasm32-unknown-unknown` exits 0
  </acceptance_criteria>
  <done>champion_pool.rs and opponents.rs use toast for all mutation feedback, skeletons for all loading states.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate post_game.rs and action_items.rs to toast + skeletons</name>
  <files>src/pages/post_game.rs, src/pages/action_items.rs</files>
  <read_first>
    - src/pages/post_game.rs (full file — find status_msg occurrences ~8, "Loading..." fallbacks ~1, and the action_item_count banner from Plan 02)
    - src/pages/action_items.rs (full file — find error_msg occurrences ~10, "Loading..." fallbacks ~2)
    - .planning/phases/05-post-game-loop-polish/05-UI-SPEC.md (copywriting + skeleton shapes)
    - .planning/phases/05-post-game-loop-polish/05-02-SUMMARY.md (to understand what Plan 02 changed in post_game.rs)
  </read_first>
  <action>
**post_game.rs:**

NOTE: Plan 02 already changed create_review/update_review return types and added action_item_count banner. This task handles the TOAST migration for remaining status_msg signals.

1. Add import: `use crate::components::ui::{ToastContext, ToastKind, SkeletonCard};`

2. Get toast context: `let toast = use_context::<ToastContext>().expect("ToastProvider");`

3. Remove `status_msg` signal declaration and its StatusMessage/inline rendering

4. Replace status_msg occurrences — the create/update success branches should now do:
   - Review created: `toast.show.run((ToastKind::Success, "Review saved".into()))` (IN ADDITION to setting action_item_count — both fire)
   - Review updated: `toast.show.run((ToastKind::Success, "Review updated".into()))`
   - Review deleted: `toast.show.run((ToastKind::Success, "Review deleted".into()))`
   - Errors: `toast.show.run((ToastKind::Error, format!("{e}")))`

5. KEEP the `action_item_count` signal and its inline banner from Plan 02 — this is separate from toast

6. Replace "Loading..." Suspense fallback:
   - Review list (line ~471): `view! { <div class="flex flex-col gap-2"><SkeletonCard height="h-12" /><SkeletonCard height="h-12" /><SkeletonCard height="h-12" /></div> }`

**action_items.rs:**

7. Add import: `use crate::components::ui::{ToastContext, ToastKind, SkeletonLine, SkeletonCard};`

8. Get toast context: `let toast = use_context::<ToastContext>().expect("ToastProvider");`

9. Remove `error_msg` signal declaration and its rendering

10. Replace occurrences — add success toasts where they're missing:
    - Action item added: `toast.show.run((ToastKind::Success, "Action item added".into()))`
    - Action item updated: `toast.show.run((ToastKind::Success, "Action item updated".into()))`
    - Marked complete: `toast.show.run((ToastKind::Success, "Marked complete".into()))`
    - Errors: `toast.show.run((ToastKind::Error, format!("{e}")))`

11. Replace "Loading..." Suspense fallbacks:
    - Stat bar (line ~258): `view! { <SkeletonLine width="w-48" height="h-5" /> }`
    - Item list (line ~384): `view! { <div class="flex flex-col gap-2"><SkeletonCard height="h-12" /><SkeletonCard height="h-12" /><SkeletonCard height="h-12" /></div> }`

Keep ErrorBanner for load-failure Err branches in both files.
  </action>
  <verify>
    <automated>cargo check --features ssr && cargo check --features hydrate --target wasm32-unknown-unknown</automated>
  </verify>
  <acceptance_criteria>
    - src/pages/post_game.rs contains `use_context::<ToastContext>`
    - src/pages/post_game.rs does NOT contain `set_status_msg` signal declaration
    - src/pages/post_game.rs contains `"Review saved"` in a toast call
    - src/pages/post_game.rs contains `"Review deleted"` in a toast call
    - src/pages/post_game.rs STILL contains `action_item_count` (Plan 02 banner preserved)
    - src/pages/post_game.rs does NOT contain `"Loading..."` in Suspense fallbacks
    - src/pages/action_items.rs contains `use_context::<ToastContext>`
    - src/pages/action_items.rs contains `"Action item added"` in a toast call
    - src/pages/action_items.rs does NOT contain `"Loading..."` in Suspense fallbacks
    - `cargo check --features ssr` exits 0
    - `cargo check --features hydrate --target wasm32-unknown-unknown` exits 0
  </acceptance_criteria>
  <done>post_game.rs and action_items.rs use toast for all mutation feedback, skeletons for all loading states. Post-game action item inline banner preserved.</done>
</task>

</tasks>

<verification>
- `cargo check --features ssr` passes
- `cargo check --features hydrate --target wasm32-unknown-unknown` passes
- No "Loading..." text in Suspense fallbacks across all 4 files
- No local status_msg/error_msg signals in any of the 4 files
</verification>

<success_criteria>
- All 4 pages use ToastContext for mutation feedback
- All Suspense fallbacks show skeleton components
- Post-game inline action item banner (PIPE-02) is preserved
- ErrorBanner remains for load-failure Err branches
</success_criteria>

<output>
After completion, create `.planning/phases/05-post-game-loop-polish/05-04-SUMMARY.md`
</output>

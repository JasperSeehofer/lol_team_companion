---
phase: 17-ui-consolidation
plan: 04
type: execute
wave: 2
depends_on: [17-01, 17-02]
files_modified:
  - src/pages/stats.rs
  - src/pages/match_detail.rs
  - src/pages/personal_learnings.rs
  - src/pages/analytics.rs
  - .planning/phases/17-ui-consolidation/17-OD-MAP.md
autonomous: false
decisions: [D-01, D-02, D-03, D-07, D-10, D-12, D-13]
requirements: [SC-2-claude-design-implementation, SC-4-ui-review-pass]
must_haves:
  truths:
    - "User visits /stats and sees the History page with imperial eyebrow + display headline + battle log + folio recap panel"
    - "User visits /match-detail and sees the full 10-player scoreboard + timeline + performance breakdown matching match-detail.jsx"
    - "User visits /personal-learnings (utility) and sees Open-Design HTML port with Card.plain layout"
    - "User visits /analytics (utility) and sees Open-Design HTML port"
    - "Each page passes per-page user review per D-10 before commit"
  artifacts:
    - path: "src/pages/stats.rs"
      provides: "Battle log + folio panel restyle"
    - path: "src/pages/match_detail.rs"
      provides: "Match detail with 10-player scoreboard + timeline"
  key_links:
    - from: "src/pages/stats.rs"
      to: "src/pages/match_detail.rs"
      via: "click row navigates to match detail"
      pattern: "match-detail|MatchDetail"
---

<objective>
Restyle 4 History hub pages to match Claude Design hero (stats, match-detail) and Open-Design utility (personal-learnings, analytics). Per D-10 each page is a per-page review gate.

Output: 4 restyled pages + per-page atomic commits.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-ui-consolidation/17-CONTEXT.md
@.planning/phases/17-ui-consolidation/17-UI-SPEC.md
@.planning/phases/17-ui-consolidation/17-PATTERNS.md
@.planning/phases/17-ui-consolidation/17-01-SUMMARY.md
@.planning/phases/17-ui-consolidation/17-02-SUMMARY.md
@/tmp/lol-design-handoff/lol-team-companion-app/project/screens/history.jsx
@/tmp/lol-design-handoff/lol-team-companion-app/project/match-detail.jsx
@.claude/rules/leptos-patterns.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Restyle src/pages/stats.rs (battle log + folio recap)</name>
  <files>src/pages/stats.rs</files>
  <read_first>
    - src/pages/stats.rs (FULL FILE — preserve match listing, queue filter, champion trends if any)
    - /tmp/lol-design-handoff/lol-team-companion-app/project/screens/history.jsx (FULL FILE)
    - .planning/phases/17-ui-consolidation/17-UI-SPEC.md (lines 484-506 — Match History & Stats Layout)
    - .planning/phases/17-ui-consolidation/17-PATTERNS.md (stats.rs noted as self-pattern)
  </read_first>
  <behavior>
    - Existing: match list with queue filter (solo/flex/all) + champion trends section preserved.
    - New visual: imperial eyebrow + display headline ("A week of {N} victories and {N} defeats.") with HeraldicDivider.
    - New visual: 1.4fr battle log + 1fr folio recap panel layout. Click match row → expands recap on right.
  </behavior>
  <action>
    Per UI-SPEC §"Match History & Stats Layout":

    1. PRESERVE all server fns, queue filter, champion trends data flow.

    2. RESTYLE outer wrapper: `canvas-grain bg-base min-h-screen px-16 py-11`.

    3. Page header:
       ```
       <div class="font-imperial uppercase tracking-[0.18em] text-[10px] text-muted">"Battle log"</div>
       <h1 class="font-display italic text-[56px] leading-tight text-primary">"A week of {wins} victories and {losses} defeats."</h1>
       <HeraldicDivider />
       ```
       Compute wins/losses from existing data; if data isn't readily available without extra server work, use the closest existing aggregate.

    4. Two-column grid: `grid grid-cols-[1.4fr_1fr] gap-8`.

    5. Battle log column (left):
       - Row template: `grid grid-cols-[8px_56px_1fr_130px_80px_24px] gap-4 items-center py-4 border-b border-divider/30`.
         - Cell 1 (8px): result bar — `bg-success` for win, `bg-danger` for loss, 4px tall, full row height.
         - Cell 2 (56px): ChampTile.
         - Cell 3 (1fr): champion name + queue + duration meta.
         - Cell 4 (130px): KDA string (`text-sm font-mono text-secondary`).
         - Cell 5 (80px): duration (`text-sm text-muted`).
         - Cell 6 (24px): expand chevron icon (rotates when active).
       - Active row: `bg-accent-soft` (define in input.css if not yet — `accent-soft = rgba(accent, 0.12)`; or use `bg-accent/10`).
       - Click row → opens recap on the right (existing match-detail link, but inline expanded preview if data is cheap; otherwise click navigates to /match-detail).

    6. Folio recap column (right):
       - `bg-elevated border border-divider rounded-xl p-6 sticky top-24 self-start`.
       - Shows expanded selected match summary.
       - "Open match detail" CTA → navigates to `/match-detail?id={match_id}`.

    7. Transition on selection: `transition-all duration-180`.

    8. NO raw hex; G-12 focus rings.
  </action>
  <verify>
    <automated>cargo check --features ssr 2>&1 | tail -3 && cargo check --features hydrate --target wasm32-unknown-unknown 2>&1 | tail -3 && grep -q "canvas-grain" src/pages/stats.rs && grep -q "font-display\|font-imperial" src/pages/stats.rs && grep -q "grid-cols-\[1.4fr" src/pages/stats.rs && ! grep -qE "#[0-9a-fA-F]{6}" src/pages/stats.rs && cd e2e && npx playwright test pages.spec.ts -g "/stats" 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - stats.rs uses canvas-grain + 1.4fr/1fr grid layout
    - battle log row template matches UI-SPEC line 501
    - Folio panel sticky-positioned
    - Both compile targets green; no raw hex
  </acceptance_criteria>
  <done>stats.rs battle log + folio panel; existing logic preserved.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: User reviews /stats</name>
  <how-to-verify>
    1. Visit `/stats`, screenshot.
    2. Compare to history.jsx.
    3. Test: queue filter still works; click match row; champion trends section (if present) still visible.
    4. Run `npx playwright test pages.spec.ts -g "stats"`.

    APPROVAL CRITERIA: Visual match; filter + click flow works.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed to /match-detail.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Restyle src/pages/match_detail.rs (10-player scoreboard + timeline)</name>
  <files>src/pages/match_detail.rs</files>
  <read_first>
    - src/pages/match_detail.rs (FULL FILE — preserve timeline event marker logic, on-demand fetch + DB cache, performance breakdown)
    - /tmp/lol-design-handoff/lol-team-companion-app/project/match-detail.jsx (FULL FILE)
    - .planning/phases/17-ui-consolidation/17-UI-SPEC.md (Route Inventory Match Detail entry)
    - .planning/phases/17-ui-consolidation/17-PATTERNS.md (match_detail.rs noted as self-pattern with timeline marker preservation)
  </read_first>
  <behavior>
    - Existing: 10-player scoreboard with KDA/items/damage/gold/vision per player.
    - Existing: timeline events with stable index keys (Phase 13 pattern, Phase 13 decision in STATE.md).
    - Existing: on-demand lazy fetch + DB cache (Resource pattern).
    - Existing: performance breakdown (damage share %, vision score, CS vs game avg).
    - New visual: match-detail.jsx layout — header with score/result, scoreboard rows, timeline filter bar + event markers, performance section.
  </behavior>
  <action>
    Per UI-SPEC and the match-detail.jsx prototype:

    1. PRESERVE all server fns, timeline event index-as-key pattern (CLAUDE.md / STATE.md Phase 13 decision), Resource lazy fetch + DB cache, "Add Learning" CTA wiring (Phase 14 pattern).

    2. RESTYLE outer: `canvas-grain bg-base min-h-screen px-8 py-6`.

    3. Header:
       - Imperial eyebrow + display headline (e.g., "Match recap" or champion-vs-champion title).
       - Result badge (win/loss) with success/danger color.
       - HeraldicDivider.

    4. Scoreboard:
       - 2 teams × 5 players = 10 rows, grouped into 2 sections (BLUE / RED) with side-tinted headers (lapis blue / oxblood red or `--info`/`--danger`).
       - Row template: `grid grid-cols-[40px_120px_80px_1fr_80px_60px_60px] gap-3 items-center py-2 border-b border-divider/30`.
         - role icon, ChampTile + name, KDA, items (5 small icons in flex gap-1), damage, gold, vision.
       - Highlight the player's own row with `bg-accent-soft`.

    5. Timeline filter bar + event markers:
       - Filter pills (dragon/baron/tower/champion-kill).
       - Markers along a horizontal timeline using existing index-as-key (CLAUDE.md rule 41 was about tree, but Phase 13 timeline markers use the same stable-key principle).
       - Each marker: small icon at the timeline position; click expands details.

    6. Performance breakdown:
       - Card.elevated showing damage share %, vision score, CS vs game avg.

    7. NO raw hex; G-12 focus rings; preserve "Add Learning" CTAs (Phase 14).
  </action>
  <verify>
    <automated>cargo check --features ssr 2>&1 | tail -3 && cargo check --features hydrate --target wasm32-unknown-unknown 2>&1 | tail -3 && grep -q "canvas-grain" src/pages/match_detail.rs && grep -q "Add Learning\|add_learning\|add-learning" src/pages/match_detail.rs && ! grep -qE "#[0-9a-fA-F]{6}" src/pages/match_detail.rs && cd e2e && npx playwright test match-detail.spec.ts 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - canvas-grain wrapper present
    - Add Learning CTA (Phase 14 integration) preserved
    - Timeline markers preserved with stable index keys
    - Both compile targets green
    - match-detail.spec.ts passes (existing test suite)
    - No raw hex
  </acceptance_criteria>
  <done>match_detail.rs scoreboard + timeline restyled; logic preserved.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: User reviews /match-detail</name>
  <how-to-verify>
    1. Visit any match via /stats → click row → /match-detail?id=...
    2. Screenshot, compare to match-detail.jsx.
    3. Test: timeline filter pills toggle; marker click expands; "Add Learning" CTA navigates to /personal-learnings/new with prefill.
    4. Run `npx playwright test match-detail.spec.ts`.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed to utility-tier pages.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Restyle utility-tier /personal-learnings and /analytics via Open-Design</name>
  <files>src/pages/personal_learnings.rs, src/pages/analytics.rs, .planning/phases/17-ui-consolidation/17-OD-MAP.md</files>
  <read_first>
    - src/pages/personal_learnings.rs (FULL FILE — preserve CRUD server fns, filter logic for champion/matchup/tag, browse + form pages)
    - src/pages/analytics.rs (FULL FILE — preserve any computed stats)
    - /home/jasper/Repositories/open-design/design-systems/lol-companion/DESIGN.md (utility-tier component patterns from plan 02)
    - .planning/phases/17-ui-consolidation/17-OD-MAP.md (update rows for these surfaces with OD UUID + path)
    - .planning/phases/17-ui-consolidation/17-UI-SPEC.md (utility tier per D-13)
  </read_first>
  <action>
    Per D-13:

    1. **Generate Open-Design HTML for personal-learnings (browse + form) and analytics**. Follow conventions from 17-OD-MAP.md "How to generate" section. Update OD-MAP rows with UUIDs + paths.

    2. **Port HTML to Leptos**:
       - `personal_learnings.rs` (browse): canvas-grain wrapper. Imperial eyebrow + display headline. Card.plain list of learnings; each card shows champion + matchup + tags + excerpt + date. Filter sidebar (champion/matchup/tag) using utility input pattern.
       - `personal_learnings.rs` (form, also in this file or `NewLearningPage`): Card.plain form with utility inputs (champion autocomplete, matchup autocomplete, tags, free-text textarea). Primary "Save" button. Preserve existing autocomplete `on_select` callback (Phase 14 + Phase 12 pattern).
       - `analytics.rs`: utility-tier dashboard with Card.plain panels showing computed stats. Use `<StatCard />` for individual metrics. Imperial eyebrow + display headline.

    3. PRESERVE all server fns and existing form/CRUD wiring.

    4. NO raw hex; G-12 focus rings.

    5. Update 17-OD-MAP.md status to `generated` for these 2 surfaces.
  </action>
  <verify>
    <automated>cargo check --features ssr 2>&1 | tail -3 && cargo check --features hydrate --target wasm32-unknown-unknown 2>&1 | tail -3 && grep -q "canvas-grain" src/pages/personal_learnings.rs && grep -q "canvas-grain" src/pages/analytics.rs && ! grep -qE "#[0-9a-fA-F]{6}" src/pages/personal_learnings.rs src/pages/analytics.rs && grep -q "personal-learnings\|personal_learnings" .planning/phases/17-ui-consolidation/17-OD-MAP.md && cd e2e && npx playwright test pages.spec.ts -g 'learnings|analytics' 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - Both pages have canvas-grain + utility-tier layout
    - Filter logic in personal_learnings.rs preserved
    - Server fns preserved; CRUD still works
    - 17-OD-MAP.md updated for both surfaces
    - Both compile targets green; no raw hex
  </acceptance_criteria>
  <done>personal_learnings.rs + analytics.rs restyled via Open-Design; logic preserved.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 6: User reviews /personal-learnings + /analytics</name>
  <how-to-verify>
    1. Visit `/personal-learnings`, screenshot.
    2. Test: filter by champion, by tag; click "New" → form pre-fill from match detail still works.
    3. Visit `/analytics`, screenshot.
    4. Run `npx playwright test pages.spec.ts -g 'learnings|analytics'`.

    APPROVAL CRITERIA: Visual matches OD HTML; filtering works; CRUD intact.
  </how-to-verify>
  <resume-signal>Type "approved" — History hub plan complete.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Riot API match-detail data → DB cache → client view | Match detail JSON deserialized + cached |
| client → personal_learning CRUD server fns | User-authored notes (XSS surface) |
| Resource (Leptos) lazy fetch | On-demand match-detail fetch trigger |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-16 | Information Disclosure | personal_learning notes are user-scoped | mitigate | Existing Phase 14 pattern: `WHERE user = type::record('user', $key)` filter; this restyle does not change query layer |
| T-17-17 | Cross-Site Scripting | free-text learning notes rendered | mitigate | Leptos `view!` macro escapes by default; restyle preserves behavior; no `inner_html` introduced |
| T-17-18 | Tampering | match_detail timeline marker keys | mitigate | Phase 13 stable-index-key pattern preserved (STATE.md decision); restyle is class-only |
| T-17-19 | Denial of Service | match-detail on-demand fetch from Riot API | mitigate | Phase 13 DB-cache pattern preserved; first fetch hits Riot, subsequent hits cache |
</threat_model>

<verification>
- 4 pages restyled and individually approved per D-10
- match-detail.spec.ts and pages.spec.ts (relevant tests) pass
- Both compile targets exit 0
- No raw hex; G-12 compliant
- 17-OD-MAP.md updated for personal-learnings + analytics
</verification>

<success_criteria>
1. /stats matches history.jsx battle log + folio panel.
2. /match-detail matches match-detail.jsx scoreboard + timeline + performance.
3. /personal-learnings + /analytics match Open-Design utility tier.
4. All existing tests pass — no behavioral regression.
5. Each page committed atomically with user approval per D-10.
</success_criteria>

<output>
After completion, create `.planning/phases/17-ui-consolidation/17-04-SUMMARY.md`.
</output>

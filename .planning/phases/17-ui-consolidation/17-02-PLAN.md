---
phase: 17-ui-consolidation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - /home/jasper/Repositories/open-design/design-systems/lol-companion/DESIGN.md
  - /home/jasper/Repositories/open-design/design-systems/lol-companion/tokens.css
  - .planning/phases/17-ui-consolidation/17-OD-MAP.md
autonomous: false
decisions: [D-03, D-11, D-12, D-13, D-21, D-22, D-23]
requirements: [SC-3-open-design-seeding]
must_haves:
  truths:
    - "Open-Design has a seeded `lol-companion` design system documenting demacia/pandemonium tokens, font assignments, and core component patterns"
    - "Open-Design utility surface generation is unblocked"
    - "A repo-side OD-MAP.md documents the project paths produced by Open-Design so utility surface plans (B/C/D/F) can reference them"
  artifacts:
    - path: "/home/jasper/Repositories/open-design/design-systems/lol-companion/DESIGN.md"
      provides: "Design system seed: tokens, fonts, component patterns, icon library"
    - path: "/home/jasper/Repositories/open-design/design-systems/lol-companion/tokens.css"
      provides: "CSS custom properties matching input.css demacia/pandemonium tokens"
    - path: ".planning/phases/17-ui-consolidation/17-OD-MAP.md"
      provides: "Per-utility-surface mapping table"
  key_links:
    - from: ".planning/phases/17-ui-consolidation/17-OD-MAP.md"
      to: "/home/jasper/Repositories/open-design/.od/projects/{uuid}/{surface}.html"
      via: "path table reference"
      pattern: "design-systems/lol-companion"
---

<objective>
Seed the Open-Design `lol-companion` design system so utility surface generation in plans 03/04/05/06 has a token + font + component-pattern reference matching the Claude Design hero tier visually.

Per D-21+D-22, Open-Design seeding must precede any utility surface work. Per D-23, no copy of Open-Design artifacts into this repo — only the resulting Leptos components are tracked here. This plan is a CROSS-REPO write to `/home/jasper/Repositories/open-design/`. It also produces a repo-side `17-OD-MAP.md` that downstream plans reference.

Output:
- `/home/jasper/Repositories/open-design/design-systems/lol-companion/DESIGN.md`
- `/home/jasper/Repositories/open-design/design-systems/lol-companion/tokens.css`
- `.planning/phases/17-ui-consolidation/17-OD-MAP.md`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/17-ui-consolidation/17-CONTEXT.md
@.planning/phases/17-ui-consolidation/17-UI-SPEC.md
@.planning/phases/17-ui-consolidation/17-RESEARCH.md
@.planning/phases/17-ui-consolidation/17-PATTERNS.md
@/tmp/lol-design-handoff/lol-team-companion-app/project/themes.css
@/tmp/lol-design-handoff/lol-team-companion-app/project/foundations.jsx
@/tmp/lol-design-handoff/lol-team-companion-app/project/components.jsx
@/home/jasper/Repositories/open-design/AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Read Open-Design AGENTS.md + existing design-systems for schema</name>
  <files>(read-only — no writes)</files>
  <read_first>
    - /home/jasper/Repositories/open-design/AGENTS.md (root agent guide)
    - /home/jasper/Repositories/open-design/design-systems/ (LIST contents — find any existing design-systems/{name}/DESIGN.md to use as schema reference)
    - /home/jasper/Repositories/open-design/.od/projects/4335183a-* (existing project; inspect file structure)
  </read_first>
  <action>
    Read Open-Design AGENTS.md end-to-end and extract:
    - Conventions for `design-systems/{name}/DESIGN.md` content
    - Whether tokens go inline in DESIGN.md or in a separate `tokens.css` file
    - Naming conventions for surfaces and projects
    - How OD projects reference their design-system

    List `/home/jasper/Repositories/open-design/design-systems/`. If any other design system exists, read its DESIGN.md to learn the exact schema. List `/home/jasper/Repositories/open-design/.od/projects/4335183a-*/` to inspect file structure.

    Document findings inline in this task's notes for the SUMMARY. NO files written this task.

    If `design-systems/` is empty, infer the seed format conservatively — markdown headings + token tables + Tailwind class strings.
  </action>
  <verify>
    <automated>test -f /home/jasper/Repositories/open-design/AGENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - Open-Design AGENTS.md was read
    - Existing design-systems contents enumerated
    - Findings noted in plan summary
  </acceptance_criteria>
  <done>Schema for `lol-companion/DESIGN.md` understood; ready to author the seed.</done>
</task>

<task type="auto">
  <name>Task 2: Author lol-companion/DESIGN.md and tokens.css</name>
  <files>/home/jasper/Repositories/open-design/design-systems/lol-companion/DESIGN.md, /home/jasper/Repositories/open-design/design-systems/lol-companion/tokens.css</files>
  <read_first>
    - .planning/phases/17-ui-consolidation/17-UI-SPEC.md (lines 50-181, 350-447, 422-447, 638-672)
    - /tmp/lol-design-handoff/lol-team-companion-app/project/foundations.jsx
    - /tmp/lol-design-handoff/lol-team-companion-app/project/components.jsx (Card, Button, Input, Badge, Icon path set)
    - /tmp/lol-design-handoff/lol-team-companion-app/project/themes.css (raw token values)
  </read_first>
  <action>
    Per D-21:

    1. `mkdir -p /home/jasper/Repositories/open-design/design-systems/lol-companion`

    2. Author **DESIGN.md** per the schema discovered in Task 1. Required sections:

    - **Identity**: name, project context (LoL team management app, Demacia/Pandemonium dual-region theming), source of truth (this seed mirrors `/tmp/lol-design-handoff/...`).
    - **Themes**: two themes — demacia (default) + pandemonium. Token table mirroring UI-SPEC lines 58-94 + extended tokens. For each token: name, demacia value, pandemonium value, semantic intent.
    - **Typography**: 5 font families (Cinzel imperial, Cormorant Garamond display, Inter ui, JetBrains Mono mono, VT323 glitch — Pandemonium only). Type scale: 12 / 14 / 16 / 18 / 22 / 28 / 36 / 44 / 56 px. Headings use `font-display italic` for hero, `font-imperial uppercase tracking-[0.18em]` for eyebrows. Body uses `font-ui` (Inter).
    - **Color Roles**: 60/30/10 split (UI-SPEC lines 88-94). Status colors (success/warning/danger/info) per theme.
    - **Components**: For each component below, document HTML structure, Tailwind class string, semantic-token-only color usage, focus-visible ring, ARIA notes.
        - Card.gilt (Demacia): `bg-elevated border border-outline rounded-xl p-6 relative` + GiltCorner ornaments at 4 corners
        - Card.plain: `bg-elevated border border-divider rounded-xl p-6`
        - Card.elevated: `bg-surface border border-outline/50 rounded-xl p-4`
        - Button.primary: `bg-accent text-accent-contrast font-semibold rounded-lg px-4 py-2 hover:bg-accent-hover focus-visible:ring-2 focus-visible:ring-accent/50 focus-visible:outline-none`
        - Button.ghost: `text-muted hover:text-secondary px-4 py-2 rounded-lg`
        - Button.destructive: `bg-danger/10 text-danger border border-danger/30 hover:bg-danger/20`
        - Input.text: `bg-surface/50 border border-outline/50 rounded-lg px-3 py-3 text-primary text-sm w-full focus-visible:ring-2 focus-visible:ring-accent/50 focus-visible:outline-none`
        - Badge.tier-S/A/B/C: tier-color left-border + `font-imperial`
        - ErrorBanner: `bg-danger/10 border border-danger/30 text-danger rounded-lg px-4 py-3 text-sm`
    - **Icon Library**: mirror `components.jsx:108-145` — list each icon name and its SVG path data inline.
    - **Ornaments (Demacia + Pandemonium)**: brief reference to the 6 ornaments (HeraldicDivider, GiltCorner, FleurDeLis, RiotTape, CompanionSigil, Crown). Full SVG lives in this repo's `src/components/ornaments.rs`. OD HTML may reference them by class hooks (e.g., `<div class='ornament-heraldic-divider'></div>`) which the Leptos port resolves.
    - **Background Imagery**: FLUX.1 generated `public/img/beta-landing-(demacia|pandemonium).jpg`. Aesthetic: Demacia warm/regal, Pandemonium cold/chaotic. Loading lazy, aria-hidden=true (decorative).
    - **Accessibility**: WCAG AA contrast (4.5:1 normal, 3:1 large). All interactive elements have `focus-visible:ring-2 focus-visible:ring-accent/50 focus-visible:outline-none` (G-12). Touch target minimum 44x44 px.

    3. Author **tokens.css** — CSS custom properties on `[data-theme='demacia']` and `[data-theme='pandemonium']` selectors, EXACTLY mirroring `input.css` token values from plan 01. Open-Design HTML output can `@import` this file or paste its contents into prototypes.

    No raw hex outside theme blocks. Include all status/extended tokens.
  </action>
  <verify>
    <automated>test -f /home/jasper/Repositories/open-design/design-systems/lol-companion/DESIGN.md && test -f /home/jasper/Repositories/open-design/design-systems/lol-companion/tokens.css && grep -q "demacia" /home/jasper/Repositories/open-design/design-systems/lol-companion/DESIGN.md && grep -q "pandemonium" /home/jasper/Repositories/open-design/design-systems/lol-companion/DESIGN.md && grep -q "data-theme" /home/jasper/Repositories/open-design/design-systems/lol-companion/tokens.css && grep -q "Cinzel" /home/jasper/Repositories/open-design/design-systems/lol-companion/DESIGN.md</automated>
  </verify>
  <acceptance_criteria>
    - DESIGN.md exists with sections: Identity, Themes, Typography, Color Roles, Components, Icon Library, Ornaments, Background Imagery, Accessibility
    - tokens.css has both `[data-theme="demacia"]` and `[data-theme="pandemonium"]` blocks
    - Token values in tokens.css match input.css exactly (e.g., `--t-accent: #d4af5a` for demacia, `#f73c8c` for pandemonium)
    - All 5 font families documented in DESIGN.md with their semantic role
    - Component patterns include focus-visible:ring (G-12) on every interactive element
  </acceptance_criteria>
  <done>Open-Design lol-companion design system seeded; tokens.css and DESIGN.md authored.</done>
</task>

<task type="auto">
  <name>Task 3: Create 17-OD-MAP.md surface mapping document</name>
  <files>.planning/phases/17-ui-consolidation/17-OD-MAP.md</files>
  <read_first>
    - .planning/phases/17-ui-consolidation/17-UI-SPEC.md (Route Inventory table, lines 184-215 — to know all utility surfaces that need OD prototypes)
    - .planning/phases/17-ui-consolidation/17-CONTEXT.md (D-13 utility-tier surface list)
  </read_first>
  <action>
    Per D-23, create `.planning/phases/17-ui-consolidation/17-OD-MAP.md` documenting which utility surfaces map to which Open-Design project paths. The map starts mostly empty (paths get filled when Open-Design generates the HTML during plans B/C/D/F task execution); this plan establishes the table structure and lists target surfaces.

    Table columns: Surface | Route | Plan that consumes it | Open-Design project UUID | OD HTML path | Status

    Initial rows (status=`pending` for all):
    | Surface | Route | Plan | OD UUID | OD HTML path | Status |
    |---------|-------|------|---------|--------------|--------|
    | Login | `/auth/login` | 06 | TBD | TBD | pending |
    | Register (invited) | `/auth/register?invite=...` | 06 | TBD | TBD | pending |
    | Team roster | `/team/roster` | 05 | TBD | TBD | pending |
    | Team builder | `/team-builder` | 05 | TBD | TBD | pending |
    | Opponents | `/opponents` | 03 | TBD | TBD | pending |
    | Action items | `/action-items` | 03 | TBD | TBD | pending |
    | Personal learnings | `/personal-learnings` | 04 | TBD | TBD | pending |
    | Analytics | `/analytics` | 04 | TBD | TBD | pending |
    | Admin invites | `/admin/invites` | 06 | TBD | TBD | pending |
    | Bug-report widget | (floating) | 06 | TBD | TBD | pending |
    | Closed-beta acceptance form | `/auth/register?invite=...` (sub-form) | 06 | TBD | TBD | pending |

    Add a header note: "When implementing each surface, the executor for the consuming plan generates the Open-Design HTML, fills in the UUID + path columns, and changes status to `generated`. Open-Design HTML lives only in `/home/jasper/Repositories/open-design/.od/projects/`; the Leptos port lives in this repo."

    Also include a section "How to generate" — document the steps to create an OD project against the `lol-companion` design system, given the conventions discovered in Task 1.
  </action>
  <verify>
    <automated>test -f .planning/phases/17-ui-consolidation/17-OD-MAP.md && grep -q "Login" .planning/phases/17-ui-consolidation/17-OD-MAP.md && grep -q "Bug-report widget\|bug-report" .planning/phases/17-ui-consolidation/17-OD-MAP.md && grep -q "Admin invites" .planning/phases/17-ui-consolidation/17-OD-MAP.md && grep -q "lol-companion" .planning/phases/17-ui-consolidation/17-OD-MAP.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists with all 11 utility surfaces listed
    - Each row has columns: Surface, Route, Plan, OD UUID, OD HTML path, Status
    - Initial status is `pending` for all
    - Document explains how to generate OD prototypes against `lol-companion`
  </acceptance_criteria>
  <done>17-OD-MAP.md tracks utility surface generation status; ready for plans 03-06 to fill in.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: User reviews seeded design system before utility-tier plans consume it</name>
  <what-built>
    - `/home/jasper/Repositories/open-design/design-systems/lol-companion/DESIGN.md` — full token table (demacia + pandemonium), typography, color roles, component patterns, icon library, ornaments, accessibility notes
    - `/home/jasper/Repositories/open-design/design-systems/lol-companion/tokens.css` — CSS custom properties matching `input.css` plan-01 token values exactly
    - `.planning/phases/17-ui-consolidation/17-OD-MAP.md` — utility surface tracking table with all 11 surfaces in `pending` status
  </what-built>
  <how-to-verify>
    1. Open `/home/jasper/Repositories/open-design/design-systems/lol-companion/DESIGN.md`. Confirm sections: Identity, Themes, Typography, Color Roles, Components, Icon Library, Ornaments, Background Imagery, Accessibility — each populated, no `TBD` or `placeholder`.
    2. Open `/home/jasper/Repositories/open-design/design-systems/lol-companion/tokens.css`. Spot-check at least 5 tokens against `input.css` from plan 01 (e.g., `--t-accent: #d4af5a` for demacia, `#f73c8c` for pandemonium). Values MUST match exactly — defective tokens here propagate silently into every utility surface generated downstream (plans 03d, 04, 05, 06).
    3. Confirm every interactive component pattern in DESIGN.md includes `focus-visible:ring-2 focus-visible:ring-accent/50 focus-visible:outline-none` (G-12). Spot-check Button.primary, Input.text.
    4. Open `.planning/phases/17-ui-consolidation/17-OD-MAP.md`. Confirm 11 surfaces listed (Login, Register, Team roster, Team builder, Opponents, Action items, Personal learnings, Analytics, Admin invites, Bug-report widget, Closed-beta acceptance form). Each in `pending` status. "How to generate" section documents the OD command/workflow.
    5. Optional: regenerate one small Open-Design prototype against the seed (e.g., a single Card.plain) to sanity-check the design system is consumable. Skip if OD tooling is not handy.

    APPROVAL CRITERIA: All sections populated; tokens.css values match input.css exactly; G-12 focus rings on every interactive component; OD-MAP table established with all 11 surfaces. Defective tokens here are MUCH cheaper to fix now than during plan 03d/04/05/06 utility-tier porting.
  </how-to-verify>
  <resume-signal>Type "approved" — Wave 1 plan 02 complete; Wave 2 utility-tier plans (03d, 04, 05) and Wave 3 plan 06 may consume the seed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| this repo → external open-design repo | Cross-repo write to `/home/jasper/Repositories/open-design/` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-08 | Tampering | cross-repo write may overwrite existing OD design system | mitigate | Task 1 reads existing `design-systems/` first; Task 2 only writes a NEW `lol-companion/` directory; aborts if directory has unexpected pre-existing files |
| T-17-09 | Information Disclosure | OD repo may be world-readable | accept | Tokens are public design decisions; no PII or secrets in DESIGN.md |
| T-17-10 | Repudiation | no commit history in cross-repo write | accept | Open-Design repo has its own git lifecycle; this plan creates files; OD's own workflow tracks them |
</threat_model>

<verification>
- All 3 files exist (DESIGN.md, tokens.css, 17-OD-MAP.md)
- DESIGN.md has all required sections per Task 2 acceptance criteria
- tokens.css token values match input.css exactly
- 17-OD-MAP.md table covers all 11 utility surfaces
</verification>

<success_criteria>
1. Open-Design `lol-companion` design system seeded with tokens, fonts, components, icons, ornaments.
2. tokens.css mirrors input.css demacia/pandemonium tokens exactly.
3. 17-OD-MAP.md establishes surface-to-OD-project tracking table.
4. Utility surface plans (03/04/05/06) can now generate Open-Design HTML against this seed.
</success_criteria>

<output>
After completion, create `.planning/phases/17-ui-consolidation/17-02-SUMMARY.md`.
</output>

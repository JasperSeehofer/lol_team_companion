---
phase: 17-ui-consolidation
plan: 05
type: execute
wave: 2
depends_on: [17-01, 17-02]
files_modified:
  - src/pages/profile.rs
  - src/pages/team/dashboard.rs
  - src/pages/team/roster.rs
  - src/pages/team_builder.rs
  - src/pages/solo_dashboard.rs
  - src/pages/home.rs
  - .planning/phases/17-ui-consolidation/17-OD-MAP.md
autonomous: false
decisions: [D-01, D-02, D-03, D-07, D-10, D-12, D-13, D-14]
requirements: [SC-2-claude-design-implementation, SC-4-ui-review-pass]
must_haves:
  truths:
    - "User visits /profile and sees Captain's Folio (screens/profile.jsx) with Riot account linking, champion pool summary, gilt cards"
    - "User visits /team/dashboard and sees Strategy Room dashboard variant from team-dashboards.jsx"
    - "User visits /team/roster (utility) and sees Open-Design HTML port"
    - "User visits /team-builder (utility) and sees Open-Design HTML port"
    - "User visits /solo-dashboard and sees solo-dashboards.jsx SoloConstellation variant"
    - "User visits / (authenticated) and sees the Strategy hub dashboard, OR redirects to /closed-beta if unauth"
    - "Each page passes per-page user review per D-10 before commit"
  artifacts:
    - path: "src/pages/profile.rs"
      provides: "Captain's Folio profile page"
    - path: "src/pages/team/dashboard.rs"
      provides: "Strategy Room dashboard with hero panels"
    - path: "src/pages/solo_dashboard.rs"
      provides: "SoloConstellation solo player dashboard"
    - path: "src/pages/home.rs"
      provides: "Auth-aware landing: redirect to /closed-beta when unauth, dashboard when auth"
  key_links:
    - from: "src/pages/home.rs"
      to: "/closed-beta"
      via: "unauth redirect"
      pattern: "closed-beta|closed_beta"
---

<objective>
Restyle 6 Profile hub pages: profile (Captain's Folio), team/dashboard (Strategy Room), solo_dashboard (SoloConstellation), home (auth-aware landing redirect), and 2 utility-tier (team/roster, team-builder via Open-Design).

Per D-10 each page is a per-page review gate. Per D-14 home.rs redirects unauth visitors to /closed-beta (visual gate only — Phase 19.1 wires real validation; this phase preserves existing logged-in dashboard).

Output: 6 restyled pages + per-page atomic commits.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-ui-consolidation/17-CONTEXT.md
@.planning/phases/17-ui-consolidation/17-UI-SPEC.md
@.planning/phases/17-ui-consolidation/17-PATTERNS.md
@.planning/phases/17-ui-consolidation/17-01-SUMMARY.md
@.planning/phases/17-ui-consolidation/17-02-SUMMARY.md
@/tmp/lol-design-handoff/lol-team-companion-app/project/screens/profile.jsx
@/tmp/lol-design-handoff/lol-team-companion-app/project/screens/dashboard.jsx
@/tmp/lol-design-handoff/lol-team-companion-app/project/team-dashboards.jsx
@/tmp/lol-design-handoff/lol-team-companion-app/project/solo-dashboards.jsx
@.claude/rules/leptos-patterns.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Restyle src/pages/profile.rs (Captain's Folio)</name>
  <files>src/pages/profile.rs</files>
  <read_first>
    - src/pages/profile.rs (FULL FILE — preserve Riot account linking server fns, champion pool summary)
    - /tmp/lol-design-handoff/lol-team-companion-app/project/screens/profile.jsx (FULL FILE)
    - .planning/phases/17-ui-consolidation/17-UI-SPEC.md (Route Inventory: /profile → screens/profile.jsx Profile hub)
    - .claude/rules/leptos-patterns.md (Resource pattern, Suspense)
  </read_first>
  <action>
    Per UI-SPEC and screens/profile.jsx:

    1. PRESERVE: server fns for Riot account linking, region selector, sync trigger, champion pool summary fetch.

    2. RESTYLE outer: `canvas-grain bg-base min-h-screen px-8 py-6`.

    3. Captain's Folio header: imperial eyebrow ("Captain's Folio"), display headline (user's summoner name in `font-display italic text-[44px]`), HeraldicDivider.

    4. Sections (Card.gilt for each Demacia-tier panel):
       - Riot account: linked status, region, "Sync now" CTA, last-sync timestamp.
       - Champion pool summary: small tile grid using existing data, link to /champion-pool.
       - Theme preference: small note about which theme is active (read-only display; toggle is in nav).
       - Account: change password / logout (via existing Logout action).

    5. Card.gilt pattern: `bg-elevated border border-outline rounded-xl p-6 relative` with optional `<GiltCorner corner="tl|tr|bl|br" />` ornaments at 4 corners.

    6. NO raw hex; G-12 focus rings.
  </action>
  <verify>
    <automated>cargo check --features ssr 2>&1 | tail -3 && cargo check --features hydrate --target wasm32-unknown-unknown 2>&1 | tail -3 && grep -q "canvas-grain" src/pages/profile.rs && grep -q "GiltCorner\|gilt" src/pages/profile.rs && ! grep -qE "#[0-9a-fA-F]{6}" src/pages/profile.rs && cd e2e && npx playwright test pages.spec.ts -g "profile" 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - canvas-grain wrapper, gilt cards, server fns preserved
    - pages.spec.ts profile test passes
    - Both compile targets green; no raw hex
  </acceptance_criteria>
  <done>profile.rs Captain's Folio; logic preserved.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: User reviews /profile</name>
  <how-to-verify>
    1. Visit `/profile`, screenshot.
    2. Compare to screens/profile.jsx.
    3. Test: link Riot account, change region, sync, view champion pool summary.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed to /team/dashboard.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Restyle src/pages/team/dashboard.rs (Strategy Room)</name>
  <files>src/pages/team/dashboard.rs</files>
  <read_first>
    - src/pages/team/dashboard.rs (FULL FILE ~2,235 lines — preserve roster slots, coach slots, join requests, all CRUD logic)
    - /tmp/lol-design-handoff/lol-team-companion-app/project/team-dashboards.jsx (FULL FILE)
    - /tmp/lol-design-handoff/lol-team-companion-app/project/screens/dashboard.jsx (Strategy hub dashboard hero)
    - .planning/phases/17-ui-consolidation/17-UI-SPEC.md (Route Inventory: /team/dashboard → team-dashboards.jsx)
  </read_first>
  <action>
    Per UI-SPEC and team-dashboards.jsx:

    1. PRESERVE: all server fns (roster CRUD, coach assignments, join request handling — including the WR-01 join-request error fix per Phase 12 STATE.md).

    2. RESTYLE outer: `canvas-grain bg-base min-h-screen px-8 py-6`.

    3. Strategy Room hero header:
       - Imperial eyebrow + display headline (e.g., "{Team Name}" or "Strategy Room").
       - HeraldicDivider.
       - Action buttons: "Invite member", "Edit team", etc.

    4. Sections (gilt cards):
       - Roster: 5 starter slots + N substitute slots, each as a slot card (role icon + champion + player name).
       - Coaches: coach slots.
       - Join requests: pending requests list with accept/reject inline buttons (preserve req_error signal pattern from MEMORY.md fix #3).
       - Recent activity feed.

    5. Use `<StatCard />` for team-level metrics (W-L, sync status, etc.).

    6. NO raw hex; G-12 focus rings; preserve existing error display patterns.
  </action>
  <verify>
    <automated>cargo check --features ssr 2>&1 | tail -3 && cargo check --features hydrate --target wasm32-unknown-unknown 2>&1 | tail -3 && grep -q "canvas-grain" src/pages/team/dashboard.rs && grep -q "GiltCorner\|gilt\|font-display\|font-imperial" src/pages/team/dashboard.rs && ! grep -qE "#[0-9a-fA-F]{6}" src/pages/team/dashboard.rs && cd e2e && npx playwright test audit-team.spec.ts 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - canvas-grain wrapper present
    - Server fns preserved (roster, coach, join request handlers)
    - Join request error display preserved (Phase 12 MEMORY.md fix #3)
    - audit-team.spec.ts passes
    - Both compile targets green; no raw hex
  </acceptance_criteria>
  <done>team/dashboard.rs Strategy Room restyle; logic preserved.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: User reviews /team/dashboard</name>
  <how-to-verify>
    1. Visit `/team/dashboard`, screenshot.
    2. Compare to team-dashboards.jsx Strategy Room variant.
    3. Test: roster CRUD, accept/reject join request, error display.
    4. Run `npx playwright test audit-team.spec.ts`.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed to /solo-dashboard.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Restyle src/pages/solo_dashboard.rs (SoloConstellation)</name>
  <files>src/pages/solo_dashboard.rs</files>
  <read_first>
    - src/pages/solo_dashboard.rs (FULL FILE — preserve Phase 12+15+16 work: ranked badge, LP graph, goal cards, sync handler, refetch hoisting from Phase 16)
    - /tmp/lol-design-handoff/lol-team-companion-app/project/solo-dashboards.jsx (FULL FILE)
    - .planning/phases/17-ui-consolidation/17-UI-SPEC.md (Route Inventory: /solo-dashboard → solo-dashboards.jsx)
    - .planning/STATE.md (Phase 16 close-out — WR-01 hoist fix; refetch lp_history + goal_progress on sync)
  </read_first>
  <action>
    Per UI-SPEC and solo-dashboards.jsx:

    1. PRESERVE: server fns; LP graph (LpHistoryGraph component); goal cards with inline edit; sync handler with `dashboard_resource.refetch()` + `lp_history_resource.refetch()` + `goal_progress_resource.refetch()` (Phase 16 close-out fix).

    2. RESTYLE outer: `canvas-grain bg-base min-h-screen px-8 py-6`.

    3. SoloConstellation hero:
       - Imperial eyebrow + display headline (e.g., summoner name + region).
       - Ranked badge (large, gilt-bordered for Demacia).
       - HeraldicDivider.

    4. Sections (gilt cards):
       - Recent matches grid (link to /match-detail per row).
       - LP history graph (preserve existing LpHistoryGraph SVG component).
       - Goal cards (rank target, CS/min, deaths) with inline edit.
       - Champion performance trends.

    5. NO raw hex; G-12 focus rings.

    6. Critical: LP graph SVG strokes use `style="stroke: var(--color-accent)"` per Pitfall 9 — theme switch updates strokes via CSS variable re-evaluation.
  </action>
  <verify>
    <automated>cargo check --features ssr 2>&1 | tail -3 && cargo check --features hydrate --target wasm32-unknown-unknown 2>&1 | tail -3 && grep -q "canvas-grain" src/pages/solo_dashboard.rs && grep -q "lp_history_resource.refetch\|goal_progress_resource.refetch" src/pages/solo_dashboard.rs && ! grep -qE "#[0-9a-fA-F]{6}" src/pages/solo_dashboard.rs && cd e2e && npx playwright test pages.spec.ts -g "solo" 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - canvas-grain wrapper; refetch on sync preserved (Phase 16 fix intact)
    - LP graph SVG uses style="stroke: var(...)"
    - pages.spec.ts solo test passes
    - Both compile targets green; no raw hex
  </acceptance_criteria>
  <done>solo_dashboard.rs SoloConstellation; Phase 16 refetch fix preserved.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 6: User reviews /solo-dashboard</name>
  <how-to-verify>
    1. Visit `/solo-dashboard`, screenshot.
    2. Compare to solo-dashboards.jsx SoloConstellation variant.
    3. Test: sync triggers refetch on LP graph + goal cards (Phase 16 fix); ranked badge displays; goal inline edit.
    4. Switch theme to Pandemonium → LP graph SVG should recolor automatically.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed to home.rs and utility surfaces.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 7: Restyle src/pages/home.rs (auth-aware redirect to /closed-beta)</name>
  <files>src/pages/home.rs</files>
  <read_first>
    - src/pages/home.rs (FULL FILE 354 lines — preserve get_dashboard server fn, Suspense + auth branching at lines 147-180)
    - /tmp/lol-design-handoff/lol-team-companion-app/project/screens/dashboard.jsx (Strategy hub dashboard for logged-in users)
    - /tmp/lol-design-handoff/lol-team-companion-app/project/screens/coming-soon.jsx (style reference for closed-beta landing)
    - .planning/phases/17-ui-consolidation/17-UI-SPEC.md (lines 188-189 / Route Inventory: `/` → HomePage; D-14 closed-beta redirect)
    - .planning/phases/17-ui-consolidation/17-PATTERNS.md (home.rs analog at lines 633-659)
  </read_first>
  <action>
    Per D-14 + UI-SPEC §"Closed-Beta Surfaces":

    1. PRESERVE: `get_dashboard` server fn, Suspense fallback skeleton, error/landing/dashboard branching at lines 147-180.

    2. CHANGE the auth branching:
       - When `data.logged_in == false` → REDIRECT to `/closed-beta` (Effect-based: `if let Some(window) = web_sys::window() { window.location().set_href("/closed-beta"); }`). Use `if let Some` chain per wasm-patterns rule 35.
       - When `data.logged_in == true && team_id is none` → existing pattern: redirect to `/team/roster` (no team) preserved.
       - When `data.logged_in == true && team_id is some` → render the Strategy Room dashboard (heavy restyle of `Dashboard` component at lines :223-323).

    3. RESTYLE the `Dashboard` component (logged-in view) per screens/dashboard.jsx:
       - canvas-grain wrapper.
       - Imperial eyebrow + display headline.
       - Gilt cards for: upcoming matches, recent drafts, recent learnings, action items quick-access.
       - HeraldicDivider section breaks.

    4. DELETE the inline `LandingPage` component (`/` for unauth users no longer renders LandingPage — it redirects to `/closed-beta` which IS the landing now). The /closed-beta page is built in plan 06.

    5. NO raw hex; G-12 focus rings.
  </action>
  <verify>
    <automated>cargo check --features ssr 2>&1 | tail -3 && cargo check --features hydrate --target wasm32-unknown-unknown 2>&1 | tail -3 && grep -q "canvas-grain" src/pages/home.rs && grep -q "closed-beta\|closed_beta" src/pages/home.rs && grep -q "get_dashboard" src/pages/home.rs && grep -q "if let Some(window)" src/pages/home.rs && ! grep -qE "#[0-9a-fA-F]{6}" src/pages/home.rs && cd e2e && npx playwright test smoke.spec.ts 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - get_dashboard server fn preserved
    - Unauth visitors redirected to /closed-beta (Effect with `if let Some(window)`)
    - Authenticated dashboard restyled with canvas-grain + gilt cards
    - smoke.spec.ts passes (homepage public access)
    - Both compile targets green; no raw hex; no .unwrap() in event handlers
  </acceptance_criteria>
  <done>home.rs auth-aware: unauth → /closed-beta; auth → Strategy Room dashboard.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 8: User reviews / (home) — auth-aware redirect</name>
  <how-to-verify>
    1. In incognito browser, visit `/` → should redirect to `/closed-beta`.
    2. Log in, visit `/` → should show Strategy Room dashboard with canvas-grain + gilt cards.
    3. Log in as user with no team, visit `/` → redirect to `/team/roster`.
    4. Run `npx playwright test smoke.spec.ts`.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed to utility-tier pages.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 9: Restyle utility-tier /team/roster and /team-builder via Open-Design</name>
  <files>src/pages/team/roster.rs, src/pages/team_builder.rs, .planning/phases/17-ui-consolidation/17-OD-MAP.md</files>
  <read_first>
    - src/pages/team/roster.rs (FULL FILE — preserve team creation/joining server fns; STATE.md notes the team-owner-into-roster fix from Phase 12)
    - src/pages/team_builder.rs (FULL FILE)
    - /home/jasper/Repositories/open-design/design-systems/lol-companion/DESIGN.md
    - .planning/phases/17-ui-consolidation/17-OD-MAP.md
    - .planning/phases/17-ui-consolidation/17-UI-SPEC.md (Route Inventory: utility tier per D-13)
  </read_first>
  <action>
    Per D-13:

    1. **Generate Open-Design HTML for team/roster and team-builder**. Update 17-OD-MAP.md.

    2. **Port HTML to Leptos**:
       - `team/roster.rs`: canvas-grain + utility-tier layout. Preserve team creation/joining server fns (including the team-owner-into-roster fix from MEMORY.md fix #4).
       - `team_builder.rs`: canvas-grain + utility-tier layout. Preserve all team builder logic.

    3. NO raw hex; G-12 focus rings.

    4. Update 17-OD-MAP.md status to `generated`.
  </action>
  <verify>
    <automated>cargo check --features ssr 2>&1 | tail -3 && cargo check --features hydrate --target wasm32-unknown-unknown 2>&1 | tail -3 && grep -q "canvas-grain" src/pages/team/roster.rs && grep -q "canvas-grain" src/pages/team_builder.rs && ! grep -qE "#[0-9a-fA-F]{6}" src/pages/team/roster.rs src/pages/team_builder.rs && cd e2e && npx playwright test pages.spec.ts -g 'roster|team-builder' 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - canvas-grain wrappers; utility-tier layouts
    - Server fns preserved (team creation, joining)
    - 17-OD-MAP.md updated for both surfaces
    - Both compile targets green; no raw hex
  </acceptance_criteria>
  <done>team/roster.rs + team_builder.rs restyled via Open-Design; logic preserved.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 10: User reviews /team/roster + /team-builder</name>
  <how-to-verify>
    1. Visit `/team/roster`, screenshot. Test: create team, join team.
    2. Visit `/team-builder`, screenshot.
    3. Run `npx playwright test pages.spec.ts -g 'roster|team-builder'`.
  </how-to-verify>
  <resume-signal>Type "approved" — Profile hub plan complete.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| home.rs auth check → redirect target | Unauth visitors routed to /closed-beta |
| Riot API region selector → store user region | User-controlled region influences solo sync |
| join request CRUD | Multiple users can request join; team owner approves |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-20 | Spoofing | home.rs unauth redirect could be bypassed via direct URL access to /team/dashboard | mitigate | Existing auth-required pattern (Resource::new(get_current_user) + client redirect Effect) preserved on each protected page; Phase 19.1 closed-beta gate is server-side enforced separately |
| T-17-21 | Tampering | join request acceptance leaks team membership | accept | Existing pattern from MEMORY.md fix #3 (per-request error signal, owner-only accept) preserved |
| T-17-22 | Information Disclosure | profile.rs Riot account linking exposes puuid | accept | Riot puuid already stored on user record (Phase 12); restyle does not change persistence |
| T-17-23 | Tampering | LP graph SVG re-evaluates `var(--color-accent)` on theme change | accept | Browser CSS-variable re-evaluation is safe; no XSS surface (no user-supplied content in SVG `style`) |
</threat_model>

<verification>
- 6 pages restyled and individually approved per D-10
- All relevant audit + pages.spec.ts tests pass
- Both compile targets exit 0
- No raw hex; G-12 compliant; no .unwrap() in event handlers
- 17-OD-MAP.md updated for team/roster + team-builder
</verification>

<success_criteria>
1. /profile = Captain's Folio with gilt cards.
2. /team/dashboard = Strategy Room.
3. /solo-dashboard = SoloConstellation with Phase 16 refetch fix preserved.
4. /home (auth) = Strategy Room dashboard; /home (unauth) → /closed-beta redirect.
5. /team/roster + /team-builder = utility tier OD ports.
6. All existing tests pass.
7. Each page committed atomically with user approval per D-10.
</success_criteria>

<output>
After completion, create `.planning/phases/17-ui-consolidation/17-05-SUMMARY.md`.
</output>

---
phase: 17-ui-consolidation
plan: 07
type: execute
wave: 4
depends_on: [17-01, 17-02, 17-03a, 17-03b, 17-03c, 17-03d, 17-04, 17-05, 17-06]
files_modified:
  - e2e/tests/visual-regression.spec.ts
  - e2e/tests/visual-regression.spec.ts-snapshots/
  - .planning/phases/17-ui-consolidation/17-UI-REVIEW.md
autonomous: false
decisions: [D-10]
requirements: [SC-4-ui-review-pass, G-01, G-12]
must_haves:
  truths:
    - "Visual-regression baselines captured for every restyled route (~20 routes); future PRs diff against baseline"
    - "/gsd-ui-review 17 produces PASS verdict on all 6 pillars (visual coherence, accessibility, responsiveness, information density, microinteractions, performance)"
    - "Final G-01 / G-12 / raw-hex grep sweeps return zero hits"
    - "All audit-*.spec.ts + pages.spec.ts + smoke.spec.ts + closed-beta-visual.spec.ts + theme.spec.ts + fonts.spec.ts pass green"
    - "17-UI-REVIEW.md documents the audit findings + dispositions"
  artifacts:
    - path: "e2e/tests/visual-regression.spec.ts-snapshots/"
      provides: "Pixel baselines for ~20 restyled routes"
    - path: ".planning/phases/17-ui-consolidation/17-UI-REVIEW.md"
      provides: "/gsd-ui-review report with 6-pillar verdicts and any HIGH/CRITICAL findings + dispositions"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "style_guardrails job"
      via: "G-01 / G-12 / raw-hex enforcement"
      pattern: "style_guardrails"
---

<objective>
Capture visual-regression baselines for all restyled routes, run `/gsd-ui-review 17` 6-pillar audit, run final G-01 / G-12 / raw-hex grep sweeps, fix any HIGH/CRITICAL findings, and produce 17-UI-REVIEW.md.

This is the final gate before phase completion. Phase 17 is not done until all 6 pillars pass.

Output: visual-regression snapshots, audit report, all gates green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-ui-consolidation/17-CONTEXT.md
@.planning/phases/17-ui-consolidation/17-UI-SPEC.md
@.planning/phases/17-ui-consolidation/17-VALIDATION.md
@.planning/phases/17-ui-consolidation/17-01-SUMMARY.md
@.planning/phases/17-ui-consolidation/17-02-SUMMARY.md
@.planning/phases/17-ui-consolidation/17-03a-SUMMARY.md
@.planning/phases/17-ui-consolidation/17-03b-SUMMARY.md
@.planning/phases/17-ui-consolidation/17-03c-SUMMARY.md
@.planning/phases/17-ui-consolidation/17-03d-SUMMARY.md
@.planning/phases/17-ui-consolidation/17-04-SUMMARY.md
@.planning/phases/17-ui-consolidation/17-05-SUMMARY.md
@.planning/phases/17-ui-consolidation/17-06-SUMMARY.md
@../professional-vault/wiki/meta/guardrails.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Capture visual-regression baselines for all restyled routes</name>
  <files>e2e/tests/visual-regression.spec.ts, e2e/tests/visual-regression.spec.ts-snapshots/</files>
  <read_first>
    - e2e/tests/visual-regression.spec.ts (current scaffolded version from plan 01 — extends with full route set)
    - e2e/tests/fixtures.ts (authedPage helper)
    - e2e/playwright.config.ts (snapshot config — confirm snapshotDir)
    - .planning/phases/17-ui-consolidation/17-UI-SPEC.md (Route Inventory — full route list to baseline)
  </read_first>
  <behavior>
    - Each route gets an `expect(page).toHaveScreenshot()` test.
    - First run captures baseline (`--update-snapshots`); subsequent runs diff against baseline.
    - Baselines committed to git in `e2e/tests/visual-regression.spec.ts-snapshots/`.
  </behavior>
  <action>
    Per Wave 0 / Validation Architecture:

    1. Extend `e2e/tests/visual-regression.spec.ts` to cover all restyled routes:

    Public routes (no auth):
    - `/` (redirects to /closed-beta — captures the redirect target render)
    - `/closed-beta` (Demacia + Pandemonium variants — capture both via `data-theme` cookie/state manipulation)
    - `/auth/login`
    - `/auth/register?invite=TEST123`
    - `/legal/impressum`
    - `/legal/datenschutz`

    Auth routes:
    - `/profile`
    - `/team/dashboard`
    - `/team/roster`
    - `/team-builder`
    - `/draft`
    - `/tree-drafter`
    - `/champion-pool`
    - `/game-plan`
    - `/post-game`
    - `/opponents`
    - `/action-items`
    - `/stats`
    - `/match-detail` (use a known match id from test fixtures)
    - `/personal-learnings`
    - `/analytics`
    - `/solo-dashboard`
    - `/admin/invites`

    Each test:
    ```typescript
    test("[route] visual baseline", async ({ authedPage }) => {
      await authedPage.goto("[route]");
      await authedPage.waitForLoadState("networkidle");
      await authedPage.waitForTimeout(500);  // wasm-patterns rule 56
      await expect(authedPage).toHaveScreenshot({ maxDiffPixelRatio: 0.02, fullPage: true });
    });
    ```

    Use `maxDiffPixelRatio: 0.02` to allow ~2% pixel drift (font rendering, anti-aliasing variance).

    2. Run `cd e2e && npx playwright test visual-regression.spec.ts --update-snapshots`. This captures the initial baselines into `e2e/tests/visual-regression.spec.ts-snapshots/`.

    3. Run again WITHOUT `--update-snapshots` → must pass (baseline matches itself).

    4. Commit baselines to git.
  </action>
  <verify>
    <automated>cd e2e && test -d tests/visual-regression.spec.ts-snapshots && ls tests/visual-regression.spec.ts-snapshots/*.png 2>/dev/null | wc -l | awk '{ if ($1 < 15) exit 1 }' && npx playwright test visual-regression.spec.ts 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - visual-regression.spec.ts has tests for at least 18 routes (all restyled pages)
    - Snapshot directory exists with ≥15 .png baselines
    - Re-running tests against committed baselines exits 0
  </acceptance_criteria>
  <done>Visual baselines captured for all restyled routes; future PRs detect regressions.</done>
</task>

<task type="auto">
  <name>Task 2: Run final G-01 / G-12 / raw-hex grep sweeps + fix any hits</name>
  <files>(any file flagged by sweeps)</files>
  <read_first>
    - .github/workflows/ci.yml (style_guardrails job from plan 01)
    - ../professional-vault/wiki/meta/guardrails.md (G-01, G-12)
    - .planning/phases/17-ui-consolidation/17-VALIDATION.md
  </read_first>
  <action>
    Per Validation Architecture phase-gate sweeps:

    1. **G-01 sweep — No Google Fonts CDN**:
    ```bash
    grep -rE "fonts\.googleapis\.com|fonts\.gstatic\.com" --include="*.css" --include="*.rs" --include="*.html" .
    ```
    Expected: zero hits. Fix any hits by removing CDN references and ensuring local `@font-face` covers the family.

    2. **G-12 sweep — No outline:none without ring replacement**:
    ```bash
    grep -rnE "outline\s*:\s*none|outline-none" --include="*.rs" --include="*.css" src/
    ```
    For each hit: confirm a `focus-visible:ring-*` or `:focus-visible { outline: ... }` exists within 3 lines of context. Fix any unpaired hits.

    3. **Raw-hex sweep — semantic tokens only in components/pages**:
    ```bash
    grep -rnE "#[0-9a-fA-F]{6}\b|#[0-9a-fA-F]{3}\b" --include="*.rs" src/components/ src/pages/ | grep -v "//"
    ```
    Expected: zero hits (theme blocks live in `input.css`, NOT in components). Fix any hits by replacing with semantic tokens (`bg-base`, `text-primary`, etc.) or `var(--color-*)`.

    4. Run the CI sweep job locally (simulate):
    ```bash
    bash -c '
      set -e
      ! grep -rE "fonts\.googleapis\.com|fonts\.gstatic\.com" --include="*.css" --include="*.rs" --include="*.html" .
      grep -rnE "outline\s*:\s*none|outline-none" --include="*.rs" --include="*.css" src/ > /tmp/g12_hits.txt || true
      # ... per-line context check (see plan 01 task 9 implementation)
      ! grep -rnE "#[0-9a-fA-F]{6}\b" --include="*.rs" src/components/ src/pages/ | grep -v "//"
    '
    ```

    5. Commit all fixes with one commit per file or per cluster.
  </action>
  <verify>
    <automated>! grep -rE "fonts\.googleapis\.com|fonts\.gstatic\.com" --include="*.css" --include="*.rs" --include="*.html" . && ! grep -rnE "#[0-9a-fA-F]{6}\b" --include="*.rs" src/components/ src/pages/ | grep -v "//"</automated>
  </verify>
  <acceptance_criteria>
    - G-01 sweep returns zero hits
    - G-12 sweep returns zero unpaired hits (every outline:none has a focus-visible:ring nearby)
    - Raw-hex sweep returns zero hits in src/components/ and src/pages/
    - All compile targets still green after any fixes
  </acceptance_criteria>
  <done>All 3 grep sweeps pass; any violations fixed and committed.</done>
</task>

<task type="auto">
  <name>Task 3: Run /gsd-ui-review 17 6-pillar audit + author 17-UI-REVIEW.md</name>
  <files>.planning/phases/17-ui-consolidation/17-UI-REVIEW.md</files>
  <read_first>
    - .planning/phases/17-ui-consolidation/17-VALIDATION.md (lines 75-86 — 6-Pillar audit acceptance criteria)
    - .planning/phases/17-ui-consolidation/17-UI-SPEC.md (full — used as the audit baseline)
    - All page summaries from plans 03-06 (review what was implemented)
    - ../professional-vault/wiki/concepts/accessibility-standards.md (a11y baseline)
  </read_first>
  <action>
    Run `/gsd-ui-review 17`. The review evaluates:

    1. **Visual coherence**: semantic tokens consistent across pages, typography hierarchy enforced, no raw hex bleed-through, ornament usage appropriate, gilt cards render correctly.

    2. **Accessibility**: focus states (G-12), color contrast WCAG AA (4.5:1 normal, 3:1 large), keyboard nav, ARIA on interactive elements, touch target ≥44×44px.

    3. **Responsiveness**: desktop-first works at 1920×1080 / 1440×900 / 1280×720. No broken layouts at these widths. (Mobile out of scope per PROJECT.md.)

    4. **Information density**: every Resource page has a Skeleton fallback; every empty state has messaging (e.g., "Sync your match history to get started." per UI-SPEC line 684); no empty-state-as-blank-screen.

    5. **Microinteractions**: toast feedback on mutations (preserved from Phase 5), hover states, focus rings (G-12), transitions on selection (UI-SPEC line 506 — `transition-all 180ms ease-out`).

    6. **Performance**: no Google Fonts CDN (G-01), local font subsets, image lazy-load (loading="lazy"), reasonable bundle size (no new heavy JS deps), FLUX images ≤400 KB each.

    Author `.planning/phases/17-ui-consolidation/17-UI-REVIEW.md` documenting:
    - Verdict per pillar (PASS / FAIL).
    - Findings (severity: CRITICAL / HIGH / MEDIUM / LOW / INFO) per pillar.
    - For each finding: file/route + description + recommended fix + status (open / fixed).
    - Disposition: any HIGH/CRITICAL findings MUST be fixed before phase complete (Phase 17 SC4); MEDIUM/LOW can be deferred to Phase 22 pre-launch review or backlog with justification.

    If `/gsd-ui-review` is unavailable, perform the audit manually using the 6-pillar criteria above. Document each pillar's verdict + evidence (e.g., "Visual coherence: PASS — grep returns 0 raw hex; all pages use canvas-grain wrapper; ornaments render in Demacia, RiotTape in Pandemonium.").
  </action>
  <verify>
    <automated>test -f .planning/phases/17-ui-consolidation/17-UI-REVIEW.md && grep -q "Visual coherence\|visual_coherence" .planning/phases/17-ui-consolidation/17-UI-REVIEW.md && grep -q "Accessibility\|accessibility" .planning/phases/17-ui-consolidation/17-UI-REVIEW.md && grep -q "Performance\|performance" .planning/phases/17-ui-consolidation/17-UI-REVIEW.md && ! grep -qE "Verdict.*FAIL|status:\s*open.*HIGH|status:\s*open.*CRITICAL" .planning/phases/17-ui-consolidation/17-UI-REVIEW.md</automated>
  </verify>
  <acceptance_criteria>
    - 17-UI-REVIEW.md exists with all 6 pillars documented
    - Each pillar has a PASS/FAIL verdict
    - All HIGH/CRITICAL findings are status `fixed` (not `open`) — phase blocked until they are
    - MEDIUM/LOW findings may be `open` with clear deferral justification
  </acceptance_criteria>
  <done>6-pillar audit complete; HIGH/CRITICAL fixed; report authored.</done>
</task>

<task type="auto">
  <name>Task 4: Final full-suite test pass + update STATE.md / ROADMAP.md</name>
  <files>.planning/STATE.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/STATE.md (current state)
    - .planning/ROADMAP.md (Phase 17 row)
    - All plan summaries 17-01 through 17-07 (just-authored)
  </read_first>
  <action>
    1. Run full test suite:
    ```bash
    just verify    # cargo check + test + clippy + fmt
    cd e2e && npx playwright test    # full suite
    ```

    All MUST be green.

    2. Update `.planning/STATE.md`:
       - `stopped_at: Phase 17 complete`
       - Add to Decisions section: `[Phase 17]: ...` (key decisions baked in: demacia/pandemonium adopted, 5-accent retired, self-hosted fonts, 4-hub IA, FLUX background imagery, Open-Design lol-companion seeded)
       - Update progress counters (completed_plans += 7).

    3. Update `.planning/ROADMAP.md`:
       - Phase 17 row: `0/?` → `7/7 Complete    | 2026-MM-DD`.
       - Mark Phase 17 [x] in the v1.3 Launch Readiness section.
  </action>
  <verify>
    <automated>cargo check --features ssr 2>&1 | tail -3 && cargo check --features hydrate --target wasm32-unknown-unknown 2>&1 | tail -3 && cargo test --features ssr --lib 2>&1 | tail -3 && grep -q "Phase 17" .planning/STATE.md && grep -q "Phase 17.*Complete\|17.*UI Consolidation.*Complete\|17. UI Consolidation.*Complete" .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `just verify` exits 0
    - `cd e2e && npx playwright test` exits 0
    - STATE.md updated with Phase 17 decisions + progress
    - ROADMAP.md Phase 17 marked Complete
  </acceptance_criteria>
  <done>All gates green; STATE/ROADMAP reflect Phase 17 shipped.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: User approves Phase 17 completion</name>
  <what-built>
    - 7 plans completed across 4 waves
    - Foundations: theme port, font self-hosting, DB theme field, 4-hub IA, ornaments, icon library, Wave 0 e2e + CI sweep (plan 01)
    - Open-Design seeded (plan 02)
    - Strategy hub: 7 pages restyled (plan 03)
    - History hub: 4 pages restyled (plan 04)
    - Profile hub: 6 pages restyled (plan 05)
    - Closed-beta + auth + admin + legal + bug-widget (plan 06)
    - Visual baselines + 6-pillar audit + final sweeps (plan 07)
  </what-built>
  <how-to-verify>
    1. Open `.planning/phases/17-ui-consolidation/17-UI-REVIEW.md` — review the 6-pillar verdicts.
    2. Confirm any HIGH/CRITICAL findings are marked `fixed`.
    3. Run `just verify && just e2e` end-to-end. All green.
    4. Run `cd e2e && npx playwright test visual-regression.spec.ts`. All baselines match.
    5. Spot-check 3-4 pages in browser: confirm visual fidelity to Claude Design / Open-Design.
    6. Switch theme demacia → pandemonium → switch back. Confirm persistence + correct behavior.
    7. Confirm STATE.md and ROADMAP.md mark Phase 17 Complete.

    APPROVAL CRITERIA: 6 pillars PASS; no open HIGH/CRITICAL; visual fidelity matches design; no behavioral regression on existing audit-*.spec.ts tests.
  </how-to-verify>
  <resume-signal>Type "approved" to mark Phase 17 complete and unblock Phase 18.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| visual-regression baselines → committed git assets | Snapshots may differ between platforms (Linux vs macOS rendering) |
| /gsd-ui-review tool → audit report | External tool produces verdict |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-33 | Tampering | visual-regression baselines could mask drift if generated on a different platform than CI | mitigate | maxDiffPixelRatio: 0.02 tolerance handles minor anti-alias variance; baselines re-captured if CI runs on different OS |
| T-17-34 | Repudiation | 6-pillar audit findings may not be acted on | mitigate | 17-UI-REVIEW.md tracks status (open / fixed); Phase 17 blocks on HIGH/CRITICAL = `fixed` (Task 3 acceptance) |
| T-17-35 | Tampering | guardrails sweeps could be bypassed by adding `// noqa` comments | accept | grep patterns are simple substring checks; if a developer disables them, that is a deliberate choice; CI catches the next PR |
</threat_model>

<verification>
- All visual baselines captured (≥15 .png snapshots)
- G-01 / G-12 / raw-hex sweeps return zero hits
- 6-pillar audit verdicts all PASS
- HIGH/CRITICAL findings all `fixed`
- Full test suite green
- STATE.md + ROADMAP.md updated
- User approves phase completion
</verification>

<success_criteria>
1. Visual-regression baselines for all restyled routes; future PRs detect drift.
2. /gsd-ui-review 17 produces PASS verdict on all 6 pillars (visual coherence, accessibility, responsiveness, information density, microinteractions, performance).
3. G-01 / G-12 / raw-hex sweeps clean.
4. All e2e + cargo tests green.
5. 17-UI-REVIEW.md documents audit findings + dispositions.
6. STATE.md and ROADMAP.md mark Phase 17 Complete.
7. Phase 17 unblocks Phase 18 (bug-report widget) execution.
</success_criteria>

<output>
After completion, create `.planning/phases/17-ui-consolidation/17-07-SUMMARY.md`.
</output>

---
phase: 17
slug: ui-consolidation
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-05-07
---

# Phase 17 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Generated by /gsd-plan-phase from 17-RESEARCH.md "Validation Architecture" section.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Rust unit tests (`cargo test --features ssr --lib`) + Playwright e2e (`npx playwright test`) + agent-browser (interactive screenshots) + `/gsd-ui-review` (6-pillar audit) |
| **Config file** | `Cargo.toml`, `e2e/playwright.config.ts` |
| **Quick run command** | `cargo check --features ssr` (Rust) / `npx playwright test <spec>` (per-page) |
| **Full suite command** | `just verify && just e2e` |
| **Estimated runtime** | ~30s Rust check, ~3-5min full e2e |

---

## Sampling Rate

- **After every task commit:** `cargo check --features ssr` + `cargo check --features hydrate --target wasm32-unknown-unknown`
- **After per-page restyle (D-10 review gate):** agent-browser screenshot + user approval before commit
- **After every plan wave:** `just verify` (cargo + tests + clippy + fmt) + relevant `npx playwright test` specs
- **Before `/gsd-verify-work`:** Full `just verify && just e2e` green; `/gsd-ui-review 17` PASS on all 6 pillars
- **Max feedback latency:** ~30s for Rust compile, ~5min for full e2e

---

## Per-Task Verification Map

> Filled by gsd-planner during planning. Each task in PLAN.md gets a row mapping it to the test/check that proves it works. Automated tasks → exact command; visual tasks → agent-browser screenshot + reviewer note.

| Task ID | Plan | Wave | Success Criterion | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------------|-----------|-------------------|-------------|--------|
| _to be filled by planner_ | _ | _ | _ | _ | _ | _ | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements (gaps to install before main waves)

- [ ] `e2e/tests/theme.spec.ts` — verifies `data-theme="demacia"` and `data-theme="pandemonium"` toggle persistence and visual snapshot
- [ ] `e2e/tests/closed-beta-visual.spec.ts` — visits `/` without invite token, asserts closed-beta landing renders (not redirect-to-login)
- [ ] `e2e/tests/visual-regression.spec.ts` — per-hub screenshot baseline + diff threshold for the 4 hubs (Strategy/Live/History/Profile)
- [ ] `e2e/tests/fonts.spec.ts` — asserts no `fonts.googleapis.com` in document head (G-01); asserts each `@font-face` resolves (200 status)
- [ ] CI sweep job: grep for `outline:none` (G-12), raw hex `#[0-9a-fA-F]{3,6}` in components, and `googleapis.com` in `dist/` build output

*Existing infrastructure already covers compile-checks, unit tests for db/auth/draft/tree/champion-pool, and 25+ Playwright specs (auth, smoke, page-render, draft flow, tree flow, etc.). Wave 0 only adds the visual-regression + theme + closed-beta + fonts coverage.*

---

## Manual-Only Verifications

| Behavior | Source Criterion | Why Manual | Test Instructions |
|----------|------------------|------------|-------------------|
| Per-page visual fidelity to Claude Design handoff | ROADMAP success #2 ("Claude Design has produced primary mockups… verified by user review pass") + D-10 | Pixel-perfect match against handoff prototype is judgment-based; AI cannot evaluate aesthetic intent | After each page restyle: `npx agent-browser screenshot http://127.0.0.1:3020/<route>` → user compares against `/tmp/lol-design-handoff/lol-team-companion-app/project/screens/<page>.jsx` rendering → approve or request revision before commit |
| FLUX background image quality (region intent: demacia warm/regal, pandemonium cold/intense) | D-18 | Aesthetic judgment; cannot be automated | User reviews each generated image alongside the prompt + seed in `.planning/assets/AI-IMAGES.md`. Re-roll until visual intent matches |
| 4-hub nav IA feels right in use | D-09 | Information architecture is a UX judgment | User clicks through Strategy → Live → History → Profile and confirms sub-tabs are discoverable and labeled appropriately |
| Closed-beta landing tone (D-15: "first surface visitors see — quality matters") | D-14, D-15 | Brand-tone judgment | User reviews landing page on a fresh browser session at `/` (no auth, no invite) and approves the messaging + visual |

---

## /gsd-ui-review Acceptance (6-Pillar Audit)

The phase's authoritative validation gate is `/gsd-ui-review 17`. Required PASS verdicts on:

1. **Visual coherence** — semantic tokens consistent, typography hierarchy enforced, no raw hex
2. **Accessibility** — focus states (G-12 ring replacement), color contrast meets WCAG AA, keyboard navigation, ARIA on interactive elements
3. **Responsiveness** — desktop-first, but no broken layouts at 1920×1080 / 1440×900 / 1280×720
4. **Information density** — no empty states without messaging, skeleton loaders on every Resource page
5. **Microinteractions** — toast feedback on mutations, hover states, focus rings
6. **Performance** — no Google Fonts CDN (G-01), local font subsets, image lazy-load, reasonable bundle size

If ANY pillar fails, the phase is not complete. `/gsd-ui-review` produces a structured report; gaps feed back into a follow-up plan or `/gsd-plan-phase 17 --gaps`.

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify command OR a Wave 0 dependency entry OR a Manual-Only entry above
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify (visual-only restyle tasks count manual review per D-10 as their verify)
- [ ] Wave 0 (theme.spec, closed-beta-visual.spec, visual-regression.spec, fonts.spec, CI sweep) installed before Wave 1 main work
- [ ] No watch-mode flags in any task command (CI must be deterministic)
- [ ] Feedback latency < 30s for Rust check, < 5min for full e2e
- [ ] `nyquist_compliant: true` set in frontmatter after planner fills the per-task map
- [ ] `/gsd-ui-review 17` PASS on all 6 pillars before phase verification

**Approval:** pending

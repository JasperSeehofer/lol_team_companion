---
phase: 7
slug: ux-polish
status: draft
shadcn_initialized: false
preset: none
created: 2026-03-22
---

# Phase 7 — UI Design Contract

> Visual and interaction contract for Phase 7: UX Polish.
> Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none — Tailwind CSS v4 with custom semantic token layer |
| Preset | not applicable |
| Component library | none (hand-rolled Leptos components) |
| Icon library | Inline SVG (heroicons-style) + Community Dragon CDN (role icons) |
| Font | System font stack (Tailwind default: `ui-sans-serif, system-ui, sans-serif`) |

Source: `input.css` — `@theme` block, no shadcn `components.json` present.

---

## Spacing Scale

Standard 8-point scale already in use project-wide via Tailwind v4 defaults.

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, inline padding, tight labels |
| sm | 8px | Compact element spacing, badge padding |
| md | 16px | Default element spacing, card padding |
| lg | 24px | Section padding, form group gaps |
| xl | 32px | Layout gaps between major sections |
| 2xl | 48px | Major section breaks |
| 3xl | 64px | Page-level spacing; **also the toast top offset** |

Exceptions:
- **Toast top offset:** `top-16` = 64px — chosen specifically to clear the `h-14` (56px) sticky nav header with 8px margin. Source: RESEARCH.md Pattern 4, CONTEXT.md D-13.
- **Role watermark icons:** `w-14 h-14` = 56px — within the D-09 specified 48-64px range, partially cropped via `translate-x-2 translate-y-2` (8px overhang into card edge). Source: RESEARCH.md Pattern 3.
- **Minimum touch target:** 44px for all interactive buttons (Tailwind `min-h-11` or `py-2.5`).

---

## Typography

Project uses Tailwind utility classes directly. No custom font scale override in `input.css` — relies on Tailwind v4 defaults.

| Role | Tailwind Class | Size | Weight | Line Height |
|------|---------------|------|--------|-------------|
| Display / Page heading | `text-3xl font-bold` | 30px | 700 | 1.2 |
| Section heading | `text-xl font-bold` | 20px | 700 | 1.25 |
| Body | `text-sm` | 14px | 400 | 1.5 |
| Label / Meta | `text-xs` | 12px | 400 | 1.4 |

Phase 7 introduces no new type sizes. All new text elements (search prompt, member count, timestamp display, search results) use `text-sm` (14px, weight 400) for body copy and `text-xs` (12px, weight 400) for meta labels.

Two weights in use:
- Regular: 400 (`font-normal` — default, no class needed)
- Bold: 700 (`font-bold`)

`font-semibold` (600) is not part of the declared scale. Any existing `font-semibold` usages encountered during Phase 7 work must be updated to `font-bold`.

Source: Observed throughout `dashboard.rs`, `nav.rs`, and `ui.rs`.

---

## Color

Project uses semantic CSS custom properties via `@theme`. All new UI must use semantic tokens, never hardcoded hex values.

| Role | Semantic Token | Dark Value (approx) | Usage |
|------|---------------|-------------------|-------|
| Dominant (60%) | `bg-base` | gray-950 (`oklch(0.13 0.004 264.665)`) | Page background |
| Secondary (30%) | `bg-surface`, `bg-elevated` | gray-900 / gray-800 | Cards, nav bar, panels, search results list |
| Accent (10%) | `bg-accent` / `text-accent` | yellow-400 (default) | See reserved list below |
| Destructive | `bg-red-700` / `text-red-400` | red-700 / red-400 | Destructive actions only (kick member, etc.) |

Accent (`--t-accent`: yellow-400 default, user-overridable) is **reserved for**:
- Team name headings on dashboard
- "Team Leader" badge text
- Active/focus ring on form inputs (`focus:border-accent`)
- Primary CTA buttons (`bg-accent text-accent-contrast`)
- Search result "Request to Join" button

**Phase 7 new elements — color assignments:**

| Element | Color |
|---------|-------|
| Search input border | `border-outline/50` (focus: `border-accent`) |
| Search input background | `bg-surface/50` |
| "Type to search..." placeholder text | `text-muted` |
| "No teams match" empty text | `text-dimmed` |
| Member count label in search result | `text-muted` |
| Region label in search result | `text-muted` |
| Timestamp display text | `text-muted` (inline, beside owning label) |
| Role watermark icon | `opacity-10 invert` over card background (no color token — uses CSS filter) |
| Toast container (no change) | existing `emerald-500/20` success, `red-500/20` error |

Source: `input.css` token definitions; CLAUDE.md theming rules.

---

## Interaction Contracts

### UX-04: Toast Positioning

- Container class: `fixed top-16 left-1/2 -translate-x-1/2 z-50 flex flex-col gap-2 items-center pointer-events-none`
- Change from `top-4` (16px) to `top-16` (64px).
- No change to z-index, animation, auto-dismiss timing, or max-visible (3) logic.
- Toasts stack downward from 64px.

### UX-05: Timestamp Formatting

- **Formatter:** Pure function `format_timestamp(s: &str) -> String` in `src/models/utils.rs`.
- Compiles for both `ssr` and `hydrate` targets (chrono 0.4 already in `Cargo.toml`).
- Input: ISO-8601 string as stored by SurrealDB (e.g. `"2026-03-19T14:30:00.000000000Z"`).
- Output rules:
  - `age < 1 min` → `"just now"`
  - `1 min ≤ age < 1 hour` → `"N minute(s) ago"` (e.g. `"23 minutes ago"`, `"1 minute ago"`)
  - `1 hour ≤ age < 24 hours` → `"N hour(s) ago"` (e.g. `"6 hours ago"`, `"1 hour ago"`)
  - `age ≥ 24 hours, same year` → `"D Mon, HH:MM"` (e.g. `"19 Mar, 14:30"`)
  - `age ≥ 24 hours, different year` → `"D Mon YYYY, HH:MM"` (e.g. `"5 Dec 2025, 14:30"`)
  - Parse failure → return raw string unchanged (graceful fallback)
  - Future timestamp (clock skew) → fall through to absolute format
- **Call site pattern:** `{note.created_at.as_deref().map(format_timestamp).unwrap_or_default()}`
- Applied at every `created_at` / `updated_at` display site across all pages.

### UX-06: Profile Link Button Dedup

- The `EmptyState` component in the unlinked profile state must have its `cta_label` and `cta_href` props removed.
- The `ActionForm` submit button labeled "Link Account" is the single canonical button — kept in all states.
- Verified states: unlinked (no Riot account), linked (re-link form still shows one button), loading.
- No visual change other than removing the duplicate anchor/button.

### UX-07: Team Join Search

- Replaces the current full-list render with a search-filtered view inside "Join an Existing Team".
- **Focal point:** The search input anchors the visual hierarchy of this section — it is the first interactive element, full-width, and draws the eye before any results appear.
- **Input component:** `<input type="text">` styled as `w-full bg-surface/50 border border-outline/50 rounded px-3 py-2 text-primary text-sm focus:outline-none focus:border-accent`
- Placeholder text: `"Search teams by name..."`
- Reactive signal: `RwSignal<String>` for the search query; filter applied client-side on the already-loaded `teams_resource`.
- **Three render states:**
  1. Empty query (`query.is_empty()` = true): show `<p class="text-muted text-sm">"Type to search for teams..."</p>`
  2. Non-empty query, results found: show filtered team cards (see card spec below)
  3. Non-empty query, no results: show `<p class="text-dimmed text-sm">"No teams match your search."</p>`
- **Search result card layout:** horizontal row — team name (`text-primary font-bold`) + region (`text-muted text-sm`) + member count (`text-muted text-sm` — e.g. `"5 members"`) + "Request to Join" button (`bg-accent hover:bg-accent-hover text-accent-contrast font-bold rounded px-3 py-1.5 text-sm`)
- Member count requires `member_count: Option<u32>` added to `Team` model and `list_all_teams()` query updated.
- Filter matches `team.name` only (case-insensitive `contains`). Source: CONTEXT.md D-08.

### UX-09: Roster Role Watermarks

- **Trigger:** Only when the assigned role has a Community Dragon icon URL (i.e. `!role_icon_url(role).is_empty()`).
- No watermark for `"unassigned"`, `"sub"`, `"coach"` roles from the `role_icon_url()` fallback branch. Source: CONTEXT.md D-11.
- **Coach cards:** Use an inline SVG clipboard/whiteboard icon (20x20, `text-muted`) as the watermark — Community Dragon has no coach SVG. The icon is rendered identically to the role watermark (absolute, bottom-right, faded).
- **Focal point:** The role watermark anchors the bottom-right of each card, creating a visual anchor that reinforces the assigned role at a glance without competing with the member name at top-left.
- **Watermark image spec:**
  - Class: `absolute bottom-0 right-0 w-14 h-14 opacity-10 invert pointer-events-none select-none translate-x-2 translate-y-2`
  - `alt=""` and `aria-hidden="true"` (purely decorative)
  - Partially crops into the card edge via `translate-x-2 translate-y-2` (8px offset)
- **Card container requirement:** Add `relative overflow-hidden` to the card container div.
  - Warning for bench row cards: the role `<select>` dropdown must remain outside the `overflow-hidden` container or the dropdown will be clipped. Apply `overflow-hidden` to the background layer only, not to the full flex row. Source: RESEARCH.md Pitfall 2.
- **Opacity:** 10% (`opacity-10`). This is within the CONTEXT.md D-09 range of 8–12%.
- **Applied to:** Starter slot cards (5 fixed role slots), bench member cards, coach member cards.

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Primary CTA — team search | "Request to Join" |
| Primary CTA — profile link | "Link Account" |
| Search empty state | "Type to search for teams..." |
| Search no-results state | "No teams match your search." |
| Timestamp: < 1 min | "just now" |
| Timestamp: N minutes | "N minute ago" / "N minutes ago" |
| Timestamp: N hours | "N hour ago" / "N hours ago" |
| Toast success (existing) | [per-action messages — no change] |
| Toast error (existing) | [per-action messages — no change] |
| Role watermark | (no text — purely visual) |
| Profile unlinked empty state heading | (existing — no change) |
| Profile unlinked empty state body | (existing — no change) |

Destructive actions in this phase: none. Phase 7 contains no destructive operations.

---

## Component Inventory

New or modified components for this phase:

| Component / Module | Change Type | File |
|-------------------|-------------|------|
| `ToastOverlay` | CSS-only (1 class change) | `src/components/ui.rs` line 110 |
| `format_timestamp()` | New function | `src/models/utils.rs` (new file) |
| `pub mod utils` | Module declaration | `src/models/mod.rs` |
| `Team` model | Add `member_count: Option<u32>` field | `src/models/team.rs` |
| `list_all_teams()` DB query | Add member count via SurrealQL subselect | `src/server/db.rs` |
| `ProfilePage` (unlinked state) | Remove `cta_label`/`cta_href` from `EmptyState` | `src/pages/profile.rs` |
| `RosterPage` (join section) | Replace list with search input + filter | `src/pages/team/roster.rs` |
| `TeamDashboard` (starter slots) | Add watermark `<img>` + `relative overflow-hidden` on container | `src/pages/team/dashboard.rs` |
| `TeamDashboard` (bench cards) | Add watermark `<img>` + `relative` on container (not `overflow-hidden` on flex row — see pitfall) | `src/pages/team/dashboard.rs` |
| `TeamDashboard` (coach cards) | Add inline SVG watermark icon | `src/pages/team/dashboard.rs` |
| All timestamp display sites | Replace raw string with `format_timestamp()` call | Multiple pages |

No new pages. No new routes. No new server functions beyond `list_all_teams()` query update.

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none | not applicable — shadcn not initialized |
| Community Dragon CDN | `role_icon_url()` SVGs (already in use) | not applicable — existing dependency, no new blocks |

No third-party registries. No registry vetting required.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending

---

*Phase: 07-ux-polish*
*UI-SPEC created: 2026-03-22*
*Pre-populated from: CONTEXT.md (15 decisions), RESEARCH.md (5 patterns + code examples), input.css (token values), codebase grep (typography, spacing, component structure)*

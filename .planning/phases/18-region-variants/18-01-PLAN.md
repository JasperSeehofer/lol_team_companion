---
phase: 18-region-variants
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/region/mod.rs
  - src/components/region/typography.rs
  - src/components/region/ornaments.rs
  - src/components/region/layout.rs
  - src/components/region/controls.rs
  - src/components/region/data_viz.rs
  - src/components/region/solo.rs
  - src/components/region/chrome.rs
  - src/components/mod.rs
  - src/components/ornaments.rs
  - src/components/nav.rs
  - src/components/draft_board.rs
  - src/pages/draft.rs
  - src/pages/match_detail.rs
  - src/pages/profile.rs
  - src/pages/home.rs
  - src/pages/auth/register.rs
  - src/pages/stats.rs
  - src/pages/solo_dashboard.rs
  - src/pages/auth/login.rs
  - src/pages/team/dashboard.rs
  - src/pages/closed_beta.rs
  - src/pages/admin/invites.rs
  - src/pages/game_plan.rs
  - src/pages/legal/impressum.rs
  - src/pages/legal/datenschutz.rs
autonomous: true
requirements: [REQ-1]
tags: [region, primitives, design-system, leptos]

must_haves:
  truths:
    - "Module `src/components/region/` exists with 7 submodules"
    - "All ~24 primitives compile under both --features ssr and --features hydrate --target wasm32-unknown-unknown"
    - "Old `src/components/ornaments.rs` is DELETED"
    - "All 7 existing import sites that reference `crate::components::ornaments::*` use `crate::components::region::*` instead"
    - "Primitive that takes `region: String` returns visibly different markup for 'demacia' vs 'pandemonium' (verified by view-string test)"
  artifacts:
    - path: "src/components/region/mod.rs"
      provides: "Re-exports all 7 submodules"
      contains: "pub use chrome::*"
    - path: "src/components/region/ornaments.rs"
      provides: "All ornaments consolidated (HeraldicDivider, GiltCorner, RiotTape, FleurDeLis, Crown, CompanionSigil)"
      contains: "pub fn CompanionSigil"
    - path: "src/components/region/typography.rs"
      provides: "Display, Imperial, H, Eyebrow, Mono, Glitch type primitives"
      contains: "pub fn Glitch"
    - path: "src/components/region/layout.rs"
      provides: "Card region+variant, SectionHead region+tone, Themed"
      contains: "pub fn Card"
    - path: "src/components/region/controls.rs"
      provides: "Btn region+tone, Badge region+tone"
      contains: "pub fn Btn"
    - path: "src/components/region/data_viz.rs"
      provides: "Stat, Sparkline, MoodMeter"
      contains: "pub fn Stat"
    - path: "src/components/region/solo.rs"
      provides: "RankBadge, LPProgress (region-branching)"
      contains: "pub fn LPProgress"
    - path: "src/components/region/chrome.rs"
      provides: "ChampPortrait, ChampTile, RoleIcon, Icon"
      contains: "pub fn ChampPortrait"
  key_links:
    - from: "src/components/mod.rs"
      to: "src/components/region/"
      via: "pub mod region"
      pattern: "pub mod region;"
    - from: "src/pages/draft.rs"
      to: "src/components/region/ornaments.rs"
      via: "use crate::components::region::"
      pattern: "use crate::components::region::"
---

<objective>
Port the ~24 region-aware primitives from `.local-design-source/lol-team-companion-app/project/pages/_shared/components.jsx` to Leptos under a NEW `src/components/region/` module split into 7 submodules (per D-01). DELETE the old `src/components/ornaments.rs` (per D-02) and migrate `HeraldicDivider`, `GiltCorner`, `RiotTape`, `FleurDeLis`, `Crown`, `CompanionSigil` into `region/ornaments.rs`. Update all 7 import sites (verified via grep) to point at the new path.

Purpose: Establishes the design-system foundation. Every subsequent Wave 2+ plan consumes these primitives. Without them, no page can render the region-flipped structural compositions.

Output: ~24 Leptos components across 7 files, dual-target compilable, with `region: String` prop on every component that has a structural Demacia↔Pandemonium difference. Reference branching pattern: existing `CompanionSigil` (ornaments.rs:128-167).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.claude/rules/leptos-patterns.md
@.claude/rules/wasm-patterns.md
@.planning/phases/18-region-variants/18-SPEC.md
@.planning/phases/18-region-variants/18-CONTEXT.md
@.planning/phases/18-region-variants/18-UI-SPEC.md
@.planning/phases/18-region-variants/18-PATTERNS.md
@.planning/phases/18-region-variants/18-RESEARCH.md
@src/components/ornaments.rs
@src/components/mod.rs
@src/components/stat_card.rs
@src/components/theme_toggle.rs
@.local-design-source/lol-team-companion-app/project/pages/_shared/components.jsx
@input.css

<interfaces>
<!-- Established Leptos 0.8 patterns for region primitives -->

From src/components/ornaments.rs (canonical AnyView branch — copy verbatim into region/ornaments.rs CompanionSigil):
```rust
let is_pandemonium = use_context::<InitialTheme>()
    .map(|t| t.0 == "pandemonium")
    .unwrap_or(false);

if is_pandemonium {
    view! { ... }.into_any()
} else {
    view! { ... }.into_any()
}
```

NOTE: `CompanionSigil` is the ONLY primitive allowed to call `use_context::<InitialTheme>()` internally
(legacy pattern, preserved). All NEW region-branching primitives MUST accept `region: String` as a prop
per SPEC Constraints — see 18-PATTERNS.md §"Region/ornaments.rs CRITICAL note".

From src/app.rs (context to consume at page entry, not inside primitives):
```rust
pub struct InitialTheme(pub String);  // "demacia" or "pandemonium"
```

From input.css:193-265 (semantic tokens used by primitives):
- bg-base, bg-surface, bg-elevated
- text-primary, text-secondary, text-muted, text-dimmed
- border-divider, border-outline
- bg-accent, text-accent-contrast
- Pandemonium CSS vars (style-attr only, not Tailwind classes): var(--accent-2), var(--accent-3), var(--gold-1), var(--gold-2), var(--gold-3), var(--gold-deep), var(--ink)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create region module skeleton + migrate ornaments + delete old file</name>
  <read_first>
    - src/components/ornaments.rs (FULL — all 191 lines; we are moving every public item)
    - src/components/mod.rs (current module list — must remove ornaments line, add region line)
    - .planning/phases/18-region-variants/18-PATTERNS.md (§"src/components/region/mod.rs" — exact mod.rs shape)
    - .planning/phases/18-region-variants/18-UI-SPEC.md (§"Submodule inventory" — confirms which primitive lives in which submodule)
  </read_first>
  <files>
    src/components/region/mod.rs
    src/components/region/ornaments.rs
    src/components/region/typography.rs
    src/components/region/layout.rs
    src/components/region/controls.rs
    src/components/region/data_viz.rs
    src/components/region/solo.rs
    src/components/region/chrome.rs
    src/components/mod.rs
    src/components/ornaments.rs (DELETED)
    src/components/nav.rs (update import)
    src/components/draft_board.rs (update import)
    src/pages/draft.rs (update import)
    src/pages/match_detail.rs (update import)
    src/pages/profile.rs (update import)
    src/pages/home.rs (update import)
    src/pages/auth/register.rs (update import)
    src/pages/stats.rs (update import)
    src/pages/solo_dashboard.rs (update import)
    src/pages/auth/login.rs (update import)
    src/pages/team/dashboard.rs (update import)
    src/pages/closed_beta.rs (update import)
    src/pages/admin/invites.rs (update import)
    src/pages/game_plan.rs (update import)
    src/pages/legal/impressum.rs (update import)
    src/pages/legal/datenschutz.rs (update import)
  </read_first>
  <action>
    1. **Create `src/components/region/mod.rs`** with this exact content (per 18-PATTERNS.md):
       ```rust
       pub mod chrome;
       pub mod controls;
       pub mod data_viz;
       pub mod layout;
       pub mod ornaments;
       pub mod solo;
       pub mod typography;

       pub use chrome::*;
       pub use controls::*;
       pub use data_viz::*;
       pub use layout::*;
       pub use ornaments::*;
       pub use solo::*;
       pub use typography::*;
       ```

    2. **Create `src/components/region/ornaments.rs`** as a verbatim move of the EXISTING `src/components/ornaments.rs` contents (all 191 lines: `HeraldicDivider`, `GiltCorner`, `RiotTape`, `FleurDeLis`, `Crown`, `CompanionSigil`). Keep all attributes, doc comments, and `use crate::app::InitialTheme;` import. The file header docstring should be updated to:
       ```rust
       //! Region-aware ornament primitives.
       //! Per phase 18 D-02, this consolidates all ornaments under region/.
       //! CompanionSigil is the legacy reference for the move || if is_pandemonium { … }.into_any() pattern.
       //! New region primitives accept `region: String` as a prop (NOT use_context) per SPEC Constraints.
       ```
       Then `CompanionSigil` retains its existing `use_context::<InitialTheme>()` pattern — do NOT change its signature.

    3. **Create 6 stub submodule files** (typography.rs, layout.rs, controls.rs, data_viz.rs, solo.rs, chrome.rs) each with:
       ```rust
       use leptos::prelude::*;
       // Components added in subsequent tasks.
       ```
       This makes the module compile after the mod.rs declaration even before Task 2/3 add the implementations.

    4. **Update `src/components/mod.rs`** — REPLACE the `pub mod ornaments;` line with `pub mod region;`. Final content:
       ```rust
       pub mod bug_report_widget;
       pub mod champion_autocomplete;
       pub mod champion_picker;
       pub mod draft_board;
       pub mod icon;
       pub mod nav;
       pub mod region;
       pub mod stat_card;
       pub mod theme_toggle;
       pub mod tree_graph;
       pub mod ui;
       ```

    5. **Update every import site** (grep verified — 17 files): replace any `use crate::components::ornaments::*;` or `use crate::components::ornaments::{HeraldicDivider, CompanionSigil, ...};` with `use crate::components::region::{HeraldicDivider, CompanionSigil, ...};` (the re-export at mod.rs makes flat imports work). Use a single sed pass per file OR run a project-wide `grep -rln "use crate::components::ornaments"` first to confirm the list of 17 files matches:
       - src/components/nav.rs
       - src/components/draft_board.rs
       - src/pages/draft.rs
       - src/pages/match_detail.rs
       - src/pages/profile.rs
       - src/pages/home.rs
       - src/pages/auth/register.rs
       - src/pages/stats.rs
       - src/pages/solo_dashboard.rs
       - src/pages/auth/login.rs
       - src/pages/team/dashboard.rs
       - src/pages/closed_beta.rs
       - src/pages/admin/invites.rs
       - src/pages/game_plan.rs
       - src/pages/legal/impressum.rs
       - src/pages/legal/datenschutz.rs

    6. **Delete `src/components/ornaments.rs`** (`rm src/components/ornaments.rs`). It must NOT exist after this task.

    7. Run `cargo check --features ssr` and `cargo check --features hydrate --target wasm32-unknown-unknown`. Both must pass before committing this task. Fix any unresolved imports that surface from missed migration sites.

    8. Commit: `refactor(18-01): migrate ornaments to region/ module + delete legacy ornaments.rs`
  </action>
  <verify>
    <automated>test ! -f src/components/ornaments.rs && cargo check --features ssr 2>&1 | tail -20 && cargo check --features hydrate --target wasm32-unknown-unknown 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -f src/components/ornaments.rs` exits 0 (old file deleted)
    - `test -f src/components/region/mod.rs` exits 0
    - `test -f src/components/region/ornaments.rs` exits 0
    - `grep -q "pub fn CompanionSigil" src/components/region/ornaments.rs` exits 0
    - `grep -q "pub mod region" src/components/mod.rs` exits 0
    - `! grep -q "pub mod ornaments" src/components/mod.rs` (old line gone)
    - `grep -rln "use crate::components::ornaments" src/ | wc -l` returns 0 (no remaining old imports)
    - `cargo check --features ssr` exits 0
    - `cargo check --features hydrate --target wasm32-unknown-unknown` exits 0
  </acceptance_criteria>
  <done>
    Old ornaments.rs deleted; region/ module exists with mod.rs + ornaments.rs (containing all 6 existing ornaments verbatim) + 6 stub submodules; all 17 import sites updated; both compile targets clean; committed.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Port typography + chrome + data_viz + solo primitives (region-neutral or string-prop)</name>
  <read_first>
    - .local-design-source/lol-team-companion-app/project/pages/_shared/components.jsx (FULL — primary source; sections: Display, Imperial, H, Eyebrow, Mono, Glitch, Stat, Sparkline, MoodMeter, RankBadge, LPProgress, ChampPortrait, ChampTile, RoleIcon, Icon)
    - .planning/phases/18-region-variants/18-UI-SPEC.md (§"Primitive Component API Contract" + §"Submodule inventory" — Phase 18 prop signatures)
    - .planning/phases/18-region-variants/18-PATTERNS.md (§"src/components/region/typography.rs", §"data_viz.rs", §"solo.rs", §"chrome.rs" — exact code patterns)
    - src/components/region/ornaments.rs (CompanionSigil reference for AnyView branch shape)
    - src/components/stat_card.rs (existing StatCard pattern — Stat primitive replaces this; do NOT delete stat_card.rs yet, leave for cleanup in 18-04/05)
    - input.css (semantic tokens + font CSS vars: --font-display, --font-imperial, --font-ui, --font-mono, --font-glitch)
    - .claude/rules/leptos-patterns.md (rules 19, 38: into_any() + recursion_limit)
  </read_first>
  <files>
    src/components/region/typography.rs
    src/components/region/chrome.rs
    src/components/region/data_viz.rs
    src/components/region/solo.rs
  </files>
  <action>
    Implement primitives in these 4 submodules. Reference the JSX source at `.local-design-source/.../_shared/components.jsx` for exact prop shapes, sizes, and class names. Translate JSX to Leptos using semantic tokens (NEVER raw hex — see CLAUDE.md "Code Style").

    **`src/components/region/typography.rs`** — 6 primitives:

    1. `Display { region: String, size: Option<String>, children: Children }` — size in {"xl","lg","md","sm"} mapping to text sizes [64,44,30,22]px (use `text-[64px]` etc.). Font-family from `--font-display`. Region-neutral classes (font controlled by `[data-theme]` CSS var). No region branching needed (font swaps automatically via the CSS var).
    2. `Imperial { children: Children, class: Option<String> }` — wraps children in `<span class="font-imperial uppercase tracking-[0.14em] {class}">`. No region prop.
    3. `H { level: u8, children: Children, class: Option<String> }` — `level` in 1..=3 maps to font sizes [30,22,17]px. Uses `--font-display`. No region prop.
    4. `Eyebrow { children: Children, class: Option<String> }` — `<div class="font-mono text-[10px] uppercase tracking-[0.16em] text-muted {class}">{children}</div>`. No region prop.
    5. `Mono { children: Children, class: Option<String> }` — `<span class="font-mono tabular-nums {class}">{children}</span>`. No region prop.
    6. `Glitch { region: String, children: ChildrenFn }` — region-branching. CRITICAL: use `ChildrenFn` (NOT `Children`) per 18-RESEARCH.md Pitfall 1, because both arms call children(). Demacia arm: plain Eyebrow treatment (`<span class="font-mono uppercase text-[10px] tracking-[0.16em] text-muted">{children()}</span>`). Pandemonium arm: glitch text-shadow (apply inline style — `text-shadow: -2px -2px 0 var(--accent-2), 2px 2px 0 var(--t-accent);` and font `--font-glitch`). Use `move || if is_pandemonium { ... .into_any() } else { ... .into_any() }`.

    **`src/components/region/chrome.rs`** — 4 primitives (all region-neutral; champion portrait/tile/role icon are visual identity not region-branching):

    1. `ChampPortrait { name: String, size: Option<u32>, kind: Option<String> }` — default size=64, default kind="square". Per 18-PATTERNS.md §chrome.rs, render `<img src=ddragon_url(name, kind)>` with `loading="lazy"` and inline style for object-fit/cover. Helper fn `champ_key(name: &str) -> String` strips spaces and special chars (mirror logic from existing `champion_picker.rs` or `data_dragon.rs`).
    2. `ChampTile { name: Option<String>, size: Option<u32>, banned: Option<bool>, locked: Option<bool>, dimmed: Option<bool> }` — default size=56. If `name` is Some, render `ChampPortrait` inside a 56×56 box; if banned, apply `grayscale` filter and diagonal bar overlay (absolute positioned `<div>` with rotated border or SVG `<line>`). If locked, render a small accent-colored dot in the top-right corner. If dimmed, apply `opacity-50`.
    3. `RoleIcon { role: String, size: Option<u32> }` — role in {"top","jungle","mid","adc","support"}. Use CSS mask-image with CommunityDragon SVG URLs (template: `https://raw.communitydragon.org/latest/plugins/rcp-fe-lol-static-assets/global/default/svg/position-{role}.svg`). Apply `bg-accent` for fill via `style="-webkit-mask: url(...) center/contain no-repeat; mask: url(...) center/contain no-repeat; background-color: var(--color-accent); width: {size}px; height: {size}px;"`. Default size=24. Touch-target wrapper at 44px per UI-SPEC accessibility note.
    4. `Icon { name: String, size: Option<u32> }` — inline SVG dispatch by name: at minimum support {"home","user","swords","tree","chart","calendar","sword","shield"}. Use match arm returning the corresponding `<svg viewBox="...">...</svg>`. Default size=18. Stroke uses `currentColor` so callers can color via `text-*` classes. For unknown names return an empty `<span></span>` so missing icons don't crash.

    **`src/components/region/data_viz.rs`** — 3 primitives (all region-neutral, font tokens carry the regional voice):

    1. `Stat { label: String, value: String, unit: Option<String>, delta: Option<f32> }` — label uses Eyebrow treatment, value uses `font-mono text-[22px] font-semibold tabular-nums text-primary`. If `unit` Some, render after value: `<span class="font-mono text-[12px] text-muted ml-1">{unit}</span>`. If `delta` Some, render colored: green via `text-success` (use existing semantic token; if missing, fall back to `text-accent`) for positive, red via `text-danger` for negative; format with leading + or -, 1 decimal place.
    2. `Sparkline { data: Vec<f64>, width: Option<u32>, height: Option<u32> }` — default width=120, height=32. SVG `<polyline points="..." stroke="var(--color-accent)" fill="none" stroke-width="1.2">`. Compute min/max from `data`, scale to width/height with 2px padding. Add gradient fill (`<defs><linearGradient><stop>...</stop></linearGradient></defs>` referenced by `<polygon>` closed shape with `fill="url(#sparkline-grad)"` at 30% opacity).
    3. `MoodMeter { value: f64 }` — clamp value to [0.0, 1.0]; render 5 horizontal segments via 5 absolutely-positioned divs OR a flex row with 5 children. Filled segments (count = `(value * 5.0).round() as usize`) use `bg-accent`; unfilled use `bg-elevated`. Each segment ~24px wide × 8px tall with 2px gap.

    **`src/components/region/solo.rs`** — 2 region-branching primitives:

    1. `RankBadge { tier: String, division: String, large: Option<bool> }` — tier in {"iron","bronze","silver","gold","platinum","emerald","diamond","master","grandmaster","challenger"}. Render an SVG shield (32×40 normal; 64×80 if `large=Some(true)`) with the tier name in Cinzel + division below in JetBrains Mono. Stroke color uses `var(--color-accent)`. Region-neutral (same shield both regions; tier colors via `[data-theme]` token swap on accent).
    2. `LPProgress { region: String, lp: u32, max: Option<u32> }` — default max=100. Region-branching with AnyView:
       - Demacia: rounded bar (`rounded-full`), gold gradient fill via inline style `background: linear-gradient(90deg, var(--gold-1) 0%, var(--gold-2) 50%, var(--gold-3) 100%);`. Width = `(lp * 100 / max)%`. Container has gilt border (`border border-outline/50 p-0.5`).
       - Pandemonium: square bar (`rounded-none`), flat fill via `bg-accent`. Width = `(lp * 100 / max)%`. Container has bracket-style border (`border-l-2 border-r-2 border-accent`).
       - Display LP/max numeric label below in mono font for Pandemonium, in Cormorant Garamond for Demacia.

    For Glitch + LPProgress: use the canonical AnyView branch pattern:
    ```rust
    let is_pandemonium = region == "pandemonium";
    move || if is_pandemonium {
        view! { /* pandemonium */ }.into_any()
    } else {
        view! { /* demacia */ }.into_any()
    }
    ```

    Run `cargo check --features ssr` and `cargo check --features hydrate --target wasm32-unknown-unknown` after each submodule. Both must pass before committing.

    Commit: `feat(18-01): port typography + chrome + data_viz + solo region primitives`
  </action>
  <verify>
    <automated>cargo check --features ssr 2>&1 | tail -10 && cargo check --features hydrate --target wasm32-unknown-unknown 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "pub fn Display" src/components/region/typography.rs`
    - `grep -q "pub fn Glitch" src/components/region/typography.rs`
    - `grep -q "ChildrenFn" src/components/region/typography.rs` (Glitch must use ChildrenFn not Children)
    - `grep -q "pub fn Eyebrow" src/components/region/typography.rs`
    - `grep -q "pub fn ChampPortrait" src/components/region/chrome.rs`
    - `grep -q "pub fn ChampTile" src/components/region/chrome.rs`
    - `grep -q "pub fn RoleIcon" src/components/region/chrome.rs`
    - `grep -q "pub fn Icon" src/components/region/chrome.rs`
    - `grep -q "pub fn Stat" src/components/region/data_viz.rs`
    - `grep -q "pub fn Sparkline" src/components/region/data_viz.rs`
    - `grep -q "pub fn MoodMeter" src/components/region/data_viz.rs`
    - `grep -q "pub fn RankBadge" src/components/region/solo.rs`
    - `grep -q "pub fn LPProgress" src/components/region/solo.rs`
    - `grep -q "is_pandemonium" src/components/region/solo.rs` (LPProgress must branch)
    - `grep -q "into_any()" src/components/region/typography.rs` (Glitch branches)
    - `! grep -nE "#[0-9a-fA-F]{6}" src/components/region/typography.rs src/components/region/chrome.rs src/components/region/data_viz.rs src/components/region/solo.rs` (no raw hex)
    - `cargo check --features ssr` exits 0
    - `cargo check --features hydrate --target wasm32-unknown-unknown` exits 0
  </acceptance_criteria>
  <done>
    Typography, chrome, data_viz, and solo submodules implement all 15 primitives. Region-branching primitives (Glitch, LPProgress) use the AnyView pattern with ChildrenFn where both arms render children. Both compile targets clean. Committed.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Port layout + controls primitives (region-branching with AnyView)</name>
  <read_first>
    - .local-design-source/lol-team-companion-app/project/pages/_shared/components.jsx (sections: Card, SectionHead, Themed, Btn, Badge)
    - .planning/phases/18-region-variants/18-PATTERNS.md (§"src/components/region/layout.rs" + §"src/components/region/controls.rs" — exact code patterns)
    - .planning/phases/18-region-variants/18-UI-SPEC.md (§"region/layout.rs" + §"region/controls.rs" — variant inventory)
    - src/components/region/ornaments.rs (existing GiltCorner reference for Card gilt variant)
    - src/components/theme_toggle.rs (existing button click + focus-visible:ring pattern)
    - .claude/rules/leptos-patterns.md (rule 19: into_any; rule 18: clone before multiple closures)
  </read_first>
  <files>
    src/components/region/layout.rs
    src/components/region/controls.rs
  </files>
  <action>
    **`src/components/region/layout.rs`** — 3 region-branching primitives:

    1. `Card { region: String, variant: Option<String>, children: ChildrenFn }` — variant in {"default","gilt","zine"}. Use ChildrenFn because both region arms render children. Pattern:
       ```rust
       use crate::components::region::ornaments::GiltCorner;
       #[component]
       pub fn Card(
           region: String,
           #[prop(optional, into)] variant: Option<String>,
           children: ChildrenFn,
       ) -> impl IntoView {
           let is_pandemonium = region == "pandemonium";
           let var = variant.unwrap_or_else(|| "default".to_string());
           let var_is_gilt = var == "gilt";
           let var_is_zine = var == "zine";
           move || if is_pandemonium {
               // Pandemonium: rounded-none, bracket corners when zine variant
               view! {
                   <div class="bg-surface border border-outline/30 rounded-none p-6 relative">
                       {if var_is_zine {
                           view! {
                               // 4 absolutely positioned 12px bracket corners using border-l/border-t etc.
                               <div class="absolute top-0 left-0 w-3 h-3 border-l-2 border-t-2 border-accent"></div>
                               <div class="absolute top-0 right-0 w-3 h-3 border-r-2 border-t-2 border-accent"></div>
                               <div class="absolute bottom-0 left-0 w-3 h-3 border-l-2 border-b-2 border-accent"></div>
                               <div class="absolute bottom-0 right-0 w-3 h-3 border-r-2 border-b-2 border-accent"></div>
                           }.into_any()
                       } else { view!{}.into_any() }}
                       {children()}
                   </div>
               }.into_any()
           } else {
               // Demacia: rounded-xl, gilt corners when gilt variant
               view! {
                   <div class="bg-surface border border-outline/50 rounded-xl p-6 relative"
                        style=if var_is_gilt { "box-shadow: inset 0 0 0 1px color-mix(in oklab, var(--color-accent) 15%, transparent);" } else { "" }>
                       {if var_is_gilt {
                           view! {
                               <div class="absolute top-1 left-1"><GiltCorner corner="tl" size=20 /></div>
                               <div class="absolute top-1 right-1"><GiltCorner corner="tr" size=20 /></div>
                               <div class="absolute bottom-1 left-1"><GiltCorner corner="bl" size=20 /></div>
                               <div class="absolute bottom-1 right-1"><GiltCorner corner="br" size=20 /></div>
                           }.into_any()
                       } else { view!{}.into_any() }}
                       {children()}
                   </div>
               }.into_any()
           }
       }
       ```

    2. `SectionHead { region: String, eyebrow: Option<String>, title: String, action: Option<Children> }` — region-branching:
       - Demacia: render Eyebrow (Cormorant Garamond italic uppercase) + H level 2 + optional action slot, separated by HeraldicDivider below
       - Pandemonium: render Glitch eyebrow with `// ` prefix + H level 2 in `font-glitch` uppercase + optional action slot, separated by a RiotTape strip below
       Pattern: same AnyView branch as Card. If `eyebrow` is Some, Pandemonium prepends `// ` to it.

    3. `Themed { region: String, children: ChildrenFn }` — sets `data-theme` attribute on a wrapper div for scoped theme application (used in component-level snapshot tests):
       ```rust
       view! {
           <div data-theme=region>
               {children()}
           </div>
       }
       ```
       NO region branching — just attribute passthrough.

    **`src/components/region/controls.rs`** — 2 primitives:

    1. `Btn { region: String, variant: Option<String>, size: Option<String>, on_click: Option<Callback<()>>, children: ChildrenFn }` — variant in {"primary","secondary","outline","ghost"}, size in {"sm","md","lg"}.
       - Demacia primary: gilt gradient background via inline style `background: linear-gradient(180deg, var(--gold-1) 0%, var(--gold-2) 50%, var(--gold-3) 100%); color: var(--ink, var(--t-accent-contrast)); border: 1px solid var(--gold-deep, var(--border-outline));`, font Cinzel uppercase
       - Pandemonium primary: `bg-accent text-accent-contrast rounded-none font-mono uppercase`, plus offset shadow via inline style `box-shadow: 3px 3px 0 var(--accent-2), 6px 6px 0 var(--accent-3);`
       - All variants/sizes use `focus-visible:ring-2 focus-visible:ring-accent/50 focus-visible:outline-none` per G-12.
       - `on_click` optional Callback<()>; if provided, wire to `on:click=move |_| { if let Some(cb) = on_click { cb.run(()); } }`. Use Callback::new pattern from leptos-patterns rule 36 if needed for closure-sharing.

    2. `Badge { tone: Option<String>, children: ChildrenFn }` — tone in {"neutral","accent","success","warning","danger","info"}. Region-neutral (same shape both regions). Per UI-SPEC: `<span class="inline-flex items-center gap-1 px-2 py-0.5 font-mono text-[10px] uppercase tracking-[0.12em] rounded-sm {tone_class}">`. Map tone to semantic token bg+text:
       - neutral → `bg-elevated text-muted`
       - accent → `bg-accent text-accent-contrast`
       - success → `bg-success/15 text-success` (if --color-success exists; else `bg-elevated text-secondary`)
       - warning → `bg-warning/15 text-warning`
       - danger → `bg-danger/15 text-danger`
       - info → `bg-info/15 text-info`
       If any tone token is missing in input.css, fall back to `bg-elevated text-muted` for unknown tones and add a comment noting the fallback.

    Run `cargo check --features ssr` and `cargo check --features hydrate --target wasm32-unknown-unknown` after this task. Both must pass.

    Commit: `feat(18-01): port Card + SectionHead + Themed + Btn + Badge region primitives`
  </action>
  <verify>
    <automated>cargo check --features ssr 2>&1 | tail -10 && cargo check --features hydrate --target wasm32-unknown-unknown 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "pub fn Card" src/components/region/layout.rs`
    - `grep -q "pub fn SectionHead" src/components/region/layout.rs`
    - `grep -q "pub fn Themed" src/components/region/layout.rs`
    - `grep -q "ChildrenFn" src/components/region/layout.rs` (Card and SectionHead use ChildrenFn)
    - `grep -q "is_pandemonium" src/components/region/layout.rs`
    - `grep -q "into_any()" src/components/region/layout.rs`
    - `grep -q "pub fn Btn" src/components/region/controls.rs`
    - `grep -q "pub fn Badge" src/components/region/controls.rs`
    - `grep -q "focus-visible:ring-2" src/components/region/controls.rs` (G-12 compliance)
    - `! grep -nE "outline:none" src/components/region/controls.rs` (G-12 — only with ring replacement)
    - `! grep -nE "#[0-9a-fA-F]{6}" src/components/region/layout.rs src/components/region/controls.rs` (no raw hex)
    - `cargo check --features ssr` exits 0
    - `cargo check --features hydrate --target wasm32-unknown-unknown` exits 0
  </acceptance_criteria>
  <done>
    Layout (Card, SectionHead, Themed) and controls (Btn, Badge) implement region branching via AnyView pattern with ChildrenFn. G-12 compliance verified (focus-visible ring on all interactive primitives). Both compile targets clean. Committed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Component prop boundary | `region: String` prop passed from page entry — values must be 'demacia' or 'pandemonium' |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01-01 | T (Tampering) | Region prop string | accept | No raw user input flows to `region` prop in this plan — value originates from `InitialTheme` context which is set by SSR auth flow from validated DB `theme` field (existing ASSERT IN ['demacia','pandemonium'] enforces values). No XSS surface; primitives never render `region` as HTML, only use it for class/attribute selection. |
| T-18-01-02 | I (Information disclosure) | DDragon URLs in ChampPortrait | accept | URLs are constructed from `name` parameter; name is validated upstream (only known champion names enter here from Riot API responses). No PII; champion names are public game data. |
| T-18-01-03 | E (Elevation of privilege) | Module reorganization | accept | Pure refactor of UI primitives; no new auth/authorization surfaces introduced. Existing import-site updates only change module paths, not access rules. |
</threat_model>

<verification>
After all 3 tasks complete:
- `cargo check --features ssr` exits 0
- `cargo check --features hydrate --target wasm32-unknown-unknown` exits 0
- `cargo test --features ssr --lib` passes (≥111 tests; no regression on existing tests)
- `ls src/components/region/` shows: chrome.rs, controls.rs, data_viz.rs, layout.rs, mod.rs, ornaments.rs, solo.rs, typography.rs
- `test ! -f src/components/ornaments.rs` exits 0
- `grep -rln "use crate::components::ornaments" src/` returns no results
</verification>

<success_criteria>
1. ~24 primitives compile cleanly on both targets
2. `src/components/region/` module replaces `src/components/ornaments.rs` per D-01/D-02
3. All region-branching primitives accept `region: String` as a prop (except legacy `CompanionSigil` which retains its `use_context` implementation)
4. `ChildrenFn` used wherever both region arms render children
5. `.into_any()` on both branch arms (avoids E0308)
6. G-12 compliance: every interactive primitive (Btn) has `focus-visible:ring-*`; no `outline:none` without ring
7. Zero raw hex colors in any `region/` file (semantic tokens only)
8. All 17 import sites updated to new module path
</success_criteria>

<output>
After completion, create `.planning/phases/18-region-variants/18-01-SUMMARY.md` documenting:
- Final primitive inventory (count per submodule)
- Confirmation that old ornaments.rs is deleted and all imports updated
- Whether `Region` enum was added (Claude's Discretion call — record decision)
- Decisions on tone palette tokens (any fallbacks used for missing success/warning/info tokens)
- File location for `PageLoading`/`PageEmpty` (defer to 18-02 — under `region/` or `src/components/skeleton.rs`)
- Reachability check: every primitive callable from `use crate::components::region::*;`
</output>

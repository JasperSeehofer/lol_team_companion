---
phase: 18-region-variants
plan: 03
type: execute
wave: 2
depends_on: [18-01, 18-02]
files_modified:
  - src/pages/tree_drafter.rs
  - src/pages/champion_pool.rs
  - src/pages/match_detail.rs
  - src/pages/post_game.rs
autonomous: true
requirements: [REQ-3]
tags: [region, ports, no-patch, tree-drafter, champion-pool, match-report]

must_haves:
  truths:
    - "/tree-drafter renders structurally different markup between Demacia and Pandemonium"
    - "/champion-pool renders structurally different markup between Demacia and Pandemonium"
    - "/match/:id renders structurally different markup between Demacia and Pandemonium"
    - "/post-game renders structurally different markup between Demacia and Pandemonium"
    - "Each page reads InitialTheme context ONCE at entry and passes region: String down as a prop"
    - "No use_context::<InitialTheme>() calls inside child components within these pages"
  artifacts:
    - path: "src/pages/tree_drafter.rs"
      provides: "Tree drafter region-aware port"
      contains: "use_context::<InitialTheme>"
    - path: "src/pages/champion_pool.rs"
      provides: "Champion pool region-aware port"
      contains: "use_context::<InitialTheme>"
    - path: "src/pages/match_detail.rs"
      provides: "Match report region-aware port"
      contains: "use_context::<InitialTheme>"
    - path: "src/pages/post_game.rs"
      provides: "Match report region-aware port (shared design with match_detail)"
      contains: "use_context::<InitialTheme>"
  key_links:
    - from: "src/pages/tree_drafter.rs"
      to: "src/components/region/*"
      via: "use crate::components::region::"
      pattern: "use crate::components::region::"
    - from: "src/pages/match_detail.rs"
      to: "src/pages/post_game.rs OR src/components/match_report.rs"
      via: "shared MatchReportView component (if extracted) or duplicated layout"
      pattern: "MatchReportView"
---

<objective>
Port the 3 "no-patch" page pairs from the canonical design source into existing Leptos routes:
- `tree-drafter` → `src/pages/tree_drafter.rs`
- `champion-pool` → `src/pages/champion_pool.rs`
- `match-report` → `src/pages/match_detail.rs` AND `src/pages/post_game.rs` (both surface `match-report` per UI-SPEC route mapping)

These pages have NO content-contract mismatches per `CONTENT-CONTRACT-AUDIT.md` — both Demacia and Pandemonium variants are already content-complete in the design source. The port is direct: read the JSX, translate to Leptos using region primitives from 18-01, preserve existing data-loading logic (server fns, Resources) verbatim.

Purpose: Demonstrate the region-port pattern on the lowest-risk pages first, before moving to patch-required pages (18-04, 18-05) and the heavy team-dashboard (18-06).

Decision required (CLAUDE'S DISCRETION from CONTEXT.md / 18-RESEARCH.md Open Question 1): match_detail.rs and post_game.rs both surface the `match-report` design page. Either (a) extract a shared `src/components/match_report.rs` consumed by both pages, or (b) duplicate the layout in each page. Decision: read both files first; if their existing data shapes are similar (both render scoreboard from `MatchDetail`), extract shared `MatchReportView { region, data }`; if they diverge (e.g., post_game.rs adds team-only learning panel), keep separate and duplicate the styling layer.

Output: 4 pages with Demacia↔Pandemonium structural difference, all dual-target compilable, using region primitives from 18-01.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.claude/rules/leptos-patterns.md
@.claude/rules/wasm-patterns.md
@.planning/phases/18-region-variants/18-SPEC.md
@.planning/phases/18-region-variants/18-CONTEXT.md
@.planning/phases/18-region-variants/18-UI-SPEC.md
@.planning/phases/18-region-variants/18-PATTERNS.md
@.planning/phases/18-region-variants/18-RESEARCH.md
@.planning/phases/18-region-variants/18-01-SUMMARY.md
@.planning/phases/18-region-variants/18-02-SUMMARY.md
@.local-design-source/lol-team-companion-app/project/pages/tree-drafter/demacia.jsx
@.local-design-source/lol-team-companion-app/project/pages/tree-drafter/pandemonium.jsx
@.local-design-source/lol-team-companion-app/project/pages/champion-pool/demacia.jsx
@.local-design-source/lol-team-companion-app/project/pages/champion-pool/pandemonium.jsx
@.local-design-source/lol-team-companion-app/project/pages/match-report/demacia.jsx
@.local-design-source/lol-team-companion-app/project/pages/match-report/pandemonium.jsx
@.local-design-source/lol-team-companion-app/project/pages/tree-drafter/README.md
@.local-design-source/lol-team-companion-app/project/pages/champion-pool/README.md
@.local-design-source/lol-team-companion-app/project/pages/match-report/README.md
@.local-design-source/lol-team-companion-app/project/scraps/CONTENT-CONTRACT-AUDIT.md
@src/pages/tree_drafter.rs
@src/pages/champion_pool.rs
@src/pages/match_detail.rs
@src/pages/post_game.rs
@src/app.rs

<interfaces>
<!-- Region context read pattern (use ONCE per page entry) -->
From 18-PATTERNS.md §"Region Prop at Page Entry":
```rust
use crate::app::InitialTheme;
use crate::components::region::*;
use crate::components::skeleton::{PageLoading, PageEmpty};

#[component]
pub fn TreeDrafterPage() -> impl IntoView {
    let theme = use_context::<InitialTheme>().unwrap_or_default();
    let region = theme.0.clone();
    // Pass `region` as String prop to all subcomponents
    // ... existing data loading (Resources, Effects, signals) unchanged ...
    view! {
        <Card region=region.clone()>
            <SectionHead region=region.clone() title="Tree Drafter" eyebrow=Some("Strategy".to_string()) />
            // ... existing tree graph rendering ...
        </Card>
    }
}
```

Region primitives available (from 18-01):
- Card { region, variant?, children: ChildrenFn }
- SectionHead { region, eyebrow?, title, action? }
- Btn { region, variant?, size?, on_click?, children: ChildrenFn }
- Badge { tone?, children: ChildrenFn }
- Stat { label, value, unit?, delta? }
- Sparkline { data, width?, height? }
- MoodMeter { value }
- RankBadge { tier, division, large? }
- LPProgress { region, lp, max? }
- ChampPortrait { name, size?, kind? }
- ChampTile { name?, size?, banned?, locked?, dimmed? }
- RoleIcon { role, size? }
- Icon { name, size? }
- Display { region, size?, children }
- H { level, children, class? }
- Eyebrow { children, class? }
- Mono { children, class? }
- Glitch { region, children: ChildrenFn }
- GiltCorner, HeraldicDivider, RiotTape, FleurDeLis, Crown, CompanionSigil (ornaments)

Skeleton primitives (from 18-02):
- PageLoading { region, variant: "draft"|"solo"|"team" }
- PageEmpty { region, kind: "draft"|"matches"|"team"|"pool"|"scout" }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Port tree-drafter + champion-pool (both single-design no-patch)</name>
  <read_first>
    - src/pages/tree_drafter.rs (FULL — existing page, ~1,610 LOC; must preserve all Resources, server fns, signal flow, tree-graph rendering)
    - src/pages/champion_pool.rs (FULL — existing page, ~1,356 LOC; must preserve champion pool CRUD + tier filter logic)
    - .local-design-source/.../tree-drafter/demacia.jsx + pandemonium.jsx (FULL — visual grammar reference)
    - .local-design-source/.../tree-drafter/README.md (region grammar definition)
    - .local-design-source/.../champion-pool/demacia.jsx + pandemonium.jsx (FULL)
    - .local-design-source/.../champion-pool/README.md
    - .planning/phases/18-region-variants/18-01-SUMMARY.md (final primitive signatures + any Region enum decision)
    - src/app.rs (InitialTheme context signature)
    - .claude/rules/leptos-patterns.md (rule 38: recursion_limit; rule 18: clone before closures)
  </read_first>
  <files>
    src/pages/tree_drafter.rs
    src/pages/champion_pool.rs
  </files>
  <action>
    **For each page (tree_drafter.rs, champion_pool.rs):**

    1. Add the region context read at the very top of the page component fn body (after any auth-check `Effect`):
       ```rust
       let theme = use_context::<InitialTheme>().unwrap_or_default();
       let region = theme.0.clone();
       ```
       Add the necessary imports at the top of the file:
       ```rust
       use crate::app::InitialTheme;
       use crate::components::region::*;
       use crate::components::skeleton::{PageLoading, PageEmpty};
       ```

    2. PRESERVE all existing data-loading code verbatim: server fn definitions, `Resource::new`, `RwSignal`, `Effect::new`, `spawn_local`, `Action::new`. Do NOT touch the business logic. The port is a PURELY VISUAL restructure.

    3. Wrap the page body in a top-level `Card region=region.clone()` (or a sequence of `Card`s for distinct sections). Use the canonical layout from the JSX source:
       - **tree-drafter Demacia**: vertical strip layout — top crest header + Display title "Tree Drafter" + HeraldicDivider + main SVG canvas area in a gilt Card + footer ledger of nodes
       - **tree-drafter Pandemonium**: monolithic grid — Glitch eyebrow `// TREE_DRAFTER` + bracket-cornered zine Card containing the SVG canvas, scattered glitch-text labels, RiotTape strip at top with name
       - **champion-pool Demacia**: 5 role columns (TOP/JG/MID/ADC/SUP) each with gilt Card listing tier-coded ChampTiles; Eyebrow tier labels (S/A/B/C); FleurDeLis separators between tiers
       - **champion-pool Pandemonium**: same 5-column structure but zine Cards with bracket corners, Glitch tier labels (S_TIER, A_TIER), accent border-left strips

    4. Replace existing low-level UI elements with region primitives:
       - Plain `<button>` → `<Btn region=region.clone() variant="primary".to_string()>` (or "secondary", "outline", "ghost")
       - Section titles → `<SectionHead region=region.clone() title="…".to_string() eyebrow=Some("…".to_string()) />`
       - Inline `<div class="bg-surface ...">` containers → `<Card region=region.clone()>`
       - Champion portraits/tiles → `<ChampPortrait name=… />` or `<ChampTile name=Some(…) banned=… />`
       - Stats displays → `<Stat label=… value=… delta=… />`

    5. For `<Suspense>` blocks: replace generic loading text with `<PageLoading region=region.clone() variant="draft".to_string() />` (for tree_drafter use "draft"; for champion_pool use "draft" — both are draft-strategy pages).

    6. For empty-state branches (e.g., "no champions in pool yet"): replace with `<PageEmpty region=region.clone() kind="pool".to_string() />`.

    7. **CRITICAL recursion-limit safety** (leptos-patterns rule 38): if either page already nests deep view! macros, split the body into smaller `#[component]` subfunctions (e.g., `TreeDrafterCanvas { region, ... }`, `TreeDrafterLedger { region, ... }`). Each subcomponent reads `region` as a `String` prop — never re-reads context. This avoids compounding recursion in a single view! tree.

    8. **Clone discipline** (leptos-patterns rule 18): `region: String` is not Copy. Clone before every closure capture: `let region_for_card = region.clone();`. Use `region.clone()` inline within `view!` macro for prop passing.

    9. Run `cargo check --features ssr` and `cargo check --features hydrate --target wasm32-unknown-unknown` after each file. Both must pass.

    10. After each page, screenshot via agent-browser OR run `cd e2e && npx playwright test smoke.spec.ts -g "tree-drafter\|champion-pool"` to verify the page still renders. (Visual approval happens in 18-10 audit; this task is structural correctness.)

    Commit (atomic per page):
    - `feat(18-03): port tree-drafter to region primitives (no-patch direct port)`
    - `feat(18-03): port champion-pool to region primitives (no-patch direct port)`
  </action>
  <verify>
    <automated>grep -q "use crate::app::InitialTheme" src/pages/tree_drafter.rs && grep -q "use crate::components::region" src/pages/tree_drafter.rs && grep -q "use crate::app::InitialTheme" src/pages/champion_pool.rs && grep -q "use crate::components::region" src/pages/champion_pool.rs && cargo check --features ssr 2>&1 | tail -5 && cargo check --features hydrate --target wasm32-unknown-unknown 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "let theme = use_context::<InitialTheme>" src/pages/tree_drafter.rs` exits 0
    - `grep -q "let theme = use_context::<InitialTheme>" src/pages/champion_pool.rs` exits 0
    - `grep -c "use_context::<InitialTheme>" src/pages/tree_drafter.rs` returns exactly 1 (read ONCE at page entry, not in sub-views)
    - `grep -c "use_context::<InitialTheme>" src/pages/champion_pool.rs` returns exactly 1
    - `grep -q "Card region=" src/pages/tree_drafter.rs` OR `grep -q "<Card" src/pages/tree_drafter.rs` exits 0
    - `grep -q "SectionHead\|<Btn\|<ChampTile\|<ChampPortrait" src/pages/tree_drafter.rs` exits 0
    - `grep -q "Card region=\|<Card" src/pages/champion_pool.rs` exits 0
    - `grep -q "SectionHead\|<Btn\|<ChampTile" src/pages/champion_pool.rs` exits 0
    - `! grep -nE "#[0-9a-fA-F]{6}" src/pages/tree_drafter.rs src/pages/champion_pool.rs` (no raw hex)
    - `cargo check --features ssr` exits 0
    - `cargo check --features hydrate --target wasm32-unknown-unknown` exits 0
  </acceptance_criteria>
  <done>
    tree_drafter.rs and champion_pool.rs use region primitives; InitialTheme read ONCE per page; region passed as String prop to all subcomponents; both compile targets clean; per-page atomic commits.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Port match-report into match_detail.rs + post_game.rs (decide shared component)</name>
  <read_first>
    - src/pages/match_detail.rs (FULL — must preserve scoreboard rendering + timeline events + on-demand fetch)
    - src/pages/post_game.rs (FULL — must preserve post-game review form + learning panel)
    - .local-design-source/.../match-report/demacia.jsx + pandemonium.jsx (FULL — visual grammar)
    - .local-design-source/.../match-report/README.md
    - .planning/phases/18-region-variants/18-RESEARCH.md (§"Open Questions" item 1: shared component decision)
    - .planning/phases/18-region-variants/18-01-SUMMARY.md
  </read_first>
  <files>
    src/pages/match_detail.rs
    src/pages/post_game.rs
    (optional) src/components/match_report.rs
  </files>
  <action>
    1. **Decide shared vs. duplicated layout** (Claude's Discretion per 18-RESEARCH.md):
       - Read both `match_detail.rs` and `post_game.rs` headers + main render bodies
       - Inspect the data shapes each consumes (likely both consume `MatchDetail` from `models/match_data.rs` or similar)
       - If their renders are STRUCTURALLY similar (both render 10-player scoreboard from same model), extract shared:
         ```rust
         // src/components/match_report.rs (NEW)
         use leptos::prelude::*;
         use crate::components::region::*;
         use crate::models::match_data::MatchDetail; // or whatever the existing type is

         #[component]
         pub fn MatchReportView(
             region: String,
             data: MatchDetail,
             #[prop(optional)] show_learning_cta: Option<bool>,
         ) -> impl IntoView {
             let is_pandemonium = region == "pandemonium";
             // ... region-branching layout per match-report/{demacia,pandemonium}.jsx ...
         }
         ```
         Register: `pub mod match_report;` in `src/components/mod.rs`. Both `match_detail.rs` and `post_game.rs` then render `<MatchReportView region=region data=match show_learning_cta=Some(true_or_false) />`.
       - If their renders DIVERGE (e.g., post_game.rs adds a team-only learning panel that match_detail.rs lacks), keep them separate. Each gets its own `let theme = use_context::<InitialTheme>()` + duplicated styling layer.
       - Record the decision in the eventual SUMMARY.md.

    2. **Apply region structure** (regardless of shared/separate path):
       - Demacia match-report: parchment scoreboard with team rows separated by HeraldicDivider; player rows have gilt outlines; gold accent on winning team header; timeline events as a vertical Cinzel-labeled ribbon on the right; "Add to Journal" → `<Btn region variant="primary">Add to Journal</Btn>`
       - Pandemonium match-report: zine-style scoreboard with bracket corners; Glitch headers for team names (`// BLUE_SIDE`, `// RED_SIDE`); RiotTape strip at top with match outcome; timeline events as a horizontal scanline-bordered strip with monospace labels; "Add to Journal" → `<Btn region variant="primary">ADD_JOURNAL</Btn>`

    3. Per-component champion/role rendering: replace existing `<img>` with `<ChampPortrait name=… />`; replace inline role labels with `<RoleIcon role=… />`.

    4. Preserve `<Suspense fallback>` blocks but swap the fallback to `<PageLoading region=region.clone() variant="solo".to_string() />` (match-report is per-game review, closest to solo variant).

    5. Per leptos-patterns rule 38: if `post_game.rs` already pushes against recursion limit (it has `#![recursion_limit = "512"]` cited in CLAUDE.md and MEMORY.md), keep its view! tree shallow by extracting subviews (`ScoreboardSection`, `TimelineSection`, etc.) as `#[component]` fns receiving `region: String` props.

    6. Run `cargo check --features ssr` and `cargo check --features hydrate --target wasm32-unknown-unknown` after each file. Both must pass.

    Commit (atomic):
    - `feat(18-03): port match-report into match_detail.rs (+ extract MatchReportView if shared)`
    - `feat(18-03): port match-report into post_game.rs`
  </action>
  <verify>
    <automated>grep -q "use crate::app::InitialTheme" src/pages/match_detail.rs && grep -q "use crate::app::InitialTheme" src/pages/post_game.rs && cargo check --features ssr 2>&1 | tail -5 && cargo check --features hydrate --target wasm32-unknown-unknown 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "let theme = use_context::<InitialTheme>" src/pages/match_detail.rs` exits 0
    - `grep -q "let theme = use_context::<InitialTheme>" src/pages/post_game.rs` exits 0
    - `grep -c "use_context::<InitialTheme>" src/pages/match_detail.rs` returns 1 (or 0 if shared MatchReportView reads region as prop only)
    - `grep -c "use_context::<InitialTheme>" src/pages/post_game.rs` returns 1
    - `grep -q "Card region=\|<Card\|MatchReportView\|<Btn region\|SectionHead region" src/pages/match_detail.rs` exits 0
    - `grep -q "Card region=\|<Card\|MatchReportView\|<Btn region\|SectionHead region" src/pages/post_game.rs` exits 0
    - If MatchReportView extracted: `test -f src/components/match_report.rs` exits 0 AND `grep -q "pub mod match_report" src/components/mod.rs` exits 0
    - `! grep -nE "#[0-9a-fA-F]{6}" src/pages/match_detail.rs src/pages/post_game.rs` (no raw hex)
    - `cargo check --features ssr` exits 0
    - `cargo check --features hydrate --target wasm32-unknown-unknown` exits 0
  </acceptance_criteria>
  <done>
    match_detail.rs and post_game.rs port the match-report design with region branching. Decision on shared MatchReportView extraction is made and applied consistently. Both compile targets clean. Atomic commits.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Region prop boundary | `region: String` passed from page entry to all subviews |
| Server fn boundary | All `#[server]` fns preserved — auth check (`auth.user.ok_or_else`) intact |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-03-01 | T (Tampering) | Region prop | accept | Region validated upstream (DB ASSERT). No XSS surface in primitives. |
| T-18-03-02 | I (Information disclosure) | match-report scoreboard | accept | Same data exposure as Phase 13/14; no new fields exposed. Visual restructure only. |
| T-18-03-03 | E (Elevation of privilege) | Page rendering | accept | No server fn signatures change; auth gates remain (per leptos-patterns rule 11/12). Pure visual restructure. |
</threat_model>

<verification>
After both tasks complete:
- `cargo check --features ssr` exits 0
- `cargo check --features hydrate --target wasm32-unknown-unknown` exits 0
- `cargo test --features ssr --lib` passes (no regression on existing tests; ≥111 tests)
- Existing e2e tests for these pages still pass: `cd e2e && npx playwright test smoke.spec.ts pages.spec.ts`
- 4 pages each read `InitialTheme` ONCE at entry
- No raw hex colors in any modified file
</verification>

<success_criteria>
1. /tree-drafter, /champion-pool, /match/:id, /post-game each render structurally different markup between Demacia and Pandemonium (verified visually + via region primitives in source)
2. Existing data-loading, server fn, and signal flow is preserved verbatim — no business logic change
3. region: String prop passed down; no use_context inside subcomponents
4. Both compile targets clean
5. Atomic commits per page
6. Decision on shared MatchReportView extraction documented
</success_criteria>

<output>
After completion, create `.planning/phases/18-region-variants/18-03-SUMMARY.md` documenting:
- Decision on shared MatchReportView extraction (yes/no + rationale + LOC saved if shared)
- Each page's pre-port LOC vs post-port LOC (rough indicator of structural change)
- Any recursion-limit issues encountered + how resolved (subcomponent extraction)
- Confirmation that no business logic was changed
</output>

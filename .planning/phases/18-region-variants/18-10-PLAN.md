---
phase: 18-region-variants
plan: 10
type: execute
wave: 5
depends_on: [18-08, 18-09]
files_modified:
  - .planning/phases/18-region-variants/18-UI-REVIEW.md
autonomous: false
requirements: [REQ-7, REQ-8]
tags: [region, audit, ui-review, 6-pillar, checkpoint, utility-route-verification]

must_haves:
  truths:
    - "18-UI-REVIEW.md exists with per-page per-region 6-pillar audit for 11 scoped pages"
    - "No FAIL verdicts; no open HIGH/CRITICAL findings"
    - "Utility routes (13+ pages) have ZERO new region conditionals verified by grep (REQ-7)"
    - "Existing Phase-17 utility-route Demacia baselines still match within maxDiffPixelRatio: 0.02"
    - "User performs manual side-by-side visual review on each of 11 scoped page pairs"
  artifacts:
    - path: ".planning/phases/18-region-variants/18-UI-REVIEW.md"
      provides: "6-pillar audit verdicts per page per region"
      contains: "Visual coherence"
  key_links:
    - from: ".planning/phases/18-region-variants/18-UI-REVIEW.md"
      to: "/gsd-ui-review audit findings"
      via: "audit narrative"
      pattern: "Verdict"
---

<objective>
Run the 6-pillar audit on Demacia + Pandemonium for each of 11 scoped pages, produce `18-UI-REVIEW.md` documenting per-region verdicts, verify utility routes have ZERO new region conditionals (REQ-7), and get user sign-off via a final manual side-by-side visual review checkpoint.

The 6 audit pillars (from Phase 17 UI review):
1. Visual coherence — does the region's grammar feel internally consistent across pages?
2. Accessibility — G-12 focus-visible:ring on every interactive; semantic tokens (no raw hex); keyboard nav
3. Responsiveness — desktop-first (mobile out of scope per SPEC); no `md:`/`lg:`/`xl:` breakpoints introduced under Pandemonium conditionals
4. Information density — required content visible per content contract (mismatch patches landed correctly)
5. Microinteractions — toggle UX feels native; loading skeletons + empty states render
6. Performance — no new heavy dependencies; existing recursion-limit-512 still sufficient; no compile-time blowup

The 11 scoped pages:
- /draft × {carousel, war-table, ledger} = 3 design pages
- /solo × {constellation, forge, journal} = 3
- /team/dashboard × {dashboard, brief} = 2
- /tree-drafter, /champion-pool, /match/:id, /post-game = 4 (match/post-game share match-report; AUDIT them once but verify both Leptos routes render correctly)

Output: 18-UI-REVIEW.md with per-region per-pillar verdict tables; utility-route grep verification documented; user manual approval recorded.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/18-region-variants/18-SPEC.md
@.planning/phases/18-region-variants/18-CONTEXT.md
@.planning/phases/18-region-variants/18-UI-SPEC.md
@.planning/phases/18-region-variants/18-01-SUMMARY.md
@.planning/phases/18-region-variants/18-02-SUMMARY.md
@.planning/phases/18-region-variants/18-03-SUMMARY.md
@.planning/phases/18-region-variants/18-04-SUMMARY.md
@.planning/phases/18-region-variants/18-05-SUMMARY.md
@.planning/phases/18-region-variants/18-06-SUMMARY.md
@.planning/phases/18-region-variants/18-07-SUMMARY.md
@.planning/phases/18-region-variants/18-08-SUMMARY.md
@.planning/phases/18-region-variants/18-09-SUMMARY.md
@.planning/phases/17-ui-consolidation/17-UI-REVIEW.md

<interfaces>
<!-- Utility routes (REQ-7) — must have ZERO new region conditionals: -->
- src/pages/auth/login.rs
- src/pages/auth/register.rs
- src/pages/admin/invites.rs
- src/pages/legal/impressum.rs
- src/pages/legal/datenschutz.rs
- src/pages/stats.rs
- src/pages/analytics.rs (if exists)
- src/pages/team/roster.rs
- src/pages/team_builder.rs
- src/pages/opponents.rs
- src/pages/action_items.rs
- src/pages/personal_learnings.rs (if exists)
- src/pages/profile.rs
- src/pages/closed_beta.rs
- src/pages/game_plan.rs

<!-- Per SPEC acceptance criteria: -->
- grep -rE "is_pandemonium|theme == \"pandemonium\"" against the above files MUST return ZERO hits
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Verify utility routes have zero new region conditionals (REQ-7 grep gate)</name>
  <read_first>
    - .planning/phases/18-region-variants/18-SPEC.md (§Acceptance Criteria — utility routes must have zero is_pandemonium/theme == "pandemonium")
    - All utility-route source files listed in `<interfaces>` above
  </read_first>
  <files>
    .planning/phases/18-region-variants/18-UI-REVIEW.md (start of file)
  </files>
  <action>
    1. Run the grep gate per SPEC acceptance criteria. For each utility-route file in the list below, verify NO matches of `is_pandemonium` or `theme == "pandemonium"`:
       ```bash
       grep -rnE 'is_pandemonium|theme == "pandemonium"' \
         src/pages/auth/login.rs \
         src/pages/auth/register.rs \
         src/pages/admin/invites.rs \
         src/pages/legal/impressum.rs \
         src/pages/legal/datenschutz.rs \
         src/pages/stats.rs \
         src/pages/team/roster.rs \
         src/pages/team_builder.rs \
         src/pages/opponents.rs \
         src/pages/action_items.rs \
         src/pages/profile.rs \
         src/pages/closed_beta.rs \
         src/pages/game_plan.rs \
         2>/dev/null
       ```
       Expected: ZERO output lines (no matches). If any file matches, INVESTIGATE — the import-site updates from 18-01 should NOT have introduced conditionals; if they did, file revision back to 18-01 with the offending file path.

       For src/pages/analytics.rs and src/pages/personal_learnings.rs: check if they exist first (`test -f`); only grep if they do. The orchestrator's CONTEXT.md mentions them but they may or may not be present.

    2. Run the existing Phase-17 visual-regression check on utility routes:
       ```bash
       cd e2e && npx playwright test visual-regression.spec.ts -g "public-|authed-profile|authed-team-roster|authed-team-builder|authed-opponents|authed-action-items|authed-stats|authed-analytics|authed-admin-invites|authed-game-plan|authed-personal-learnings"
       ```
       Per SPEC: utility baselines must match within `maxDiffPixelRatio: 0.02` after Phase 18 lands. If any utility test fails, it indicates 18-01's import-site updates regressed a utility page — file revision.

    3. **Start `.planning/phases/18-region-variants/18-UI-REVIEW.md`** with a top section recording the grep gate verification:
       ```markdown
       # Phase 18 Region Variants — UI Review

       **Audit date:** 2026-05-14 (adjust to actual)
       **Auditor:** gsd-ui-checker (this plan)

       ## REQ-7 Verification: Utility routes have zero new region conditionals

       Run on {date}:
       ```bash
       grep -rnE 'is_pandemonium|theme == "pandemonium"' src/pages/auth/login.rs ...
       ```
       **Result:** {0 matches | N matches in <file>}

       ## REQ-7 Verification: Utility baselines unchanged within tolerance

       `cd e2e && npx playwright test visual-regression.spec.ts -g "public-|authed-..."` exits {0|N}.

       Per SPEC acceptance: 15 utility baselines from Phase 17 still match within `maxDiffPixelRatio: 0.02`.

       ## 6-Pillar Audit (per scoped page per region)

       _Populated by Task 2 below._
       ```

    4. Commit: `docs(18-10): start 18-UI-REVIEW.md with utility-route REQ-7 grep verification`
  </action>
  <verify>
    <automated>OUT=$(grep -rnE 'is_pandemonium|theme == "pandemonium"' src/pages/auth/login.rs src/pages/auth/register.rs src/pages/admin/invites.rs src/pages/legal/impressum.rs src/pages/legal/datenschutz.rs src/pages/stats.rs src/pages/team/roster.rs src/pages/team_builder.rs src/pages/opponents.rs src/pages/action_items.rs src/pages/profile.rs src/pages/closed_beta.rs src/pages/game_plan.rs 2>/dev/null) && [ -z "$OUT" ] && test -f .planning/phases/18-region-variants/18-UI-REVIEW.md && grep -q "REQ-7 Verification" .planning/phases/18-region-variants/18-UI-REVIEW.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rnE 'is_pandemonium|theme == "pandemonium"' src/pages/auth/login.rs src/pages/auth/register.rs src/pages/admin/invites.rs src/pages/legal/impressum.rs src/pages/legal/datenschutz.rs src/pages/stats.rs src/pages/team/roster.rs src/pages/team_builder.rs src/pages/opponents.rs src/pages/action_items.rs src/pages/profile.rs src/pages/closed_beta.rs src/pages/game_plan.rs 2>/dev/null` returns empty output (ZERO matches)
    - `test -f .planning/phases/18-region-variants/18-UI-REVIEW.md` exits 0
    - `grep -q "REQ-7 Verification" .planning/phases/18-region-variants/18-UI-REVIEW.md` exits 0
    - `grep -q "Utility routes have zero" .planning/phases/18-region-variants/18-UI-REVIEW.md` exits 0
    - Utility-route visual-regression tests pass (15 baselines exit 0 in the targeted run)
  </acceptance_criteria>
  <done>
    Grep gate verified zero matches across 13 utility files; utility baselines still pass; 18-UI-REVIEW.md initialized with REQ-7 section; committed.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Run 6-pillar audit + populate 18-UI-REVIEW.md with per-region per-page verdicts</name>
  <read_first>
    - .planning/phases/17-ui-consolidation/17-UI-REVIEW.md (FULL — structural reference for the 6-pillar table format)
    - .planning/phases/18-region-variants/18-UI-REVIEW.md (current state from Task 1)
    - .planning/phases/18-region-variants/18-09-SUMMARY.md (baseline count + region-diff results)
    - .planning/phases/18-region-variants/18-08-SUMMARY.md (mode toggle wiring final state)
    - .planning/phases/18-region-variants/18-06-SUMMARY.md (team-dashboard Pandemonium rebuild — placeholder vs real data per section)
  </read_first>
  <files>
    .planning/phases/18-region-variants/18-UI-REVIEW.md
  </files>
  <action>
    For each of the 11 scoped pages × 2 regions, audit against the 6 pillars and record a Verdict (PASS | PASS-WITH-DEFERRED | FAIL) + findings table.

    Approach (per page):
    1. Start the dev server in background: `cargo leptos watch &`; wait for ready
    2. Visit the page in browser via agent-browser at both regions; take screenshots
    3. Check each pillar:
       - **Visual coherence:** Does the markup match the design source? Are region primitives used consistently? (Compare with `.local-design-source/.../{page-slug}/{region}.jsx`)
       - **Accessibility:** Run `grep -nE 'outline:none' src/pages/{page}.rs` — must be 0 OR every match must also have a `focus-visible:ring-*` sibling. Run `grep -nE '#[0-9a-fA-F]{6}' src/pages/{page}.rs` — must be 0 (semantic tokens only, exception: `text-white` on colored buttons per CLAUDE.md).
       - **Responsiveness:** Run `grep -nE '(md|lg|xl):' src/pages/{page}.rs | grep -E 'pandemonium' || true` — must be empty. Pandemonium variants must NOT introduce new breakpoint classes (per SPEC out-of-scope: mobile responsiveness).
       - **Information density:** Content contract from CONTENT-CONTRACT-AUDIT.md + 18-07-CONTENT-CONTRACTS.md — every required element grep-able in source (already verified by 18-04/05/06 acceptance criteria; cross-link those).
       - **Microinteractions:** Mode toggle reacts to clicks (e2e smoke); Suspense fallback shows PageLoading; empty state shows PageEmpty (visited manually if applicable).
       - **Performance:** `cargo check --features ssr` and `cargo check --features hydrate --target wasm32-unknown-unknown` both exit 0; no new heavy npm deps introduced (only pixelmatch + pngjs which are 18-09 e2e-only); recursion-limit-512 still sufficient.

    4. Append the audit narrative to `18-UI-REVIEW.md`:
       ```markdown
       ## 6-Pillar Audit: /draft (carousel)

       ### Demacia

       | Pillar | Verdict | Findings |
       |--------|---------|----------|
       | Visual coherence | PASS | Gilt Cards + HeraldicDivider + Cinzel labels per design source |
       | Accessibility | PASS | Btn primitive uses focus-visible:ring-2; no raw hex |
       | Responsiveness | PASS | No new breakpoints under conditionals |
       | Information density | PASS | onDeck halo present (CONTENT-CONTRACT-AUDIT mismatch patch applied) |
       | Microinteractions | PASS | Mode toggle functional; PageLoading draft variant renders |
       | Performance | PASS | Both compile targets clean |

       ### Pandemonium

       | Pillar | Verdict | Findings |
       |--------|---------|----------|
       | Visual coherence | PASS | Zine Cards + RiotTape + Glitch labels; bg-scanline applied |
       | Accessibility | PASS | Btn primitive uses focus-visible:ring-2; no raw hex |
       | Responsiveness | PASS | No new breakpoints |
       | Information density | PASS | Confidence "0.71" + "1,400 similar comps" present per AUDIT |
       | Microinteractions | PASS | Mode toggle functional |
       | Performance | PASS | Both compile targets clean |

       **Verdict for /draft (carousel):** PASS

       ## 6-Pillar Audit: /draft (war-table)
       ... [same structure] ...

       ## 6-Pillar Audit: /draft (ledger)
       ... [same structure] ...

       ## 6-Pillar Audit: /solo (constellation)
       ... [same structure] ...

       ## 6-Pillar Audit: /solo (forge)
       ... [same structure] ...

       ## 6-Pillar Audit: /solo (journal)
       ... [same structure] ...

       ## 6-Pillar Audit: /team/dashboard (dashboard)
       ... [same structure] ...

       ## 6-Pillar Audit: /team/dashboard (brief)
       ... [same structure] ...

       ## 6-Pillar Audit: /tree-drafter
       ... [same structure, no mode subsection] ...

       ## 6-Pillar Audit: /champion-pool
       ... [same structure] ...

       ## 6-Pillar Audit: /match-report (covers /match/:id + /post-game per UI-SPEC route mapping)
       ... [same structure] ...

       ## Open Findings

       _List any non-PASS findings here. Each finding includes:_
       - ID (UI-18-XX)
       - Severity (LOW | MEDIUM | HIGH | CRITICAL)
       - Page + region affected
       - Description
       - Disposition: FIX_NOW | DEFER (with rationale)

       _Per SPEC acceptance: NO open HIGH/CRITICAL findings. MEDIUM/LOW deferrals allowed with explicit justification._

       ## Summary

       Total scoped pages audited: 11
       Total (page × region) verdicts: 22
       PASS: ?
       PASS-WITH-DEFERRED: ?
       FAIL: ? (must be 0)
       Open HIGH/CRITICAL: ? (must be 0)
       ```

    5. If any FAIL surfaces OR an open HIGH/CRITICAL finding is unavoidable, the plan does NOT proceed to Task 3 (user checkpoint). Instead, file revisions back to the appropriate Wave 2/3 plan (18-03 through 18-07) and rerun the audit afterward.

    6. Commit: `docs(18-10): populate 18-UI-REVIEW.md with 6-pillar audit verdicts for 11 scoped pages`
  </action>
  <verify>
    <automated>grep -c "6-Pillar Audit" .planning/phases/18-region-variants/18-UI-REVIEW.md | awk '{exit ($1<11)?1:0}' && grep -c "Demacia\|Pandemonium" .planning/phases/18-region-variants/18-UI-REVIEW.md | awk '{exit ($1<22)?1:0}' && ! grep -qE "FAIL$" .planning/phases/18-region-variants/18-UI-REVIEW.md && ! grep -qE "status:\s*open.*(HIGH|CRITICAL)" .planning/phases/18-region-variants/18-UI-REVIEW.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "6-Pillar Audit" .planning/phases/18-region-variants/18-UI-REVIEW.md` returns at least 11 (one section per scoped page)
    - `grep -c "### Demacia" .planning/phases/18-region-variants/18-UI-REVIEW.md` returns at least 11
    - `grep -c "### Pandemonium" .planning/phases/18-region-variants/18-UI-REVIEW.md` returns at least 11
    - `grep -c "Verdict for" .planning/phases/18-region-variants/18-UI-REVIEW.md` returns at least 11 (one verdict per page)
    - `! grep -qE "Verdict.*FAIL" .planning/phases/18-region-variants/18-UI-REVIEW.md` (no FAIL — per SPEC)
    - `! grep -qE "status:\s*open.*(HIGH|CRITICAL)" .planning/phases/18-region-variants/18-UI-REVIEW.md` (no open HIGH/CRITICAL)
    - `grep -q "Open Findings" .planning/phases/18-region-variants/18-UI-REVIEW.md` exits 0
    - `grep -q "Summary" .planning/phases/18-region-variants/18-UI-REVIEW.md` exits 0
  </acceptance_criteria>
  <done>
    18-UI-REVIEW.md has 22 region-scoped audit verdicts across 11 scoped pages. No FAIL; no open HIGH/CRITICAL. Open findings (if any) are MEDIUM/LOW with explicit disposition. Committed.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: User manual side-by-side visual review of 11 scoped page pairs</name>
  <what-built>
    Phase 18 complete:
    - 24 region-aware primitives under src/components/region/
    - PageLoading + PageEmpty per-region skeleton + empty states
    - 11 scoped pages ported with Demacia and Pandemonium structural variants
    - Mode toggles wired on /draft, /solo, /team/dashboard with DB persistence
    - 26 visual-regression baselines + region-diff spec asserting pixelDiffRatio > 0.40
    - 6-pillar audit complete with 22 verdicts in 18-UI-REVIEW.md
    - Utility routes verified untouched (zero new region conditionals)
  </what-built>
  <how-to-verify>
    1. Start dev server in background: `cargo leptos watch`
    2. Wait for ready: `./scripts/wait_for_server.sh 120`
    3. For each of the 11 scoped pages, open both regions side-by-side and confirm "genuinely different":

       **Multi-mode pages — visit each mode in each region:**
       - `/draft` carousel — Demacia (gilt + Cinzel + carousel layout) vs Pandemonium (zine + mono + scanline)
       - `/draft` war-table — Demacia (gilt + composition pillars) vs Pandemonium (war-table layout in zine grammar)
       - `/draft` ledger — Demacia (medieval double-entry) vs Pandemonium (brutalist ledger)
       - `/solo` constellation — Demacia (starry-night + parchment) vs Pandemonium (forge in constellation mode... or sub-default)
       - `/solo` forge — Demacia (smith's workbench) vs Pandemonium (locker workbench)
       - `/solo` journal — Demacia (parchment diary) vs Pandemonium (photocopied fanzine)
       - `/team/dashboard` dashboard — Demacia (gilt strategy room) vs Pandemonium (full 7-section data surface)
       - `/team/dashboard` brief — Demacia (newspaper) vs Pandemonium (xeroxed zine)

       **Single-mode pages:**
       - `/tree-drafter` — Demacia vs Pandemonium
       - `/champion-pool` — Demacia vs Pandemonium
       - `/match/:id` (use a real match ID from your dev DB) — Demacia vs Pandemonium
       - `/post-game` — Demacia vs Pandemonium

    4. For each pair, confirm:
       - The two regions render STRUCTURALLY different (not just different colors)
       - The mismatch patches are visible:
         - draft-carousel Pandemonium: "conf 0.71 · 1,400 similar comps" label per pick
         - draft-carousel Demacia: gold ring/halo on next-pick slot
         - draft-war-table Demacia: 4 composition pillars (DPS/FRONT/POKE/UTIL) + composite score "81 / 100"
         - solo-constellation Demacia: pool-gap panel ("No reliable engage support"), last-10 W/L pip row, sort/filter buttons
         - solo-constellation Pandemonium: tier crest + 4-card grid (KDA/CS/min/DMG Share/Vision/min)
         - team-dashboard Pandemonium: all 7 sections present (RiotTape header, 5-col roster with MoodMeter, captain's note, reasoned bans, pool-ready, their pattern, threats with "If you let it through:")
       - Mode toggle works (click it, view changes) and persists across page reload
       - PageLoading skeleton renders on initial load (visit a slow page to see it)
       - Region toggle in nav still works (Demacia ↔ Pandemonium global swap)

    5. Compare against Phase 17 utility routes (login, profile, opponents, etc.) — they should LOOK IDENTICAL to before Phase 18 (no structural change, only color-token swap on theme switch).

    If any pair feels like just-color-swap (not structurally different), name the route+mode and what's wrong (e.g., "/post-game Pandemonium feels identical to Demacia just darker"). Revisions route back to the relevant port plan (18-03 / 18-04 / 18-05 / 18-06 / 18-07).

    If all pairs feel genuinely different, type "approved" to close Phase 18.
  </how-to-verify>
  <resume-signal>Type "approved" to close Phase 18, OR list specific page+mode combinations needing revision with what's wrong (e.g., "/draft pandemonium ledger: zine corners not visible; team-dashboard demacia: no gilt borders on roster rows"). The orchestrator will route revisions back to gsd-planner for a re-plan.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Audit narrative boundary | 18-UI-REVIEW.md is a documentation artifact; no security surface |
| Grep gate boundary | REQ-7 utility-route grep enforces invariant from SPEC |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-10-01 | I (Information disclosure) | 18-UI-REVIEW.md | accept | Documentation only; no PII; same model as 17-UI-REVIEW.md. |
| T-18-10-02 | T (Tampering) | Grep gate verification | accept | Verification is human-readable + grep-verifiable; future regressions would be caught by re-running the same grep in CI (post-launch). |
</threat_model>

<verification>
After all 3 tasks complete:
- `grep -rnE 'is_pandemonium|theme == "pandemonium"' src/pages/{auth,admin,legal,team,team_builder,opponents,action_items,profile,closed_beta,game_plan,stats}/**.rs` returns 0 lines
- `cd e2e && npx playwright test visual-regression.spec.ts -g "public-|authed-profile|authed-team-roster|authed-stats|authed-opponents"` passes
- `cd e2e && npx playwright test region-diff.spec.ts` passes (asserts already from 18-09)
- 18-UI-REVIEW.md exists with 22 region-scoped verdicts
- No FAIL verdicts; no open HIGH/CRITICAL findings
- User checkpoint approved
- `cargo check --features ssr` exits 0
- `cargo check --features hydrate --target wasm32-unknown-unknown` exits 0
- `cargo test --features ssr --lib` exits 0
</verification>

<success_criteria>
1. REQ-7 verified: utility routes have zero new region conditionals (grep gate clean)
2. Utility baselines from Phase 17 still match within tolerance
3. 18-UI-REVIEW.md exists with 11 page sections × 2 region verdicts × 6 pillars
4. No FAIL verdicts; no open HIGH/CRITICAL findings
5. User manual side-by-side review approved
6. All Phase 18 acceptance criteria from SPEC satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/18-region-variants/18-10-SUMMARY.md` documenting:
- Final audit summary: total PASS / PASS-WITH-DEFERRED / FAIL counts
- Open findings list (LOW/MEDIUM only) with disposition
- User approval timestamp
- Confirmation of REQ-7 grep gate result
- Pointers to artifacts: 18-UI-REVIEW.md, 18-09 baseline directory, 18-CONTENT-CONTRACTS.md
- Phase 18 closure marker for STATE.md update
</output>

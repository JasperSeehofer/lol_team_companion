---
phase: 01.1-playwright-bug-audit
plan: 02
type: execute
wave: 2
depends_on: ["01.1-01"]
files_modified:
  - e2e/tests/audit-game-plan.spec.ts
  - e2e/tests/audit-post-game.spec.ts
  - e2e/tests/audit-champion-pool.spec.ts
  - e2e/tests/audit-team.spec.ts
  - e2e/tests/audit-misc-pages.spec.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "Game plan CRUD flows are tested (create, list, update, delete)"
    - "Post-game review CRUD flows are tested"
    - "Champion pool add/remove/tier flows are tested"
    - "Team create and roster management flows are tested"
    - "Stats, opponents, action-items, team-builder, and profile pages are tested for basic load + empty state"
    - "All console errors and WASM panics are captured per test"
  artifacts:
    - path: "e2e/tests/audit-game-plan.spec.ts"
      provides: "Game plan CRUD audit"
      min_lines: 40
    - path: "e2e/tests/audit-post-game.spec.ts"
      provides: "Post-game review CRUD audit"
      min_lines: 40
    - path: "e2e/tests/audit-champion-pool.spec.ts"
      provides: "Champion pool interaction audit"
      min_lines: 30
    - path: "e2e/tests/audit-team.spec.ts"
      provides: "Team management audit"
      min_lines: 30
    - path: "e2e/tests/audit-misc-pages.spec.ts"
      provides: "Misc pages load + empty state audit"
      min_lines: 30
  key_links:
    - from: "e2e/tests/audit-game-plan.spec.ts"
      to: "e2e/tests/fixtures.ts"
      via: "import teamPage fixture"
      pattern: "import.*fixtures"
    - from: "e2e/tests/audit-champion-pool.spec.ts"
      to: "e2e/tests/fixtures.ts"
      via: "import teamPage fixture"
      pattern: "import.*fixtures"
---

<objective>
Write audit tests for all remaining features: game plan, post-game, champion pool, team management, and miscellaneous pages (stats, profile, opponents, action-items, team-builder).

Purpose: Complete feature coverage so every user-facing flow is exercised by Playwright, capturing any bugs for the final report.
Output: 5 new audit test files covering all remaining routes.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01.1-playwright-bug-audit-identify-and-document-existing-bugs-across-all-features/01.1-RESEARCH.md
@.planning/phases/01.1-playwright-bug-audit-identify-and-document-existing-bugs-across-all-features/01.1-01-SUMMARY.md
@e2e/tests/fixtures.ts
@e2e/playwright.config.ts

<interfaces>
<!-- From Plan 01: teamPage fixture and createTeam helper -->
From e2e/tests/fixtures.ts (after Plan 01 updates):
```typescript
export const test = base.extend<Fixtures>({
  authedPage: async ({ page }, use) => { ... },
  teamPage: async ({ page }, use) => { ... },
});
export async function createTeam(page: Page, name: string): Promise<void>;
export { expect };
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write game plan and post-game audit tests</name>
  <files>e2e/tests/audit-game-plan.spec.ts, e2e/tests/audit-post-game.spec.ts</files>
  <action>
**audit-game-plan.spec.ts** — Use `teamPage` fixture. Console error capture per test.

Test 1: "game-plan: create a new game plan"
- Navigate to `/game-plan`, wait for networkidle + 1000ms
- Look for a plan name input (placeholder containing "Comp" or "Plan" or similar)
- Fill with `AuditPlan_${Date.now()}`
- Click "Save Plan" button
- Wait 2000ms
- Assert success message visible (text matching /created|saved/i) or "Update" button appears
- Assert no real console errors

Test 2: "game-plan: plan appears in saved plans list"
- Navigate to `/game-plan`, create and save a plan
- Look for a saved plans section (heading, sidebar, or dropdown)
- Assert the plan name appears in the list
- If no list section visible: note this as a potential issue (don't test.fail unless it's clearly a bug vs just no list UI)

Test 3: "game-plan: draft_id prefill works"
- This is partially covered by pipeline.spec.ts but audit it independently:
- Navigate to `/game-plan?draft_id=draft:nonexistent`
- Wait for load
- Verify page does not crash, no WASM panics
- Navigate to `/game-plan` (no param) — verify clean load

Test 4: "game-plan: checklist interaction"
- Navigate to `/game-plan`, create a plan
- Look for checklist/objective inputs or "Add" buttons for objectives/win conditions
- If found: add an item, verify it appears, toggle it if checkboxes exist
- If not found: skip with message

**audit-post-game.spec.ts** — Use `teamPage` fixture. Console error capture.

Test 1: "post-game: create a new review"
- Navigate to `/post-game`, wait for networkidle + 1000ms
- Look for a form — game result selector (win/loss), notes textarea, pattern inputs
- Fill available fields with test data
- Click save/submit button
- Wait 2000ms
- Assert success indicator (message, button change, or list update)

Test 2: "post-game: draft_id param loads without crash"
- Navigate to `/post-game?draft_id=draft:nonexistent`
- Wait for load + 1000ms
- Verify no WASM panics, page renders content
- Check for any pre-filled fields or "Source Draft" indicators

Test 3: "post-game: review appears in saved list"
- Create a review, then check if it appears in any saved reviews section
- If no list UI visible: skip

Test 4: "post-game: no console errors during form interactions"
- Navigate to `/post-game`, interact with all visible form elements
- Assert no real console errors
  </action>
  <verify>
    <automated>cd /home/jasper/Repositories/lol_team_companion/e2e && npx playwright test tests/audit-game-plan.spec.ts tests/audit-post-game.spec.ts --project=chromium 2>&1 | tail -15</automated>
  </verify>
  <done>audit-game-plan.spec.ts has 4 tests. audit-post-game.spec.ts has 4 tests. All tests either pass or document bugs.</done>
</task>

<task type="auto">
  <name>Task 2: Write champion pool, team management, and misc pages audit tests</name>
  <files>e2e/tests/audit-champion-pool.spec.ts, e2e/tests/audit-team.spec.ts, e2e/tests/audit-misc-pages.spec.ts</files>
  <action>
**audit-champion-pool.spec.ts** — Use `teamPage` fixture. Console error capture.

Test 1: "champion-pool: page loads with empty state"
- Navigate to `/champion-pool`, wait for networkidle + 1000ms
- Verify page renders (not error banner)
- Look for champion grid or "add champion" UI elements
- Assert no console errors

Test 2: "champion-pool: add a champion to pool"
- Navigate to `/champion-pool`, wait for load
- Look for an autocomplete input, champion search, or champion grid
- If autocomplete/search: type a champion name (e.g. "Ahri"), select from dropdown
- If grid: click a champion button
- Look for tier selection (S/A/B/C tiers mentioned in research)
- After adding: verify champion appears in the pool list/section
- Assert no console errors

Test 3: "champion-pool: remove a champion from pool"
- Add a champion first (from Test 2 pattern)
- Find a remove/delete button near the added champion
- Click it, wait 1000ms
- Verify champion is removed from the pool display
- If no remove button found: skip

**audit-team.spec.ts** — Use `authedPage` fixture (NOT teamPage, since we test team creation itself).

Test 1: "team: create a new team"
- Navigate to `/team/roster`, wait for load
- Fill team name input with `AuditTeam_${Date.now()}`
- Select region "NA"
- Click "Create Team"
- Wait for redirect to `/team/dashboard` (timeout 15000ms)
- Verify dashboard loads with team name visible

Test 2: "team: dashboard shows roster slots"
- Use teamPage fixture for this test (team already exists)
- Navigate to `/team/dashboard`, wait for load
- Verify roster section is visible (look for "Roster" heading or role slots like "Top", "Jungle", etc.)
- Assert no console errors

Test 3: "team: join with invalid code shows error"
- Use authedPage (fresh user, no team)
- Navigate to `/team/roster`, wait for load
- Look for a "Join Team" section with a code input
- If found: enter an invalid code like "INVALID_CODE_12345", submit
- Verify an error message appears (not a crash)
- If no join section: skip

**audit-misc-pages.spec.ts** — Use `teamPage` fixture for team-required pages, `authedPage` for others.

Test 1: "stats: page loads with empty state"
- Navigate to `/stats`, wait for load
- Verify page renders meaningful content (not just error banner)
- Look for empty state message or "no matches" indicator
- Assert no console errors

Test 2: "profile: page loads and shows username"
- Navigate to `/profile`, wait for load
- Verify username or email is displayed somewhere on the page
- Assert no console errors

Test 3: "opponents: page loads"
- Navigate to `/opponents`, wait for load
- Verify page renders (not error, not blank)
- Note: opponents page is not in existing pages.spec.ts AUTHED_PAGES list
- Assert no console errors or WASM panics

Test 4: "action-items: page loads"
- Navigate to `/action-items`, wait for load
- Verify page renders (not error, not blank)
- Note: action-items page is not in existing pages.spec.ts AUTHED_PAGES list
- Assert no console errors or WASM panics

Test 5: "team-builder: page loads and basic interaction"
- Navigate to `/team-builder`, wait for load
- Verify page renders
- Look for any interactive elements (dropdowns, champion selectors), try one interaction
- Assert no console errors

For ALL tests: capture pageerror and console.error events. Filter out favicon and 404 errors. Any unexpected errors should cause test failure (these become bug candidates).
  </action>
  <verify>
    <automated>cd /home/jasper/Repositories/lol_team_companion/e2e && npx playwright test tests/audit-champion-pool.spec.ts tests/audit-team.spec.ts tests/audit-misc-pages.spec.ts --project=chromium 2>&1 | tail -15</automated>
  </verify>
  <done>audit-champion-pool.spec.ts has 3 tests, audit-team.spec.ts has 3 tests, audit-misc-pages.spec.ts has 5 tests. All features have basic coverage. Bugs are either annotated with test.fail() or left as natural failures for documentation.</done>
</task>

</tasks>

<verification>
Run all audit tests together:
```bash
cd e2e && npx playwright test tests/audit-*.spec.ts --project=chromium 2>&1 | tail -25
```
All tests either pass or have documented failures.
</verification>

<success_criteria>
- 5 new audit test files created covering game plan, post-game, champion pool, team, and misc pages
- Every authenticated route in the app has at least one audit test
- Console error capture is present in every test
- Tests that discover new bugs are left as natural failures (for Plan 03 to document)
</success_criteria>

<output>
After completion, create `.planning/phases/01.1-playwright-bug-audit-identify-and-document-existing-bugs-across-all-features/01.1-02-SUMMARY.md`
</output>

---
phase: 05-post-game-loop-polish
plan: 03
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/pages/game_plan.rs
  - src/pages/tree_drafter.rs
  - src/pages/draft.rs
autonomous: true
requirements:
  - UX-02
  - UX-03

must_haves:
  truths:
    - "Game plan page uses toast for all mutation feedback instead of local status_msg"
    - "Tree drafter page uses toast for all mutation feedback instead of local status_msg"
    - "Draft page uses toast for all mutation feedback instead of local save_result"
    - "All Suspense fallbacks on these 3 pages show skeleton components instead of Loading... text"
  artifacts:
    - path: "src/pages/game_plan.rs"
      provides: "Toast-migrated mutation feedback + skeleton fallbacks"
      contains: "use_context::<ToastContext>"
    - path: "src/pages/tree_drafter.rs"
      provides: "Toast-migrated mutation feedback + skeleton fallbacks"
      contains: "use_context::<ToastContext>"
    - path: "src/pages/draft.rs"
      provides: "Toast-migrated mutation feedback + skeleton fallbacks"
      contains: "use_context::<ToastContext>"
  key_links:
    - from: "src/pages/game_plan.rs"
      to: "src/components/ui.rs"
      via: "use_context::<ToastContext>()"
      pattern: "ToastContext"
    - from: "src/pages/tree_drafter.rs"
      to: "src/components/ui.rs"
      via: "use_context::<ToastContext>()"
      pattern: "ToastContext"
    - from: "src/pages/draft.rs"
      to: "src/components/ui.rs"
      via: "use_context::<ToastContext>()"
      pattern: "ToastContext"
---

<objective>
Migrate the 3 most complex pages (game_plan, tree_drafter, draft) from local status_msg/save_result signals to the toast system, and replace all "Loading..." Suspense fallbacks with skeleton components.

Purpose: These pages have the highest density of mutation feedback callsites (game_plan: 22, tree_drafter: 25, draft: 10). Migrating them first covers the bulk of the UX-03 work.
Output: 3 pages fully using toast system and skeleton loading.
</objective>

<execution_context>
@/home/jasper/Repositories/lol_team_companion/.claude/get-shit-done/workflows/execute-plan.md
@/home/jasper/Repositories/lol_team_companion/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-post-game-loop-polish/05-CONTEXT.md
@.planning/phases/05-post-game-loop-polish/05-RESEARCH.md
@.planning/phases/05-post-game-loop-polish/05-UI-SPEC.md

@.planning/phases/05-post-game-loop-polish/05-01-SUMMARY.md

<interfaces>
From src/components/ui.rs (created in Plan 01):
```rust
#[derive(Clone, Copy)]
pub struct ToastContext { pub show: Callback<(ToastKind, String)> }

#[derive(Clone, Debug, PartialEq)]
pub enum ToastKind { Success, Error }

pub fn SkeletonLine(width: &'static str, height: &'static str) -> impl IntoView
pub fn SkeletonCard(height: &'static str) -> impl IntoView
pub fn SkeletonGrid(cols: u8, rows: u8, card_height: &'static str) -> impl IntoView
```

Usage pattern:
```rust
let toast = use_context::<ToastContext>().expect("ToastProvider");
// In spawn_local:
Ok(_) => toast.show.run((ToastKind::Success, "Game plan saved".into())),
Err(e) => toast.show.run((ToastKind::Error, format!("{e}"))),
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate game_plan.rs to toast + skeletons</name>
  <files>src/pages/game_plan.rs</files>
  <read_first>
    - src/pages/game_plan.rs (full file — find all status_msg/set_status_msg occurrences, all "Loading..." fallbacks)
    - src/components/ui.rs (ToastContext, ToastKind, SkeletonLine, SkeletonCard imports)
    - .planning/phases/05-post-game-loop-polish/05-UI-SPEC.md (copywriting contract for toast messages + skeleton shapes)
  </read_first>
  <action>
**Toast migration:**

1. Add import: `use crate::components::ui::{ToastContext, ToastKind, SkeletonLine, SkeletonCard};`

2. In the component body, get toast context: `let toast = use_context::<ToastContext>().expect("ToastProvider");`

3. Remove the `status_msg` signal declaration (e.g., `let (status_msg, set_status_msg) = signal(...)` or `let status_msg = RwSignal::new(...)`)

4. Remove any `<StatusMessage>` rendering in the view that displays `status_msg`

5. Replace every `set_status_msg.set(Some("...".into()))` in spawn_local Ok branches with toast calls:
   - Save success: `toast.show.run((ToastKind::Success, "Game plan saved".into()))`
   - Delete success: `toast.show.run((ToastKind::Success, "Game plan deleted".into()))`
   - Error branches: `toast.show.run((ToastKind::Error, format!("{e}")))`

6. Search for ALL occurrences of `status_msg` and `set_status_msg` — there are ~22 across the file. Each one must be replaced or removed. If `status_msg` is read in any conditional view logic (e.g., `{move || status_msg.get().map(...)}`), remove that entire block.

**Skeleton fallbacks:**

7. Replace `"Loading..."` text in Suspense fallbacks:
   - Plan list (line ~980): `view! { <div class="flex flex-col gap-2"><SkeletonCard height="h-10" /><SkeletonCard height="h-10" /><SkeletonCard height="h-10" /></div> }`
   - Draft selector (line ~1082): `view! { <SkeletonLine width="w-full" height="h-9" /> }`
   - Win-rate panel (line ~1366): `view! { <SkeletonCard height="h-32" /> }`

Keep ErrorBanner for Err branches inside Suspense content (load failures stay inline per RESEARCH.md recommendation).
  </action>
  <verify>
    <automated>cargo check --features ssr && cargo check --features hydrate --target wasm32-unknown-unknown</automated>
  </verify>
  <acceptance_criteria>
    - src/pages/game_plan.rs contains `use_context::<ToastContext>`
    - src/pages/game_plan.rs does NOT contain `set_status_msg` or `status_msg` signal declaration
    - src/pages/game_plan.rs contains `"Game plan saved"` in a toast call
    - src/pages/game_plan.rs contains `"Game plan deleted"` in a toast call
    - src/pages/game_plan.rs contains `SkeletonCard` in Suspense fallbacks
    - src/pages/game_plan.rs does NOT contain `"Loading..."` text in Suspense fallbacks
    - `cargo check --features ssr` exits 0
    - `cargo check --features hydrate --target wasm32-unknown-unknown` exits 0
  </acceptance_criteria>
  <done>game_plan.rs uses toast for all mutation feedback, skeletons for all loading states. No local status_msg signal remains.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate tree_drafter.rs and draft.rs to toast + skeletons</name>
  <files>src/pages/tree_drafter.rs, src/pages/draft.rs</files>
  <read_first>
    - src/pages/tree_drafter.rs (full file — find all status_msg/set_status_msg, all "Loading..." fallbacks)
    - src/pages/draft.rs (full file — find all save_result/set_save_result, all "Loading..." fallbacks)
    - src/components/ui.rs (ToastContext, ToastKind, SkeletonLine, SkeletonCard, SkeletonGrid imports)
    - .planning/phases/05-post-game-loop-polish/05-UI-SPEC.md (copywriting + skeleton shapes)
  </read_first>
  <action>
**tree_drafter.rs — Toast migration:**

1. Add import: `use crate::components::ui::{ToastContext, ToastKind, SkeletonLine, SkeletonCard, SkeletonGrid};`

2. Get toast context: `let toast = use_context::<ToastContext>().expect("ToastProvider");`

3. Remove `status_msg` signal declaration

4. Replace all ~25 occurrences of `set_status_msg.set(...)` / `status_msg.set(...)`:
   - Tree saved: `toast.show.run((ToastKind::Success, "Tree saved".into()))`
   - Node saved: `toast.show.run((ToastKind::Success, "Node saved".into()))`
   - Delete success: `toast.show.run((ToastKind::Success, "Tree deleted".into()))`
   - Errors: `toast.show.run((ToastKind::Error, format!("{e}")))`

5. Remove StatusMessage rendering block for status_msg

6. Replace "Loading..." Suspense fallbacks:
   - Tree list (line ~763): 3x `<SkeletonCard height="h-10" />`
   - Champion grid (line ~1324): `<SkeletonGrid cols=4 rows=3 card_height="h-12" />`
   - Node editor (line ~1444): `<SkeletonCard height="h-48" />`
   - Node actions (line ~1482): `<SkeletonLine width="w-32" height="h-8" />`

**draft.rs — Toast migration:**

7. Add import: `use crate::components::ui::{ToastContext, ToastKind, SkeletonLine, SkeletonCard, SkeletonGrid};`

8. Get toast context: `let toast = use_context::<ToastContext>().expect("ToastProvider");`

9. Remove `save_result` signal declaration

10. Replace all ~10 occurrences of `set_save_result.set(...)` / `save_result`:
    - Draft saved: `toast.show.run((ToastKind::Success, "Draft saved".into()))`
    - Draft deleted: `toast.show.run((ToastKind::Success, "Draft deleted".into()))`
    - Errors: `toast.show.run((ToastKind::Error, format!("{e}")))`

11. Remove any StatusMessage/inline rendering of save_result

12. Replace "Loading..." Suspense fallbacks:
    - Team select (line ~1321): `<SkeletonLine width="w-48" height="h-9" />`
    - Opponent intel (line ~2099): `<SkeletonCard height="h-48" />`
    - Champion picker (line ~2453): `<SkeletonGrid cols=4 rows=3 card_height="h-12" />`
    - Ban priorities (line ~3149): `<SkeletonCard height="h-24" />`

Keep ErrorBanner for load-failure Err branches in both files.

**Important:** The `toast` variable is `Copy` (because `ToastContext` contains a `Callback` which is `Copy`), so it can be used across multiple `move` closures without cloning.
  </action>
  <verify>
    <automated>cargo check --features ssr && cargo check --features hydrate --target wasm32-unknown-unknown</automated>
  </verify>
  <acceptance_criteria>
    - src/pages/tree_drafter.rs contains `use_context::<ToastContext>`
    - src/pages/tree_drafter.rs does NOT contain `set_status_msg` signal declaration
    - src/pages/tree_drafter.rs contains `"Tree saved"` and `"Node saved"` in toast calls
    - src/pages/tree_drafter.rs contains `SkeletonCard` and `SkeletonGrid` in Suspense fallbacks
    - src/pages/tree_drafter.rs does NOT contain `"Loading..."` in Suspense fallbacks
    - src/pages/draft.rs contains `use_context::<ToastContext>`
    - src/pages/draft.rs does NOT contain `set_save_result` signal declaration
    - src/pages/draft.rs contains `"Draft saved"` in a toast call
    - src/pages/draft.rs contains `SkeletonLine` and `SkeletonGrid` in Suspense fallbacks
    - src/pages/draft.rs does NOT contain `"Loading..."` in Suspense fallbacks
    - `cargo check --features ssr` exits 0
    - `cargo check --features hydrate --target wasm32-unknown-unknown` exits 0
  </acceptance_criteria>
  <done>tree_drafter.rs and draft.rs use toast for all mutation feedback, skeletons for all loading states. No local status signals remain in either file.</done>
</task>

</tasks>

<verification>
- `cargo check --features ssr` passes
- `cargo check --features hydrate --target wasm32-unknown-unknown` passes
- No "Loading..." text remains in Suspense fallbacks of these 3 files
- No local status_msg/save_result signals remain in these 3 files
</verification>

<success_criteria>
- All 3 pages (game_plan, tree_drafter, draft) use ToastContext for mutation feedback
- All Suspense fallbacks show skeleton components
- ErrorBanner remains for load-failure Err branches (not migrated to toast)
- Both SSR and WASM targets compile clean
</success_criteria>

<output>
After completion, create `.planning/phases/05-post-game-loop-polish/05-03-SUMMARY.md`
</output>
